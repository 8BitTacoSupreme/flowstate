---
phase: quick-260708-r6n
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/longmemeval_qa.py
  - tests/test_longmemeval_qa.py
  - pyproject.toml
autonomous: true
requirements: [R6N-01, R6N-02, R6N-03]
must_haves:
  truths:
    - "importing bench.longmemeval_qa works with openai NOT installed (lazy import)"
    - "--judge-provider openai without OPENAI_API_KEY or openai pkg prints a clear message and returns 1 (no claude fallback)"
    - "_judge_openai returns True/False/None and never raises"
    - "provider=claude path is byte-identical to today (existing tests pass unchanged)"
    - "--sample N --seed S yields a reproducible subset spanning >1 question_type"
    - "output JSON records judge_provider, sample, seed, question_type_distribution"
  artifacts:
    - path: bench/longmemeval_qa.py
      provides: "_openai_chat seam, _judge_openai, provider-aware _judge_one, sampling + hard-check in _run_qa, new CLI flags"
    - path: tests/test_longmemeval_qa.py
      provides: "offline tests for openai judge, routing, hard-error, seeded sampling"
    - path: pyproject.toml
      contains: 'eval = ["openai>=1.0"]'
  key_links:
    - from: bench/longmemeval_qa.py::_judge_one
      to: bench/longmemeval_qa.py::_judge_openai
      via: 'provider=="openai" routing'
      pattern: "_judge_openai"
    - from: bench/longmemeval_qa.py::_judge_openai
      to: bench/longmemeval_qa.py::_openai_chat
      via: "lazy openai chat seam"
      pattern: "_openai_chat"
---

<objective>
Extend `bench/longmemeval_qa.py` with (1) an OpenAI/GPT-4o judge provider for an
officially-comparable QA number, and (2) representative seeded sampling to fix
first-N question-type bias. Add an optional `[eval]` pip extra.

Purpose: produce a paper-comparable QA number (GPT-4o judge) and unbiased bounded
runs, without touching the claude judge path (which must stay byte-identical).
Output: modified longmemeval_qa.py + its test + pyproject.toml. NOTHING else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhogan/frameworx/CLAUDE.md

<interfaces>
Target module bench/longmemeval_qa.py (read in full):
- `_judge_one(answer: str, instance: dict, judge_model: str) -> bool | None` (line ~105)
  routes `_abs` types → `_g._judge_rejection`, else `_g._factcheck`.
- `_run_qa(args, instances) -> int` (line ~139): applies `instances[:args.limit]`,
  loops arms, calls `_judge_one(answer, instance, args.judge_model)`, builds `output`
  dict (keys: benchmark, n_instances, limit, backend, k, reader_model, judge_model, arms),
  prints console table, returns 1 if total_n==0 else 0. Whole function wrapped in
  try/except → return 1.
- `_build_parser()` (line ~278): argparse with --data/--backend/--k/--arms/--reader-model
  /--judge-model/--embed-model/--char-budget/--limit/--out.
- `main(argv)` (line ~320): parse → `_lme._load_data` → `_run_qa`.
- Module uses attribute-access imports (`_g`, `_r`, `_lme`) so tests monkeypatch on
  the owning module. Preserve this idiom.

bench/grounding.py judge idiom to mirror (do NOT modify grounding.py):
- `_factcheck(answer, ground_truth, model) -> bool|None`: builds a "Reply with ONLY
  'YES' or 'NO'" prompt, one call, parses first yes/no via `_YESNO_RE`, never raises.
- `_judge_rejection(answer, model) -> bool|None`: abstention judge.

Fixture bench/fixtures/lme_smoke.json is a list of 3 instances with question_types
{single_session, abstention, multi_session} — spans >1 type (use for representativeness).

pyproject.toml [project.optional-dependencies] has `semantic = ["fastembed>=0.3"]`
at line ~32 — mirror its formatting for the new `eval` extra.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — offline tests for openai judge, routing, hard-error, seeded sampling</name>
  <files>tests/test_longmemeval_qa.py</files>
  <behavior>
    Update the `_make_args` helper to add the new namespace fields with defaults:
    `judge_provider="claude"`, `sample=None`, `seed=0`. (Existing tests that call
    `_make_args(...)` must keep passing unchanged — this is the byte-identical guard.)

    New tests (all OFFLINE — never invoke real openai/claude, no OPENAI_API_KEY):

    - test__judge_openai_yes_true: monkeypatch `qa._openai_chat` to return "yes" →
      `qa._judge_openai("q","gold","ans","gpt-4o")` is True.
    - test__judge_openai_no_false: `_openai_chat` returns "no" → False.
    - test__judge_openai_garbage_none: `_openai_chat` returns "maybe??" → None.
    - test__judge_openai_returns_none_seam_none: `_openai_chat` returns None → None.
    - test__judge_openai_never_raises: `_openai_chat` raises → result is None (no raise).
    - test__judge_one_routes_openai: monkeypatch `qa._judge_openai` to record it was
      called; `qa._judge_one("ans", {"question_type":"single_session","question":"q",
      "answer":"gold"}, "gpt-4o", provider="openai")` calls `_judge_openai` and NOT
      `_g._factcheck` (spy _g._factcheck to assert 0 calls).
    - test__judge_one_routes_claude_default: provider defaults to "claude" → calls
      `_g._factcheck`, NOT `_judge_openai`.
    - test_run_qa_openai_missing_key_returns_one: monkeypatch os.environ so
      OPENAI_API_KEY is unset AND spy both `_g._factcheck` and `qa._judge_openai`;
      args with judge_provider="openai" → `qa._run_qa` returns 1 and neither judge
      helper was called (no silent fallback).
    - test_run_qa_openai_pkg_missing_returns_one: monkeypatch `qa._openai_available`
      → False (set OPENAI_API_KEY so only the pkg check fails), provider="openai" →
      returns 1, no judge calls.
    - test_run_qa_sampling_reproducible: build `_make_instances(n_single=5, n_multi=5)`,
      args with sample=4, seed=0 → run twice (bm25, monkeypatched _build_docs/bm25_rank/
      _answer/_factcheck) → identical set of scored question_ids across the two runs
      (capture ids via a spy on _build_docs or _answer, or compare output n + a spy).
      seed=1 → (very likely) different subset. sample=None → all/first-N unchanged.
    - test_run_qa_sample_spans_multiple_types: sample=4 over 5 single + 5 multi →
      output JSON `question_type_distribution` has >1 key (representativeness).
    - test_run_qa_records_new_json_fields: output JSON contains judge_provider, sample,
      seed, question_type_distribution keys.

    Seam note for tests: introduce and patch `qa._openai_chat(model, system, user)
    -> str | None` (the lazy-import + client-call seam) and `qa._openai_available()
    -> bool`. Tests never import the real openai SDK.
  </behavior>
  <action>
    Add the new tests and update `_make_args` per the behavior block. Do NOT weaken or
    delete any existing test. These tests reference `qa._openai_chat`, `qa._openai_available`,
    `qa._judge_openai`, and the provider kwarg / sampling fields that do not exist yet —
    so this task's new tests MUST FAIL (RED) until Task 2 lands. Commit as RED:
    `test(260708-r6n): add failing tests for openai judge + seeded sampling`.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python3 -m pytest tests/test_longmemeval_qa.py -q 2>&1 | tail -20; echo "EXPECT: new tests fail (RED), pre-existing tests still pass"</automated>
  </verify>
  <done>New tests fail referencing not-yet-implemented symbols; all pre-existing tests still pass; committed with a test(...) message.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — openai judge, provider routing, seeded sampling, hard-check, pyproject eval extra</name>
  <files>bench/longmemeval_qa.py, pyproject.toml</files>
  <action>
    Implement against the Task 1 tests. Preserve `from __future__ import annotations`,
    ruff format (line-length 100, double quotes), snake_case, and the attribute-access
    import idiom. Add `import os` and `import random` (stdlib) at the top; do NOT add a
    top-level `import openai`.

    1. Lazy openai seam (never-raises):
       - `_openai_available() -> bool`: try `import openai` inside; return True on success,
         False on ImportError. Never raises.
       - `_openai_chat(model: str, system: str, user: str) -> str | None`: LAZILY
         `import openai` inside; construct a client from OPENAI_API_KEY (openai reads it
         from env, or pass explicitly); call chat.completions.create with the given
         model, messages=[system, user], `temperature=0`, `max_tokens=10`; return the
         first choice message content string, or None on ANY exception. Never raises.
       - `_judge_openai(question: str, gold: str, answer: str, model: str) -> bool | None`:
         build the paper-style judge prompt — system: "You are a strict grader." user:
         asks "Is the model's ANSWER correct given the QUESTION and the correct/gold
         answer? Reply ONLY 'yes' or 'no'." including question, gold, answer. Call
         `_openai_chat`. If it returns None → None. Parse the first yes/no token
         (case-insensitive; a small regex, e.g. mirror grounding's `_YESNO_RE` style but
         lowercase 'yes'/'no') → True/False; unparseable → None. Never raises.

    2. Provider-aware `_judge_one(answer, instance, judge_model, *, provider="claude")`:
       - Add the keyword-only `provider` param defaulting to "claude" (keeps the existing
         3-positional-arg call signature byte-identical).
       - `_abs` types → `_g._judge_rejection(answer, judge_model)` regardless of provider
         (documented: cleaned-S has no `_abs`; reusing the claude abstention path under
         openai is acceptable for zero-`_abs` datasets).
       - Non-`_abs` + provider=="openai" → `_judge_openai(instance["question"],
         instance["answer"], answer, judge_model)`.
       - Non-`_abs` + provider=="claude" → `_g._factcheck(answer, instance["answer"],
         judge_model)` (unchanged). Keep the outer try/except → None.

    3. `_run_qa` changes:
       - Judge model defaulting: `judge_model = args.judge_model`; if
         `args.judge_provider == "openai" and judge_model == "sonnet": judge_model =
         "gpt-4o"` (document: openai provider left at the claude default upgrades to gpt-4o).
       - Openai prerequisite HARD-CHECK (the one deliberate hard-stop) BEFORE the arm/
         instance loops: if `args.judge_provider == "openai"` and NOT
         (`os.environ.get("OPENAI_API_KEY")` and `_openai_available()`): print
         "--judge-provider openai requires OPENAI_API_KEY and `pip install -e .[eval]`"
         and return 1. Must NOT fall back to claude.
       - Sampling (replace the `instances[:args.limit]` line): apply --sample first, then
         --limit. `processed = instances`; if `args.sample is not None: processed =
         random.Random(args.seed).sample(processed, min(args.sample, len(processed)))`;
         if `args.limit is not None: processed = processed[:args.limit]`;
         `instances_to_score = processed`. Document precedence in a comment (sample then
         limit).
       - Thread `provider=args.judge_provider` and the defaulted `judge_model` into the
         `_judge_one(...)` call.
       - Output JSON: add `"judge_provider": args.judge_provider`, `"sample": args.sample`,
         `"seed": args.seed`, `"judge_model": judge_model` (the defaulted one), and
         `"question_type_distribution"`: a dict of question_type → count over
         `instances_to_score`.
       - Console summary: add a line noting provider + sample/seed.
       Keep the whole function's never-raises outer try/except intact (the hard-check
       return-1 is inside it and is fine).

    4. `_build_parser`: add `--judge-provider` (choices ("claude","openai"), default
       "claude"), `--sample` (type=int, default=None), `--seed` (type=int, default=0),
       with helpful help strings (note openai needs the [eval] extra + OPENAI_API_KEY).

    5. pyproject.toml: under [project.optional-dependencies], add a new extra mirroring
       `semantic` formatting:
         eval = ["openai>=1.0"]
       Do NOT add openai to core `dependencies`.

    Update the module docstring's judge note to mention the optional openai provider.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python3 -c "import bench.longmemeval_qa" && python3 -m pytest tests/test_longmemeval_qa.py -q 2>&1 | tail -15 && ruff check bench/longmemeval_qa.py tests/test_longmemeval_qa.py && ruff format --check bench/longmemeval_qa.py tests/test_longmemeval_qa.py && python3 -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q 2>&1 | tail -5 && git diff --stat bench/longmemeval.py bench/_retrieval.py bench/grounding.py flowstate/ | tail -3 && echo "OUT-OF-SCOPE FILES ABOVE MUST BE EMPTY"</automated>
  </verify>
  <done>
    All tests pass OFFLINE (no key, no SDK). `import bench.longmemeval_qa` works with
    openai NOT installed. ruff check + format --check clean. Coverage holds >=80%. The
    git diff --stat for bench/longmemeval.py, bench/_retrieval.py, bench/grounding.py,
    and flowstate/ is EMPTY (out-of-scope files untouched). Committed with feat(...).
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_longmemeval_qa.py -q` passes fully OFFLINE (no real
  openai/claude, no OPENAI_API_KEY set).
- `python3 -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` holds 80%.
- `ruff check` + `ruff format --check` clean on the two touched source/test files.
- `python3 -c "import bench.longmemeval_qa"` succeeds with openai NOT installed
  (proves lazy import).
- `git diff` proves ONLY bench/longmemeval_qa.py, tests/test_longmemeval_qa.py, and
  pyproject.toml changed; bench/longmemeval.py, bench/_retrieval.py, bench/grounding.py,
  and flowstate/ are UNCHANGED.
- Note in SUMMARY: `pip install -e .[eval]` resolves openai (do NOT require it for the
  test suite).
</verification>

<success_criteria>
- GPT-4o judge provider (`_judge_openai`) available and wired through `_judge_one` +
  `--judge-provider openai`, with a lazy openai import.
- The one deliberate hard-stop: `--judge-provider openai` without OPENAI_API_KEY or the
  openai package prints a clear actionable message and returns 1 — no claude fallback.
- Representative seeded sampling (`--sample N --seed S`) is reproducible and spans
  multiple question types; output JSON records judge_provider, sample, seed, and
  question_type_distribution.
- claude provider path is byte-identical (all pre-existing tests pass unchanged).
- `eval = ["openai>=1.0"]` optional extra added; openai NOT in core deps.
</success_criteria>

<output>
Create `.planning/quick/260708-r6n-extend-bench-longmemeval-qa-py-add-judge/260708-r6n-SUMMARY.md`
(and a bare `SUMMARY.md`) when done, with `status: complete` frontmatter.
</output>
