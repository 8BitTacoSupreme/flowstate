---
phase: quick-260709-j8q
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/locomo_qa.py
  - tests/test_locomo_qa.py
autonomous: true
requirements: [LOCOMO-QA-01]
must_haves:
  truths:
    - "python -m bench.locomo_qa --data <file> --backend bm25 --reader-provider claude scores QA accuracy per LoCoMo category and overall with NO LLM judge"
    - "Scoring uses LoCoMo official string metrics: normalized token-overlap F1 + exact-match, plus the category-5 adversarial rule"
    - "Module imports without openai installed; never raises except the deliberate openai canary/prereq hard-error"
    - "git diff shows ONLY bench/locomo_qa.py and tests/test_locomo_qa.py changed"
  artifacts:
    - path: "bench/locomo_qa.py"
      provides: "LoCoMo QA-accuracy harness (retrieval + oracle arms, string metrics, main(argv)->int)"
    - path: "tests/test_locomo_qa.py"
      provides: "Offline tests for metrics, adversarial rule, _run aggregation, sampling, mass-failure guard"
  key_links:
    - from: "bench/locomo_qa.py"
      to: "bench._retrieval.bm25_rank/semantic_rank"
      via: "module-attribute access (import bench._retrieval as _r)"
      pattern: "_r\\.(bm25_rank|semantic_rank)"
    - from: "bench/locomo_qa.py"
      to: "bench.locomo._load_data/_build_docs"
      via: "reuse loader + doc-builder"
      pattern: "_loc\\.(_load_data|_build_docs)"
    - from: "bench/locomo_qa.py"
      to: "bench.grounding._answer / _wilson"
      via: "claude reader + Wilson CI"
      pattern: "_g\\.(_answer|_wilson)"
---

<objective>
Build `bench/locomo_qa.py` — a LoCoMo QA-accuracy harness that measures reader
answer quality using LoCoMo's OFFICIAL string-based metrics (token-overlap F1 +
exact-match + the category-5 adversarial rule), with NO LLM judge.

Purpose: Complements the existing `bench/locomo.py` (retrieval-coverage only) with
a downstream answer-accuracy signal, mirroring the `longmemeval_qa.py` shape but
swapping the LLM judge for LoCoMo's deterministic string metrics.
Output: Two new files only — `bench/locomo_qa.py` + `tests/test_locomo_qa.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhogan/frameworx/CLAUDE.md

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

Reuse via MODULE-ATTRIBUTE access (so tests can monkeypatch on the owning module):
    import bench._retrieval as _r
    import bench.grounding as _g
    import bench.locomo as _loc

bench.locomo (bench/locomo.py):
  _load_data(path) -> list[dict] | None          # never raises; None on any error
  _build_docs(conv: dict) -> list[tuple[str,str]] # (dia_id, text) over all session_N turns;
                                                  # skips "*_date_time" keys; never raises

bench._retrieval (bench/_retrieval.py):
  bm25_rank(docs, query, k) -> list[str]                       # doc-ids, never raises -> []
  semantic_rank(docs, query, k, embed_fn) -> list[str]         # never raises -> []
  semantic_backend_available(embed_model) -> (embed_fn|None, bool)

bench.grounding (bench/grounding.py):
  _answer(prefix, question, model, *, instruction="Answer concisely and specifically.") -> str
     # claude --print reader; "" when no binary / all attempts fail; never raises
  _wilson(successes: int, n: int) -> tuple[float, float]       # Wilson CI, n==0 -> (0.0,0.0)

Copy verbatim from bench/longmemeval_qa.py (do NOT import — replicate the seam):
  _OPENAI_MAX_RETRIES = 10 ; _OPENAI_TIMEOUT = 120.0
  _openai_available() -> bool                                  # lazy import; never raises
  _openai_chat(model, system, user) -> str | None             # openai.OpenAI(max_retries=10,
                                                              #  timeout=120); temperature=0
  Openai prereq hard-check + upfront per-model canary probe (the ONE deliberate hard-stop,
  return 1) — mirror longmemeval_qa._run_qa lines ~314-335.

LoCoMo conversation schema:
  {sample_id, speaker_a, speaker_b,
   conversation:{session_N:[{speaker, dia_id, text}], session_N_date_time:"..."},
   qa:[{question, answer, category(int 1-5), evidence:[dia_id,...]}]}
  category 5 == adversarial. evidence == gold dia_ids (oracle context source).
</interfaces>

<fixture_note>
The shared `bench/fixtures/locomo_smoke.json` uses STRING categories
("single_hop"/"multi_hop"/"abstention") and has NO category-5 item — and it is
owned by test_locomo.py. Do NOT modify it. tests/test_locomo_qa.py MUST define its
own tiny INLINE LoCoMo fixture (a Python dict/list written to tmp_path, or passed
directly to the scored function) with INTEGER categories including a category-5
adversarial QA and a qa with empty evidence (oracle-skip case).
</fixture_note>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests for metrics, adversarial rule, aggregation, guards</name>
  <files>tests/test_locomo_qa.py</files>
  <behavior>
    Normalization: lowercase, strip punctuation, drop articles (a/an/the), collapse
      whitespace. e.g. "The acks=all" ~ "acks all".
    _f1(pred, gold): token-overlap F1 with light suffix-stripping stem. Known pairs:
      identical -> 1.0; disjoint -> 0.0; partial overlap -> expected precision/recall F1.
    _exact_match(pred, gold): normalized token-SET equality -> 1.0/0.0.
    Adversarial (category 5): score==1.0 iff pred contains "no information available"
      OR "not mentioned" (case-insensitive); else 0.0 — regardless of gold.
    _run/_score aggregation: monkeypatch the reader (bench.grounding._answer AND/OR
      the module-local _answer_one) to return known answers; construct an inline fixture
      with 2 categories (one all-correct, one all-wrong) and verify per-category AND
      overall mean_f1 / mean_em / n, plus Wilson CI on the EM rate.
    Oracle arm: context built from the qa's gold `evidence` dia_ids' text (spy that the
      evidence turns are used); qa with empty evidence -> skipped + counted.
    Retrieval arm: monkeypatch _r.bm25_rank to a fixed id list; ranker output feeds context.
    Seeded sampling determinism (--sample N --seed) + --limit caps scored items.
    Mass-failure guard: force >30% reader_empty ("" answers) -> unreliable:true + exit 2.
    Import-without-openai: `import bench.locomo_qa` succeeds; never-raises on bad data.
  </behavior>
  <action>
    Create tests/test_locomo_qa.py following the OFFLINE idiom of tests/test_locomo.py and
    tests/test_longmemeval_qa.py: `import bench.locomo_qa as qa`, and monkeypatch every LLM/
    ranker collaborator on ITS OWNING MODULE (e.g. monkeypatch.setattr(qa, "_openai_chat", ...),
    monkeypatch.setattr(_g, "_answer", ...), monkeypatch.setattr(_r, "bm25_rank", ...)).
    Define an inline LoCoMo fixture with INTEGER categories including a category-5 adversarial
    item and an empty-evidence item (per <fixture_note> — do NOT touch locomo_smoke.json).
    Write tests for: _f1, _exact_match, normalization helper, adversarial cat-5 rule (yes/no
    phrase), the _run/_score aggregator (per-category + overall F1/EM math with known answers),
    oracle-uses-evidence, retrieval-uses-ranker, seeded-sample determinism, --limit cap,
    mass-failure guard (exit 2 + unreliable), and import-without-openai. Tests reference the
    public/underscore API names Task 2 will implement. This task ends RED (module absent or
    functions unimplemented -> failures/collection error).
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_locomo_qa.py -q; test $? -ne 0 &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>tests/test_locomo_qa.py exists; suite fails (RED) because bench/locomo_qa.py is not yet implemented; test names cover every behavior bullet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement bench/locomo_qa.py until tests pass</name>
  <files>bench/locomo_qa.py</files>
  <action>
    Create bench/locomo_qa.py: `from __future__ import annotations`, stdlib + bench.* +
    lazy openai only; ruff-clean (100 cols, double quotes). Structure mirrors
    bench/longmemeval_qa.py but with STRING metrics instead of a judge:

    1. Module-attribute imports: `import bench._retrieval as _r`, `import bench.grounding as _g`,
       `import bench.locomo as _loc`. Copy the OpenAI seam verbatim from longmemeval_qa
       (_OPENAI_MAX_RETRIES=10, _OPENAI_TIMEOUT=120.0, _openai_available, _openai_chat).
    2. Normalization + metrics (LoCoMo official, string-based, NO judge):
       - `_normalize(s)`: lowercase, strip punctuation, drop articles a/an/the, collapse ws.
       - `_stem(token)`: SMALL suffix-stripping stem (e.g. strip -ing/-ed/-es/-s/-ly);
         docstring MUST state it APPROXIMATES NLTK PorterStemmer and deliberately adds no nltk dep.
       - `_f1(pred, gold)`: normalized+stemmed token-overlap F1 (multiset precision/recall).
       - `_exact_match(pred, gold)`: normalized token-SET equality -> 1.0/0.0.
       - `_score_item(pred, gold, category)`: if category==5 -> em=f1=1.0 iff pred
         (case-insensitive) contains "no information available" or "not mentioned" else 0.0;
         else return (_f1, _exact_match). Adversarial contributes to both F1 and EM as its 0/1.
    3. Reader: `_answer_one(question, context, reader_model, *, provider="claude")` — claude via
       `_g._answer(context, question, reader_model, instruction=<concise LoCoMo reader instr>)`;
       openai via `_openai_chat`. char_budget truncation on the context string.
    4. Context builders: oracle -> join text of the qa's gold `evidence` dia_ids (from _build_docs
       id->text map); retrieval -> `_r.bm25_rank`/`_r.semantic_rank` top-k over _build_docs docs
       (resolve semantic embed_fn once via `_r.semantic_backend_available`, skip arm if unavailable,
       Task A parity). Skip + count qa with empty evidence in the oracle arm.
    5. `_run(args, data) -> int`: sample (seeded) then limit; per-arm loop over conversations/qa;
       aggregate PER category (1-5) AND overall: mean_f1, mean_em, n, `_g._wilson` CI on the EM
       rate; tally reader_empty; mass-failure guard (reader_empty/total > --max-failure-rate
       default 0.30 -> unreliable + exit 2); openai prereq hard-check + per-model canary (the ONE
       deliberate hard-stop -> return 1). never-raises (outer try/except -> 1).
    6. JSON output: {benchmark:"locomo_qa", n, sample, seed, backend, reader_provider, reader_model,
       arms:{retrieval:{overall:{f1,em,n,em_wilson_ci}, by_category:{...}}, oracle:{...}},
       unreliable, failure_rate}. Console summary table. --out writes JSON.
    7. CLI `_build_parser` + `main(argv)->int`: --data(required), --backend semantic|bm25,
       --k 5, --arms retrieval,oracle, --reader-provider claude|openai, --reader-model,
       --embed-model, --char-budget, --sample, --seed, --limit, --max-failure-rate,
       --out. `if __name__=="__main__": sys.exit(main())`.
    Do NOT modify any other file. After GREEN, run `ruff check --fix tests/test_locomo_qa.py`
    (I001 import-sort gotcha) then `ruff format`.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -c "import bench.locomo_qa" &amp;&amp; python -m pytest tests/test_locomo_qa.py -q &amp;&amp; ruff check bench/locomo_qa.py tests/test_locomo_qa.py &amp;&amp; ruff format --check bench/locomo_qa.py tests/test_locomo_qa.py</automated>
  </verify>
  <done>bench/locomo_qa.py implemented; tests/test_locomo_qa.py passes (GREEN); ruff check + format --check clean; `import bench.locomo_qa` works without openai installed.</done>
</task>

</tasks>

<verification>
Run from repo root (/Users/jhogan/frameworx):

1. Targeted: `python -m pytest tests/test_locomo_qa.py -q` -> all pass.
2. Full suite + coverage gate: `python -m pytest tests/ --cov=flowstate --cov-fail-under=80` -> pass.
   (bench/ is not under --cov=flowstate; the gate must remain green regardless.)
3. Lint/format: `ruff check bench/locomo_qa.py tests/test_locomo_qa.py` and
   `ruff format --check bench/locomo_qa.py tests/test_locomo_qa.py` -> clean.
4. Import without openai: `python -c "import bench.locomo_qa"` -> exit 0.
5. Scope proof: `git status --porcelain` shows ONLY bench/locomo_qa.py and
   tests/test_locomo_qa.py (plus this plan dir). `git diff --stat` touches no other
   bench/ or flowstate/ file and does NOT touch pyproject.toml.
</verification>

<success_criteria>
- bench/locomo_qa.py scores LoCoMo QA accuracy with official string metrics (F1 + EM +
  category-5 adversarial rule), NO LLM judge.
- retrieval + oracle arms; per-category (1-5) and overall aggregation with Wilson CI on EM.
- Seeded --sample/--seed, --limit, and mass-failure guard (exit 2 + unreliable) all work.
- Openai reader path is lazy; module imports without openai; never-raises except the
  deliberate openai canary/prereq hard-error.
- Only the two new files changed; pyproject.toml untouched; full suite green at >=80%.
</success_criteria>

<output>
Create `.planning/quick/260709-j8q-build-bench-locomo-qa-py-locomo-qa-accur/SUMMARY.md`
and `.planning/quick/260709-j8q-build-bench-locomo-qa-py-locomo-qa-accur/260709-j8q-SUMMARY.md`
(both, per the SUMMARY convention) when done, with `status: complete` frontmatter.
</output>
