---
phase: quick-260616-x15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/state.py
  - flowstate/orchestrator.py
  - tests/test_state.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [QUICK-260616-x15]
must_haves:
  truths:
    - "ProjectPreferences().enable_prompt_caching_1h is True by default and settable to False"
    - "Existing flowstate.json without the field loads cleanly with default True (no migration error)"
    - "_make_bridge threads preferences.enable_prompt_caching_1h into BridgeConfig (True and False); None preferences leaves BridgeConfig default False"
  artifacts:
    - path: "flowstate/state.py"
      provides: "enable_prompt_caching_1h field on ProjectPreferences"
      contains: "enable_prompt_caching_1h: bool = True"
    - path: "flowstate/orchestrator.py"
      provides: "_make_bridge threading of the caching preference"
      contains: "enable_prompt_caching_1h"
  key_links:
    - from: "flowstate/orchestrator.py:_make_bridge"
      to: "flowstate/bridge.py:BridgeConfig.enable_prompt_caching_1h"
      via: "kwargs['enable_prompt_caching_1h'] = preferences.enable_prompt_caching_1h"
      pattern: "enable_prompt_caching_1h"
---

<objective>
Expose `enable_prompt_caching_1h` through `ProjectPreferences` and wire it into
`orchestrator._make_bridge` so multi-step pipeline runs keep Anthropic prompt-cache
hits past the 5-minute default TTL (cross-step eviction fix).

Purpose: FlowState emits a byte-identical CAG prefix across pipeline steps, but the
5-min default cache TTL evicts it between steps. Raising to 1h retains the cache.
Output: One additive bool preference (default True), threaded into BridgeConfig.

Surgical: only `state.py` + `orchestrator.py` + their tests. Do NOT touch `bridge.py`
(BridgeConfig already accepts the field; `run()` already injects the env when True),
the CANON, or anything else. No new runtime deps. Use `.venv/bin/python`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
flowstate/state.py — ProjectPreferences (~line 38):
```python
class ProjectPreferences(BaseModel):
    project_name: str = ""
    dry_run: bool = False
    auto_branch_on_hardening: bool = True
    model: str = ""
    max_budget_usd: float | None = None
    effort: str = ""
```

flowstate/orchestrator.py — _make_bridge (~line 104):
```python
def _make_bridge(root: Path, dry_run: bool, preferences=None) -> ClaudeBridge:
    kwargs: dict = {"project_root": root, "allowed_tools": ["mcp__repomix"]}
    if preferences:
        if preferences.model:
            kwargs["model"] = preferences.model
        if preferences.max_budget_usd is not None:
            kwargs["max_budget_usd"] = preferences.max_budget_usd
        if preferences.effort:
            kwargs["effort"] = preferences.effort
    config = BridgeConfig(**kwargs)
    return ClaudeBridge(config=config, dry_run=dry_run)
```

flowstate/bridge.py — BridgeConfig (~line 123, DO NOT MODIFY):
```python
enable_prompt_caching_1h: bool = False   # run() injects ENABLE_PROMPT_CACHING_1H=1 when True
```

Test style references:
- tests/test_state.py: test_preferences_model_fields (~133), test_preferences_model_roundtrip (~141), test_load_v010_state_file (~155, builds a raw dict without new field).
- tests/test_orchestrator.py: TestMakeBridgeAllowedTools.test_mcp_repomix_with_preferences (~162) — pattern for building FlowStateModel and calling _make_bridge with preferences.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add enable_prompt_caching_1h preference and thread it through _make_bridge</name>
  <files>flowstate/state.py, flowstate/orchestrator.py, tests/test_state.py, tests/test_orchestrator.py</files>
  <behavior>
    - test_state: ProjectPreferences().enable_prompt_caching_1h is True by default.
    - test_state: field can be set False and survives save_state/load_state roundtrip.
    - test_state: a raw flowstate.json dict WITHOUT the field (e.g. existing v0.x preferences block) loads via load_state and yields enable_prompt_caching_1h is True (default applied, no migration error).
    - test_orchestrator: _make_bridge(tmp_path, dry_run=True, preferences=prefs) with enable_prompt_caching_1h=True produces bridge.config.enable_prompt_caching_1h is True.
    - test_orchestrator: same with preferences.enable_prompt_caching_1h=False produces bridge.config.enable_prompt_caching_1h is False.
    - test_orchestrator: _make_bridge(tmp_path, dry_run=True) with no preferences leaves bridge.config.enable_prompt_caching_1h at BridgeConfig default False.
  </behavior>
  <action>
    In flowstate/state.py: add `enable_prompt_caching_1h: bool = True` to ProjectPreferences, placed immediately after `effort: str = ""` (grouped with the other bridge-related fields). Default TRUE — enabling the cross-step cache overlay is the whole point; it is additive and optional so pydantic backfills the default on existing flowstate.json (no migration bump). Add a brief one-line comment noting the 1h TTL trades higher cache-write cost for cross-step cache retention on eligible API-key accounts (harmless no-op otherwise).

    In flowstate/orchestrator.py: inside `_make_bridge`, within the `if preferences:` block, after the `effort` handling, add `kwargs["enable_prompt_caching_1h"] = preferences.enable_prompt_caching_1h`. Set it unconditionally from the preference (it is a bool with a meaningful False) — do not guard with truthiness. Do NOT modify bridge.py.

    Tests: in tests/test_state.py add a default-value assertion (extend or mirror test_preferences_model_fields), a roundtrip assertion for False (mirror test_preferences_model_roundtrip), and a backward-compat assertion that a raw preferences dict lacking the field loads with default True (mirror test_load_v010_state_file's dict-construction style). In tests/test_orchestrator.py add cases under TestMakeBridgeAllowedTools (or a sibling test) for True, False, and None-preferences default, mirroring test_mcp_repomix_with_preferences.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && .venv/bin/python -m pytest tests/test_state.py tests/test_orchestrator.py -x -q && .venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q && .venv/bin/python -m ruff check flowstate/ bench/ tests/ && .venv/bin/python -m ruff format --check flowstate/ bench/ tests/</automated>
  </verify>
  <done>
    ProjectPreferences().enable_prompt_caching_1h defaults True and is settable False; a field-less flowstate.json loads with default True (no migration error); _make_bridge reflects the preference for True/False and leaves BridgeConfig default False when preferences is None; full suite green at >=80% coverage; ruff check and format --check both pass. bridge.py, CANON, and all other files unchanged.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80` exits 0.
- `.venv/bin/python -m ruff check flowstate/ bench/ tests/` passes.
- `.venv/bin/python -m ruff format --check flowstate/ bench/ tests/` passes.
- `git diff --name-only` shows only flowstate/state.py, flowstate/orchestrator.py, tests/test_state.py, tests/test_orchestrator.py.
</verification>

<success_criteria>
- ProjectPreferences().enable_prompt_caching_1h is True by default; settable False.
- Existing flowstate.json without the field loads and yields enable_prompt_caching_1h=True (no migration error).
- _make_bridge produces a ClaudeBridge whose config.enable_prompt_caching_1h matches the preference (True and False); None preferences -> BridgeConfig default False.
- Full suite green at >=80% coverage; ruff clean.
- bridge.py untouched.
</success_criteria>

<output>
Create `.planning/quick/260616-x15-expose-enable-prompt-caching-1h-via-pref/260616-x15-SUMMARY.md` and a bare `SUMMARY.md` (per repo convention) when done, with `status: complete` frontmatter.
</output>
