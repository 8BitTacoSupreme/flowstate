---
phase: quick-260618-p97
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/grounding.py
  - bench/fixtures/rgb_probes.example.json
  - tests/test_bench_grounding.py
autonomous: true
requirements: [RGB-01, RGB-02, RGB-03, RGB-04]
must_haves:
  truths:
    - "Running with no --mode flag (default) executes today's arm loop BYTE-FOR-BYTE unchanged"
    - "`--mode rgb` dispatches to _run_rgb and never touches the arm loop"
    - "noise axis: every probe context contains its gold + exactly floor(ratio*k) distractors"
    - "negative axis: context has NO gold; correct == answer declined (judge or regex fast-path)"
    - "integration axis runs only on >=2-gold probes; others return None (skipped)"
    - "counterfactual axis computes robust vs misled; missing fields return None (skipped)"
    - "All RGB helpers never raise and need no claude binary / network / fastembed in tests"
    - "Coverage stays >=80%; ruff check + format clean"
  artifacts:
    - path: "bench/grounding.py"
      provides: "_rgb_distractors, _rgb_noise, _rgb_negative, _rgb_integration, _rgb_counterfactual, _judge_rejection, _run_rgb, --mode/--axes/--noise-ratios/--rgb-k CLI, _answer instruction= kwarg"
      contains: "def _run_rgb"
    - path: "bench/fixtures/rgb_probes.example.json"
      provides: "Example probes with gold/counterfactual/wrong_answer and >=1 multi-gold probe"
    - path: "tests/test_bench_grounding.py"
      provides: "Offline RGB tests (7 cases) with monkeypatched _answer/_factcheck/_judge_rejection"
  key_links:
    - from: "bench/grounding.py:main"
      to: "_run_rgb"
      via: "if args.mode == 'rgb' dispatch"
      pattern: "args\\.mode == \"rgb\""
    - from: "_rgb_noise / _rgb_negative / _rgb_integration / _rgb_counterfactual"
      to: "_answer / _factcheck / _judge_rejection / _wilson"
      via: "reuse of existing never-raise helpers"
      pattern: "_factcheck|_answer|_wilson|_judge_rejection"
---

<objective>
Add the RGB (Retrieval-Augmented Generation Benchmark) four-axes evaluation to
`bench/grounding.py` as an ADDITIVE `--mode rgb`. RGB measures RAG grounding under
adversarial retrieval contexts across four axes: noise robustness, negative rejection,
information integration, counterfactual robustness.

Purpose: extend the grounding harness with adversarial-context measurement without
disturbing the existing layer-comparison arm loop (none/pack/memory/wiki/full/wikirag/wikivec).

Output:
- New never-raise private helpers + `_run_rgb` dispatcher in `bench/grounding.py`
- `--mode {layers,rgb}` (default `layers`) plus RGB-only args, all additive
- `bench/fixtures/rgb_probes.example.json` showing the extended probe schema
- Offline RGB tests in `tests/test_bench_grounding.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@bench/grounding.py
@tests/test_bench_grounding.py
@/Users/jhogan/frameworx/CLAUDE.md
@/Users/jhogan/CLAUDE.md

<interfaces>
<!-- Existing contracts in bench/grounding.py the RGB code MUST reuse verbatim. -->

Module separator (used everywhere a multi-doc prefix is joined):
  "\n\n---\n\n"

_answer(prefix: str, question: str, model: str) -> str
  - Builds prompt: (prefix + "\n\n---\n\n" if prefix else "") + "Question: " + question + "\nAnswer concisely and specifically."
  - Never raises; returns "" if no claude binary or all attempts empty.
  - RGB needs prompt variants -> add optional instruction= kwarg whose DEFAULT reproduces
    today's exact "Answer concisely and specifically." trailer byte-for-byte.

_factcheck(answer: str, ground_truth: str, model: str) -> bool | None
  - YES/NO majority judge; one claude --print call; returns True/False/None. Never raises.

_judge_rejection(answer: str, model: str) -> bool | None   # NEW — mirror _factcheck
  - Asks "did the answer decline / say it lacks info? YES/NO"; one call; True/False/None.

_wilson(successes: int, n: int) -> tuple[float, float]   # reuse as-is
_load_probes(path: Path) -> list[dict] | None            # reuse as-is (extended fields are optional)
_YESNO_RE = re.compile(r"\b(yes|no)\b", re.IGNORECASE)   # reuse for regex fast-path tokens

Probe schema (existing required): {id, question, ground_truth}
Probe schema (NEW optional): gold (str | list[str]), counterfactual (str), wrong_answer (str)

main() invariants that MUST be preserved:
  - FLOWSTATE_CONTEXT_BUDGET_TOKENS save -> set -> restore in try/finally
  - _load_probes guard returning 1 on None
  - the arm loop block (lines ~344-449) BYTE-FOR-BYTE when mode != "rgb"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RGB helpers + _judge_rejection + _answer instruction kwarg</name>
  <files>bench/grounding.py, tests/test_bench_grounding.py</files>
  <read_first>
    bench/grounding.py lines 222-282 (_answer, _factcheck — mirror their never-raise/claude --print idiom),
    lines 184-219 (_load_probes, _wilson), lines 34-43 (constants, _YESNO_RE).
    tests/test_bench_grounding.py lines 100-174 (_factcheck fake-run pattern),
    lines 250-313 (end-to-end main with fake _answer/subprocess).
  </read_first>
  <behavior>
    - _rgb_distractors(probe, probes, n): returns up to n passages drawn from the `gold`
      of OTHER probes (a str gold counts as one passage; a list gold contributes each item),
      selected deterministically by probe id order, EXCLUDING self. Probes with no gold
      contribute nothing. Returns [] when no others have gold.
    - _judge_rejection("I cannot answer due to insufficient information", "m") -> True via
      regex fast-path (no subprocess). Phrases matched (case-insensitive substrings):
      "cannot answer", "insufficient information", "no information", "don't know",
      "unable to", "not enough". A non-refusal answer falls through to a one-shot YES/NO
      claude --print judge ("did the answer decline / say it lacks info?"); parse via _YESNO_RE.
      Returns True/False/None; never raises; None when no claude binary.
    - _answer(prefix, q, model): with no instruction kwarg, the produced prompt is
      byte-identical to today's. With instruction="...", the trailer string is swapped.
    - _rgb_noise(probe, probes, noise_ratio, k, answer_model, judge_models): context =
      1 gold (first gold passage) + floor(noise_ratio*k) distractors, total docs capped at k,
      GOLD ALWAYS PRESENT; join with "\n\n---\n\n"; call _answer then majority _factcheck vs
      ground_truth. Returns a per-probe record dict {probe_id, noise_ratio, n_distractors, majority}.
    - _rgb_negative(probe, probes, k, answer_model, judge_models): context = k distractors
      ONLY (no gold); _answer with a refusal-permitting instruction; score via _judge_rejection
      majority. Correct == rejecting. Returns {probe_id, rejected: bool}.
    - _rgb_integration(probe, probes, k, answer_model, judge_models): ONLY when probe gold is a
      list of >=2 passages; context = all gold + distractors up to k; majority _factcheck vs
      ground_truth. Returns None (and the caller logs "skipped: needs >=2 gold") otherwise.
    - _rgb_counterfactual(probe, answer_model, judge_models): ONLY when probe has both
      `counterfactual` and `wrong_answer`; context = the counterfactual doc; _answer with an
      instruction noting docs MAY contain errors; robust = _factcheck(answer, ground_truth) is True,
      misled = _factcheck(answer, wrong_answer) is True. Returns {probe_id, robust, misled} or
      None when fields missing.
  </behavior>
  <action>
    Add the new helpers to bench/grounding.py in a new "RGB axes" section placed AFTER
    _factcheck and BEFORE the CLI section. Do NOT alter any existing function body except
    _answer, where you add a keyword-only `instruction` parameter (signature
    `_answer(prefix, question, model, *, instruction=<today's exact trailer>)`) so existing
    positional callers stay byte-identical. The default instruction value MUST be the exact
    current string "Answer concisely and specifically." and the prompt assembly order is
    unchanged.

    Implement _judge_rejection mirroring _factcheck's claude --print one-shot structure
    (_locate_claude guard, single subprocess.run with _JUDGE_TIMEOUT, _YESNO_RE parse,
    returns True/False/None, never raises). Add a regex/substring fast-path checked BEFORE the
    subprocess so common refusals score True with zero subprocess calls; define the refusal
    phrase set as a module-level frozenset constant near _YESNO_RE.

    All _rgb_* helpers: deterministic (no random), never-raise (wrap risky logic, return safe
    default — None for skip cases, a record dict otherwise), majority computed exactly like the
    arm loop (`yes > len(judge_models)/2`, None votes count as not-yes). Distractor selection
    uses sorted-by-id order excluding self. floor via int() or math.floor on noise_ratio*k.

    Honor RGB-01 (noise), RGB-02 (negative), RGB-03 (integration), RGB-04 (counterfactual).

    Tests (ADD-ONLY, fully offline) in tests/test_bench_grounding.py:
    - test_rgb_distractors_excludes_self_deterministic_and_count
    - test_rgb_noise_context_has_gold_and_floor_distractors  (assert gold present and distractor
      count == floor(ratio*k), monkeypatch _answer/_factcheck with deterministic fakes)
    - test_rgb_negative_excludes_gold_and_scores_rejection  (regex fast-path True without
      subprocess; also a fall-through case using a fake _judge_rejection)
    - test_rgb_integration_requires_two_gold_else_none
    - test_rgb_counterfactual_robust_vs_misled_and_none_when_missing
    Monkeypatch bench.grounding._answer, ._factcheck, ._judge_rejection with deterministic fakes;
    no claude binary, no network, no subprocess for the _rgb_* paths.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_grounding.py -k "rgb or judge_rejection" -q</automated>
  </verify>
  <done>
    All five helpers + _judge_rejection exist, never raise, are deterministic; _answer with no
    instruction kwarg produces a byte-identical prompt (existing _answer tests still pass);
    new helper tests pass offline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _run_rgb dispatcher, CLI flags, aggregation + console table</name>
  <files>bench/grounding.py, tests/test_bench_grounding.py</files>
  <read_first>
    bench/grounding.py lines 290-308 (_build_parser), lines 316-457 (main: budget save/restore,
    probes guard, arm loop, aggregation, console summary).
    tests/test_bench_grounding.py lines 251-432 (end-to-end main fakes + arm-loop assertions).
  </read_first>
  <behavior>
    - --mode default "layers" -> main() runs the EXISTING arm loop with no behavioral change.
    - --mode rgb -> main() dispatches to _run_rgb(args, probes); the arm loop block does not run.
    - --axes default = all four (noise,negative,integration,counterfactual); accepts a comma subset.
    - --noise-ratios default "0.0,0.4,0.8" parsed to floats; --rgb-k default 5.
    - _run_rgb sweeps noise_ratio for the noise axis and emits a per-axis JSON block:
        noise -> {per_ratio: {<ratio>: {accuracy, n, wilson_ci}}}
        negative -> {rejection_rate, n, wilson_ci}
        integration -> {accuracy, n, wilson_ci, skipped}
        counterfactual -> {robust_rate, misled_rate, n, wilson_ci, skipped}
      plus per_probe records per axis, reusing _wilson for every CI (as [low, high]).
    - Prints a console table styled like the existing main() summary.
  </behavior>
  <action>
    Extend _build_parser: add `--mode` with choices ("layers","rgb") default "layers";
    add `--axes` (default "noise,negative,integration,counterfactual"), `--noise-ratios`
    (default "0.0,0.4,0.8"), `--rgb-k` (type int, default 5). These are additive; existing
    args (--answer-model, --judge-models, --probes, --root, etc.) are reused, not duplicated.

    In main(): keep the budget-env save/set/restore try/finally and the _load_probes guard
    EXACTLY as-is. Immediately after the probes guard, branch: `if args.mode == "rgb": return
    _run_rgb(args, probes)` — placed so the existing arm-loop block is reached only when
    mode != "rgb" and remains byte-for-byte unchanged.

    Implement _run_rgb(args, probes) -> int in a new section before main(): parse axes,
    noise_ratios, judge_models; for each requested axis iterate probes calling the matching
    _rgb_* helper (noise swept over ratios), collect records, aggregate accuracy/rate +
    _wilson CI per the JSON shape above, count skipped (None) for integration/counterfactual,
    write JSON to args.out when provided (mirror main's write-with-warning), print the console
    table, return 0 (return 1 only if every axis produced zero usable records). Never raises;
    reuse the existing constants and helpers.

    Tests (ADD-ONLY, offline):
    - test_run_rgb_end_to_end_emits_per_axis_json: tiny probe list (incl. one multi-gold and one
      with counterfactual+wrong_answer), monkeypatch _answer/_factcheck/_judge_rejection with
      deterministic fakes, run g.main(["--mode","rgb",...,"--out",...]); assert the four axis
      blocks exist with expected keys (per_ratio, rejection_rate, integration.skipped,
      counterfactual.robust_rate/misled_rate) and Wilson CIs present.
    - test_mode_layers_default_runs_arm_loop_unchanged: run g.main without --mode (default) on a
      single probe with stubbed build_context_prefix/_answer/_factcheck and assert the output has
      the "arms" block (proving the layers path, not RGB, executed).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_grounding.py -q</automated>
  </verify>
  <done>
    `--mode rgb` produces the four-axis JSON; default/`--mode layers` still emits the arms block;
    all existing grounding tests pass unchanged; _run_rgb never raises.
  </done>
</task>

<task type="auto">
  <name>Task 3: Example fixture, module docstring, full gates</name>
  <files>bench/fixtures/rgb_probes.example.json, bench/grounding.py</files>
  <read_first>
    bench/grounding.py lines 1-15 (module docstring to extend).
  </read_first>
  <action>
    Create bench/fixtures/rgb_probes.example.json: a small valid probe list (3-4 probes) using
    the extended schema. Include at least one probe with a string `gold`, at least one probe with
    `counterfactual` + `wrong_answer`, and at least one multi-gold probe whose `gold` is a list of
    >=2 passages (for the integration axis). Every probe keeps the required id/question/ground_truth.
    Use realistic Kafka/Confluent-flavored content (no foo/bar). Valid JSON, ruff-irrelevant.

    Extend the bench/grounding.py module docstring (top-of-file) to document `--mode rgb` and the
    four axes (noise / negative / integration / counterfactual), noting it is additive and that
    fastembed/sqlite_vec are NOT required for RGB. Keep the existing ADD-ONLY / never-raises /
    stdlib-only wording intact; append, do not rewrite.

    Run the full gates and fix any ruff/line-length(100)/double-quote/coverage issues.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('bench/fixtures/rgb_probes.example.json').read_text()); assert isinstance(d,list) and any(isinstance(p.get('gold'),list) and len(p['gold'])>=2 for p) and any('counterfactual' in p and 'wrong_answer' in p for p), 'fixture missing multi-gold or counterfactual probe'" && .venv/bin/ruff check bench/grounding.py tests/test_bench_grounding.py && .venv/bin/ruff format --check bench/grounding.py tests/test_bench_grounding.py && .venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <done>
    Fixture is valid JSON with a multi-gold probe and a counterfactual probe; docstring documents
    --mode rgb and the four axes; ruff check + format clean; full suite passes with coverage >=80%.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probes JSON file -> _load_probes | Untrusted file contents parsed as probe dicts |
| answer/judge text -> claude --print | Text crosses into subprocess args (offline in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p97-01 | Tampering | malformed/extended probe JSON | mitigate | _load_probes already never-raises; RGB treats gold/counterfactual/wrong_answer as OPTIONAL and skips (returns None) when absent — no KeyError path |
| T-p97-02 | Denial of Service | claude --print hang in _judge_rejection | mitigate | reuse _JUDGE_TIMEOUT one-shot pattern; regex fast-path short-circuits common refusals with zero subprocess |
| T-p97-03 | Information Disclosure | RGB perturbs existing layers behavior | accept | additive only; `--mode` defaults to layers and the arm-loop block is preserved byte-for-byte; guard test enforces it |
| T-p97-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies (stdlib-only; fastembed/sqlite_vec untouched and not required for RGB) |
</threat_model>

<verification>
- Existing arm-loop tests pass unchanged (no regression to layers mode).
- `--mode rgb` end-to-end test produces the four-axis JSON with Wilson CIs.
- All RGB helpers exercised offline (no claude binary / network / fastembed).
- `.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0.
- ruff check + ruff format --check clean on edited files.
</verification>

<success_criteria>
- bench/grounding.py gains _rgb_distractors, _rgb_noise, _rgb_negative, _rgb_integration,
  _rgb_counterfactual, _judge_rejection, _run_rgb and the new CLI flags — all additive.
- `_answer` gains an `instruction=` kwarg whose default keeps existing callers byte-identical.
- Default invocation (no --mode) runs today's arm loop unchanged.
- bench/fixtures/rgb_probes.example.json exists with a multi-gold probe and a counterfactual probe.
- Module docstring documents `--mode rgb` and the four axes.
- Full suite green, coverage >=80%, ruff clean.
</success_criteria>

<output>
Create `.planning/quick/260618-p97-wire-rgb-four-axes-into-bench-grounding-/SUMMARY.md` and
`.planning/quick/260618-p97-wire-rgb-four-axes-into-bench-grounding-/260618-p97-SUMMARY.md`
when done (status: complete).
</output>
