---
phase: quick-260615-d4p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/context_prefix.py
  - bench/compound_eval.py
  - bench/replicate.py
  - bench/judge.py
  - bench/wikigen.py
  - tests/test_context_prefix.py
  - tests/test_bench_judge.py
  - tests/test_bench_replicate.py
  - tests/test_bench_wikigen.py
autonomous: true
requirements: [260615-d4p]

must_haves:
  truths:
    - "include_layers=None produces byte-identical output to current behavior (no wiki, golden test unchanged)"
    - "include_layers={'fixtures','wiki'} with wiki.md present emits a '## Codebase Wiki' layer between fixtures and pack"
    - "bench --layers wiki is accepted by compound_eval and replicate argparse"
    - "judge_run retries up to 3 times on an unparseable score and returns the first good score"
    - "python -m bench.wikigen generates .planning/codebase/wiki.md from the repomix pack via one claude call"
  artifacts:
    - path: "flowstate/context_prefix.py"
      provides: "_WIKI_PATH const, _read_wiki_layer helper, opt-in wiki layer in build_context_prefix"
      contains: "_read_wiki_layer"
    - path: "bench/wikigen.py"
      provides: "one-time distilled-CAG wiki generator (main, argparse, single claude call)"
      contains: "def main"
    - path: "bench/judge.py"
      provides: "_JUDGE_MAX_ATTEMPTS bounded retry in judge_run"
      contains: "_JUDGE_MAX_ATTEMPTS"
  key_links:
    - from: "bench/compound_eval.py::_LAYERS_MAP"
      to: "flowstate/context_prefix.py::build_context_prefix include_layers"
      via: "frozenset({'fixtures','wiki'})"
      pattern: "_LAYERS_MAP\\[.wiki.\\]"
    - from: "flowstate/context_prefix.py::build_context_prefix"
      to: "_read_wiki_layer"
      via: "wiki_included gate (include_layers is not None and 'wiki' in include_layers)"
      pattern: "wiki_included"
---

<objective>
Add a `wiki` arm (distilled-CAG) to the bench and a judge retry to stop trial-voiding.

Five components: an opt-in `wiki` context layer in `context_prefix.py`, bench-arm wiring
in `compound_eval.py` + `replicate.py`, a bounded retry in `judge.py`, and a new one-time
`bench/wikigen.py` generator. All four edited files plus the new module get tests.

Purpose: enable a distilled-architecture-wiki context arm for the compounding bench, and
fix trial-voiding at the source (judge returning None) via a bounded retry rather than by
dropping runs (which would misalign replicate's per-run paired trajectory indices).

Output: edited context_prefix.py, compound_eval.py, replicate.py, judge.py; new
bench/wikigen.py; new/extended tests; full suite + ruff green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./.claude/CLAUDE.md

@flowstate/context_prefix.py
@bench/compound_eval.py
@bench/replicate.py
@bench/judge.py
@flowstate/tools/research.py
@tests/test_bench_judge.py
@tests/test_context_prefix.py
@tests/test_bench_replicate.py

<interfaces>
<!-- Extracted contracts the executor needs — no codebase exploration required. -->

context_prefix.py existing shape (flowstate/context_prefix.py):
- Consts (~line 50): `_SEPARATOR`, `_PACK_PATH = ".planning/codebase/repomix-pack.xml"`, `_FIXTURE_PATH`, `_CHARS_PER_TOKEN`.
- `_read_pack_layer(root: Path) -> str` (~283): `pack_path = root / _PACK_PATH; if not pack_path.exists(): return ""; try: return pack_path.read_text() except Exception: return ""`. (Note: fixtures returns a heading-wrapped block; pack returns verbatim. Wiki must return a heading-wrapped block per task spec.)
- `build_context_prefix(root, memory, query, *, budget_tokens=None, include_layers: frozenset[str] | None = None, console=None) -> str` (~302).
- `_included(key)` (~358): `return include_layers is None or key in include_layers`. DO NOT route wiki through this — it would include wiki on the default path.
- Layer locals built ~362-373: `fixtures_layer`, `gotchas_layer`, `memory_layer`, `since_last_run_layer`.
- Pack fit-ladder ~375-432: builds `candidate` / `candidate2` via `_SEPARATOR.join(filter(None, [fixtures_layer, pack_raw, gotchas_layer, memory_layer, since_last_run_layer]))`.
- Final budget guard ~437-456: `full_assembly = _SEPARATOR.join(filter(None, [fixtures_layer, pack_layer, gotchas_layer, memory_layer, since_last_run_layer]))`.
- Final list ~459: `layers = [fixtures_layer, pack_layer, gotchas_layer, memory_layer, since_last_run_layer]`.

judge.py existing shape (bench/judge.py):
- `_locate_claude() -> str | None` (~39, stdlib-only, never raises).
- `_parse_score(out: str) -> tuple[float | None, str]` (~92).
- `judge_run(run_index, artifacts, fixture, *, model=None) -> JudgeResult` (~104):
  early return when `not claude or not artifacts.strip()`; builds `cmd`; single
  `subprocess.run(cmd, capture_output=True, text=True, timeout=_JUDGE_TIMEOUT)`;
  `_parse_score(proc.stdout)`; returns `JudgeResult(run_index, score, rationale)`.
- `JudgeResult(run_index: int, score: float | None, rationale: str)`.

research retry idiom to mirror (flowstate/tools/research.py ~101):
```
for _ in range(_RESEARCH_MAX_ATTEMPTS):
    br = self.bridge.run(...)
    if br.success and br.output.strip():
        sections.append(br.output.strip()); break
else:
    sections.append(f"## {topic}\n\n*Research failed...*")
```

compound_eval.py (~60): `_LAYERS_MAP: dict[str, frozenset[str] | None]` with keys
`full`/`none`/`pack`/`memory`; `--layers` choices `("full","none","pack","memory")` (~76).

replicate.py (~100): `--layers nargs="+" choices=("full","pack","memory","none")
default=["full","pack","memory","none"]`.

judge test mocking idiom (tests/test_bench_judge.py ~52):
```
monkeypatch.setattr(judge_mod, "_locate_claude", lambda: "/bin/claude")
class _P: stdout = '{"score": 8, "rationale": "specific"}'
monkeypatch.setattr(subprocess, "run", lambda *a, **k: _P())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add opt-in wiki layer to context_prefix.py + tests</name>
  <files>flowstate/context_prefix.py, tests/test_context_prefix.py</files>
  <behavior>
    - include_layers=None with wiki.md present on disk → output contains NO "## Codebase Wiki" (default path unchanged).
    - Existing byte-identical golden test → still passes with ZERO edits to it.
    - include_layers=frozenset({"fixtures","wiki"}) with wiki.md present → output contains "## Codebase Wiki" AND fixtures content, but NOT pack/gotchas/memory/since_last_run content.
    - include_layers=frozenset({"fixtures","wiki"}) with wiki.md ABSENT → wiki omitted, no exception, fixtures still present.
    - _read_wiki_layer on absent file or read error → returns "" (never raises).
    - Ordering: wiki appears immediately AFTER fixtures and BEFORE pack when both present.
  </behavior>
  <action>
    Add const `_WIKI_PATH = ".planning/codebase/wiki.md"` alongside `_PACK_PATH` (~line 52).

    Add `_read_wiki_layer(root: Path) -> str` mirroring `_read_pack_layer` (place it right
    after `_read_pack_layer`, ~295): build `wiki_path = root / _WIKI_PATH`; if not exists
    return ""; `try: text = wiki_path.read_text()` wrapped so ANY exception returns "".
    When `text` (after read) is non-empty, return `"## Codebase Wiki\n\n" + text`; else "".
    Strip is NOT required by spec — use the raw text for the non-empty check via
    `if text:` after read (return "" when falsy). Never raise.

    In `build_context_prefix`, do NOT route wiki through `_included`. Immediately after the
    fixtures_layer line (~362) add a SEPARATE gate:
    `wiki_included = include_layers is not None and "wiki" in include_layers`
    then `wiki_layer = _read_wiki_layer(root) if wiki_included else ""`.

    Insert `wiki_layer` into the pack-layer order (after fixtures, before pack) in ALL three
    assembly sites:
    (1) the fit-ladder `candidate` list → `[fixtures_layer, wiki_layer, pack_raw, gotchas_layer, memory_layer, since_last_run_layer]`
    (2) the fit-ladder `candidate2` list → same insertion (`wiki_layer` before `pack_compressed`)
    (3) the final budget-guard `full_assembly` (~437) → `[fixtures_layer, wiki_layer, pack_layer, gotchas_layer, memory_layer, since_last_run_layer]`; and the intermediate re-joins inside the since-last-run / gotchas drop branches must also carry `wiki_layer` after `fixtures_layer`.
    (4) the final `layers` list (~459) → `[fixtures_layer, wiki_layer, pack_layer, gotchas_layer, memory_layer, since_last_run_layer]`.

    INVARIANT: when include_layers is None, wiki_included is False, wiki_layer is "", and
    filter(None, ...) drops it everywhere → output byte-identical to current. Do not touch
    `_included`, the docstring layer list, or any other layer's logic.

    Tests in tests/test_context_prefix.py (mirror existing fixture/mock style): write a
    wiki.md under `<root>/.planning/codebase/`, then assert the four behaviors above. Add a
    direct `_read_wiki_layer` unit test for absent-file → "".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_context_prefix.py -x -q</automated>
  </verify>
  <done>New wiki tests pass; the pre-existing byte-identical golden test passes UNCHANGED; _read_wiki_layer never raises on missing/unreadable file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire wiki arm into compound_eval + replicate, add judge retry + tests</name>
  <files>bench/compound_eval.py, bench/replicate.py, bench/judge.py, tests/test_bench_judge.py, tests/test_bench_replicate.py</files>
  <behavior>
    - compound_eval: _LAYERS_MAP["wiki"] == frozenset({"fixtures","wiki"}); argparse accepts --layers wiki.
    - replicate: argparse accepts --layers wiki (default arm list UNCHANGED — no "wiki" added to default).
    - judge_run: bad-then-good subprocess (side_effect) → returns the good score, exactly 2 subprocess.run calls.
    - judge_run: first-try-good → exactly 1 subprocess.run call.
    - judge_run: all-bad over 3 attempts → score is None after exactly 3 calls, rationale is the last "(no rationale parsed)".
    - judge_run: subprocess raising on an attempt counts as a failed attempt and retries; never raises.
    - judge_run: existing early-return (no claude / no artifacts → None) still holds, 0 subprocess calls.
  </behavior>
  <action>
    bench/compound_eval.py: add `"wiki": frozenset({"fixtures", "wiki"})` to `_LAYERS_MAP`
    (~60). Add `"wiki"` to the `--layers` `choices` tuple (~76) and append a one-line help
    fragment (e.g. "wiki=fixtures+wiki (distilled-CAG only).").

    bench/replicate.py: add `"wiki"` to the `--layers` `choices` tuple (~103). Leave the
    `default=[...]` list unchanged (no "wiki").

    bench/judge.py: add const `_JUDGE_MAX_ATTEMPTS = 3` near `_JUDGE_TIMEOUT` (~25). In
    `judge_run`, keep the existing early return for no-claude/no-artifacts. Replace the
    single try/except subprocess block with a bounded loop mirroring research.py's idiom:
    keep `last_rationale = "(no rationale parsed)"`; `for _ in range(_JUDGE_MAX_ATTEMPTS):`
    run `subprocess.run(cmd, ...)` inside try/except; on success call `_parse_score(proc.stdout)`;
    if score is not None → `return JudgeResult(run_index, score, rationale or "(no rationale parsed)")`;
    if score is None set `last_rationale = rationale or last_rationale` and continue; on
    subprocess Exception set `last_rationale = f"judge error: {exc}"` and continue. After the
    loop, `return JudgeResult(run_index, None, last_rationale)`. Do NOT modify _build_prompt,
    _parse_score, _locate_claude, collect_artifacts, summarize, the prompt, or cmd construction.
    Preserve never-raises.

    Tests:
    - tests/test_bench_judge.py: use `subprocess.run` `side_effect` (a list / callable) per the
      existing monkeypatch idiom. Add: bad-then-good (assert score returned + call count == 2);
      first-try-good (call count == 1); all-bad-3x (score None + call count == 3); a subprocess
      raising on attempt 1 then good on attempt 2 (assert score returned, never raises). Use a
      counter (e.g. a mutable list or `unittest.mock.Mock`) to assert call counts.
    - tests/test_bench_replicate.py: add a parser test asserting `--layers wiki` parses (mirror
      any existing arg-parse test there; if none, build the parser and parse_args(["--root",".","--layers","wiki"])).
    - Add a compound_eval _LAYERS_MAP assertion (in test_bench_judge.py is wrong file — put the
      compound_eval assertion in the bench test module that covers compound_eval; if compound_eval
      has its own test file use it, otherwise add a small test asserting
      `compound_eval._LAYERS_MAP["wiki"] == frozenset({"fixtures","wiki"})` in test_bench_replicate.py
      or a dedicated test_bench_compound module — check which exists first and place accordingly).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_judge.py tests/test_bench_replicate.py -x -q</automated>
  </verify>
  <done>--layers wiki accepted by both parsers; _LAYERS_MAP["wiki"] correct; judge retry call-count tests pass; judge never raises; replicate default arm list unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New bench/wikigen.py generator + tests</name>
  <files>bench/wikigen.py, tests/test_bench_wikigen.py</files>
  <behavior>
    - Missing pack (<root>/.planning/codebase/repomix-pack.xml absent) → prints a clear message, returns non-zero, makes NO subprocess call.
    - Existing wiki.md without --force → prints "wiki up to date; skipping (use --force)", returns 0, NO subprocess call.
    - Existing wiki.md WITH --force → proceeds to subprocess.
    - claude not found (_locate_claude → None) → clear message, non-zero, no write.
    - Mocked subprocess returncode==0 + non-empty stdout → writes stdout to <root>/.planning/codebase/wiki.md, prints the path, returns 0.
    - Mocked subprocess returncode!=0 OR empty stdout → clear error, non-zero, no write.
    - Subprocess raising → caught, clear error, non-zero, never raises.
    - Pack text > 120000 chars is truncated before embedding in the prompt.
  </behavior>
  <action>
    Create bench/wikigen.py (research tooling; no production CLI registration; no UI). Header
    docstring noting it is a one-time distilled-CAG generator. stdlib + subprocess only — no
    new runtime deps. Reuse judge's claude locator: `from bench.judge import _locate_claude`
    (importing it is acceptable per spec).

    Consts: `_PACK_REL = ".planning/codebase/repomix-pack.xml"`,
    `_WIKI_REL = ".planning/codebase/wiki.md"`, `_MAX_PACK_CHARS = 120_000`,
    `_WIKIGEN_TIMEOUT` (reuse a sane value, e.g. 600). Define `PROMPT_HEADER` (the exact intent
    string below) and build the final prompt as `PROMPT_HEADER + pack_text` where pack_text is
    truncated to `_MAX_PACK_CHARS`.

    PROMPT_HEADER (exact): "Produce a dense, high-signal architecture wiki for this codebase,
    to be used as durable LLM context (not prose for humans). Sections: (1) one-paragraph
    system overview; (2) module/architecture map — per module: responsibility, key files
    (paths), key public functions/classes; (3) control/data flow; (4) key abstractions &
    invariants; (5) gotchas/constraints. Use concrete names and paths from the code. No filler.
    Markdown. Target ~3000-6000 tokens.\n\nCODEBASE PACK:\n\n"

    `main(argv=None) -> int`:
    1. argparse: `--root` (type=Path, required=True), `--force` (store_true),
       `--model` (default="opus").
    2. pack_path = root / _PACK_REL; if not exists → print clear message, return 1 (no subprocess).
    3. wiki_path = root / _WIKI_REL; if wiki_path.exists() and not args.force → print
       "wiki up to date; skipping (use --force)", return 0 (no subprocess).
    4. claude = _locate_claude(); if not claude → print clear message, return 1.
    5. Read pack text (try/except → on read failure clear message, return 1); truncate to
       _MAX_PACK_CHARS; prompt = PROMPT_HEADER + pack_text.
    6. cmd = `[claude, "--print", "--max-turns", "1", "--model", args.model, "--", prompt]`
       (no web tools). Wrap subprocess.run in try/except (never raise). If returncode == 0 and
       stdout.strip() → wiki_path.parent.mkdir(parents=True, exist_ok=True);
       wiki_path.write_text(stdout); print the wiki_path; return 0. Else → clear error
       (include returncode / "(empty output)"), return 1.

    End with `if __name__ == "__main__": sys.exit(main())` so `python -m bench.wikigen` works.

    Tests in tests/test_bench_wikigen.py (mirror tests/test_bench_judge.py monkeypatch style):
    monkeypatch `_locate_claude` on the wikigen module (since it imports the symbol, patch
    `wikigen_mod._locate_claude`) and `subprocess.run`. Cover every behavior above; use a Mock
    or counter to assert subprocess is NOT called in the missing-pack and skip-without-force
    cases. For the truncation test, write a >120000-char pack and assert the prompt passed to
    subprocess.run is truncated (capture cmd via the mock).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_wikigen.py -x -q</automated>
  </verify>
  <done>wikigen writes wiki.md from mocked stdout; missing-pack and skip-without-force make no subprocess call and exit non-zero/zero respectively; runs via `python -m bench.wikigen`; never raises.</done>
</task>

</tasks>

<verification>
Full suite + coverage gate, then ruff lint + format check:

```
.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80
.venv/bin/python -m ruff check flowstate/ bench/ tests/
.venv/bin/python -m ruff format --check
```

All three must exit 0.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench/wikigen → claude subprocess | repomix pack content embedded into a prompt and passed to the local claude CLI; output written to disk |
| context_prefix wiki layer → downstream LLM prompt | wiki.md content read verbatim and injected into the CAG prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d4p-01 | Denial of Service | wikigen pack embedding | mitigate | truncate pack to 120000 chars before prompt build; bounded subprocess timeout |
| T-d4p-02 | Tampering | wikigen file write | accept | local research tooling; writes only to <root>/.planning/codebase/wiki.md under operator-supplied --root |
| T-d4p-03 | Information Disclosure | wiki layer injection | accept | wiki.md is operator-generated local artifact; opt-in only (include_layers must explicitly request "wiki") |
| T-d4p-SC | Tampering | npm/pip/cargo installs | mitigate | no new runtime deps added; nothing to install |
</threat_model>

<success_criteria>
- context_prefix: existing byte-identical golden test passes UNCHANGED.
- context_prefix: include_layers=None excludes wiki even when wiki.md exists (no "## Codebase Wiki").
- context_prefix: include_layers=frozenset({"fixtures","wiki"}) with wiki.md present → output has "## Codebase Wiki" + fixtures, NOT pack/gotchas/memory; wiki.md absent → omitted, no error.
- compound_eval: _LAYERS_MAP["wiki"] == frozenset({"fixtures","wiki"}); --layers wiki accepted.
- replicate: --layers wiki accepted; default arm list unchanged.
- judge: bad-then-good → 2 calls + score; first-try-good → 1 call; all-bad → None after 3 calls; never raises.
- wikigen: mocked subprocess → writes wiki.md; missing pack → non-zero + message, no subprocess; existing wiki.md w/o --force → skipped, no subprocess.
- `.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80` exits 0.
- `.venv/bin/python -m ruff check flowstate/ bench/ tests/` and `ruff format --check` pass.
- No new runtime deps; never-raises preserved in bench + context_prefix; no UI; strategy/gsd adapters, RESEARCH_SYSTEM_PROMPT, _split_topics, scaffold untouched.
</success_criteria>

<output>
Create `.planning/quick/260615-d4p-wiki-arm-distilled-cag-in-bench-judge-re/SUMMARY.md` and `.planning/quick/260615-d4p-wiki-arm-distilled-cag-in-bench-judge-re/260615-d4p-SUMMARY.md` when done (both forms per repo convention), with `status:` frontmatter.

Atomic commits — one per task:
- `feat(context): add opt-in wiki layer to context_prefix`
- `feat(bench): wire wiki arm + bounded judge retry`
- `feat(bench): add wikigen distilled-CAG generator`
</output>
