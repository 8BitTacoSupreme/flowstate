---
phase: quick-260613-ga5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/context_prefix.py
  - tests/test_context_prefix.py
  - bench/compound_eval.py
  - bench/replicate.py
  - tests/test_bench_judge.py
  - tests/test_bench_replicate.py
autonomous: true
requirements: [GA5-LAYERS, GA5-PAIRED]

must_haves:
  truths:
    - "build_context_prefix(..., include_layers=None) is byte-identical to the no-kwarg call"
    - "include_layers={subset} assembles ONLY the named layers; others resolve to empty"
    - "Layer selection is gated at assembly time, not by re-splitting the joined string"
    - "compound_eval exposes --layers {full,none,pack,memory} mapped to include_layers"
    - "_run_one installs/restores the build_context_prefix patch for every non-full mode"
    - "replicate loops over --layers arms and supports --paired within-trial normalization"
    - "Cohen's d is computed per arm vs the none arm"
  artifacts:
    - path: "flowstate/context_prefix.py"
      provides: "build_context_prefix with include_layers kwarg, assembly-time gating"
      contains: "include_layers"
    - path: "bench/compound_eval.py"
      provides: "--layers option threaded through _run_one and _real_loop"
      contains: "--layers"
    - path: "bench/replicate.py"
      provides: "multi-arm loop, --paired normalization, per-arm Cohen's d"
      contains: "--paired"
    - path: "tests/test_bench_replicate.py"
      provides: "unit tests for paired normalization and arm aggregation"
  key_links:
    - from: "bench/compound_eval.py::_run_one"
      to: "flowstate.orchestrator.build_context_prefix"
      via: "wrapper monkeypatch injecting include_layers kwarg"
      pattern: "include_layers"
    - from: "bench/compound_eval.py::_run_one"
      to: "flowstate/context_prefix.py::build_context_prefix"
      via: "_LAYERS_MAP mode -> include_layers frozenset|None"
      pattern: "_LAYERS_MAP|include_layers"
---

<objective>
Make the bench harness attribute output-quality gains to individual context layers
(RAG/pack vs compounding/memory) with cross-arm baseline noise removed.

Three source modules + tests, per bench/PAIRED_DESIGN_RUNBOOK.md "Prerequisite code
changes" 1 and 2. This is a precise, pre-designed change — implement it faithfully.

CRITICAL DESIGN OVERRIDE: The runbook (section 1) suggests filtering layers by splitting
the assembled string on `_SEPARATOR` and dropping `## ` heading blocks. DO NOT do that —
it is fragile (pack XML and memory markdown can themselves contain `"\n\n---\n\n"`).
Instead gate each layer at ASSEMBLY TIME inside build_context_prefix via an additive
`include_layers` kwarg. This task_specification supersedes the runbook prose.

Purpose: enable per-layer A/B attribution (pack vs memory) and paired-design statistics.
Output: include_layers kwarg in flowstate/, --layers wiring in compound_eval, --paired +
multi-arm + per-arm Cohen's d in replicate, with mirrored tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./.claude/CLAUDE.md
@bench/PAIRED_DESIGN_RUNBOOK.md

<interfaces>
<!-- Current build_context_prefix signature (flowstate/context_prefix.py:285) -->
def build_context_prefix(
    root: Path,
    memory: Any,
    query: str,
    *,
    budget_tokens: int | None = None,
    console: Console | None = None,
) -> str

<!-- The five layer source variables built inside build_context_prefix and their keys: -->
<!--   fixtures_layer        ("fixtures")      <- _read_fixtures_layer(root) -->
<!--   pack_layer            ("pack")          <- _read_pack_layer + fit-ladder -->
<!--   gotchas_layer         ("gotchas")       <- _read_gotchas_layer(root, memory) -->
<!--   memory_layer          ("memory")        <- memory.get_context(query) if query else "" -->
<!--   since_last_run_layer  ("since_last_run")<- _read_since_last_run_layer(root, memory) -->
<!-- Final assembly: _SEPARATOR.join([l for l in [fixtures,pack,gotchas,memory,since_last_run] if l]) -->

<!-- Current _run_one signature (bench/compound_eval.py:124): -->
def _run_one(root: Path, *, dry_run: bool, inject: bool = True) -> None
<!-- inject=False path monkeypatches orch.build_context_prefix to `lambda *a,**k: ""` in try/finally. -->

<!-- Existing mirror-target test (tests/test_bench_judge.py:100): -->
<!--   test_run_one_inject_off_suppresses_and_restores -->
<!--   asserts seen["p"] == "" during run AND orch.build_context_prefix is orig after. -->

<!-- Current replicate (bench/replicate.py): _run_trial(inject, runs, root, label), -->
<!--   _agg(trials)->dict, _cohens_d(on, off)->float|None, main loops over ("on","off"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add include_layers assembly-time gating to build_context_prefix + golden/per-layer tests</name>
  <read_first>
    - flowstate/context_prefix.py (whole file; focus on build_context_prefix lines ~285-429 and the layer reader helpers)
    - tests/test_context_prefix.py lines 1-60 (fixtures: _make_pack_file, _make_fixture_file, _make_memory_stub, _make_run_entry) plus any existing byte-identical test to mirror its style
  </read_first>
  <behavior>
    - Golden: build_context_prefix(root, memory, query, include_layers=None) is byte-identical
      to build_context_prefix(root, memory, query) for a root with multiple layers present.
    - include_layers=frozenset({"fixtures","pack"}): result contains the fixtures heading and
      pack content; contains NO gotchas/memory/since-last-run content.
    - include_layers=frozenset({"gotchas","memory","since_last_run"}): result contains those
      layers; contains NO fixtures heading and NO pack XML.
    - include_layers=frozenset(): returns "" (empty prefix).
    - Excluded layers are not built/assembled (assembly-time gate), and the pack fit-ladder +
      final budget guard still operate correctly over whatever subset is selected.
    - Never raises on missing artifacts (preserve existing discipline).
  </behavior>
  <action>
    Add an ADDITIVE keyword-only parameter to build_context_prefix:
    `include_layers: frozenset[str] | None = None` (place after `budget_tokens`, before
    `console`, or anywhere in the keyword-only block — additive, no positional change).

    Gate at ASSEMBLY TIME. The cleanest approach with zero default-path drift: define a
    local helper or inline guard `included(key) -> bool` that returns True when
    `include_layers is None or key in include_layers`. Then:
      - fixtures_layer = _read_fixtures_layer(root) if included("fixtures") else ""
      - gotchas_layer  = _read_gotchas_layer(root, memory) if included("gotchas") else ""
      - memory_layer   = (memory.get_context(query) if query else "") if included("memory") else ""
      - since_last_run_layer = _read_since_last_run_layer(root, memory) if included("since_last_run") else ""
      - For pack: keep `pack_exists = pack_path.exists()`, but skip the entire pack block
        (set pack_layer = "") when not included("pack"). Guard with
        `if pack_exists and included("pack"):` so run_pack/compress is never invoked for an
        excluded pack. The fit-ladder candidate estimates already filter empties via
        `filter(None, [...])`, so excluded (now empty) layers naturally drop out of budget
        accounting and the final guard.

    Layer keys are EXACTLY: "fixtures", "pack", "gotchas", "memory", "since_last_run".
    When include_layers is None, every guard short-circuits to the existing behavior — the
    default code path must be byte-identical (no reordering of layer builds, no new joins).
    DO NOT split/re-join on _SEPARATOR. DO NOT post-filter the assembled string.
    Update the docstring Args section to document include_layers (None = all layers).

    Add tests to tests/test_context_prefix.py mirroring existing fixture helpers:
      - test_include_layers_none_is_byte_identical
      - test_include_layers_pack_only_excludes_compounding
      - test_include_layers_memory_only_excludes_pack_and_fixtures
      - test_include_layers_empty_frozenset_returns_empty
    Reuse _make_pack_file, _make_fixture_file, _make_memory_stub, _make_run_entry. For the
    memory-only test, give the memory stub a non-empty get_context return ("## Prior Knowledge\n...")
    and at least one RUN entry so since_last_run is non-empty; assert pack XML substring absent.
  </action>
  <files>flowstate/context_prefix.py, tests/test_context_prefix.py</files>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_context_prefix.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_context_prefix.py -x -q` exits 0
    - build_context_prefix(root, mem, q, include_layers=None) == build_context_prefix(root, mem, q) (string equality)
    - pack-only result has no "## Gotchas"/"## Prior Knowledge"/"## Since Last Run"; memory-only result has no "## Eval Fixtures" and no pack XML substring
    - frozenset() selection returns ""
  </acceptance_criteria>
  <done>include_layers added as additive keyword-only param; assembly-time gating implemented; default path byte-identical; four new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace --inject with --layers in compound_eval, thread through _run_one/_real_loop + per-arm tests</name>
  <read_first>
    - bench/compound_eval.py (whole file; focus on _build_parser ~56-83, _run_one ~124-145, _real_loop ~181-230, main ~250-282)
    - tests/test_bench_judge.py lines 100-124 (test_run_one_inject_off_suppresses_and_restores — the mirror target)
  </read_first>
  <behavior>
    - argparse exposes --layers with choices {full,none,pack,memory}, default "full"; --inject removed.
    - Mode -> include_layers mapping:
        full   -> None (all layers; no patch — call run_pipeline directly)
        none   -> frozenset() (empty prefix; == old inject=off)
        pack   -> frozenset({"fixtures","pack"})
        memory -> frozenset({"gotchas","memory","since_last_run"})
    - For each non-full mode, _run_one monkeypatches orch.build_context_prefix with a WRAPPER
      that calls the ORIGINAL build_context_prefix with include_layers injected, in try/finally,
      restoring the original afterward.
    - Per-arm unit tests (mirroring test_run_one_inject_off_suppresses_and_restores) for
      full/none/pack/memory: _run_one produces exactly the expected layer set and restores the patch.
    - Never raises (preserve discipline).
  </behavior>
  <action>
    In _build_parser: delete the --inject argument; add
      parser.add_argument("--layers", choices=("full","none","pack","memory"), default="full",
        help="real mode: per-layer context attribution. full=all, none=control (empty prefix), "
             "pack=fixtures+pack (RAG), memory=gotchas+memory+since_last_run (compounding).")

    Add a module-level mapping near _PROBE_QUERY:
      _LAYERS_MAP: dict[str, frozenset[str] | None] = {
          "full": None,
          "none": frozenset(),
          "pack": frozenset({"fixtures", "pack"}),
          "memory": frozenset({"gotchas", "memory", "since_last_run"}),
      }

    Rewrite _run_one to take the layer mode instead of `inject: bool`. New signature:
      def _run_one(root: Path, *, dry_run: bool, layers: str = "full") -> None
    Body:
      - load state, set dry_run as today.
      - include = _LAYERS_MAP[layers]
      - if include is None: orch.run_pipeline(state, root); return  (full — no patch)
      - else: original = orch.build_context_prefix
        define wrapper that forwards to original with include_layers=include:
          def _wrapped(*a, **k):
              k.setdefault("include_layers", include)  # respect explicit kwarg if passed
              return original(*a, **k)
        orch.build_context_prefix = _wrapped
        try: orch.run_pipeline(state, root) finally: orch.build_context_prefix = original
      (Note: full path keeps cheap _cheap_loop's `_run_one(target, dry_run=True)` working since
       layers defaults to "full".)

    Update _real_loop: replace the `inject: bool = True` param with `layers: str = "full"` and
    pass `layers=layers` into _run_one. Update its call site in main().

    Update main(): replace `inject=(args.inject == "on")` with `layers=args.layers`.

    Add tests to tests/test_bench_judge.py mirroring test_run_one_inject_off_suppresses_and_restores.
    Use the same monkeypatch pattern: patch fstate.load_state to a stub _State with
    preferences.dry_run, and patch orch.run_pipeline to a lambda that records
    orch.build_context_prefix(root, mem, "q", ...) output into a dict. For each arm, supply a
    fake build_context_prefix as the ORIGINAL (so the wrapper calls into a controllable double)
    OR call the real one against a tmp root with fixtures/pack/memory present and assert the
    selected-vs-excluded layers — mirror whichever the existing test's style supports most simply.
    Required assertions per arm:
      - full: orch.build_context_prefix NOT patched during run (is orig inside run_pipeline) and is orig after.
      - none/pack/memory: during run the effective prefix equals what the original returns with
        include_layers == _LAYERS_MAP[arm]; AND orch.build_context_prefix is restored to orig after _run_one returns.
    Suggested test names: test_run_one_layers_full_no_patch, test_run_one_layers_none_empty_prefix,
    test_run_one_layers_pack_selects_rag, test_run_one_layers_memory_selects_compounding.
  </action>
  <files>bench/compound_eval.py, tests/test_bench_judge.py</files>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_judge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `--inject` no longer accepted; `--layers` with the four choices is parsed (default full)
    - `.venv/bin/python -m pytest tests/test_bench_judge.py -x -q` exits 0
    - For full: orch.build_context_prefix is unpatched during the run and restored after
    - For none/pack/memory: wrapper injects the correct include_layers frozenset and patch is restored after _run_one
    - _cheap_loop still runs (defaults layers="full")
  </acceptance_criteria>
  <done>--inject replaced by --layers end-to-end; _run_one wrapper injects include_layers and restores patch; four mirrored per-arm tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-arm loop + --paired normalization + per-arm Cohen's d in replicate + tests</name>
  <read_first>
    - bench/replicate.py (whole file: _run_trial, _agg, _cohens_d, main)
    - tests/test_bench_judge.py lines 100-124 (style reference for bench unit tests; no existing replicate test)
  </read_first>
  <behavior>
    - --layers accepts one or more of {full,pack,memory,none}; default = all four.
    - Each arm invokes compound_eval with `--layers <arm>` (replacing `--inject on|off`).
    - --paired mode: before aggregating, subtract each trial's run-0 score from that trial's own
      scores so each trajectory starts at 0 (cancels cross-arm run-0 baseline noise).
    - RAW per-run/improvement numbers are retained in the summary ALONGSIDE the normalized ones.
    - Cohen's d computed per arm vs the `none` arm — on the paired-normalized improvement when
      --paired, on raw improvement otherwise.
    - Never raises on a missing/failed trial (preserve current None-skip discipline).
  </behavior>
  <action>
    argparse: add
      ap.add_argument("--layers", nargs="+", choices=("full","pack","memory","none"),
                      default=["full","pack","memory","none"])
      ap.add_argument("--paired", action="store_true",
                      help="Subtract each trial's run-0 from its own scores before aggregating.")

    Generalize _run_trial: rename the `inject` param to `arm` and change the subprocess cmd to
    use `"--layers", arm` instead of `"--inject", inject`. (Signature: _run_trial(arm, runs, root, label).)

    Add a normalizer:
      def _paired_normalize(trials: list[list[float]]) -> list[list[float]]:
          return [[s - t[0] for s in t] for t in trials]   # each trajectory starts at 0

    Keep _agg(trials) as the raw aggregator. In main():
      - collected: dict[str, list[list[float]]] keyed by each arm in a.layers.
      - loop `for arm in a.layers: for t in range(a.trials): scores = _run_trial(arm, a.runs, a.root, f"{arm}{t}")`.
      - For each arm build BOTH:
          raw_agg = _agg(trials)
          norm_agg = _agg(_paired_normalize(trials)) if trials else {"n": 0}
        Store under summary["arms"][arm] = {"raw": raw_agg, "paired": norm_agg}
        (when --paired absent, still compute paired for reference but mark which metric drove d).
      - Effect size: compute per non-none arm vs none. Pick the metric:
          metric = "paired" if a.paired else "raw"
        d = _cohens_d(summary["arms"][arm][metric], summary["arms"]["none"][metric]) when "none" present.
        Store summary["effect_size_cohens_d"] = {arm: d for arm in arms if arm != "none"}.
      - Also retain improvement_delta vs none per arm for both metrics.
      - Keep the existing _cohens_d helper unchanged (it already takes two _agg dicts).
    Preserve: trials_requested, runs_per_trial in summary; print(json.dumps(summary, indent=2));
    optional --out write.

    Add tests/test_bench_replicate.py (new file, under tests/ per coverage convention):
      - test_paired_normalize_starts_trajectories_at_zero: feed [[5,6,7],[6,6,8]] ->
        each row[0] == 0; deltas preserved ([0,1,2] and [0,0,2]).
      - test_agg_on_paired_vs_raw_retains_both: assert _agg(raw) and _agg(_paired_normalize(raw))
        differ in per_run_mean[0] (raw nonzero, paired==0) while improvement_mean is equal.
      - test_cohens_d_uses_selected_metric (light): construct two _agg dicts with known
        improvement_mean/std and assert _cohens_d returns the expected rounded value.
    Do NOT invoke subprocess/compound_eval in tests (no live LLM). Test the pure helpers only.
  </action>
  <files>bench/replicate.py, tests/test_bench_replicate.py</files>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_bench_replicate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - replicate accepts `--layers full pack memory none` (multi) and `--paired`
    - _paired_normalize makes every trajectory start at 0 while preserving inter-run deltas
    - summary["arms"][arm] contains BOTH "raw" and "paired" aggregates
    - summary["effect_size_cohens_d"] is per-arm vs none, on paired metric when --paired else raw
    - `.venv/bin/python -m pytest tests/test_bench_replicate.py -x -q` exits 0
  </acceptance_criteria>
  <done>replicate loops over --layers arms invoking compound_eval --layers; --paired normalizes each trial to its run-0; raw retained; per-arm Cohen's d vs none; pure-helper tests pass.</done>
</task>

</tasks>

<verification>
Full suite + coverage gate + lint must pass after all three tasks:

```
.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80
.venv/bin/python -m ruff check flowstate/ bench/ tests/
.venv/bin/python -m ruff format --check flowstate/ bench/ tests/
```

Note: the context_prefix change is in flowstate/ and IS coverage-gated — the new tests in
Task 1 must keep flowstate coverage >= 80%. bench/ may be outside the gate, but bench tests
still live under tests/ per existing convention.
</verification>

<success_criteria>
- build_context_prefix(root, memory, query, include_layers=None) byte-identical to the no-kwarg call.
- Per-arm _run_one tests (full/none/pack/memory): exactly-expected layers, patch installed for non-full and restored after return.
- replicate --paired: per-trial normalized trajectories start at 0; raw numbers retained in summary.
- `.venv/bin/python -m pytest tests/ --cov=flowstate --cov-fail-under=80` exits 0.
- `.venv/bin/python -m ruff check flowstate/ bench/ tests/` and `ruff format --check` pass.
- No new runtime dependencies; never-raises discipline preserved in context_prefix and bench.
</success_criteria>

<output>
Create `.planning/quick/260613-ga5-per-layer-context-prefix-toggle-paired-d/SUMMARY.md` and
`.planning/quick/260613-ga5-per-layer-context-prefix-toggle-paired-d/260613-ga5-SUMMARY.md`
when done (status: complete).
</output>
