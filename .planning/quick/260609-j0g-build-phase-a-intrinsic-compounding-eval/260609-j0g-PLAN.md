---
phase: quick-260609-j0g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/__init__.py
  - bench/metrics.py
  - bench/capture.py
  - bench/project.py
  - bench/compound_eval.py
  - bench/report.py
  - bench/fixtures/sample_project/flowstate.json
  - bench/fixtures/sample_project/.planning/fixtures/starter.json
  - bench/fixtures/sample_project/.planning/phases/01-foundation/01-VERIFICATION.md
  - tests/test_bench_compound.py
autonomous: true
requirements: [PHASE-A]
must_haves:
  truths:
    - "compute_scorecard turns K RunSnapshots into a 4-axis verdict where each axis is compounding/flat/regressing and CompoundingScore = (#compounding) - (#regressing) in [-4,+4]"
    - "K=1 yields all axes flat/insufficient-data and never raises"
    - "capture_run_snapshot reads a project root purely (memory, verify, prefix) and never raises, attributing new-vs-reencounter gotchas by run_id first then created_at window"
    - "_LAYER_HEADINGS in capture.py matches the actual layer headings emitted by context_prefix.py, enforced by a test that fails loudly if a heading changes"
    - "python -m bench.compound_eval --mode cheap --runs 5 --root bench/fixtures/sample_project prints a trend table + scorecard panel and writes deterministic JSON"
    - "every report header prints the honest caveat that cheap mode validates the apparatus, not causation"
    - "python -m pytest tests/ --cov=flowstate --cov-fail-under=80 stays green"
  artifacts:
    - path: "bench/metrics.py"
      provides: "RunSnapshot, 4 axis functions, compute_scorecard, verdict — pure no-IO"
      contains: "def compute_scorecard"
    - path: "bench/capture.py"
      provides: "capture_run_snapshot + single _LAYER_HEADINGS constant"
      contains: "_LAYER_HEADINGS"
    - path: "bench/project.py"
      provides: "scaffold + mutate_for_run for the synthetic target"
      contains: "def scaffold"
    - path: "bench/compound_eval.py"
      provides: "argparse runner, K-run loop, mode dispatch, judge stub"
      contains: "argparse"
    - path: "bench/report.py"
      provides: "JSON writer + Rich trend table + scorecard panel + caveat header"
      contains: "sort_keys"
    - path: "bench/fixtures/sample_project/flowstate.json"
      provides: "checked-in synthetic target state with install_manifest"
    - path: "tests/test_bench_compound.py"
      provides: "metric-core, cheap-seed e2e, cheap-dry smoke, report-render, heading-coupling tests"
      contains: "_LAYER_HEADINGS"
  key_links:
    - from: "bench/capture.py"
      to: "flowstate/context_prefix.py"
      via: "build_context_prefix + _LAYER_HEADINGS parsing"
      pattern: "build_context_prefix"
    - from: "bench/capture.py"
      to: "flowstate/verify.py"
      via: "run_verify(load_state(root), root)"
      pattern: "run_verify"
    - from: "bench/compound_eval.py"
      to: "flowstate/orchestrator.py"
      via: "run_pipeline with dry_run for cheap-dry mode"
      pattern: "run_pipeline"
    - from: "bench/project.py"
      to: "flowstate/context.py"
      via: "generate_starter_fixture for synthetic fixture"
      pattern: "generate_starter_fixture"
---

<objective>
Build Phase A from the approved plan: a standalone `bench/` package that measures whether
FlowState run N+1 beats run N on the same project, using signals FlowState already exposes
(journal artifact deltas, gotcha dedup, verify gates, prefix enrichment).

This is a developer/research tool, NOT a `flowstate` CLI subcommand. It imports flowstate
internals and is invoked via `python -m bench.compound_eval`.

Purpose: an on-thesis, CI-cheap, pure-Python regression guard for the compounding measurement
apparatus — with an honest caveat that only `--mode real` tests causation.
Output: `bench/` package (6 modules + sample_project fixture) and a thorough test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/Users/jhogan/frameworx/CLAUDE.md
@/Users/jhogan/frameworx/.claude/CLAUDE.md
@/Users/jhogan/.claude/plans/concurrent-hatching-chipmunk.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

flowstate/memory.py (MemoryStore):
- get_gotchas() -> list[MemoryEntry]   # gotcha-tagged INSIGHT, created_at DESC; metadata: signature, source, severity, first_seen, last_seen, count
- get_by_kind(kind, *, limit=20) -> list[MemoryEntry]   # use MemoryKind.RUN; RUN metadata: snapshot, steps, artifacts_changed, gotchas, delta_line, dry_run, run_id
- search(query, *, kind=None, limit=10) -> list[SearchResult]   # SearchResult(entry, score)
- count(kind=None, *, run_id=None) -> int
- MemoryEntry fields: id, kind, content, summary, source, tags(list), metadata(dict), created_at(datetime), run_id
- MemoryKind: RESEARCH STRATEGY DECISION TOOL_RUN INSIGHT RUN

flowstate/verify.py:
- run_verify(state, root) -> list[VerifyResult]   # never raises
- VerifyResult(frozen): gate, status (Literal pass|fail|skip), message, fixture

flowstate/context_prefix.py:
- build_context_prefix(root, memory, query, *, budget_tokens=None, console=None) -> str
- Layers joined by the separator "\n\n---\n\n". The layer heading strings emitted (verbatim):
    "## Eval Fixtures"      (fixtures)
    (pack layer is raw repomix XML — NO markdown heading; detect via "<repomix" / "<file " tag presence)
    "## Gotchas"            (gotchas)
    "## Prior Knowledge"    (memory — emitted by MemoryStore.get_context)
    "## Since Last Run"     (journal deltas)
  NOTE: only 4 of the 5 layers carry a "## " heading; pack is headerless XML.

flowstate/context.py:
- generate_starter_fixture(answers, project_name="") -> dict
    keys: retrieval_questions, acceptance_gates, forbidden_actions, system_contract, few_shot_exemplars

flowstate/orchestrator.py:
- run_pipeline(state, root) -> FlowStateModel   # dry_run via state.preferences.dry_run

flowstate/state.py:
- load_state(root=None) -> FlowStateModel
- FlowStateModel: install_manifest list[InstallEntry]; preferences.dry_run bool; preferences.project_name
- InstallEntry: path, owner, kind (Literal config|context|memory|research|artifact|pack|fixture), created_at, checksum (str|None)

CLI verify Rich-table idiom to mirror (cli.py:905-924): rich.table.Table, columns Gate/Status/Fixture/Message,
status_style = pass:green / fail:red / skip:dim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bench package + pure metrics core (RunSnapshot, 4 axes, scorecard, verdict)</name>
  <files>bench/__init__.py, bench/metrics.py, tests/test_bench_compound.py</files>
  <read_first>
    /Users/jhogan/.claude/plans/concurrent-hatching-chipmunk.md (Phase A metrics section, lines 39-45)
    /Users/jhogan/frameworx/flowstate/verify.py (VerifyResult shape for the verify-axis snapshot fields)
  </read_first>
  <behavior>
    - Convergence axis: artifacts_changed counts trending down toward a floor yields compounding; flat sequence yields flat; rising yields regressing; fewer than 2 runs yields flat (insufficient data).
    - Gotcha-learning axis: new_gotcha_count decaying while re-encounters plateau yields compounding; new count rising yields regressing; no movement yields flat.
    - Verify-non-regression axis: pass count never drops and fail count never rises across runs yields compounding if it improves, flat if steady, regressing if any later run has more fails or fewer passes than an earlier run.
    - Enrichment axis: prefix mem_hits / layer-count / prefix_tokens grow across runs yields compounding; shrink yields regressing; steady yields flat.
    - compute_scorecard with a single snapshot yields all axes flat (insufficient data), CompoundingScore 0, verdict not compounding; never raises.
    - CompoundingScore equals (#compounding axes) minus (#regressing axes), clamped to [-4,+4].
    - verdict is compounding iff score >= +2 AND Enrichment == compounding AND no axis == regressing.
    - Each axis function accepts a list of RunSnapshot and returns one of compounding/flat/regressing; pure, no IO, never raises on empty/short input.
  </behavior>
  <action>
    Create bench/__init__.py (empty, package marker). Create bench/metrics.py with `from __future__ import
    annotations`, stdlib-only imports (dataclasses, statistics if needed). Define a RunSnapshot dataclass holding
    the per-run signals the axes consume: run_index, run_id, artifacts_changed, new_gotchas, reencountered_gotchas,
    verify_pass, verify_fail, verify_skip, prefix_tokens, mem_hits, layers_present (a tuple of heading strings).
    Define the four pure axis functions (axis_convergence, axis_gotcha_learning, axis_verify_non_regression,
    axis_enrichment), each returning a Literal compounding/flat/regressing given a list of RunSnapshot; treat
    len<2 as flat. Define a Scorecard dataclass (the four axis verdicts, compounding_score, verdict, plus the
    snapshots). Define compute_scorecard(snapshots) -> Scorecard computing the four axes, the headline score
    (#compounding minus #regressing, clamped to [-4,+4]), and the verdict per the rule in <behavior>. All
    functions pure, no IO, never raise — guard empty/short inputs by returning flat/insufficient-data. Keep trend
    detection deterministic and simple with no new deps (e.g. first-vs-last with a tolerance band, or count of
    down-steps vs up-steps). In tests/test_bench_compound.py add metric-core unit tests asserting each axis fires
    for a hand-built compounding sequence, each axis detects regression for a worsening sequence, the K=1
    insufficient-data case returns all-flat and score 0, and a never-raises test passing an empty list and a single
    snapshot. Follow ruff: line-length 100, double quotes, snake_case.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_bench_compound.py -q && ruff check bench/metrics.py bench/__init__.py tests/test_bench_compound.py && ruff format --check bench/metrics.py</automated>
  </verify>
  <done>RunSnapshot + 4 axes + compute_scorecard + verdict exist as pure no-IO functions; metric-core tests (each axis fires, each detects regression, K=1 insufficient-data, never-raises) pass; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture.py (pure snapshot reads) + project.py (synthetic scaffold + mutation)</name>
  <files>bench/capture.py, bench/project.py, tests/test_bench_compound.py</files>
  <read_first>
    /Users/jhogan/frameworx/flowstate/context_prefix.py (layer heading strings; lines 248-263, 168-245)
    /Users/jhogan/frameworx/flowstate/memory.py (get_gotchas/get_by_kind/search/count; MemoryEntry.metadata keys)
    /Users/jhogan/frameworx/flowstate/verify.py (run_verify signature + VerifyResult)
    /Users/jhogan/frameworx/flowstate/context.py (generate_starter_fixture schema; lines 144-223)
    /Users/jhogan/frameworx/flowstate/gotchas.py (harvest_planning_gotchas reads VERIFICATION.md gaps; lines 235-346)
    /Users/jhogan/frameworx/flowstate/state.py (FlowStateModel, InstallEntry, load_state; lines 47-82, 208+)
  </read_first>
  <behavior>
    - capture_run_snapshot(root, probe_query, prior=None) returns a RunSnapshot and NEVER raises (any read failure degrades to zeros / empty layers, run_index derived from prior).
    - _LAYER_HEADINGS is a single module-level constant tuple of the 4 markdown headings build_context_prefix emits (## Eval Fixtures, ## Gotchas, ## Prior Knowledge, ## Since Last Run); layers_present is derived by substring-scanning the assembled prefix for each heading (plus a pack flag via XML tag detect).
    - Gotcha attribution: a gotcha entry is new this run if its run_id equals the current run's run_id; fallback when run_id is empty/unknown is first_seen within the created_at window since prior's capture timestamp. Re-encounters are gotchas with count>1 or last_seen advanced but not new.
    - scaffold(root) is idempotent: writes .planning/fixtures/starter.json (via generate_starter_fixture), flowstate.json with an install_manifest, and a .planning/phases/.../*-VERIFICATION.md containing a Gaps section so harvest_planning_gotchas has input. Re-running scaffold does not duplicate or corrupt.
    - mutate_for_run(root, i) makes a deterministic between-run change so artifact checksums shift LESS each successive run (convergence) and gotcha gaps resolve over runs — same i always yields the same mutation.
  </behavior>
  <action>
    Create bench/capture.py with `from __future__ import annotations`. Define the single _LAYER_HEADINGS constant:
    the tuple of the four heading strings (## Eval Fixtures, ## Gotchas, ## Prior Knowledge, ## Since Last Run).
    Implement capture_run_snapshot(root, probe_query, prior=None) -> RunSnapshot performing PURE reads: open a
    MemoryStore(root=root) (close it), call get_gotchas(), get_by_kind(MemoryKind.RUN, limit=1) for the latest RUN
    entry's metadata artifacts_changed length, search(probe_query) for mem_hits, run_verify(load_state(root), root)
    for pass/fail/skip counts, and build_context_prefix(root, store, probe_query) then derive prefix_tokens
    (len//4, matching the codebase 4-chars/token convention), layers_present by scanning for each _LAYER_HEADINGS
    entry, and a pack flag via "<repomix" or "<file " tag presence. Compute new vs reencountered gotchas by the
    run_id-first / created_at-window-fallback rule in <behavior>, using prior to bound the window. Wrap the whole
    body so it NEVER raises (return a zeroed RunSnapshot on any failure). Import RunSnapshot from bench.metrics.
    Create bench/project.py with scaffold(root) and mutate_for_run(root, i): scaffold uses
    generate_starter_fixture(InterviewAnswers(...)) to write .planning/fixtures/starter.json, writes a flowstate.json
    (FlowStateModel with a small install_manifest of real files it also creates, checksummed) via save_state, and
    writes a phases/01-foundation/01-VERIFICATION.md with frontmatter status plus a Gaps bullet list so
    harvest_planning_gotchas captures gotchas; make it idempotent (overwrite-in-place, stable content).
    mutate_for_run applies a deterministic mutation keyed on i that shrinks artifact deltas and removes one gap per
    run. Add tests: a heading-coupling test asserting every string in _LAYER_HEADINGS appears in
    context_prefix.py's source (read the module file and assert substring) so it FAILS LOUDLY if a heading changes;
    a capture never-raises test against an empty tmp dir; a scaffold idempotency test (scaffold twice yields
    identical fixture/state); a gotcha-attribution test. ruff clean.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_bench_compound.py -q && ruff check bench/capture.py bench/project.py && ruff format --check bench/capture.py bench/project.py</automated>
  </verify>
  <done>capture_run_snapshot reads purely and never raises; _LAYER_HEADINGS is the single source and a test asserts it matches context_prefix.py; scaffold is idempotent and produces a VERIFICATION.md with gaps; mutate_for_run is deterministic; tests pass; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: compound_eval.py runner (argparse, mode dispatch) + report.py (JSON + Rich + caveat)</name>
  <files>bench/compound_eval.py, bench/report.py, tests/test_bench_compound.py</files>
  <read_first>
    /Users/jhogan/frameworx/flowstate/orchestrator.py (run_pipeline + dry_run wiring; lines 164-264)
    /Users/jhogan/frameworx/flowstate/cli.py (verify Rich-table idiom; lines 905-924)
    /Users/jhogan/frameworx/flowstate/state.py (load_state, preferences.dry_run)
  </read_first>
  <action>
    Create bench/report.py with `from __future__ import annotations`. Implement write_json(scorecard, out_path)
    using json.dump with sort_keys=True and indent=2 for deterministic diffs (serialize RunSnapshots + axis
    verdicts + score + verdict). Implement render_report(scorecard, *, console=None, markdown=False): print the
    HONEST CAVEAT header VERBATIM first — cheap mode validates the measurement apparatus, not causation; only
    --mode real tests whether FlowState's prefix actually causes the LLM to compound — then a Rich trend Table
    (one row per run: run index, delta artifacts, new/re-enc gotchas, verify P/F/S, prefix tokens, mem hits,
    layers count) mirroring the cli.py verify table idiom, then a scorecard Panel showing the 4 axis verdicts +
    CompoundingScore + overall verdict. Console is injectable (default module Console). If markdown=True, also
    emit a RUNLOG-style markdown record. Create bench/compound_eval.py with `from __future__ import annotations`
    using stdlib argparse (NOT Click): flags --mode {cheap,real} default cheap, --runs (int, default 5), --root
    (Path), --out (Path, optional), --markdown (flag), --judge (flag, default off). Implement main(argv=None) that:
    for cheap mode runs the K-run loop driving the real substrate — cheap-dry calls run_pipeline with
    state.preferences.dry_run=True against --root (mutate_for_run between runs), cheap-seed deterministically seeds
    memory/gotchas/journal; capture a RunSnapshot per run via capture_run_snapshot; real mode runs run_pipeline
    live (guard: never under CI, requires explicit invocation). After the loop call compute_scorecard and
    render_report + write_json when --out. Add a guarded judge stub: if --judge is set, refuse unless --mode real
    plus an --allow-llm style guard, print a clear judge-is-spec-only/not-implemented message, and NEVER let judge
    output touch the mechanical score. Add the `if __name__ == "__main__": raise SystemExit(main())` entry. Keep
    the thin runner's untested real-mode branches minimal. Add tests: a report-render test (assert the caveat
    string is present in captured Console output and the table + panel render without error) and a
    judge-stub-refusal test. ruff clean.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_bench_compound.py -q && ruff check bench/compound_eval.py bench/report.py && ruff format --check bench/compound_eval.py bench/report.py</automated>
  </verify>
  <done>python -m bench.compound_eval --mode cheap runs a K-run loop and prints caveat + trend table + scorecard panel; --out writes sort_keys JSON; --judge is a guarded spec-only stub excluded from the score; argparse (not Click) is used; tests pass; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 4: checked-in sample_project fixture + cheap-seed e2e + cheap-dry smoke tests</name>
  <files>bench/fixtures/sample_project/flowstate.json, bench/fixtures/sample_project/.planning/fixtures/starter.json, bench/fixtures/sample_project/.planning/phases/01-foundation/01-VERIFICATION.md, tests/test_bench_compound.py</files>
  <read_first>
    /Users/jhogan/frameworx/tests/test_orchestrator.py (dry-run pipeline test idiom; lines 9-72)
    /Users/jhogan/frameworx/tests/test_verify.py (tmp .planning + install_manifest + fixtures idiom; lines 16-90)
    /Users/jhogan/frameworx/flowstate/state.py (FlowStateModel/InstallEntry JSON shape for the checked-in flowstate.json)
  </read_first>
  <action>
    Generate the checked-in synthetic target under bench/fixtures/sample_project/ by running bench.project.scaffold
    against that directory (or hand-author equivalent stable content), then commit the produced files:
    .planning/fixtures/starter.json (a generate_starter_fixture dict), flowstate.json (a FlowStateModel with a
    small valid install_manifest pointing at files that exist in the fixture), and
    .planning/phases/01-foundation/01-VERIFICATION.md with frontmatter status and a Gaps bullet section. Ensure the
    fixture is self-consistent so `python -m bench.compound_eval --mode cheap --runs 5 --root
    bench/fixtures/sample_project` runs clean. In tests/test_bench_compound.py add: (1) a cheap-seed 3-iteration
    end-to-end test that deterministically seeds memory/gotchas/journal across 3 runs and asserts the expected axis
    verdicts fire (convergence + gotcha-learning + enrichment register compounding given the seeded trend); (2) a
    cheap-dry smoke test that copies the checked-in sample_project into tmp_path and runs the runner's main() (or a
    run_pipeline dry-run loop) for 2 runs, asserting it completes, produces a Scorecard, and never raises; (3) a
    JSON-determinism test asserting two write_json calls on the same scorecard produce byte-identical output. Use
    tmp_path and copy the fixture (never mutate the checked-in copy). ruff clean.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_bench_compound.py -q && python -m bench.compound_eval --mode cheap --runs 5 --root bench/fixtures/sample_project && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q && ruff check bench tests/test_bench_compound.py && ruff format --check bench tests/test_bench_compound.py</automated>
  </verify>
  <done>The checked-in sample_project runs clean under the cheap runner; cheap-seed 3-iteration e2e asserts axes fire; cheap-dry smoke test never raises; JSON output is byte-deterministic; full suite stays green at >=80% flowstate coverage; ruff clean.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_bench_compound.py -q` passes (metric-core + cheap-seed e2e + cheap-dry smoke + report-render + heading-coupling).
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` stays green (bench/ is new top-level code; coverage target is flowstate, so bench/ does not lower the gate — but bench/ tests make the harness trustworthy).
- `python -m bench.compound_eval --mode cheap --runs 5 --root bench/fixtures/sample_project` prints the caveat header, a trend table, a scorecard panel, and writes deterministic JSON with --out.
- `ruff check bench tests/test_bench_compound.py` and `ruff format --check bench` clean.
- No new runtime dependency added (stdlib argparse/json/statistics + existing rich only).
- The judge path remains a guarded spec-only stub excluded from the mechanical score.
</verification>

<success_criteria>
- bench/ package exists with metrics.py (pure), capture.py (never-raises reads + single _LAYER_HEADINGS), project.py (idempotent scaffold + deterministic mutate), compound_eval.py (argparse runner), report.py (JSON + Rich + caveat).
- The 4-axis scorecard computes CompoundingScore in [-4,+4]; verdict==compounding requires score>=+2, Enrichment==compounding, and no regressing axis.
- K=1 yields all-flat/insufficient-data and never raises.
- A test asserts _LAYER_HEADINGS matches context_prefix.py and fails loudly on heading drift.
- The checked-in sample_project is a runnable synthetic target with a VERIFICATION.md gaps section feeding harvest_planning_gotchas.
- bench/ is NOT a flowstate CLI subcommand; never-raises discipline matches verify/journal/gotchas.
</success_criteria>

<output>
Create `.planning/quick/260609-j0g-build-phase-a-intrinsic-compounding-eval/SUMMARY.md` and
`.planning/quick/260609-j0g-build-phase-a-intrinsic-compounding-eval/260609-j0g-SUMMARY.md` when done
(both, per the project SUMMARY-frontmatter convention), each with `status: complete`.
</output>
