---
phase: 15-bundle-gsd
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - flowstate/launcher.py
  - tests/test_launcher.py
autonomous: true
requirements: [GSD-03]
must_haves:
  truths:
    - "flowstate launch gsd <N> emits a working handoff against the vendored GSD with nothing separately installed"
    - "The launcher's GSD detect-and-suggest path is neutralized — no 'GSD not detected, run /gsd:new-project' branch"
    - "GSD is treated as always present because FlowState installs it"
  artifacts:
    - path: "flowstate/launcher.py"
      provides: "GSD launch handoff with the detect-and-suggest branch removed"
    - path: "tests/test_launcher.py"
      provides: "Tests asserting the neutralized detect path and a working gsd launch command"
  key_links:
    - from: "flowstate/launcher.py"
      to: "flowstate launch gsd"
      via: "unconditional GSD handoff (no marker gate)"
      pattern: "gsd"
---

<objective>
Neutralize the launcher's GSD detect-and-suggest path so `flowstate launch gsd <N>` always
produces a working handoff — GSD is now ALWAYS present because FlowState vendors and installs it.
Remove the "GSD not detected, run /gsd:new-project" branch and the `.planning`-marker proxy that
gated GSD availability.

Purpose: Success Criterion 3 — a fresh project with no separately-installed GSD still launches.
Output: updated `flowstate/launcher.py` + tests in `tests/test_launcher.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From flowstate/launcher.py (current state to change):
- `TOOL_MARKERS = {"gsd": [".planning"], "strategy": [], "discipline": []}` — the `.planning` proxy for GSD presence
- `detect_tools(root) -> dict[str, bool]` — sets gsd availability from the `.planning` marker
- `launch_command(tool, phase, root) -> str` — dispatches; `_SKILL_HANDOFFS` gate stays for strategy/discipline
- `_gsd_command(phase) -> str` — returns "/gsd:plan-phase {phase}" or "/gsd:progress"
- `print_next_steps(state, root)` — prints the "GSD not detected. Run /gsd:new-project..." dim branch
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the GSD detect-and-suggest path and make launch gsd unconditional</name>
  <read_first>
    - flowstate/launcher.py (the `TOOL_MARKERS["gsd"]` proxy, `detect_tools` gsd branch, the `print_next_steps` "GSD not detected. Run /gsd:new-project" dim line, and `launch_command`/`_gsd_command`)
    - tests/test_launcher.py and tests/test_launcher_skills.py (existing launcher test patterns to extend; leave test_launcher_skills.py — the strategy/discipline gate — unchanged)
  </read_first>
  <acceptance_criteria>
    - `flowstate launch gsd 1` output contains the `/gsd:plan-phase 1` handoff and has NO "not detected" / "run /gsd:new-project" branch
    - `print_next_steps` never emits the "GSD not detected" suggestion — GSD is always presented as available
    - The `.planning` marker no longer gates GSD availability (GSD treated as always present)
    - The strategy/discipline skill-gated handoffs (`_SKILL_HANDOFFS`) are untouched and still gate on installed namespaces
    - `flowstate launch gsd` (no phase) still returns the `/gsd:progress` handoff
  </acceptance_criteria>
  <behavior>
    - Test: launch_command("gsd", 1, tmp_path) returns a string containing "/gsd:plan-phase 1"
    - Test: launch_command("gsd", None, tmp_path) returns a string containing "/gsd:progress"
    - Test: in a fresh tmp project with no .planning dir, launch gsd still yields a working handoff (no "not detected" text)
    - Test: print_next_steps output for a project without .planning contains no "not detected" / "new-project" string
    - Test: strategy/discipline handoffs still return the install prompt when their namespace is absent (unchanged behavior)
  </behavior>
  <action>
    In `flowstate/launcher.py`, treat GSD as always present. Remove the `.planning` proxy that
    gated GSD in `TOOL_MARKERS`/`detect_tools` (drop the "gsd" marker entry or hardwire it True so
    `detect_tools` always reports gsd available), and delete the `print_next_steps` else-branch that
    prints "GSD not detected. Run /gsd:new-project in a Claude Code session." so the suggested-command
    block always offers `flowstate launch gsd 1`. Keep `launch_command`/`_gsd_command` producing the
    `/gsd:plan-phase {N}` and `/gsd:progress` handoffs. Do NOT touch the `_SKILL_HANDOFFS`
    strategy/discipline gate — those correctly stay namespace-gated. The handoff string stays a fixed
    literal; never interpolate vendored content into the emitted command.
  </action>
  <verify>
    <automated>python -m pytest tests/test_launcher.py -x -q</automated>
  </verify>
  <done>The GSD detect-and-suggest path is gone; launch gsd is unconditional and tested; strategy/discipline gating is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher → emitted command string | Text printed for the user to run in a Claude Code session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | emitted launch command | mitigate | Handoff strings stay fixed literals (`/gsd:plan-phase {N}`, `/gsd:progress`); no vendored content is interpolated into the command |
| T-15-09 | Spoofing | assumed-present GSD | accept | Neutralizing detection is the intended design — GSD is guaranteed present by the installer (Plan 15-02); the assumption holds because FlowState controls the install |
</threat_model>

<verification>
- `flowstate launch gsd 1` emits `/gsd:plan-phase 1` with no "not detected" branch.
- A fresh project (no `.planning`) still produces a working GSD handoff.
- strategy/discipline handoffs remain namespace-gated.
</verification>

<success_criteria>
Success Criterion 3 is met: `flowstate launch gsd <N>` works against the vendored GSD with nothing
separately installed; the detect-and-suggest path is neutralized.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-03-SUMMARY.md` when done.
</output>
