---
phase: 15-bundle-gsd
plan: 05
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - README.md
autonomous: true
requirements: [GSD-05]
must_haves:
  truths:
    - "README describes GSD as bundled and auto-installed — no separate GSD install required"
    - "The Prerequisites section no longer lists 'GSD (optional, install separately)'"
    - "The GSD acknowledgment reflects the vendored-and-installed reality, not delegate-only framing"
    - "The README test count matches the true post-phase pytest --collect-only total (never a stale 803/947/1000)"
  artifacts:
    - path: "README.md"
      provides: "Docs reconciled to the bundled-GSD reality with an accurate test count"
      contains: "vendors"
  key_links:
    - from: "README.md Prerequisites"
      to: "bundled GSD reality"
      via: "removal of 'GSD (optional, install separately)'"
      pattern: "GSD"
---

<objective>
Update the README to describe the bundled-and-auto-installed GSD reality: FlowState vendors GSD and
installs it unconditionally — no separate GSD install. Drop "GSD (optional, install separately)"
from Prerequisites, rewrite the GSD acknowledgment away from the delegate-only framing, fix the
stale GSD URL, and re-reconcile the README test count to the true post-phase collection total.

Purpose: Success Criterion coverage for GSD-05 — every GSD claim in the README matches the code
that now makes it true.
Output: updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the README to the bundled-and-auto-installed GSD reality + true test count</name>
  <files>README.md</files>
  <read_first>
    - README.md (Prerequisites line "GSD (optional, install separately)"; the Management row + "Writes context files for GSD skills"; the Acknowledgments GSD line "FlowState generates the context files GSD consumes ... hand off to native GSD execution"; the "1000 tests" line; the `gsd-build/gsd-2` URL)
    - .planning/REQUIREMENTS.md (GSD-05 exact wording — what the acknowledgment must now say)
  </read_first>
  <acceptance_criteria>
    - The Prerequisites section no longer contains "GSD (optional, install separately)" (or any "install separately" GSD line)
    - The README states FlowState vendors and installs GSD, and that no separate GSD install is required
    - The GSD Acknowledgment describes the bundled/auto-installed reality, not the "generates context files ... hand off to native GSD execution" delegate-only framing
    - The GSD URL points to the canonical MIT repo `gsd-build/get-shit-done` (not `gsd-build/gsd-2`)
    - The README test count equals the actual `pytest --collect-only` total after Plans 15-02/15-03/15-04 added their test files — re-derived, never a hardcoded 803/947/1000
    - `grep -q "install separately" README.md` returns no GSD "install separately" line
  </acceptance_criteria>
  <action>
    Edit `README.md` so every GSD claim matches the shipped reality. Remove the Prerequisites bullet
    "GSD (optional, for Management phase) — .../gsd-2"; replace it (if a line is kept) with a note
    that GSD is bundled and auto-installed by FlowState. Rewrite the Acknowledgments GSD entry from
    "FlowState generates the context files GSD consumes ... hand off to native GSD execution" to the
    bundled-and-auto-installed framing ("FlowState vendors the MIT GSD distribution and installs it
    unconditionally into `.claude/`; no separate GSD install is required"), keeping the MIT
    attribution (© Lex Christopherson). Fix the GSD repo URL to `github.com/gsd-build/get-shit-done`.
    Re-derive the true test count by running `pytest --collect-only` in the project environment
    (Python 3.12 / Flox) AFTER 15-02/15-03/15-04 have landed their tests, and write that exact number
    wherever the README quotes a test count (the "1000 tests" line and any doctor/summary counts).
    Do not invent or reuse a stale number. Touch only README.md.
  </action>
  <verify>
    <automated>! grep -n "gsd-2" README.md &amp;&amp; ! grep -iq "gsd.*install separately\|install separately.*gsd" README.md &amp;&amp; grep -q "get-shit-done" README.md</automated>
  </verify>
  <done>The README describes GSD as bundled + auto-installed, drops the separate-install prerequisite, fixes the URL, and quotes the true post-phase test count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README claims → user expectations | Documentation is the contract a user relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Repudiation | stale/false README claims | mitigate | Re-derive the test count from `pytest --collect-only` post-phase; never hardcode a stale number; every GSD claim traced to the code that makes it true |
| T-15-14 | Repudiation | third-party attribution in docs | mitigate | Keep the GSD MIT attribution (© Lex Christopherson) and the canonical `gsd-build/get-shit-done` URL |
</threat_model>

<verification>
- Prerequisites has no "GSD (optional, install separately)" line.
- Acknowledgments describes vendored + auto-installed GSD.
- The GSD URL is `gsd-build/get-shit-done`.
- The quoted test count matches `pytest --collect-only`.
</verification>

<success_criteria>
GSD-05 is met: the README GSD acknowledgment + install docs describe the bundled-and-auto-installed
reality, the separate-install prerequisite is dropped, and the test count is accurate.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-05-SUMMARY.md` when done.
</output>
