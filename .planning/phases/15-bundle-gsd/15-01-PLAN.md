---
phase: 15-bundle-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/vendor/gsd/**
  - flowstate/vendor/VENDORING.md
  - NOTICE
  - pyproject.toml
autonomous: true
requirements: [GSD-01]
must_haves:
  truths:
    - "flowstate/vendor/gsd/ holds the pinned npm distribution get-shit-done-cc@1.42.3 (skills + get-shit-done runtime incl. the dependency-free gsd-tools.cjs + prebuilt sdk/dist), with NO node_modules"
    - "The upstream MIT LICENSE is captured verbatim and a VERSION file records npm version 1.42.3 + tarball sha256 + npm integrity for provenance"
    - "NOTICE carries the GSD MIT attribution (Lex Christopherson)"
    - "The dependency-free gsd-tools.cjs runs a REAL query (roadmap get-phase) from the vendored tree and exits 0 — recorded in the SUMMARY before Wave 2 relies on it"
    - "Vendored JS/CJS/MD/test files are excluded from pytest collection and coverage measurement"
  artifacts:
    - path: "flowstate/vendor/gsd/LICENSE"
      provides: "Upstream MIT license verbatim"
      contains: "MIT"
    - path: "flowstate/vendor/gsd/VERSION"
      provides: "Provenance — npm version 1.42.3 + tarball sha256 + npm integrity"
      contains: "1.42.3"
    - path: "flowstate/vendor/gsd/get-shit-done/bin/gsd-tools.cjs"
      provides: "The dependency-free GSD query/skill CLI (invokable via node, no node_modules)"
    - path: "flowstate/vendor/VENDORING.md"
      provides: "The one canonical, reproducible vendoring procedure (shared source of truth with 15-04 refresh)"
    - path: "NOTICE"
      provides: "Third-party attribution including GSD"
      contains: "Lex Christopherson"
  key_links:
    - from: "pyproject.toml"
      to: "flowstate/vendor/*"
      via: "coverage omit + pytest collect-ignore"
      pattern: "vendor"
---

<objective>
Vendor a pinned, MIT-licensed GSD working distribution into `flowstate/vendor/gsd/` from the
**npm-published package `get-shit-done-cc@1.42.3`** (obtained reproducibly via `npm pack`, NOT a git
clone — a git checkout lacks the `tsc`-built `sdk/dist/` and ships a non-invokable CLI). The npm
package ships prebuilt `sdk/dist/` and the dependency-free `get-shit-done/bin/gsd-tools.cjs` with NO
`node_modules`, so the "no node_modules / no new deps" constraint holds. Capture the LICENSE
verbatim, record the npm version + tarball integrity for provenance, add the GSD attribution to
NOTICE, document the canonical vendoring procedure, and exclude the vendored tree from pytest
collection and coverage.

Purpose: Foundation for GSD-02/03/04 — nothing installs, launches, or refreshes a vendored GSD that
does not yet exist on disk. The vendored tree must be provably invokable before Wave 2 depends on it.
Output: `flowstate/vendor/gsd/**`, `.../gsd/LICENSE`, `.../gsd/VERSION`, `flowstate/vendor/VENDORING.md`,
updated `NOTICE`, updated `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<empirical_findings>
Verified by the planner against get-shit-done-cc@1.42.3 (npm), from a CLEAN environment (no ancestor node_modules):
- `npm pack get-shit-done-cc@1.42.3` tarball top-level: agents, bin, commands, get-shit-done, hooks, scripts, sdk, sdk-bundle, LICENSE, package.json, README.md + 4 localized READMEs (README.ja-JP.md / ko-KR / pt-BR / zh-CN). NO node_modules. ~13M extracted (sdk/ 7.4M, sdk/src 3.1M).
- `node get-shit-done/bin/gsd-tools.cjs --help` -> exit 0 (self-contained CJS; commands include roadmap, phase, state, frontmatter, verify, commit, graphify, progress, history-digest, find-phase, ...). This is the dependency-free workhorse the GSD skills use.
- `node get-shit-done/bin/gsd-tools.cjs --cwd <project-with-.planning> roadmap get-phase 15` -> exit 0, returns real JSON. USE THIS as the invokability gate.
- `node bin/gsd-sdk.js ...` and `node sdk/dist/cli.js ...` and the `sdk-bundle` cli -> exit 1 (ERR_MODULE_NOT_FOUND '@anthropic-ai/claude-agent-sdk'). sdk/dist is prebuilt but its deps (@anthropic-ai/claude-agent-sdk ^0.2.84, ws ^8.20.0) are NOT bundled. `gsd-sdk` (agent-session CLI) is vendored as DATA and lights up only with a full `npm i -g get-shit-done-cc`; it is NOT the dependency-free invokability gate.
- Provenance: version 1.42.3; tarball sha256 `ced3fa71842c857cb5320b847227eb50246481004e365e20fe8edaf2d2a2a52f`; npm integrity `sha512-3sQoRYFl7v7dju3LXq7sE3pnufGHF7R6xfDU1DaH2+YYe4V6+dhoaUo4KoBHvjtLQ8UATUX3hWofHg+tAUUALQ==` (re-verify at execution; do not trust these blindly).
</empirical_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the pinned npm package get-shit-done-cc@1.42.3 and prove the dependency-free CLI is invokable</name>
  <files>flowstate/vendor/gsd/**, flowstate/vendor/gsd/LICENSE, flowstate/vendor/gsd/VERSION, flowstate/vendor/VENDORING.md</files>
  <read_first>
    - flowstate/skills/ (how Phase 14 vendored MIT trees as data-only, for structural precedent)
    - NOTICE (existing gstack + superpowers MIT attributions — append, do not rewrite)
    - The npm package's own `bin/install.js` (authoritative mapping of upstream dirs -> the installed `.claude/` layout that 15-02 will reproduce)
  </read_first>
  <acceptance_criteria>
    - `flowstate/vendor/gsd/get-shit-done/bin/gsd-tools.cjs` exists
    - `flowstate/vendor/gsd/commands/gsd/` exists (source of the gsd-* skills), and `flowstate/vendor/gsd/get-shit-done/` contains `workflows/`, `references/`, `templates/`, `contexts/`, `bin/`
    - `flowstate/vendor/gsd/LICENSE` exists and contains "MIT"
    - `flowstate/vendor/gsd/VERSION` records npm version `1.42.3`, the tarball sha256, and the npm integrity hash
    - NO `node_modules/` anywhere under `flowstate/vendor/gsd/`; none of the 4 localized READMEs (README.ja-JP.md / ko-KR / pt-BR / zh-CN) are present
    - INVOKABILITY GATE: `node flowstate/vendor/gsd/get-shit-done/bin/gsd-tools.cjs --cwd <a project with .planning/ROADMAP.md> roadmap get-phase 15` exits 0 from the vendored tree with no node_modules — this exact command + its exit code is recorded in the SUMMARY
    - `flowstate/vendor/VENDORING.md` documents the exact reproducible procedure (npm pack pinned version -> extract -> subset/exclusion -> invokability-verify) so 15-04's refresh cannot diverge
  </acceptance_criteria>
  <action>
    Obtain the distribution reproducibly with `npm pack get-shit-done-cc@1.42.3` and extract the
    tarball (do NOT git-clone — a git checkout lacks the built `sdk/dist/` and yields a non-invokable
    CLI). Record the tarball sha256 and the npm `dist.integrity`. Read the extracted `bin/install.js`
    to confirm the upstream->`.claude/` mapping that 15-02 will reproduce. Copy the package's runtime
    contents into `flowstate/vendor/gsd/` preserving layout: `agents`, `bin`, `commands`,
    `get-shit-done` (this carries the dependency-free `bin/gsd-tools.cjs`), `hooks`, `scripts`, `sdk`
    (prebuilt `dist` + supporting files), `LICENSE`, `package.json`. EXCLUDE only the 4 localized
    READMEs (README.ja-JP.md / ko-KR / pt-BR / zh-CN) and any obvious dev-only cruft that install.js
    proves unnecessary; do NOT add an exclusion criterion for a file the copy actually needs (e.g. do
    not forbid `sdk/tsconfig.json` if it is part of the package). The `sdk/` tree is vendored as DATA
    (the `gsd-sdk` agent-session CLI needs @anthropic-ai/claude-agent-sdk + ws and is not the
    dependency-free gate). Optionally trim `sdk/src` (~3.1M) to shrink footprint — but ONLY if you
    re-run the invokability gate afterward and it still exits 0. Capture the upstream `LICENSE`
    verbatim to `flowstate/vendor/gsd/LICENSE`. Write `flowstate/vendor/gsd/VERSION` recording npm
    version `1.42.3` + tarball sha256 + npm integrity. Write `flowstate/vendor/VENDORING.md`
    documenting the canonical, reproducible procedure (this is the single source of truth 15-04
    reuses). Never execute any vendored file except the dependency-free `gsd-tools.cjs` invokability
    check. Record the exact invokability command + exit 0 in the SUMMARY before Wave 2 begins.
  </action>
  <verify>
    <automated>test -f flowstate/vendor/gsd/get-shit-done/bin/gsd-tools.cjs &amp;&amp; test -f flowstate/vendor/gsd/LICENSE &amp;&amp; test -d flowstate/vendor/gsd/commands/gsd &amp;&amp; grep -q "1.42.3" flowstate/vendor/gsd/VERSION &amp;&amp; ! find flowstate/vendor/gsd -type d -name node_modules | grep -q . &amp;&amp; ! ls flowstate/vendor/gsd/README.*-*.md 2>/dev/null | grep -q . &amp;&amp; node flowstate/vendor/gsd/get-shit-done/bin/gsd-tools.cjs --cwd "$PWD" roadmap get-phase 15 >/dev/null 2>&amp;1</automated>
  </verify>
  <done>The pinned npm distribution is vendored (no node_modules, no localized READMEs) with LICENSE + VERSION provenance + VENDORING.md, and the dependency-free gsd-tools.cjs is proven invokable via a real query recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Add GSD attribution to NOTICE and exclude the vendored tree from coverage + pytest collection</name>
  <files>NOTICE, pyproject.toml</files>
  <read_first>
    - NOTICE (append a GSD stanza mirroring the existing gstack/superpowers stanzas)
    - pyproject.toml (existing `[tool.coverage.run] omit = [... "flowstate/skills/*"]`, the wheel `artifacts` list, and `[tool.pytest.ini_options]`)
  </read_first>
  <acceptance_criteria>
    - `NOTICE` contains "Lex Christopherson" and cites `get-shit-done-cc` (`gsd-build/get-shit-done`) under MIT
    - `pyproject.toml` `[tool.coverage.run] omit` includes `flowstate/vendor/*`
    - The wheel `artifacts` list includes `flowstate/vendor/**/*` so vendored data ships in the package
    - Pytest does not collect any file under `flowstate/vendor/` — verified with a check that FAILS if a vendored file is collected: `! (python -m pytest --collect-only -q 2>&1 | grep -q "flowstate/vendor/")`
    - `pytest --collect-only` still succeeds overall
  </acceptance_criteria>
  <action>
    Append a GSD MIT attribution stanza to `NOTICE` (© 2025 Lex Christopherson,
    `gsd-build/get-shit-done`, npm `get-shit-done-cc`, MIT), matching the format of the existing
    gstack/superpowers entries. In `pyproject.toml`: add `flowstate/vendor/*` to
    `[tool.coverage.run] omit` alongside the existing `flowstate/skills/*`; add
    `flowstate/vendor/**/*` to the hatch wheel `artifacts` list so the vendored data is packaged; and
    ensure pytest never collects the vendored tree — add a `collect_ignore_glob` for
    `flowstate/vendor` (root `conftest.py`) and/or a `norecursedirs` entry under
    `[tool.pytest.ini_options]`, so vendored `.py`/test files are excluded from collection. Do not
    add any new Python runtime dependency. Do not bump the package version in this task.
  </action>
  <verify>
    <automated>grep -q "Lex Christopherson" NOTICE &amp;&amp; grep -q "flowstate/vendor" pyproject.toml &amp;&amp; ! (python -m pytest --collect-only -q 2>&amp;1 | grep -q "flowstate/vendor/")</automated>
  </verify>
  <done>NOTICE carries the GSD MIT attribution; the vendored tree is omitted from coverage, shipped in the wheel, and provably not collected by pytest (a positive-match check that fails if any vendored file is collected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> vendored tree | Third-party MIT package copied into the FlowState package as data |
| vendored tree -> package build/test | Vendored JS/CJS could distort coverage or be executed if collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | npm pack of get-shit-done-cc | mitigate | Pin the exact npm version (1.42.3); record + re-verify the tarball sha256 and npm integrity in VERSION and SUMMARY — the snapshot is reproducible and auditable |
| T-15-02 | Repudiation | third-party license attribution | mitigate | Capture upstream LICENSE verbatim + add MIT attribution (Lex Christopherson) to NOTICE — legal reuse is auditable |
| T-15-03 | Elevation of Privilege | vendored Node/JS files | mitigate | Copy as data only; the ONLY vendored file executed is the dependency-free `gsd-tools.cjs` invokability check; exclude the tree from pytest collection so no vendored code runs in the test process |
| T-15-SC | Tampering | supply-chain of the npm package | mitigate | Use `npm pack` at a pinned version (not a moving tag); no `node_modules` vendored and no nested `npm install` run, so no transitive install executes; `gsd-sdk` deps are absent by design |
</threat_model>

<verification>
- `flowstate/vendor/gsd/` contains skills source (`commands/gsd/`), the `get-shit-done/` runtime with the dependency-free `bin/gsd-tools.cjs`, and prebuilt `sdk/`, with LICENSE + VERSION + no node_modules.
- The invokability gate (`gsd-tools.cjs roadmap get-phase 15`) exits 0 and is recorded in the SUMMARY.
- `NOTICE` carries the GSD MIT attribution.
- `pytest --collect-only` provably excludes the vendored tree; coverage omits `flowstate/vendor/*`.
</verification>

<success_criteria>
Success Criterion 1 is met: the pinned npm GSD distribution is vendored with LICENSE verbatim, a
recorded npm-version/integrity provenance, a NOTICE attribution, and a proven-invokable
dependency-free CLI; the vendored tree does not distort the test gate.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-01-SUMMARY.md` when done, recording the npm version, tarball sha256/integrity, and the exact `gsd-tools.cjs` invokability command + exit 0.
</output>
