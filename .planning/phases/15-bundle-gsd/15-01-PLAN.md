---
phase: 15-bundle-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/vendor/gsd/**
  - flowstate/vendor/gsd/VERSION
  - flowstate/vendor/gsd/LICENSE
  - flowstate/vendor/VENDORING.md
  - NOTICE
  - pyproject.toml
  - .pre-commit-config.yaml
autonomous: true
requirements: [GSD-01]
must_haves:
  truths:
    - "flowstate/vendor/gsd/ holds a lean full-parity GSD install: get-shit-done-cc@1.42.3 + its production node_modules (~51M), with the ~197M optional platform binary (@anthropic-ai/claude-agent-sdk-*/) EXCLUDED"
    - "gsd-sdk has FULL parity from the vendored tree: node .../get-shit-done-cc/bin/gsd-sdk.js query roadmap.get-phase 15 returns 'Bundle GSD'"
    - "Provenance is pinned + reproducible: npm version 1.42.3 + the captured package-lock.json, recorded in VERSION; GSD LICENSE verbatim; NOTICE carries the GSD MIT attribution + a pointer to bundled transitive-dep licenses"
    - "The vendored tree is excluded from the large-file hook, pytest collection, and coverage"
  artifacts:
    - path: "flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js"
      provides: "The full-parity gsd-sdk CLI, invokable via node with its vendored node_modules"
    - path: "flowstate/vendor/gsd/LICENSE"
      provides: "Upstream GSD MIT license verbatim"
      contains: "MIT"
    - path: "flowstate/vendor/gsd/VERSION"
      provides: "Provenance — npm version 1.42.3 + package-lock reference"
      contains: "1.42.3"
    - path: "flowstate/vendor/VENDORING.md"
      provides: "The one canonical, reproducible vendoring procedure (shared source of truth with 15-04 refresh)"
    - path: "NOTICE"
      provides: "GSD MIT attribution + bundled transitive-dep license pointer"
      contains: "Lex Christopherson"
  key_links:
    - from: "pyproject.toml"
      to: "flowstate/vendor/*"
      via: "coverage omit + wheel force-include + pytest collect-ignore"
      pattern: "vendor"
---

<objective>
Vendor a LEAN, FULL-PARITY GSD install into `flowstate/vendor/gsd/` so `gsd-sdk` works directly with
zero user install. Produce the tree with `npm install get-shit-done-cc@1.42.3 --omit=optional
--omit=dev` (51M) and vendor the resulting `node_modules/`. This deliberately EXCLUDES the ~197M
optional platform binary `@anthropic-ai/claude-agent-sdk-darwin-arm64/claude` (the Claude Code CLI
itself — redundant with the user's `claude` prereq, platform-locked, 400× over the large-file hook);
the `gsd-sdk query` path never needs it. Capture provenance (pinned version + lockfile), the GSD
LICENSE verbatim, NOTICE attributions (GSD + bundled transitive deps), document the canonical
procedure, and exclude the tree from the large-file hook, pytest collection, and coverage.

Purpose: Foundation for GSD-02/03/04. The vendored tree must prove FULL `gsd-sdk` parity before
Wave 2 depends on it. The GSD skills call `gsd-sdk query …` in 104 files, so a broken/shimmed CLI is
not acceptable — this ships the real thing.
Output: `flowstate/vendor/gsd/**` (incl. `node_modules/`), `.../gsd/VERSION`, `.../gsd/LICENSE`,
`flowstate/vendor/VENDORING.md`, updated `NOTICE`, `pyproject.toml`, `.pre-commit-config.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<empirical_findings>
Verified by the planner against get-shit-done-cc@1.42.3 (npm):
- `npm install get-shit-done-cc@1.42.3 --omit=optional --omit=dev` -> `node_modules` = 51M, ~100 production dep packages; `find node_modules -type f -size +10M` returns NOTHING; the optional platform dir `@anthropic-ai/claude-agent-sdk-*/` is ABSENT.
- FULL parity from the vendored tree (run from a project root with .planning/ROADMAP.md): `node node_modules/get-shit-done-cc/bin/gsd-sdk.js query roadmap.get-phase 15` -> `"phase_name": "Bundle GSD"`; `query config-get commit_docs` -> `true`. The query path never needs the platform binary (that is only for spawning claude sessions, which the user's own `claude` handles).
- 5 files exceed 500KB (large-file hook `--maxkb=500`): `@anthropic-ai/claude-agent-sdk/{browser-sdk.js,bridge.mjs,sdk.mjs,assistant.mjs}` (assistant.mjs ~1.5M) and `get-shit-done-cc/sdk-bundle/gsd-sdk.tgz`. None exceed 10M.
- ~98 LICENSE files exist across the vendored node_modules (third-party MIT/ISC/Apache deps) -> NOTICE must point at them.
- Cross-platform: no platform binary is vendored, so `gsd-sdk query` works on every platform; agent-session spawning falls back to the user's own `claude` on PATH.
</empirical_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the lean 51M install and vendor node_modules with full gsd-sdk parity</name>
  <files>flowstate/vendor/gsd/**, flowstate/vendor/gsd/VERSION, flowstate/vendor/gsd/LICENSE, flowstate/vendor/VENDORING.md</files>
  <read_first>
    - flowstate/skills/ (how Phase 14 vendored MIT trees as data-only, for structural precedent)
    - The generated `get-shit-done-cc/bin/install.js` (authoritative mapping of upstream dirs -> the installed `.claude/` layout that 15-02 reproduces)
  </read_first>
  <acceptance_criteria>
    - `flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js` exists, and `.../get-shit-done-cc/commands/gsd/` and `.../get-shit-done-cc/get-shit-done/` (with `workflows/`, `references/`, `templates/`, `contexts/`, `bin/`) exist
    - PLATFORM BINARY ABSENT: `find flowstate/vendor/gsd -type f -size +10M` returns nothing, and no `@anthropic-ai/claude-agent-sdk-*/` platform dir is present
    - Total vendored tree ~51M (record the exact `du -sh` in the SUMMARY)
    - `flowstate/vendor/gsd/LICENSE` exists and contains "MIT" (GSD's own license, verbatim)
    - `flowstate/vendor/gsd/VERSION` records npm version `1.42.3` and references the captured `package-lock.json` for reproducible deps
    - `flowstate/vendor/VENDORING.md` documents the exact reproducible procedure (npm install pinned version with --omit=optional --omit=dev -> vendor node_modules -> exclude platform binary -> verify gsd-sdk parity) so 15-04's refresh cannot diverge
    - INVOKABILITY GATE (authoritative): `node flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js query roadmap.get-phase 15` run from the repo root returns output containing `"Bundle GSD"` — recorded exactly (command + output) in the SUMMARY before Wave 2 begins
  </acceptance_criteria>
  <action>
    In a clean scratch dir with a minimal `package.json`, run `npm install get-shit-done-cc@1.42.3
    --omit=optional --omit=dev` (this omits the 197M platform binary and all dev deps, yielding ~51M).
    Capture the generated `package-lock.json`. Vendor the resulting `node_modules/` tree into
    `flowstate/vendor/gsd/node_modules/` (the `get-shit-done-cc` package + its ~100 production deps).
    Verify and enforce that the optional platform binary is absent: `find flowstate/vendor/gsd -type f
    -size +10M` must return nothing and no `@anthropic-ai/claude-agent-sdk-*/` dir may exist (if npm
    pulled it despite --omit=optional on some environment, delete that platform dir and re-verify).
    Copy the `get-shit-done-cc` package's `LICENSE` to `flowstate/vendor/gsd/LICENSE` verbatim. Write
    `flowstate/vendor/gsd/VERSION` recording npm version `1.42.3` and pointing to the captured
    `package-lock.json` (store the lockfile under `flowstate/vendor/gsd/`). Write
    `flowstate/vendor/VENDORING.md` documenting the canonical reproducible procedure (the single
    source of truth 15-04 reuses), including the cross-platform note: no platform binary is vendored,
    so `gsd-sdk query` works everywhere and agent-session spawning falls back to the user's own
    `claude` on PATH. Do NOT execute any vendored file except the dependency-satisfied `gsd-sdk`
    invokability check. Record the exact invokability command + its "Bundle GSD" output in the SUMMARY.
  </action>
  <verify>
    <automated>test -f flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js &amp;&amp; test -f flowstate/vendor/gsd/LICENSE &amp;&amp; test -d flowstate/vendor/gsd/node_modules/get-shit-done-cc/commands/gsd &amp;&amp; grep -q "1.42.3" flowstate/vendor/gsd/VERSION &amp;&amp; [ -z "$(find flowstate/vendor/gsd -type f -size +10M)" ] &amp;&amp; node flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js query roadmap.get-phase 15 2>/dev/null | grep -q "Bundle GSD"</automated>
  </verify>
  <done>The lean 51M full-parity GSD tree is vendored (platform binary absent), gsd-sdk proven working via a real query recorded in the SUMMARY, with LICENSE + VERSION + lockfile + VENDORING.md.</done>
</task>

<task type="auto">
  <name>Task 2: NOTICE attributions, large-file-hook exclusion, coverage + wheel + pytest-collection handling</name>
  <files>NOTICE, pyproject.toml, .pre-commit-config.yaml</files>
  <read_first>
    - NOTICE (append GSD stanza; mirror existing gstack/superpowers entries)
    - pyproject.toml (`[tool.coverage.run] omit = [... "flowstate/skills/*"]`, wheel `artifacts` list, `[tool.pytest.ini_options]`)
    - .pre-commit-config.yaml (the `check-added-large-files --maxkb=500` hook + the existing `exclude: ^flowstate/skills/` pattern on trailing-whitespace/end-of-file-fixer)
  </read_first>
  <acceptance_criteria>
    - `NOTICE` contains "Lex Christopherson" (GSD, `get-shit-done-cc`, MIT) AND a line pointing to `flowstate/vendor/gsd/node_modules/` for the bundled third-party deps' own LICENSE files
    - `.pre-commit-config.yaml` `check-added-large-files` hook has `exclude: ^flowstate/vendor/`; the global `--maxkb=500` threshold is UNCHANGED; the whitespace/eof hooks also exclude `^flowstate/vendor/`
    - `pyproject.toml` `[tool.coverage.run] omit` includes `flowstate/vendor/*`
    - The wheel force-includes `flowstate/vendor/**/*` (artifacts list) so the 51M tree ships; the ~51M size is noted in-plan/SUMMARY (under PyPI's 100M limit)
    - Pytest does not collect any file under `flowstate/vendor/` — verified with a check that FAILS if a vendored file is collected: `! (python -m pytest --collect-only -q 2>&1 | grep -q "flowstate/vendor/")`
    - `git add flowstate/vendor && pre-commit run check-added-large-files` (or an equivalent staged run) passes with the exclusion
  </acceptance_criteria>
  <action>
    Append a GSD MIT attribution stanza to `NOTICE` (© 2025 Lex Christopherson, `gsd-build/
    get-shit-done`, npm `get-shit-done-cc`, MIT) plus a line noting that
    `flowstate/vendor/gsd/node_modules/` bundles ~100 third-party packages (MIT/ISC/Apache) whose own
    LICENSE files travel with them in that tree. In `.pre-commit-config.yaml`, add `exclude:
    ^flowstate/vendor/` to the `check-added-large-files` hook (do NOT raise `--maxkb`), and add the
    same `^flowstate/vendor/` exclusion to the `trailing-whitespace` and `end-of-file-fixer` hooks so
    vendored JS/mjs is not reformatted. In `pyproject.toml`: add `flowstate/vendor/*` to
    `[tool.coverage.run] omit`; add `flowstate/vendor/**/*` to the hatch wheel `artifacts` list so the
    tree ships in the wheel (note the ~51M size, under PyPI's 100M cap); and ensure pytest never
    collects the vendored tree via a root `conftest.py` `collect_ignore_glob` for `flowstate/vendor`
    and/or a `norecursedirs` entry. Add no new Python runtime dependency; do not bump the package
    version here.
  </action>
  <verify>
    <automated>grep -q "Lex Christopherson" NOTICE &amp;&amp; grep -q "flowstate/vendor" .pre-commit-config.yaml &amp;&amp; grep -q "flowstate/vendor" pyproject.toml &amp;&amp; ! (python -m pytest --collect-only -q 2>&amp;1 | grep -q "flowstate/vendor/")</automated>
  </verify>
  <done>NOTICE carries GSD + bundled-dep attributions; the vendored tree is excluded from the large-file hook and pytest collection, omitted from coverage, and force-included in the wheel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry + transitive deps -> vendored tree | A third-party package AND its ~100 transitive deps are copied into FlowState as data |
| vendored node_modules -> package build/test/commit | Vendored JS/mjs could distort coverage, trip the large-file hook, or execute if collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | npm install of get-shit-done-cc | mitigate | Pin version 1.42.3 + capture package-lock.json for reproducible transitive deps; record in VERSION and SUMMARY — the 51M snapshot is reproducible and auditable |
| T-15-02 | Repudiation | third-party license attribution | mitigate | GSD LICENSE captured verbatim + NOTICE MIT attribution (Lex Christopherson) + NOTICE pointer to the ~98 bundled dep LICENSE files that travel in node_modules — all reuse is auditable |
| T-15-03 | Elevation of Privilege | vendored node_modules (JS/mjs) | mitigate | Copy as data only; the ONLY vendored file executed is `gsd-sdk` for the invokability check; exclude the tree from pytest collection so no vendored code runs in the test process; no nested `npm install`/postinstall runs at vendor time |
| T-15-13 | Denial of Service | 197M platform binary bloat | mitigate | `--omit=optional` excludes the platform `claude` binary; enforced by `find -size +10M` returning nothing + large-file-hook exclusion scoped to `^flowstate/vendor/` (global 500KB threshold unchanged) |
| T-15-SC | Tampering | supply-chain of ~100 transitive deps | mitigate | Lockfile-pinned install at a fixed GSD version; no moving tags; platform binary excluded; vendored code never auto-executed at install (15-02 copies as data) |
</threat_model>

<verification>
- `flowstate/vendor/gsd/node_modules/get-shit-done-cc/bin/gsd-sdk.js` exists; the tree is ~51M with no file >10M and no platform binary dir.
- The invokability gate (`gsd-sdk query roadmap.get-phase 15` -> "Bundle GSD") passes and is recorded in the SUMMARY.
- `NOTICE` carries the GSD MIT attribution + bundled-dep pointer.
- The large-file hook excludes `^flowstate/vendor/`; pytest excludes the tree; coverage omits it; the wheel force-includes it.
</verification>

<success_criteria>
Success Criterion 1 is met: a lean, full-parity, MIT-attributed GSD install (get-shit-done-cc@1.42.3
+ production node_modules, platform binary excluded) is vendored with reproducible provenance, and
`gsd-sdk` is proven invokable directly from the vendored tree; the tree does not distort the test or
commit gates.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-01-SUMMARY.md` when done, recording the npm version, the vendored size (du -sh), and the exact `gsd-sdk query roadmap.get-phase 15` command + "Bundle GSD" output.
</output>
