---
phase: 15-bundle-gsd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/vendor/gsd/**
  - NOTICE
  - pyproject.toml
autonomous: true
requirements: [GSD-01]
must_haves:
  truths:
    - "flowstate/vendor/gsd/ holds the pinned GSD working distribution (skills + get-shit-done runtime + gsd-sdk CLI)"
    - "The upstream MIT LICENSE is captured verbatim and a VERSION/commit provenance file records the pinned SHA"
    - "NOTICE carries the GSD MIT attribution (Lex Christopherson)"
    - "Vendored JS/CJS/MD/test files are excluded from pytest collection and coverage measurement"
  artifacts:
    - path: "flowstate/vendor/gsd/LICENSE"
      provides: "Upstream MIT license verbatim"
      contains: "MIT"
    - path: "flowstate/vendor/gsd/VERSION"
      provides: "Provenance — pinned upstream version + commit SHA"
    - path: "flowstate/vendor/gsd/get-shit-done"
      provides: "GSD Node runtime (bin, contexts, references, templates, workflows)"
    - path: "NOTICE"
      provides: "Third-party attribution including GSD"
      contains: "Lex Christopherson"
  key_links:
    - from: "pyproject.toml"
      to: "flowstate/vendor/*"
      via: "coverage omit + pytest norecursedirs/collect_ignore"
      pattern: "vendor"
---

<objective>
Vendor a pinned, MIT-licensed GSD working distribution into `flowstate/vendor/gsd/` so
FlowState ships GSD self-contained. Capture the upstream LICENSE verbatim, record the pinned
commit SHA + upstream VERSION for provenance, add the GSD attribution to NOTICE, and exclude the
vendored tree from pytest collection and coverage measurement.

Purpose: This is the foundation for GSD-02/03/04 — nothing can install, launch, or refresh a
vendored GSD that does not yet exist on disk.
Output: `flowstate/vendor/gsd/**`, `flowstate/vendor/gsd/LICENSE`, `flowstate/vendor/gsd/VERSION`,
updated `NOTICE`, updated `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone GSD at a pinned commit and vendor the working-distribution subset</name>
  <read_first>
    - flowstate/skills/ (how Phase 14 vendored MIT trees as data-only, for structural precedent)
    - NOTICE (existing gstack + superpowers MIT attributions — append, do not rewrite)
    - The upstream repo's own `bin/install.js` and `package.json` `files` array (authoritative source of what a working install requires)
  </read_first>
  <acceptance_criteria>
    - `flowstate/vendor/gsd/bin/gsd-sdk.js` exists (the self-contained Node CLI)
    - `flowstate/vendor/gsd/get-shit-done/` exists and contains `workflows/`, `references/`, `templates/`, `contexts/`, `bin/`
    - `flowstate/vendor/gsd/commands/gsd/` exists (source of the gsd-* skills)
    - `flowstate/vendor/gsd/LICENSE` exists and contains "MIT"
    - `flowstate/vendor/gsd/VERSION` records both the upstream VERSION string and the pinned commit SHA
    - No `node_modules/`, `tests/`, `docs/`, `.changeset/`, vitest/tsconfig build config, or multi-language READMEs under `flowstate/vendor/gsd/`
  </acceptance_criteria>
  <action>
    Clone `https://github.com/gsd-build/get-shit-done` at a PINNED commit (do not track a moving
    branch); fetch fresh for reproducibility rather than trusting any prior scratchpad clone.
    Record the resolved SHA. Read the upstream `bin/install.js` together with `package.json`'s
    `files` array to derive the EXACT working-distribution subset authoritatively — do not guess.
    The `files` manifest is bin, commands, get-shit-done, agents, hooks, scripts, and
    sdk/{src,shared,prompts,dist,package.json,package-lock.json,tsconfig.json}. Copy that subset
    (as data — never execute any vendored file) into `flowstate/vendor/gsd/`, preserving the
    upstream directory layout so install.js-equivalent mappings resolve later. Confirm from
    install.js whether the base skill/query workflow needs the two sdk npm deps
    (@anthropic-ai/claude-agent-sdk, ws) or runs off the prebuilt `sdk/dist`; if the base workflow
    runs off dist, do NOT vendor node_modules and add no Python dep. EXCLUDE dev-only content not
    needed at runtime: `tests/`, `docs/`, multi-language READMEs, `.changeset/`, vitest/tsconfig/
    node build config, and any `node_modules/`. Capture the upstream `LICENSE` verbatim into
    `flowstate/vendor/gsd/LICENSE`. Write `flowstate/vendor/gsd/VERSION` recording the upstream
    VERSION string and the pinned commit SHA for provenance. Record the SHA + VERSION in the plan
    SUMMARY.
  </action>
  <verify>
    <automated>test -f flowstate/vendor/gsd/bin/gsd-sdk.js &amp;&amp; test -f flowstate/vendor/gsd/LICENSE &amp;&amp; test -f flowstate/vendor/gsd/VERSION &amp;&amp; test -d flowstate/vendor/gsd/get-shit-done/workflows &amp;&amp; test -d flowstate/vendor/gsd/commands/gsd &amp;&amp; ! find flowstate/vendor/gsd -type d -name node_modules | grep -q . &amp;&amp; grep -qi MIT flowstate/vendor/gsd/LICENSE</automated>
  </verify>
  <done>The pinned GSD distribution subset is on disk under flowstate/vendor/gsd/ with LICENSE + VERSION, no dev-only or node_modules content, and the SHA is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Add GSD attribution to NOTICE and exclude the vendored tree from coverage + pytest collection</name>
  <read_first>
    - NOTICE (append a GSD stanza mirroring the existing gstack/superpowers stanzas)
    - pyproject.toml (existing `[tool.coverage.run] omit = [... "flowstate/skills/*"]`, the wheel `artifacts` list, and `[tool.pytest.ini_options]`)
  </read_first>
  <acceptance_criteria>
    - `NOTICE` contains "Lex Christopherson" and cites `gsd-build/get-shit-done` under MIT
    - `pyproject.toml` `[tool.coverage.run] omit` includes `flowstate/vendor/*`
    - The wheel `artifacts` list includes `flowstate/vendor/**/*` so vendored data ships in the package
    - Pytest does not collect any file under `flowstate/vendor/` (a `*.js`/`*.cjs`/vendored test file cannot distort the gate or slow collection)
    - `pytest --collect-only` succeeds and reports zero collected items under `flowstate/vendor/`
  </acceptance_criteria>
  <action>
    Append a GSD MIT attribution stanza to `NOTICE` (© 2025 Lex Christopherson, `gsd-build/
    get-shit-done`, MIT), matching the format of the existing gstack/superpowers entries. In
    `pyproject.toml`: add `flowstate/vendor/*` to `[tool.coverage.run] omit` alongside the existing
    `flowstate/skills/*`; add `flowstate/vendor/**/*` to the hatch wheel `artifacts` list so the
    vendored data is packaged; and ensure pytest never collects the vendored tree — add a
    `norecursedirs`/`collect_ignore_glob` entry for `flowstate/vendor` under
    `[tool.pytest.ini_options]` (or a `conftest.py` `collect_ignore_glob`) so vendored `.py`/test
    files are excluded from collection. Do not add any new Python runtime dependency. Do not bump
    the package version in this task.
  </action>
  <verify>
    <automated>grep -q "Lex Christopherson" NOTICE &amp;&amp; grep -q "flowstate/vendor" pyproject.toml &amp;&amp; python -m pytest --collect-only -q 2>&amp;1 | grep -vq "flowstate/vendor/"</automated>
  </verify>
  <done>NOTICE carries the GSD MIT attribution; the vendored tree is omitted from coverage, shipped in the wheel, and never collected by pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream repo → vendored tree | Third-party MIT code copied into the FlowState package as data |
| vendored tree → package build/test | Vendored JS/CJS could distort coverage or be executed if collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | clone of gsd-build/get-shit-done | mitigate | Pin an exact commit SHA (never a moving branch); record SHA + VERSION in VERSION file and SUMMARY for provenance |
| T-15-02 | Repudiation | third-party license attribution | mitigate | Capture upstream LICENSE verbatim + add MIT attribution (Lex Christopherson) to NOTICE — legal reuse is auditable |
| T-15-03 | Elevation of Privilege | vendored Node/JS files | mitigate | Copy as data only; never execute during vendoring; exclude from pytest collection so no vendored code runs in the test process |
| T-15-SC | Tampering | npm/node build config in upstream | mitigate | Exclude node_modules and all build/dev scaffolding; base workflow verified to run off prebuilt sdk/dist so no dependency install is required |
</threat_model>

<verification>
- `flowstate/vendor/gsd/` contains skills source (`commands/gsd/`), the `get-shit-done/` runtime, and `bin/gsd-sdk.js`, with LICENSE + VERSION.
- `NOTICE` carries the GSD MIT attribution.
- `pytest --collect-only` excludes the vendored tree; coverage omits `flowstate/vendor/*`.
</verification>

<success_criteria>
Success Criterion 1 is met: the pinned GSD distribution is vendored with LICENSE verbatim, a
recorded VERSION/commit, and a NOTICE attribution; the vendored tree does not distort the test
gate.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-01-SUMMARY.md` when done, recording the pinned commit SHA and upstream VERSION.
</output>
