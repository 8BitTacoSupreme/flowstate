---
phase: 15-bundle-gsd
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - flowstate/gsd_vendor.py
  - flowstate/cli.py
  - tests/test_gsd_vendor.py
autonomous: true
requirements: [GSD-04]
must_haves:
  truths:
    - "A documented refresh path updates the pinned GSD snapshot deliberately (never silently)"
    - "The vendored GSD VERSION/commit is inspectable via a flowstate command"
    - "Staleness is reported by mirroring the flowstate pack manifest/staleness pattern"
  artifacts:
    - path: "flowstate/gsd_vendor.py"
      provides: "GSD refresh + staleness/provenance service (mirrors pack.py)"
      contains: "def "
    - path: "flowstate/cli.py"
      provides: "flowstate gsd-refresh (or gsd) CLI surface to inspect version + refresh the pin"
    - path: "tests/test_gsd_vendor.py"
      provides: "Offline tests for version inspection + staleness + deliberate-refresh guard"
  key_links:
    - from: "flowstate/cli.py"
      to: "flowstate/gsd_vendor.py"
      via: "CLI command invokes the refresh/version service"
      pattern: "gsd_vendor"
---

<objective>
Add a documented refresh/staleness path for the pinned vendored GSD, mirroring the `flowstate pack`
manifest/staleness pattern. The vendored `VERSION`/commit must be inspectable, and updating the pin
must be a deliberate, explicit action (never a silent auto-update).

Purpose: Success Criterion 4 — the pinned GSD snapshot can be updated on purpose and its provenance
is visible.
Output: new `flowstate/gsd_vendor.py`, a CLI surface in `flowstate/cli.py`, and
`tests/test_gsd_vendor.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From flowstate/pack.py (the staleness/provenance pattern to mirror):
- `_find_repomix() -> str` — locator function (mirror for a git/CLI locator if needed)
- `@dataclass PackResult(success, output_path, exit_code, error)` and `@dataclass PackConfig(...)`
- `run_pack(root, *, compress=False) -> PackResult` — locate → run → register manifest → result
- `is_pack_stale(root, state) -> bool` — reads the install_manifest entry, compares mtimes

From flowstate/cli.py:
- `@main.command("pack")` (line 754) — the CLI shape to mirror for a GSD refresh/version command
- `resolve_root(...)`, `_root_was_explicit()`, `console`, `BANNER`, `__version__`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the GSD refresh + version-inspection service (mirror pack.py)</name>
  <read_first>
    - flowstate/pack.py (the full locator + result/config dataclass + run/staleness shape to mirror)
    - flowstate/vendor/gsd/VERSION (the provenance file written by 15-01 — the source of truth to read/report)
    - flowstate/state.py (InstallEntry; how pack registers a manifest entry for staleness)
  </read_first>
  <acceptance_criteria>
    - A `read_vendored_version()` (or equivalent) returns the pinned upstream VERSION + commit SHA from `flowstate/vendor/gsd/VERSION`
    - A refresh function re-clones GSD at a caller-supplied commit/ref and rewrites `flowstate/vendor/gsd/` + VERSION, and is a NO-OP unless the caller explicitly requests the update (no silent auto-refresh)
    - A staleness/provenance check reports the currently pinned version (mirroring `is_pack_stale`'s manifest-driven approach) without mutating anything
    - The service adds no new Python runtime dependency and never executes vendored GSD code
  </acceptance_criteria>
  <behavior>
    - Test: read_vendored_version() returns the SHA + VERSION string written into flowstate/vendor/gsd/VERSION
    - Test: the refresh function does nothing when not explicitly invoked (guarded), i.e. inspection alone never rewrites the vendor tree
    - Test: the version/staleness reporter runs offline against the on-disk VERSION and returns a structured result (no network)
    - Test: refresh targets a pinned ref (fails or refuses if asked to track a moving branch without an explicit ref)
  </behavior>
  <action>
    Create `flowstate/gsd_vendor.py` mirroring `flowstate/pack.py`'s structure: a locator (git, if
    the refresh shells out to clone), a `@dataclass` result, and public functions. Provide (a) a
    version/provenance reader that returns the pinned upstream VERSION + commit SHA from
    `flowstate/vendor/gsd/VERSION`, and (b) a deliberate `refresh` function that re-clones
    `gsd-build/get-shit-done` at an explicitly-supplied commit/ref, re-derives the working subset
    (same exclusions as 15-01), and rewrites `flowstate/vendor/gsd/` + `VERSION`. The refresh MUST be
    explicit — never run as a side effect of inspection, and never track a moving branch silently
    (require a pinned ref). Mirror `is_pack_stale`'s manifest/mtime approach for a staleness/
    provenance report that only reads. Add no new Python dependency; do not execute any vendored GSD
    file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gsd_vendor.py -x -q</automated>
  </verify>
  <done>The GSD refresh/version service exists, mirrors pack.py, inspects the pinned VERSION, and only updates the pin on explicit request — covered by offline tests.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the refresh/version path on the CLI and document it</name>
  <read_first>
    - flowstate/cli.py (the `@main.command("pack")` at line 754 — mirror its option/console/root-resolution shape; add a GSD command near it)
    - flowstate/gsd_vendor.py (the service built in Task 1)
  </read_first>
  <acceptance_criteria>
    - A `flowstate` CLI command prints the pinned GSD VERSION + commit (inspectable provenance) and exits 0
    - The same command (or a `--refresh <ref>` flag) performs the deliberate refresh only when the update is explicitly requested
    - The command help text documents the refresh path (how to deliberately update the pin)
    - `pytest` exits 0
  </acceptance_criteria>
  <action>
    Add a CLI command to `flowstate/cli.py` (e.g. `flowstate gsd-version` / `flowstate gsd-refresh`,
    or a single `flowstate gsd` command with a `--refresh <ref>` flag) that, by default, prints the
    pinned GSD VERSION + commit from the vendor service (inspectable provenance), and only performs
    the deliberate re-clone/refresh when the operator explicitly passes the refresh flag with a
    pinned ref. Mirror the `pack` command's root resolution, banner, and Rich output. Write the
    command docstring/help so the refresh procedure is documented at the CLI surface. Add a CLI-level
    test (Click `CliRunner`, offline) asserting the version is printed and that refresh is not
    triggered by the default invocation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gsd_vendor.py -q &amp;&amp; python -m pytest -q 2>&amp;1 | tail -1</automated>
  </verify>
  <done>The pinned GSD VERSION is inspectable via the CLI, the deliberate refresh is documented and gated behind an explicit flag, and the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refresh → vendored tree | Re-cloning third-party code overwrites the committed vendor snapshot |
| CLI → git clone subprocess | Refresh shells out to fetch upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | silent snapshot drift | mitigate | Refresh is explicit-only (requires operator action + a pinned ref); inspection never rewrites the tree — no silent auto-update |
| T-15-11 | Elevation of Privilege | re-clone of upstream | mitigate | Refresh re-applies the 15-01 exclusion set and never executes vendored code; a moving branch is refused without an explicit pinned ref |
| T-15-12 | Repudiation | provenance visibility | mitigate | VERSION + commit SHA are inspectable via the CLI so the pinned snapshot is always auditable |
</threat_model>

<verification>
- The pinned GSD VERSION + commit is printed by a `flowstate` command.
- A default invocation never rewrites the vendor tree.
- Refresh updates the pin only on an explicit, pinned-ref request.
</verification>

<success_criteria>
Success Criterion 4 is met: a documented refresh path (mirroring `flowstate pack`) updates the
pinned GSD snapshot deliberately, and the vendored VERSION is inspectable.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-04-SUMMARY.md` when done.
</output>
