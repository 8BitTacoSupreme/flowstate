---
phase: 15-bundle-gsd
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - flowstate/gsd_vendor.py
  - flowstate/cli.py
  - tests/test_gsd_vendor.py
autonomous: true
requirements: [GSD-04]
must_haves:
  truths:
    - "A documented refresh path updates the pinned GSD snapshot deliberately (never silently) — an npm-version bump + lockfile update, encoding the SAME procedure 15-01 used"
    - "The vendored GSD npm version + lockfile reference is inspectable via a flowstate command"
    - "Staleness/provenance is reported by mirroring the flowstate pack manifest/staleness pattern"
  artifacts:
    - path: "flowstate/gsd_vendor.py"
      provides: "GSD refresh + version/provenance service encoding the canonical lean-npm-install vendoring procedure (mirrors pack.py)"
      contains: "def "
    - path: "flowstate/cli.py"
      provides: "flowstate CLI surface to inspect the pinned GSD version + deliberately refresh the pin"
    - path: "tests/test_gsd_vendor.py"
      provides: "Offline tests for version inspection + staleness + deliberate-refresh guard"
  key_links:
    - from: "flowstate/cli.py"
      to: "flowstate/gsd_vendor.py"
      via: "CLI command invokes the refresh/version service"
      pattern: "gsd_vendor"
---

<objective>
Add a documented refresh/staleness path for the pinned vendored GSD, mirroring the `flowstate pack`
manifest/staleness pattern. The vendored npm version + lockfile must be inspectable, and updating
the pin must be a deliberate, explicit action (never a silent auto-update). Crucially, the refresh
encodes the SAME canonical procedure 15-01 used and documented in `flowstate/vendor/VENDORING.md`
(`npm install get-shit-done-cc@<pinned> --omit=optional --omit=dev` -> vendor node_modules ->
exclude the platform binary -> verify `gsd-sdk` full parity), so the initial snapshot and any refresh
never diverge. Refresh is an npm-version bump + lockfile update.

Purpose: Success Criterion 4 — the pinned GSD snapshot can be updated on purpose, its provenance is
visible, and there is ONE shared source of truth for how the snapshot is built.
Output: new `flowstate/gsd_vendor.py`, a CLI surface in `flowstate/cli.py`, and
`tests/test_gsd_vendor.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From flowstate/pack.py (the staleness/provenance pattern to mirror):
- `_find_repomix() -> str` — locator function (mirror for an `npm` locator)
- `@dataclass PackResult(success, output_path, exit_code, error)` and `@dataclass PackConfig(...)`
- `run_pack(root, *, compress=False) -> PackResult` — locate -> run -> register manifest -> result
- `is_pack_stale(root, state) -> bool` — reads the install_manifest entry, compares mtimes

From flowstate/cli.py:
- `@main.command("pack")` (line 754) — the CLI shape to mirror for a GSD refresh/version command
- `resolve_root(...)`, `_root_was_explicit()`, `console`, `BANNER`, `__version__`

From 15-01 (the shared source of truth this plan reuses):
- `flowstate/vendor/gsd/VERSION` — npm version (1.42.3) + package-lock.json reference
- `flowstate/vendor/gsd/package-lock.json` — captured lockfile for reproducible deps
- `flowstate/vendor/VENDORING.md` — the canonical reproducible vendoring procedure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the GSD refresh + version-inspection service encoding the canonical lean-npm-install procedure</name>
  <files>flowstate/gsd_vendor.py, tests/test_gsd_vendor.py</files>
  <read_first>
    - flowstate/pack.py (the full locator + result/config dataclass + run/staleness shape to mirror)
    - flowstate/vendor/gsd/VERSION and flowstate/vendor/VENDORING.md (the provenance + canonical procedure written by 15-01 — the source of truth to read and re-apply)
    - flowstate/state.py (InstallEntry; how pack registers a manifest entry for staleness)
  </read_first>
  <acceptance_criteria>
    - A `read_vendored_version()` (or equivalent) returns the pinned npm version + lockfile reference from `flowstate/vendor/gsd/VERSION`
    - A refresh function performs the SAME procedure as 15-01 (`npm install get-shit-done-cc@<pinned> --omit=optional --omit=dev` -> vendor node_modules -> exclude the platform binary dir + assert no file >10M -> verify `gsd-sdk` parity) and rewrites `flowstate/vendor/gsd/` + VERSION + lockfile; it is a NO-OP unless the caller explicitly requests the update (no silent auto-refresh)
    - The refresh requires a PINNED npm version (a semver, not a moving `latest` tag)
    - The refresh re-excludes the optional platform binary (`@anthropic-ai/claude-agent-sdk-*/`) and fails if any vendored file exceeds 10M
    - A staleness/provenance check reports the currently pinned version (mirroring `is_pack_stale`'s manifest-driven approach) without mutating anything
    - The service adds no new Python runtime dependency and never executes vendored GSD code except the `gsd-sdk` invokability verification
  </acceptance_criteria>
  <behavior>
    - Test: read_vendored_version() returns the npm version + lockfile reference written into flowstate/vendor/gsd/VERSION by 15-01
    - Test: the refresh function does nothing when not explicitly invoked (guarded) — inspection alone never rewrites the vendor tree
    - Test: the version/staleness reporter runs offline against the on-disk VERSION and returns a structured result (no network)
    - Test: refresh refuses a moving tag (e.g. "latest") — a pinned semver is required
  </behavior>
  <action>
    Create `flowstate/gsd_vendor.py` mirroring `flowstate/pack.py`'s structure: a locator (for the
    `npm` binary, used by refresh), a `@dataclass` result, and public functions. Provide (a) a
    version/provenance reader that returns the pinned npm version + lockfile reference from
    `flowstate/vendor/gsd/VERSION`, and (b) a deliberate `refresh(version, ...)` function that
    executes the canonical procedure documented in `flowstate/vendor/VENDORING.md`: `npm install
    get-shit-done-cc@<pinned-version> --omit=optional --omit=dev` into a clean dir, capture the new
    `package-lock.json`, vendor the resulting `node_modules/` into `flowstate/vendor/gsd/`, EXCLUDE
    the optional platform binary (`@anthropic-ai/claude-agent-sdk-*/`) and assert `find -size +10M`
    is empty, rewrite `VERSION` + lockfile, and VERIFY parity by running `node
    .../get-shit-done-cc/bin/gsd-sdk.js query roadmap.get-phase <N>` and asserting the expected output
    before committing the refresh. The refresh MUST be explicit — never a side effect of inspection —
    and MUST require a pinned semver (refuse `latest`/moving tags). Mirror `is_pack_stale`'s
    manifest/mtime approach for a read-only staleness/provenance report. Encode the procedure ONCE
    here so it is the executable form of VENDORING.md; add no new Python dependency; never run any
    vendored GSD file, npm postinstall, or nested install other than the pinned refresh install +
    the `gsd-sdk` parity check.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gsd_vendor.py -x -q</automated>
  </verify>
  <done>The GSD refresh/version service exists, mirrors pack.py, reads the pinned npm version/lockfile, re-applies the canonical lean-install procedure (platform binary excluded, parity verified) on explicit request only, and refuses moving tags — covered by offline tests.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the refresh/version path on the CLI and document it</name>
  <files>flowstate/cli.py, tests/test_gsd_vendor.py</files>
  <read_first>
    - flowstate/cli.py (the `@main.command("pack")` at line 754 — mirror its option/console/root-resolution shape; add a GSD command near it)
    - flowstate/gsd_vendor.py (the service built in Task 1)
  </read_first>
  <acceptance_criteria>
    - A `flowstate` CLI command prints the pinned GSD npm version + lockfile reference (inspectable provenance) and exits 0
    - The same command (or a `--refresh <version>` flag) performs the deliberate refresh only when an explicit pinned version is requested
    - The command help text documents the refresh path (how to deliberately update the pin, and that it mirrors `flowstate pack` staleness)
    - `pytest` exits 0
  </acceptance_criteria>
  <action>
    Add a CLI command to `flowstate/cli.py` (e.g. `flowstate gsd-version` / `flowstate gsd-refresh`,
    or a single `flowstate gsd` command with a `--refresh <version>` flag) that, by default, prints
    the pinned GSD npm version + lockfile reference from the vendor service (inspectable provenance),
    and only performs the deliberate re-install/refresh when the operator explicitly passes the
    refresh flag with a pinned semver. Mirror the `pack` command's root resolution, banner, and Rich
    output. Write the command docstring/help so the refresh procedure is documented at the CLI surface
    (and points to `flowstate/vendor/VENDORING.md`). Add a CLI-level test (Click `CliRunner`, offline)
    asserting the version is printed and that refresh is not triggered by the default invocation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gsd_vendor.py -q &amp;&amp; python -m pytest -q 2>&amp;1 | tail -1</automated>
  </verify>
  <done>The pinned GSD version/lockfile is inspectable via the CLI, the deliberate refresh is documented and gated behind an explicit pinned-version flag, and the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refresh -> vendored tree | Re-installing third-party code + its deps overwrites the committed vendor snapshot |
| CLI -> npm install subprocess | Refresh shells out to fetch the pinned upstream package + production deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | silent snapshot drift | mitigate | Refresh is explicit-only (requires operator action + a pinned semver); inspection never rewrites the tree — no silent auto-update |
| T-15-11 | Elevation of Privilege | re-install of upstream + deps | mitigate | Refresh re-applies the 15-01 exclusion (platform binary removed, no file >10M), pins the semver + captures the lockfile, and never executes vendored code beyond the `gsd-sdk` parity check; a moving tag is refused |
| T-15-12 | Repudiation | provenance visibility | mitigate | npm version + lockfile are inspectable via the CLI so the pinned snapshot is always auditable; the procedure is the single documented source of truth (VENDORING.md) shared with 15-01 |
</threat_model>

<verification>
- The pinned GSD npm version + lockfile is printed by a `flowstate` command.
- A default invocation never rewrites the vendor tree.
- Refresh updates the pin only on an explicit, pinned-version request, re-excluding the platform binary and re-verifying `gsd-sdk` parity.
</verification>

<success_criteria>
Success Criterion 4 is met: a documented refresh path (mirroring `flowstate pack`, encoding the one
canonical lean-install vendoring procedure) updates the pinned GSD snapshot deliberately, and the
vendored version/lockfile is inspectable.
</success_criteria>

<output>
Create `.planning/phases/15-bundle-gsd/15-04-SUMMARY.md` when done.
</output>
