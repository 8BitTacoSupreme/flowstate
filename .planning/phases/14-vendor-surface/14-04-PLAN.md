---
phase: 14-vendor-surface
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - flowstate/launcher.py
  - flowstate/cli.py
  - tests/test_launcher_skills.py
  - README.md
autonomous: true
requirements: [VEND-04, VEND-05]
must_haves:
  truths:
    - "flowstate launch strategy prints the claude + /office-hours handoff when gstack skills are installed"
    - "flowstate launch discipline prints the superpowers test-driven-development handoff when installed"
    - "The handoffs surface ONLY when the vendored skills are present in .claude/skills/"
    - "When skills are absent, launch tells the user to run flowstate install-skills instead of a broken handoff"
    - "The shipped README test count equals the collected count AFTER all Phase-14 test files exist (never 803/947)"
  artifacts:
    - path: "flowstate/launcher.py"
      provides: "strategy→/office-hours and discipline→TDD handoffs gated on installed skills"
      contains: "office-hours"
    - path: "flowstate/cli.py"
      provides: "launch Choice extended to include discipline"
      contains: "discipline"
    - path: "tests/test_launcher_skills.py"
      provides: "offline tests: handoff strings present when installed, install prompt when absent"
    - path: "README.md"
      provides: "final test-count reconciliation to the true post-phase collected total"
  key_links:
    - from: "flowstate/launcher.py launch_command"
      to: ".claude/skills/gstack | .claude/skills/superpowers"
      via: "detect installed skill namespace before emitting handoff"
      pattern: "\\.claude/skills/(gstack|superpowers)"
---

<objective>
Wire `flowstate launch strategy` to gstack's `/office-hours` handoff and `flowstate launch discipline` to the superpowers `test-driven-development` skill handoff — surfaced ONLY when the vendored skills are installed in the project. Mirror the existing `_gsd_command`/`launch_command` pattern. Requires the skills to exist in-repo (14-01) and the installer (14-03), and shares `cli.py`/`README.md` with earlier plans, so it runs last (Wave 3).

Because this is the FINAL Phase-14 plan and it adds a new test file, it also owns the closing VEND-05 chore: re-derive the true post-phase test count (now that 14-03's `test_installer.py` and this plan's `test_launcher_skills.py` exist) and reconcile README's test-count line to it, so no stale count ships.

Purpose: the vendored upstream skills become discoverable through the same `flowstate launch` surface as GSD, and the README test count reflects reality at phase end.
Output: updated `flowstate/launcher.py`, the `launch` command's tool Choice extended to include `discipline`, offline tests, and a reconciled README test count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@flowstate/launcher.py
@flowstate/cli.py
@README.md

<interfaces>
<!-- launcher.py::launch_command(tool, phase, root) today dispatches gsd/research/strategy; _gsd_command builds "/gsd:plan-phase N"; the returned string is "cd {dir} && claude\n  → {cmd}". -->
<!-- cli.py::launch (line ~271): @click.argument("tool", type=click.Choice(["gsd","research","strategy"])) — "discipline" is NOT yet a valid choice and MUST be added. -->
<!-- LOCKED surface mappings (VEND-04): strategy → gstack /office-hours; discipline → superpowers test-driven-development. Surface only when .claude/skills/gstack/ (strategy) / .claude/skills/superpowers/ (discipline) are present. -->
<!-- The installer (14-03) writes .claude/skills/gstack/ and .claude/skills/superpowers/ — detection target for gating. -->
<!-- 14-02 already set README's test-count line (self-reconciling); this plan re-derives it once test_installer.py + test_launcher_skills.py exist. NEVER write 803 or 947. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate strategy→/office-hours and discipline→TDD handoffs on installed skills in launcher.py</name>
  <files>flowstate/launcher.py, tests/test_launcher_skills.py</files>
  <read_first>
    - flowstate/launcher.py (launch_command, _gsd_command, the "cd {dir} && claude → {cmd}" format, detect_tools)
    - flowstate/installer.py (14-03 — the .claude/skills/<namespace> layout it writes)
  </read_first>
  <behavior>
    - launch_command("strategy", root=<installed>) output contains "/office-hours" and a `claude` invocation
    - launch_command("discipline", root=<installed>) output contains "test-driven-development" and a `claude` invocation
    - When .claude/skills/gstack/ is absent, launch_command("strategy", ...) does NOT emit /office-hours and instead prompts `flowstate install-skills`
    - When .claude/skills/superpowers/ is absent, launch_command("discipline", ...) does NOT emit the TDD handoff and instead prompts `flowstate install-skills`
  </behavior>
  <action>
    In `launch_command`, add a `strategy` branch that, when `(root / ".claude/skills/gstack").exists()`, returns a handoff mirroring `_gsd_command`'s shape referencing gstack's `/office-hours` (the YC-office-hours pressure-test skill the strategy adapter mirrors); when absent, return a message directing the user to run `flowstate install-skills`. Add a `discipline` branch that, when `(root / ".claude/skills/superpowers").exists()`, returns a handoff referencing the superpowers `test-driven-development` skill; when absent, the same install prompt. Keep the existing `gsd`/`research` behavior unchanged. Use a small helper to check installed-skill presence so Phase 15 can extend it.
  </action>
  <verify>
    <automated>python -m pytest tests/test_launcher_skills.py -q</automated>
  </verify>
  <acceptance_criteria>
    - With `.claude/skills/gstack/` present, `launch_command("strategy", root=...)` output contains `/office-hours`
    - With `.claude/skills/superpowers/` present, `launch_command("discipline", root=...)` output contains `test-driven-development`
    - With the namespace absent, the respective output contains `install-skills` and NOT the handoff token
    - `python -m pytest tests/test_launcher_skills.py -q` exits 0
  </acceptance_criteria>
  <done>Both handoffs are emitted only when the vendored skills are installed; otherwise the user is told to install them.</done>
</task>

<task type="auto">
  <name>Task 2: Add discipline to the launch CLI Choice and confirm end-to-end surfacing</name>
  <files>flowstate/cli.py</files>
  <read_first>
    - flowstate/cli.py (launch command ~line 271, its click.Choice and docstring examples)
    - flowstate/launcher.py (Task 1 — new strategy/discipline branches)
  </read_first>
  <action>
    Extend the `launch` command's `@click.argument("tool", type=click.Choice([...]))` to include `"discipline"` so `flowstate launch discipline` is accepted (it currently rejects it). Update the command's example docstring to mention the strategy→/office-hours and discipline→TDD handoffs. Do not change the `gsd`/`research`/`strategy` invocation plumbing beyond adding the choice.
  </action>
  <verify>
    <automated>python -c "from click.testing import CliRunner; from flowstate.cli import main; import tempfile,pathlib; d=tempfile.mkdtemp(); CliRunner().invoke(main,['install-skills','--root',d]); r=CliRunner().invoke(main,['launch','discipline','--root',d]); assert r.exit_code==0, r.output; assert 'test-driven-development' in r.output, r.output; s=CliRunner().invoke(main,['launch','strategy','--root',d]); assert 'office-hours' in s.output, s.output; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `flowstate launch discipline` is an accepted choice (no click usage error) and its output contains `test-driven-development` when skills are installed
    - `flowstate launch strategy` output contains `office-hours` when skills are installed
    - Full suite `python -m pytest -q` exits 0 with coverage ≥80%
  </acceptance_criteria>
  <done>`discipline` is a valid launch tool and both new handoffs surface end-to-end through the CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the README test count to the true post-phase total (closing VEND-05)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the test-count line, e.g. "985 tests, 92% coverage", set by 14-02)
  </read_first>
  <action>
    Now that every Phase-14 test file exists (`tests/test_installer.py` from 14-03 and `tests/test_launcher_skills.py` from this plan), re-derive the real collected count with `python -m pytest --collect-only -q` and update README's test-count line to that exact number. This closes VEND-05's "no stale factual claim ships in the same phase as the code that changes it" — the count must reflect the full post-phase test suite. Never write 803 or 947; do not hardcode a literal — read the number from `--collect-only` at runtime.
  </action>
  <verify>
    <automated>N=$(python -m pytest --collect-only -q 2>/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '[0-9]+' | head -1); test -n "$N" && grep -q "$N tests" README.md && ! grep -qE "\b(803|947)\b" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README's test-count line equals the `python -m pytest --collect-only -q` count AFTER `test_installer.py` and `test_launcher_skills.py` exist
    - README never contains `803` or `947`
  </acceptance_criteria>
  <done>The shipped README test count matches the true post-phase collected total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher output → user shell | printed handoff commands the user may paste/run |
| docs → user expectation | a stale test count misrepresents the shipped suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Spoofing | launch handoff for uninstalled skills | mitigate | gate each handoff on `.claude/skills/<namespace>` presence; emit an install prompt (not a broken/misleading command) when absent |
| T-14-13 | Tampering | handoff string construction | mitigate | build handoffs from fixed literals mirroring `_gsd_command`; no interpolation of untrusted/vendored content into the command |
| T-14-14 | Elevation | executing vendored content | accept | launcher only PRINTS a handoff; it never reads or executes the vendored skill files |
| T-14-15 | Repudiation | README test count | mitigate | derive the shipped count from `--collect-only` after all Phase-14 test files exist; never a hardcoded/stale literal |
</threat_model>

<verification>
- `python -m pytest tests/test_launcher_skills.py -q` exits 0
- `flowstate launch strategy` output contains `office-hours` and `flowstate launch discipline` output contains `test-driven-development` when skills are installed
- Both emit `install-skills` guidance when the namespace is absent
- README test-count line equals the post-phase `--collect-only` count; never 803/947
- Full suite `python -m pytest -q` exits 0, coverage ≥80%
</verification>

<success_criteria>
`flowstate launch strategy` surfaces gstack's `/office-hours` and `flowstate launch discipline` surfaces the superpowers `test-driven-development` skill — only when the vendored skills are installed — with `discipline` added to the launch Choice and a clear install prompt when skills are absent; and the shipped README test count is reconciled to the true post-phase collected total (never 803/947).
</success_criteria>

<output>
Create `.planning/phases/14-vendor-surface/14-04-SUMMARY.md` when done.
</output>
