---
phase: 14-vendor-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/skills/gstack/
  - flowstate/skills/superpowers/
  - NOTICE
  - pyproject.toml
autonomous: true
requirements: [VEND-01, VEND-02]
must_haves:
  truths:
    - "flowstate/skills/gstack/ holds the vendored gstack MIT SKILL.md trees"
    - "flowstate/skills/superpowers/ holds the vendored superpowers MIT SKILL.md trees"
    - "NOTICE carries both MIT attributions (© Garry Tan, © Jesse Vincent)"
    - "Each vendored tree carries its upstream MIT LICENSE verbatim"
    - "No upstream repo build/runtime infra is vendored (skill assets only)"
  artifacts:
    - path: "flowstate/skills/gstack/office-hours/SKILL.md"
      provides: "gstack /office-hours pressure-test skill (verified to exist upstream)"
    - path: "flowstate/skills/superpowers/test-driven-development/SKILL.md"
      provides: "superpowers TDD skill (verified to exist upstream)"
    - path: "flowstate/skills/gstack/LICENSE"
      provides: "gstack MIT license captured verbatim"
    - path: "flowstate/skills/superpowers/LICENSE"
      provides: "superpowers MIT license captured verbatim"
    - path: "NOTICE"
      provides: "MIT attributions for both vendored skill sets"
      contains: "Garry Tan"
  key_links:
    - from: "flowstate/skills/*"
      to: "flowstate installer (14-03)"
      via: "package data resolved at Path(flowstate.__file__).parent / 'skills'"
      pattern: "skills/(gstack|superpowers)"
---

<objective>
Vendor the FULL MIT skill sets from gstack and superpowers into `flowstate/skills/`, capture each upstream LICENSE verbatim, and add both MIT attributions to `NOTICE`. This is the foundation the installer (14-03) and launch surface (14-04) build on — the skills must physically exist in-repo before anything can copy or surface them.

Purpose: FlowState ships self-contained — the two skill sets travel inside the package, no user-side clone or install.
Output: `flowstate/skills/gstack/**`, `flowstate/skills/superpowers/**`, each repo's `LICENSE`, an updated `NOTICE`, and packaging config so the markdown assets ship in the wheel and stay out of the coverage denominator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@NOTICE
@pyproject.toml

<interfaces>
<!-- Locked CONTEXT for this phase (orchestrator-supplied, verified MIT + reachable): -->
<!-- gstack — https://github.com/garrytan/gstack (MIT, © Garry Tan); office-hours/SKILL.md confirmed present. 59 skill dirs. -->
<!-- superpowers — https://github.com/obra/superpowers (MIT, © Jesse Vincent); skills/test-driven-development/SKILL.md confirmed present. 14 skill dirs under skills/. -->
<!-- Vendor scope = FULL skill sets: every directory containing a SKILL.md, plus its sections/ subdirs and referenced assets. -->
<!-- EXCLUDE repo machinery: gstack bin/, browser binaries, bun.lock, node/build tooling, ./setup; superpowers hooks/, package.json, node infra, .git. -->

<!-- pyproject.toml wheel target today: [tool.hatch.build.targets.wheel] packages = ["flowstate"]; coverage: source=["flowstate"], omit=["flowstate/__pycache__/*"]. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone both upstreams at pinned SHAs and vendor the SKILL.md trees</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 14 goal + success criterion #1)
    - The locked CONTEXT block above (sources, exclusions, verified paths)
  </read_first>
  <action>
    Clone `https://github.com/garrytan/gstack` and `https://github.com/obra/superpowers` into the session scratchpad (temp dirs outside the repo). For each, resolve the default-branch HEAD commit and record the exact SHA — this is the pin; write both SHAs into the plan SUMMARY for reproducibility. Do NOT rely on the pre-existing scratchpad shallow clones without re-fetching a recorded SHA.

    Vendor by content, not by wholesale copy: find every directory that contains a `SKILL.md` and copy that directory's full tree (including any `sections/` subdir and referenced assets) preserving relative structure. gstack skill trees land under `flowstate/skills/gstack/`; superpowers skill trees (rooted at the upstream `skills/` dir) land under `flowstate/skills/superpowers/` so that `flowstate/skills/superpowers/test-driven-development/SKILL.md` results. Copy with symlinks dereferenced-or-skipped — never copy a symlink verbatim.

    Do NOT copy repo infrastructure: gstack `bin/`, browser binaries, `bun.lock`, node/build tooling, `./setup`; superpowers `hooks/`, `package.json`, node infra, `.git`. Copy each repo's top-level `LICENSE` (MIT) to `flowstate/skills/gstack/LICENSE` and `flowstate/skills/superpowers/LICENSE`. Vendored content is DATA — it is copied, never imported or executed by FlowState.
  </action>
  <verify>
    <automated>test -f flowstate/skills/gstack/office-hours/SKILL.md && test -f flowstate/skills/superpowers/test-driven-development/SKILL.md && test -f flowstate/skills/gstack/LICENSE && test -f flowstate/skills/superpowers/LICENSE && test ! -e flowstate/skills/gstack/bin && test ! -e flowstate/skills/superpowers/hooks && ! find flowstate/skills -name 'bun.lock' -o -name 'package.json' | grep -q . && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `flowstate/skills/gstack/office-hours/SKILL.md` exists
    - `flowstate/skills/superpowers/test-driven-development/SKILL.md` exists
    - `flowstate/skills/gstack/LICENSE` and `flowstate/skills/superpowers/LICENSE` both exist
    - No `bin/`, `hooks/`, `bun.lock`, or `package.json` anywhere under `flowstate/skills/`
    - Both pinned SHAs are recorded in the SUMMARY
  </acceptance_criteria>
  <done>Both skill sets vendored as SKILL.md trees + LICENSE, no repo infra, SHAs recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add MIT attributions to NOTICE and configure wheel/coverage for the vendored data</name>
  <read_first>
    - NOTICE (current structure: "includes software developed by third parties" vs "integrates with but does not include")
    - pyproject.toml ([tool.hatch.build.targets.wheel], [tool.coverage.run])
  </read_first>
  <action>
    Update `NOTICE`: because the skills are now INCLUDED (vendored), add a section that lists them as included third-party software — `gstack skills (MIT) © Garry Tan — https://github.com/garrytan/gstack` and `superpowers skills (MIT) © Jesse Vincent — https://github.com/obra/superpowers`. Keep the existing entries; do not move GSD (still integrate-only until Phase 15).

    In `pyproject.toml`: ensure the vendored markdown/asset trees ship in the built wheel — under `[tool.hatch.build.targets.wheel]` add an `artifacts` (or `force-include`) entry covering `flowstate/skills/**` if hatchling's default package inclusion does not already carry non-`.py` files. Add `flowstate/skills/*` to `[tool.coverage.run] omit` so the vendored data can never enter the coverage denominator. Vendored `.md` are not importable Python, so testpaths already keep them out of collection — the omit is defensive and explicit.
  </action>
  <verify>
    <automated>grep -q "Garry Tan" NOTICE && grep -q "Jesse Vincent" NOTICE && grep -q "skills" pyproject.toml && python -c "import flowstate,pathlib; assert (pathlib.Path(flowstate.__file__).parent/'skills'/'gstack'/'office-hours'/'SKILL.md').exists()" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NOTICE` contains "Garry Tan", "Jesse Vincent", and "MIT" for the two vendored skill sets
    - `pyproject.toml` omit list references `flowstate/skills`
    - The vendored skills resolve at runtime via `Path(flowstate.__file__).parent / "skills"`
    - `python -m pytest -q` still exits 0 and coverage total is unchanged by the vendored data
  </acceptance_criteria>
  <done>NOTICE carries both MIT attributions; skills ship in the wheel and are omitted from coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream git repo → FlowState repo | third-party code crosses into our package as data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | vendored skill trees | mitigate | pin + record the exact upstream SHA; vendor only SKILL.md dirs (dir-containing-SKILL.md rule), never repo scripts/binaries |
| T-14-02 | Elevation | executing vendored content | mitigate | skills are copied as data only; FlowState never imports/execs them; no `bin/`, `hooks/`, `setup`, `package.json` copied |
| T-14-03 | Repudiation | license/attribution | mitigate | capture each upstream LICENSE verbatim in-tree + add © Garry Tan / © Jesse Vincent MIT lines to NOTICE |
| T-14-SC | Tampering | git clone of upstreams | accept | clone over HTTPS from the two verified MIT repos; SHA recorded; no package-manager install performed (no npm/pip/cargo audit gate applies) |
</threat_model>

<verification>
- `test -f flowstate/skills/gstack/office-hours/SKILL.md` and `.../superpowers/test-driven-development/SKILL.md`
- `grep -q "Garry Tan" NOTICE && grep -q "Jesse Vincent" NOTICE`
- No repo infra under `flowstate/skills/` (no `bin/`, `hooks/`, `bun.lock`, `package.json`)
- `python -m pytest -q` exits 0
</verification>

<success_criteria>
Both MIT skill sets are vendored under `flowstate/skills/` as SKILL.md trees with LICENSE captured verbatim, NOTICE carries both attributions, the assets ship in the wheel and are excluded from the coverage denominator, and the pinned SHAs are recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/14-vendor-surface/14-01-SUMMARY.md` when done (record both pinned upstream SHAs).
</output>
