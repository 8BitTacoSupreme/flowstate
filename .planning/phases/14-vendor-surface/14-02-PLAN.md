---
phase: 14-vendor-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VEND-05]
must_haves:
  truths:
    - "README's test-count line equals the current pytest --collect-only count and is never 803 or 947"
    - "The Superpowers URL is obra/superpowers, not the 404 obra/claude-code-superpowers"
    - "The doctor line lists all 6 checks including stale_status"
    - "sqlite-vec is described as a core dep; only fastembed is the [semantic] extra"
    - "The three adapter acknowledgments describe what Phase 13 actually built — no unbuilt mechanism"
    - "REQUIREMENTS.md VEND-05 no longer references the stale 803→947; it agrees with README and ROADMAP"
  artifacts:
    - path: "README.md"
      provides: "reconciled acknowledgments + factual claims matching v0.6.1 code"
      contains: "obra/superpowers"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VEND-05 test-count reference aligned with the real current count"
  key_links:
    - from: "README.md acknowledgments"
      to: "flowstate/research.py, strategy.py, discipline.py (Phase 13 mechanisms)"
      via: "descriptive prose matching shipped behavior"
      pattern: "keep/discard|ship/pivot/kill|git state"
---

<objective>
Finish the VEND-05 README reconciliation: every claim must match the shipped v0.6.1 code. Part is already done (commit 845f648 corrected the test count and documented `flowstate discipline` — do NOT touch or revert those). This plan lands the REST: the Superpowers URL, the doctor check count, the sqlite-vec/fastembed wording, and the three adapter Acknowledgment bullets rewritten to describe what Phase 13 actually built. It also aligns REQUIREMENTS.md's stale test-count reference.

Purpose: no acknowledgment may describe an unbuilt mechanism; no factual claim may be stale.
Output: an updated `README.md` and `.planning/REQUIREMENTS.md`.

Note on the test count: this plan (Wave 1) sets the count to whatever `pytest --collect-only` reports at its runtime — it MUST NOT hardcode a terminal number. Plan 14-04 (Wave 3, the last to land) re-derives and reconciles the count AFTER 14-03/14-04 add their new test files, so the shipped README reflects the true post-phase total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md

<interfaces>
<!-- Phase 13 shipped mechanisms (source of truth for the rewritten acknowledgments): -->
<!-- research (Autoresearch): measure→keep/discard groundedness over OUTPUT — scores each section vs the fixture's retrieval_questions, bounded retry, discards weak sections; all-discarded → success=False. -->
<!-- strategy (Gstack): scored rubric — five 0–10 dimensions + a ship/pivot/kill verdict; an unparseable/missing rubric is a failure. -->
<!-- discipline (Superpowers): runs the project's tests, reads real git state (dirty/branch/ahead-behind), checks hook contents — and can now fail. -->

<!-- flowstate/doctor.py run_doctor registers SIX checks: manifest_integrity, memory_schema, root_resolution, claude_cli, stale_status, orphan_files. -->
<!-- README today: line ~219 doctor line lists 5 (omits stale_status); line ~254 "985 tests, 92% coverage"; line ~375 Gstack; ~377 Superpowers URL obra/claude-code-superpowers (404); ~381 sqlite-vec+fastembed both implied optional. -->
<!-- REQUIREMENTS.md VEND-05 bullet says "test count `803 → 947`" — STALE; must reference the real current count instead. -->
<!-- LOCKED: sqlite-vec is a CORE dep (pyproject base deps); only fastembed is behind the [semantic] extra. NEVER write 803 or 947 into README. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix factual claims — Superpowers URL, doctor count, sqlite-vec wording, self-reconciling test count, REQUIREMENTS alignment</name>
  <files>README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md (the doctor line ~219, the "985 tests" structure line ~254, the Superpowers acknowledgment ~377, the sqlite-vec/fastembed acknowledgment ~381)
    - .planning/REQUIREMENTS.md (VEND-05 bullet with the stale "803 → 947")
    - flowstate/doctor.py (run_doctor's six registered checks — authoritative count)
    - pyproject.toml (confirm sqlite-vec is a base dep and fastembed sits under the [semantic] extra)
  </read_first>
  <action>
    Change the Superpowers acknowledgment URL from `https://github.com/obra/claude-code-superpowers` (404) to `https://github.com/obra/superpowers`. In the `flowstate doctor` capabilities line, replace the 5-item parenthetical (manifest, memory schema, root, claude CLI, orphans) with all SIX check names, adding `stale_status`, so the documented count matches `run_doctor`. Rewrite the sqlite-vec/fastembed acknowledgment so it states that `sqlite-vec` is a CORE dependency (always installed) and only `fastembed` is the optional `[semantic]` extra — the current wording implies both are optional, which is wrong.

    Make the test count self-reconciling, not hardcoded: derive the real count with `python -m pytest --collect-only -q` at this plan's runtime and set the README test-count line to exactly that number. Do NOT treat any literal (985 or otherwise) as terminal, and never write 803 or 947. Plan 14-04 re-derives the final count once its own and 14-03's test files exist.

    In `.planning/REQUIREMENTS.md`, update the VEND-05 bullet's stale "test count `803 → 947`" reference so it points to the real current count (985+, whatever `--collect-only` reports) — keeping REQUIREMENTS, ROADMAP, and README in agreement.
  </action>
  <verify>
    <automated>N=$(python -m pytest --collect-only -q 2>/dev/null | grep -oE '[0-9]+ tests? collected' | grep -oE '[0-9]+' | head -1); test -n "$N" && grep -q "$N tests" README.md && grep -q "obra/superpowers" README.md && ! grep -q "obra/claude-code-superpowers" README.md && grep -qi "stale_status\|stale status" README.md && ! grep -qE "\b(803|947)\b" README.md && ! grep -qE "803 . 947" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README's test-count line equals the number from `python -m pytest --collect-only -q` at runtime, and is neither 803 nor 947
    - README contains `obra/superpowers` and no longer contains `obra/claude-code-superpowers`
    - The doctor capabilities line lists 6 checks including `stale_status`
    - The sqlite-vec/fastembed acknowledgment states sqlite-vec is core and fastembed is the `[semantic]` extra
    - `.planning/REQUIREMENTS.md` VEND-05 no longer contains "803 → 947"; it references the real current count
  </acceptance_criteria>
  <done>Factual claims (URL, doctor count, dep wording, self-reconciling test count) match the code; REQUIREMENTS agrees.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the three adapter Acknowledgment bullets to Phase 13 reality</name>
  <files>README.md</files>
  <read_first>
    - README.md (Autoresearch bullet ~374, Gstack bullet ~375, Superpowers bullet ~377)
    - flowstate/research.py, flowstate/strategy.py, flowstate/discipline.py (the shipped mechanisms — source of truth)
  </read_first>
  <action>
    Rewrite the three adapter acknowledgment bullets from the old "draws on the idea / implements a similar" framing to describe what the adapters ACTUALLY now do after Phase 13:
    - Autoresearch (research adapter): scores each generated topic section for groundedness against the active fixture's retrieval_questions and retries-or-discards weak sections within a bounded budget — measure→keep/discard applied to output, recording kept vs discarded.
    - Gstack (strategy adapter): emits a scored rubric — five 0–10 dimensions plus a ship/pivot/kill verdict — which the adapter validates; an unparseable rubric is a failure.
    - Superpowers (discipline adapter): runs the project's tests, reads real git state (dirty tree / branch / ahead-behind), and checks hook contents — and can now fail the pipeline.
    No bullet may describe a mechanism that is not built. Leave the GSD, ECC, Karpathy-Skills, Repomix, and sqlite-vec bullets' adapter-unrelated content intact (GSD's bundled-reality rewrite is Phase 15's GSD-05, not this phase).
  </action>
  <verify>
    <automated>grep -qi "keep/discard\|keep vs discard\|discard" README.md && grep -qi "ship/pivot/kill" README.md && grep -qi "git state\|ahead-behind\|hook contents" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Autoresearch bullet describes measure→keep/discard groundedness over output
    - The Gstack bullet describes the five-dimension 0–10 scored rubric + ship/pivot/kill verdict
    - The Superpowers bullet describes running tests + real git state + hook contents that can fail
    - No adapter bullet retains "draws on the idea" / "implements a similar" framing
  </acceptance_criteria>
  <done>All three adapter acknowledgments describe shipped Phase 13 mechanisms; none describes an unbuilt one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user expectation | inaccurate claims mislead users about what the code does / where to fetch upstreams |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Spoofing | Superpowers upstream URL | mitigate | replace the 404 `obra/claude-code-superpowers` with the real `obra/superpowers`, preventing a dead/typosquattable link |
| T-14-05 | Repudiation | adapter capability claims | mitigate | rewrite acknowledgments strictly against shipped Phase 13 code; no unbuilt-mechanism claims |
| T-14-06 | Information disclosure | dep-optionality + count wording | accept | correcting sqlite-vec (core) vs fastembed (`[semantic]`) and deriving the count from `--collect-only`; docs-only, no runtime effect |
</threat_model>

<verification>
- README test-count line equals `python -m pytest --collect-only -q` count; never 803/947
- `grep -q "obra/superpowers" README.md && ! grep -q "obra/claude-code-superpowers" README.md`
- README doctor line lists 6 checks including `stale_status`
- Adapter bullets contain "keep/discard", "ship/pivot/kill", and "git state"/"hook contents"
- REQUIREMENTS.md VEND-05 no longer references "803 → 947"
</verification>

<success_criteria>
Every remaining VEND-05 reconciliation is landed: the Superpowers URL is `obra/superpowers`, the doctor check count is 6 (includes `stale_status`), the sqlite-vec/fastembed wording is correct, the test count is derived from `--collect-only` (never 803/947), the three adapter acknowledgments describe only shipped mechanisms, and REQUIREMENTS.md agrees. Final count reconciliation lands in 14-04.
</success_criteria>

<output>
Create `.planning/phases/14-vendor-surface/14-02-SUMMARY.md` when done.
</output>
