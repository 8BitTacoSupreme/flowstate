---
phase: 14-vendor-surface
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - flowstate/installer.py
  - flowstate/cli.py
  - tests/test_installer.py
autonomous: true
requirements: [VEND-03]
must_haves:
  truths:
    - "flowstate install-skills copies the vendored skills into the project's .claude/skills/"
    - "init and kickoff auto-invoke the installer so a fresh project needs zero manual skill install"
    - "The installer is idempotent and never clobbers user-authored skills outside the vendored namespaces"
    - "--dry-run is side-effect-free (no copying)"
    - "Installed skill namespaces are recorded in install_manifest so fresh/doctor stay honest"
  artifacts:
    - path: "flowstate/installer.py"
      provides: "pure-Python shutil-based skill installer, path-safe + idempotent + extensible for Phase 15"
      exports: ["install_skills"]
    - path: "flowstate/cli.py"
      provides: "install-skills command + init/kickoff auto-invoke"
      contains: "install-skills"
    - path: "tests/test_installer.py"
      provides: "offline temp-dir tests (copy, idempotence, no-clobber, dry-run, manifest)"
  key_links:
    - from: "flowstate/installer.py"
      to: "flowstate/skills/*"
      via: "Path(flowstate.__file__).parent / 'skills' → root/.claude/skills"
      pattern: "\\.claude/skills"
    - from: "flowstate/cli.py init/kickoff"
      to: "flowstate/installer.py install_skills"
      via: "auto-invoke respecting dry_run"
      pattern: "install_skills"
---

<objective>
Build `flowstate install-skills`: a pure-Python (`shutil`) installer that copies the vendored `flowstate/skills/*` trees into the project's `.claude/skills/`, and auto-invoke it from `init` and `kickoff` so a fresh project needs no manual skill install. Idempotent, path-safe, dry-run-safe, and DESIGNED EXTENSIBLE — Phase 15 reuses this same installer to install GSD unconditionally.

Purpose: the vendored skills (14-01) become live in any project with zero user action.
Output: `flowstate/installer.py`, a new `install-skills` CLI command, `init`/`kickoff` hooks, and offline tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@flowstate/cli.py
@flowstate/state.py
@flowstate/context.py

<interfaces>
<!-- InstallEntry (flowstate/state.py): fields path, owner, kind, created_at, checksum. -->
<!--   kind Literal is ["config","context","memory","research","artifact","pack","fixture"] — REUSE "artifact" (do NOT extend the Literal; avoids a state migration). -->
<!-- context.py::_register shows the manifest idempotence pattern: drop existing entries for the same rel path, then append. Dir entries must use checksum=None (sha256 of a dir would fail). -->
<!-- cli.py init (line ~69) and kickoff (line ~107): both resolve root, load_state, write context; kickoff calls write_context_files then run_pack then save_state. init runs run_pipeline. dry_run lives on state.preferences.dry_run (init sets it from --dry-run; kickoff has no dry-run today). -->
<!-- fresh (cli.py ~473) iterates install_manifest and shutil.rmtree dir entries — dir-level manifest entries clean up correctly. -->
<!-- Vendored skills resolve at Path(flowstate.__file__).parent / "skills" (14-01 Task 2). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build flowstate/installer.py — path-safe, idempotent, extensible install_skills</name>
  <read_first>
    - flowstate/context.py (_register manifest pattern; how paths are made relative to root)
    - flowstate/state.py (InstallEntry model, kind Literal, install_manifest)
    - flowstate/skills/ (the vendored source trees from 14-01)
  </read_first>
  <behavior>
    - install_skills into an empty temp root creates .claude/skills/gstack/office-hours/SKILL.md and .claude/skills/superpowers/test-driven-development/SKILL.md
    - Running install_skills twice on the same root is idempotent (no error, same tree)
    - A pre-existing user file .claude/skills/mine/custom.md survives an install (no clobber outside vendored namespaces gstack/ and superpowers/)
    - dry_run=True writes nothing (no .claude/skills created) and returns the paths that WOULD be written
    - When a state is passed, install_manifest gains dir-level entries for .claude/skills/gstack and .claude/skills/superpowers (kind="artifact", owner="skills", checksum=None), idempotently replaced on re-run
    - Destination paths are confined to root/.claude/skills; a source symlink escaping the tree is skipped, never followed
  </behavior>
  <action>
    Create `flowstate/installer.py` exporting `install_skills(root: Path, *, dry_run: bool = False, state: FlowStateModel | None = None) -> list[Path]`. Resolve the vendored source via `Path(flowstate.__file__).parent / "skills"`. Structure the copy around a small helper `_install_tree(src_dir, dest_dir, *, dry_run)` and drive it from an explicit list of `(source_subdir, dest_namespace)` pairs — `("gstack", "gstack")`, `("superpowers", "superpowers")` — so Phase 15 extends the list to add GSD without rewriting the copy logic.

    Copy with `shutil.copytree(..., dirs_exist_ok=True, symlinks=False)` but first assert each resolved destination is inside `(root / ".claude" / "skills").resolve()` (reject path traversal), and only overwrite within the vendored namespaces `gstack/` and `superpowers/` — never touch sibling user dirs under `.claude/skills/`. Skip any symlink encountered in the source. Content is copied as DATA and never imported or executed.

    When `state` is provided and not dry_run, record one dir-level InstallEntry per installed namespace (path `.claude/skills/gstack`, `.claude/skills/superpowers`; owner "skills"; kind "artifact"; checksum None), replacing any existing entry for the same path first (mirror context._register's idempotence). Return the list of top-level installed namespace paths (or the would-be paths when dry_run).
  </action>
  <verify>
    <automated>python -m pytest tests/test_installer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `flowstate/installer.py` exports `install_skills`
    - Installing into a temp root creates `.claude/skills/superpowers/test-driven-development/SKILL.md`
    - Second install is a no-op error-free (idempotent)
    - A pre-existing `.claude/skills/mine/custom.md` is still present after install
    - `dry_run=True` creates no `.claude/skills/` directory
    - With a state passed, `install_manifest` contains entries for both vendored namespaces
    - `python -m pytest tests/test_installer.py -q` exits 0
  </acceptance_criteria>
  <done>install_skills copies both namespaces path-safely, idempotently, dry-run-safe, records the manifest, and is structured for Phase 15 extension.</done>
</task>

<task type="auto">
  <name>Task 2: Add the install-skills CLI command and auto-invoke from init + kickoff</name>
  <read_first>
    - flowstate/cli.py (init ~line 69, kickoff ~line 107, command registration + --root/--dry-run patterns)
    - flowstate/installer.py (Task 1 — install_skills signature)
  </read_first>
  <action>
    Add a `@main.command("install-skills")` with `--root` and `--dry-run` options that resolves root, loads state, calls `install_skills(root, dry_run=dry_run, state=state)`, saves state (unless dry_run), and prints the installed namespaces via the existing Rich console (mirror the `context` command's created-files output style). On dry_run, print what would be installed and do not save.

    Auto-invoke the installer from `init` and `kickoff`: in `init`, after `run_pipeline(...)` and before/with the final `save_state`, call `install_skills(root, dry_run=state.preferences.dry_run, state=state)`. In `kickoff`, after `write_context_files(...)`, call `install_skills(root, dry_run=False, state=state)` before the final `save_state`. Both must leave a fresh project needing zero manual skill install and must stay side-effect-free under dry-run.
  </action>
  <verify>
    <automated>python -m pytest tests/test_installer.py -q && python -c "from click.testing import CliRunner; from flowstate.cli import main; import tempfile,pathlib; d=tempfile.mkdtemp(); r=CliRunner().invoke(main,['install-skills','--root',d]); assert r.exit_code==0, r.output; assert (pathlib.Path(d)/'.claude/skills/gstack/office-hours/SKILL.md').exists(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `flowstate install-skills --root <tmp>` exits 0 and creates `.claude/skills/gstack/office-hours/SKILL.md`
    - `flowstate install-skills --root <tmp> --dry-run` creates no `.claude/skills/` directory
    - `init` and `kickoff` both call `install_skills` (grep-verifiable) and respect dry_run
    - `python -m pytest -q` (full suite) exits 0 with coverage ≥80%
  </acceptance_criteria>
  <done>install-skills is a first-class command and init/kickoff auto-install skills, dry-run-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package data → project filesystem | installer writes vendored trees into the user's `.claude/skills/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | copytree destination | mitigate | assert each resolved dest is inside `(root/.claude/skills).resolve()`; reject path traversal; only overwrite within vendored namespaces `gstack/`,`superpowers/` |
| T-14-08 | Elevation | source symlinks | mitigate | `shutil.copytree(symlinks=False)` + skip any symlink; never follow a symlink out of the tree |
| T-14-09 | Tampering | user-authored skills | mitigate | never clobber sibling dirs under `.claude/skills/`; idempotent overwrite scoped to the two vendored namespaces only |
| T-14-10 | Denial of service | dry-run side effects | mitigate | dry_run path performs zero writes and no state save; unit-tested |
| T-14-11 | Elevation | executing vendored content | mitigate | installer copies data only; FlowState never imports/execs the skill files |
</threat_model>

<verification>
- `python -m pytest tests/test_installer.py -q` exits 0
- `flowstate install-skills --root <tmp>` creates the two vendored namespaces; `--dry-run` creates nothing
- `grep -q install_skills flowstate/cli.py` (init + kickoff wired)
- Full suite `python -m pytest -q` exits 0, coverage ≥80%
</verification>

<success_criteria>
`flowstate install-skills` (and `init`/`kickoff`) copies the vendored skills into `.claude/skills/` — a fresh project needs no manual skill install — idempotently, path-safely, dry-run-safe, with installed namespaces recorded in `install_manifest`, and the installer is structured so Phase 15 can add GSD.
</success_criteria>

<output>
Create `.planning/phases/14-vendor-surface/14-03-SUMMARY.md` when done.
</output>
