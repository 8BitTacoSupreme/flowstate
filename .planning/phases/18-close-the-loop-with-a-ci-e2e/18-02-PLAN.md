---
phase: 18-close-the-loop-with-a-ci-e2e
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - bench/close_loop.py
  - tests/test_bench_close_loop.py
autonomous: true
requirements: [HAR-04]
must_haves:
  truths:
    - "One command runs prior-runs → distill → inject → judge on a fixture end-to-end and returns a CI'd delta, not a single-shot score."
    - "The command has a cheap/deterministic mode that runs with NO live LLM/bridge (CI-safe)."
    - "The distill step reuses bench.prepare_fixture/bench.distiller; the judge step reuses bench.replicate; the CI reuses bench.bootstrap — nothing is rebuilt."
    - "The CI'd delta is Track-2 output and never touches the deterministic compounding_score."
  artifacts:
    - path: "bench/close_loop.py"
      provides: "the one-command prior-runs→distill→inject→judge→CI driver (main entry point)"
      contains: "def main"
    - path: "tests/test_bench_close_loop.py"
      provides: "CI-safe end-to-end test asserting a CI'd delta is returned"
      contains: "def test_"
  key_links:
    - from: "bench/close_loop.py"
      to: "bench/prepare_fixture.py"
      via: "distill/inject-prep step (memory→wiki corpus)"
      pattern: "prepare_fixture"
    - from: "bench/close_loop.py"
      to: "bench/bootstrap.py"
      via: "paired_bootstrap_ci on the per-trial judge deltas"
      pattern: "paired_bootstrap_ci"
---

<objective>
Ship the ONE command HAR-04 sentence 2 / criterion 2 demands: a single entry point that
chains **prior-runs → distill → inject → judge → paired-bootstrap CI** on a fixture and
returns a CI'd delta (not a single-shot score), reusing the Phase-17 producers and the
Plan 01 helper rather than rebuilding anything.

Purpose: Close the v0.6.2 "Make the Harness Real" loop — the harness can now run the full
compounding-value chain end to end and report a confidence-interval'd result. Per the
critical domain facts, this picks the SMALLER surface (a dedicated thin driver module that
composes existing entry points) and provides a `--mode cheap` path so the loop is provably
CI-safe with no live LLM.

Output: `bench/close_loop.py` with a `main(argv)` entry point, invocable as
`python -m bench.close_loop --root <fixture> --mode cheap`, plus a CI-safe test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-close-the-loop-with-a-ci-e2e/18-01-PLAN.md

<interfaces>
<!-- Composition targets — reuse these entry points; do NOT reimplement them. -->

bench/prepare_fixture.py::main(argv) -> int
  - `main(["--root", str(root), "--arms", "wiki"])` distills memory→wiki corpus via
    bench.distiller and (for pack) flowstate.pack.run_pack. Returns 0 iff all producers succeed.

bench/distiller.py::main(argv) -> int
  - `--root <path>` [--force] [--llm --model <m>]; writes `.planning/codebase/wiki/` corpus.

bench/replicate.py (multi-sample judge trajectories to REUSE):
  - `_run_trial(arm, runs, root, label) -> list[float] | None` — one compound_eval real-mode
    invocation, returns per-run judge scores (live LLM). Monkeypatch this in tests for CI-safety.
  - `_agg(trials) -> dict` returns `improvements` (per-trial last-first list).

bench/bootstrap.py (Plan 01):
  - `paired_bootstrap_ci(deltas, *, resamples=2000, seed=_BOOTSTRAP_SEED, confidence=0.95) -> dict`
    with keys n/mean/ci_low/ci_high/resamples/seed/confidence.

bench/compound_eval.py:
  - `--layers` arms: full|none|pack|memory|wiki; `--mode {cheap,real}`; judge requires
    `--mode real --allow-llm`; `_EXIT_PRODUCER_ABSENT = 3`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bench/close_loop.py — the prior-runs→distill→inject→judge→CI driver</name>
  <files>bench/close_loop.py</files>
  <read_first>
    - bench/prepare_fixture.py (whole file) — the distill/inject-prep entry point to call.
    - bench/replicate.py lines 29-91 — `_run_trial`, `_agg`, `_paired_normalize` (the judge-trajectory machinery to reuse for the real path).
    - bench/bootstrap.py (Plan 01) — `paired_bootstrap_ci`.
    - bench/compound_eval.py lines 246-268 (`_cheap_loop`) and 133-167 (`_build_parser`) — the cheap-vs-real philosophy and arm/mode vocabulary to mirror.
    - bench/project.py lines 192, 284 (`scaffold`, `mutate_for_run`) — how a fixture worktree is seeded, for the cheap deterministic path.
  </read_first>
  <action>
    Create `bench/close_loop.py` (module docstring: the ONE end-to-end command closing the
    v0.6.2 harness loop; research/bench tooling, not a flowstate CLI subcommand; Track-2 CI
    output EXCLUDED from compounding_score; `--mode cheap` is CI-safe with no LLM). Implement
    `main(argv=None) -> int` with argparse: `--root` (required Path), `--arm` (default `wiki`,
    the treatment arm), `--baseline` (default `none`), `--trials` (default 5), `--runs`
    (default 3), `--mode {cheap,real}` (default `cheap`), `--seed` (default reuse
    `bench.bootstrap._BOOTSTRAP_SEED`), `--out` (Path, optional).
    Pipeline steps, each printed on one line:
      (1) DISTILL/INJECT-PREP: call `bench.prepare_fixture.main(["--root", str(root), "--arms", arm])`
          when the arm has a producer (wiki/pack); on non-zero return, fail loud (return its code).
      (2) JUDGE (multi-sample): in `--mode real`, collect per-run judge trajectories for `arm`
          and `baseline` by reusing `bench.replicate._run_trial` across `--trials`; in
          `--mode cheap`, synthesize deterministic per-trial trajectories from a
          `random.Random(seed)` (NO LLM, NO bridge — this is the CI-safe apparatus check, mirroring
          compound_eval's cheap mode) so the plumbing is exercised end to end.
      (3) CI: compute per-trial paired deltas (arm improvement minus baseline improvement, via
          `_agg(...)['improvements']` paired by index) and pass them to
          `bench.bootstrap.paired_bootstrap_ci(..., seed=seed)`.
    Emit a JSON result dict `{"mode", "arm", "baseline", "trials", "runs",
    "note": "Tier-2 judge CI — EXCLUDED from compounding_score", "bootstrap_ci_delta_vs_baseline": <ci dict>}`
    to stdout and to `--out` when given. Never import or call `bench.metrics` /
    `compute_scorecard` (the CI is Track-2). Never raises — wrap the pipeline in guards that
    return a non-zero code on failure rather than propagating.
  </action>
  <verify>
    <automated>uv run python -m bench.close_loop --root bench/fixtures/sample_project --mode cheap --trials 3 --runs 3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m bench.close_loop --root bench/fixtures/sample_project --mode cheap --trials 3 --runs 3` exits 0 and prints JSON containing a `bootstrap_ci_delta_vs_baseline` object with numeric `mean`, `ci_low`, `ci_high` — with NO `claude` binary required (assert no live LLM path is taken in cheap mode).
    - `grep -n "import.*metrics\|compute_scorecard" bench/close_loop.py` returns nothing.
    - `grep -c "prepare_fixture\|paired_bootstrap_ci\|_run_trial" bench/close_loop.py` >= 3 (composition of the three reuse targets, not a rebuild).
    - The emitted JSON's `note` contains `EXCLUDED from compounding_score`.
  </acceptance_criteria>
  <done>`bench.close_loop` runs the full prior-runs→distill→inject→judge chain and returns a CI'd delta; cheap mode is LLM-free.</done>
</task>

<task type="auto">
  <name>Task 2: CI-safe end-to-end test for the close-loop command</name>
  <files>tests/test_bench_close_loop.py</files>
  <read_first>
    - bench/close_loop.py (Task 1) — the `main` entry point and its cheap path.
    - tests/test_bench_prepare_fixture.py — existing patterns for invoking a bench `main(argv)` and monkeypatching producers under tmp_path.
    - tests/test_bench_replicate.py — existing patterns for monkeypatching `_run_trial` to avoid live LLM.
  </read_first>
  <action>
    Create `tests/test_bench_close_loop.py`. Add: (a) a cheap-mode test that scaffolds a
    fixture under `tmp_path` (reuse `bench.project.scaffold`) or points `--root` at
    `bench/fixtures/sample_project` copied into tmp, calls `bench.close_loop.main(["--root",
    ..., "--mode", "cheap", "--trials", "3", "--runs", "3", "--out", str(out)])`, asserts return
    0 and that the written JSON has `bootstrap_ci_delta_vs_baseline` with numeric mean/ci_low/ci_high
    and `ci_low <= mean <= ci_high`; (b) a determinism test that two identical cheap-mode
    invocations with the same `--seed` produce identical CI bounds; (c) a real-path plumbing
    test that monkeypatches `bench.replicate._run_trial` to return fixed trajectories and
    `bench.prepare_fixture.main` to a no-op success, then asserts `main([... "--mode", "real"])`
    returns a CI'd delta WITHOUT invoking any real subprocess. No test may require a `claude`
    binary or network.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_close_loop.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_bench_close_loop.py -q` is green with no `claude`/network dependency.
    - The determinism test asserts byte-identical `ci_low`/`ci_high` across two same-seed cheap runs.
    - The real-path test monkeypatches `_run_trial` (grep the test: `grep -c "_run_trial" tests/test_bench_close_loop.py` >= 1) so no live LLM is invoked.
    - `uv run python -m pytest --cov=bench --cov=flowstate --cov-fail-under=80 -q` passes (close_loop.py is covered ≥80%).
  </acceptance_criteria>
  <done>The close-loop command has CI-safe, deterministic tests proving it returns a CI'd delta end to end with no live LLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close_loop → producer subprocesses | In real mode, invokes `claude --print` on LOCAL content only (via replicate/compound_eval); cheap mode invokes no subprocess. No external attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of service | cheap mode accidentally invoking live LLM in CI | mitigate | cheap path uses a seeded `random.Random`; test asserts no `claude` binary is required |
| T-18-04 | Information disclosure | judge CI feeding the deterministic score | mitigate | grep-gate asserts close_loop never imports metrics/compute_scorecard; JSON keeps EXCLUDED note |
| T-18-SC | Tampering | producer subprocesses (`claude --print`) | accept | local content only, no package installs in this phase; no new deps introduced |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_bench_close_loop.py -q` green.
- `uv run python -m bench.close_loop --root bench/fixtures/sample_project --mode cheap` exits 0 and emits a CI'd delta with no claude binary present.
- `uv run ruff check bench/close_loop.py` clean.
</verification>

<success_criteria>
- One command runs prior-runs→distill→inject→judge→CI on a fixture and returns a CI'd delta.
- Cheap mode is CI-safe (no live LLM/bridge).
- Reuses prepare_fixture/distiller + replicate + bootstrap; never touches compounding_score.
</success_criteria>

<output>
Create `.planning/phases/18-close-the-loop-with-a-ci-e2e/18-02-SUMMARY.md` when done.
</output>
