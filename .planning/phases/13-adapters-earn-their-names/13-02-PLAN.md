---
phase: 13-adapters-earn-their-names
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/tools/strategy.py
  - tests/test_strategy_rubric.py
autonomous: true
requirements: [MECH-02]
must_haves:
  truths:
    - "The strategy adapter's bridge call emits parseable per-dimension scores (0-10) and a verdict (ship/pivot/kill)"
    - "The adapter validates the rubric; an unparseable or missing rubric returns ToolResult(success=False) via the existing HON-04 failure path"
    - "A valid rubric writes the parsed scores + verdict into research/strategy.md and reports success"
    - "The --dry-run MOCK path is byte-behaviorally unchanged"
  artifacts:
    - path: "flowstate/tools/strategy.py"
      provides: "Scored-rubric parse + verdict validation (Gstack pattern)"
      contains: "_parse_rubric"
    - path: "tests/test_strategy_rubric.py"
      provides: "Offline tests for rubric parse, verdict, unparseable-failure, MOCK preservation"
  key_links:
    - from: "flowstate/tools/strategy.py::pressure_test"
      to: "_parse_rubric"
      via: "validate bridge output before writing artifact"
      pattern: "_parse_rubric\\("
    - from: "flowstate/tools/strategy.py"
      to: "ToolResult(success=False)"
      via: "unparseable/missing rubric -> HON-04 failure"
      pattern: "success=False"
---

<objective>
Give the strategy adapter Gstack's core mechanism: a scored review. The bridge call must emit parseable per-dimension scores (0-10) and a verdict (ship/pivot/kill); the adapter parses and validates the rubric. An unparseable or missing rubric is a FAILURE surfaced through the Phase 12 HON-04 path (`ToolResult(success=False)`), not a silently-written weak artifact.

Purpose: MECH-02 — the `strategy` adapter currently makes one call and writes whatever comes back. It must now enforce a machine-checkable rubric so "strategy ran" means "a scored verdict was produced and validated."
Output: An upgraded `flowstate/tools/strategy.py` and offline tests in `tests/test_strategy_rubric.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

From flowstate/tools/base.py:
```python
@dataclass
class ToolResult:
    success: bool
    output: str
    artifacts: list[str] = field(default_factory=list)
    error: str | None = None
```

From flowstate/bridge.py:
```python
@dataclass
class BridgeResult:
    success: bool
    output: str
    exit_code: int = 0
    error: str | None = None
# bridge.run(prompt, *, system_prompt=None, allowed_tools=None,
#            output_format="text", max_turns=None, model=_SENTINEL) -> BridgeResult
```

Current strategy failure path (Phase 12 / HON-04) already returns:
`ToolResult(success=False, output=br.output, error=br.error or "strategy produced empty output", artifacts=[])`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the strategy call a scored rubric with a validated verdict</name>
  <files>flowstate/tools/strategy.py</files>
  <read_first>
    - flowstate/tools/strategy.py (the file being modified — note MOCK_STRATEGY, `STRATEGY_SYSTEM_PROMPT` with its 5 numbered evaluation dimensions, the `if self.dry_run` early return, and the existing HON-04 `success=False` path at the bottom of `pressure_test`)
    - flowstate/tools/base.py (ToolResult contract)
    - .planning/phases/12-adapters-earn-their-names/12-02-SUMMARY.md if present, else .planning/phases/12*/12-02-SUMMARY.md (how Phase 12 made strategy surface failure — reuse that failure shape, do not reinvent it)
  </read_first>
  <behavior>
    - A bridge output containing a well-formed scores block (five 0-10 integer dimension scores) and a verdict line with one of ship/pivot/kill parses successfully; the parsed rubric is written into strategy.md and ToolResult.success is True.
    - A bridge output missing the scores block OR missing a valid verdict returns ToolResult(success=False) with an error naming what was unparseable; no success artifact is written with a bad rubric.
    - An empty/failed bridge call keeps the existing HON-04 failure behavior.
    - `--dry-run` still writes MOCK_STRATEGY unchanged and returns success.
  </behavior>
  <action>
    Define module constants: `_RUBRIC_DIMENSIONS = ("problem_clarity", "ten_x_potential", "feasibility", "risk", "recommendation")` and `_VERDICTS = ("ship", "pivot", "kill")`.

    Extend `STRATEGY_SYSTEM_PROMPT` so the model must APPEND a machine-readable rubric block after its prose. Instruct it to emit a fenced ```rubric block containing one `dimension: <0-10>` line per dimension key in `_RUBRIC_DIMENSIONS`, followed by a `verdict: <ship|pivot|kill>` line. Keep the human prose section too. Do NOT change the prompt at runtime based on prior output (no self-modification).

    Add a module function `_parse_rubric(output: str) -> tuple[dict[str, int], str] | None` that:
    - locates the rubric block (tolerant: search the whole output, not only a fenced block),
    - extracts each `dimension: N` pair with a strict regex per known dimension key, clamping/validating N is an integer in 0-10,
    - extracts the verdict via regex and validates membership in `_VERDICTS` (case-insensitive, normalized to lowercase),
    - returns `(scores, verdict)` only when ALL five dimensions AND a valid verdict are present; otherwise returns `None`.
    Use `re` only — NEVER `eval`/`exec`/`ast.literal_eval`/`json` over the whole model output.

    In `pressure_test()`, after a successful non-empty bridge call, call `_parse_rubric(br.output)`. If it returns `None`, return `ToolResult(success=False, output=br.output, error="unparseable rubric: missing per-dimension 0-10 scores or ship/pivot/kill verdict", artifacts=[])` and do NOT write strategy.md. If it parses, write `br.output` to strategy.md PLUS a normalized `## Rubric` section listing the parsed scores and verdict, and return `ToolResult(success=True, output=f"Strategy verdict: {verdict} (...scores...)", artifacts=[str(strategy_path)])`.

    Do NOT touch the `if self.dry_run:` branch or `MOCK_STRATEGY`.
  </action>
  <acceptance_criteria>
    - `flowstate/tools/strategy.py` contains `def _parse_rubric(` and the `_RUBRIC_DIMENSIONS` and `_VERDICTS` constants with values `ship`, `pivot`, `kill`.
    - `grep -n "eval(\|exec(\|literal_eval" flowstate/tools/strategy.py` returns no matches.
    - The unparseable-rubric path returns `ToolResult(success=False, ...)` and writes no strategy.md (verified by test).
    - The `if self.dry_run:` branch and `MOCK_STRATEGY` are byte-identical to the pre-change version (`git diff` shows no changes inside that branch).
    - `ruff check flowstate/tools/strategy.py` and `ruff format --check flowstate/tools/strategy.py` pass.
  </acceptance_criteria>
  <verify>
    <automated>ruff check flowstate/tools/strategy.py && python -c "import flowstate.tools.strategy as s; assert hasattr(s,'_parse_rubric') and s._VERDICTS==('ship','pivot','kill')"</automated>
  </verify>
  <done>The strategy adapter emits and validates a per-dimension scored rubric + ship/pivot/kill verdict; unparseable -> HON-04 failure; MOCK/dry-run untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline tests for rubric parse, verdict validation, and failure</name>
  <files>tests/test_strategy_rubric.py</files>
  <read_first>
    - tests/test_tools.py lines 39-110 (existing strategy test patterns: `test_strategy_dry_run`, `test_strategy_passes_model_to_bridge` with a `MagicMock()` bridge returning `BridgeResult`)
    - flowstate/tools/strategy.py (post-Task-1: `_parse_rubric`, constants, the success/failure branches under test)
  </read_first>
  <behavior>
    - Unit: `_parse_rubric` returns `(scores, verdict)` for a well-formed rubric with all five dimensions + a valid verdict; scores are ints in 0-10.
    - Unit: `_parse_rubric` returns `None` for (a) missing a dimension, (b) an out-of-range score, (c) a verdict not in ship/pivot/kill, (d) no rubric block at all.
    - Integration: a MagicMock bridge returning a valid-rubric output makes `pressure_test` write strategy.md and return success True with the verdict in output.
    - Integration: a MagicMock bridge returning prose-without-rubric makes `pressure_test` return success False and write NO strategy.md.
    - Golden: `--dry-run` writes MOCK_STRATEGY.format(...) exactly and returns success.
  </behavior>
  <action>
    Create `tests/test_strategy_rubric.py`. Cover `_parse_rubric` directly with table-style inputs for the valid and each invalid case. Drive `pressure_test` with a `MagicMock()` bridge (`bridge.run.return_value = BridgeResult(...)`) for the success and unparseable-failure integration cases, asserting `strategy.md` existence/absence with `tmp_path`. Include the dry-run golden assertion against `MOCK_STRATEGY.format(problem=..., vision=...)`.
  </action>
  <acceptance_criteria>
    - `tests/test_strategy_rubric.py` exists covering: valid parse, each invalid parse, success integration, unparseable-failure integration, dry-run golden.
    - `python -m pytest tests/test_strategy_rubric.py -q` exits 0.
    - `python -m pytest tests/test_tools.py -q` still exits 0.
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0.
    - No test requires a live `claude` CLI or network.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_strategy_rubric.py tests/test_tools.py -q</automated>
  </verify>
  <done>Offline tests prove rubric parsing, verdict validation, HON-04 failure on unparseable output, and MOCK invariance; suite stays >=80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → rubric parser | `_parse_rubric` consumes untrusted model text (scores + verdict) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | `_parse_rubric` parsing of model rubric text | mitigate | Regex-only extraction with per-dimension key allow-list + 0-10 range validation + verdict membership check; NO eval/exec/literal_eval and no json.loads over whole output; malformed -> `None` -> HON-04 failure (never a crash, never a fake pass) |
| T-13-05 | Repudiation | strategy reporting success on a weak/empty rubric | mitigate | Unparseable/missing rubric returns `ToolResult(success=False)` and writes no artifact — a bad review cannot be recorded as a passed one |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib `re` only; no new runtime deps |
</threat_model>

<verification>
- `python -m pytest tests/test_strategy_rubric.py tests/test_tools.py -q` passes.
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` passes.
- `grep -n "eval(\|exec(\|literal_eval" flowstate/tools/strategy.py` returns nothing.
- `--dry-run` strategy artifact is byte-identical to MOCK_STRATEGY.
</verification>

<success_criteria>
The strategy adapter emits parseable per-dimension scores (0-10) and a verdict (ship/pivot/kill), validates them, and treats an unparseable/missing rubric as a failure surfaced via HON-04. MOCK/dry-run paths are unchanged. All new tests are offline.
</success_criteria>

<output>
Create `.planning/phases/13-adapters-earn-their-names/13-02-SUMMARY.md` when done.
</output>
