---
phase: 17-no-silent-no-op-arms-producers-wired-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/distiller.py
  - tests/test_bench_distiller.py
autonomous: true
requirements: [HAR-03]
must_haves:
  truths:
    - "`python -m bench.distiller --root <proj>` reads memory.db and writes ≥2 *.md article files under .planning/codebase/wiki/"
    - "The article corpus the distiller writes is the exact directory the Phase-11 semantic wiki reader globs (_WIKI_CORPUS_DIR)"
    - "An empty/absent memory.db causes a non-zero exit with a message and writes no partial corpus (fail loud, never a silent empty wiki)"
    - "The distiller never raises — subprocess/IO failures degrade to a non-zero return or deterministic fallback text"
  artifacts:
    - path: "bench/distiller.py"
      provides: "memory→wiki distiller producing the article-corpus directory"
      contains: "def main"
    - path: "tests/test_bench_distiller.py"
      provides: "behavior tests for the distiller (corpus shape, empty-memory guard, --llm fallback)"
  key_links:
    - from: "bench/distiller.py"
      to: ".planning/codebase/wiki/*.md"
      via: "writes one article per non-empty MemoryKind into _WIKI_CORPUS_DIR"
      pattern: "codebase/wiki"
    - from: "bench/distiller.py"
      to: "flowstate.memory.MemoryStore"
      via: "get_by_kind reads accumulated memories"
      pattern: "get_by_kind"
---

<objective>
Promote the deleted `scratchpad/distill_spike.py` into a first-class bench producer:
`bench/distiller.py`, a memory→wiki distiller (HAR-03a). It reads the accumulated
`memory.db` and writes an ARTICLE CORPUS — multiple `*.md` files under
`.planning/codebase/wiki/` — which is exactly the directory the Phase-11 semantic
wiki retriever globs (`flowstate/context_prefix.py::_semantic_wiki_layer`,
`_WIKI_CORPUS_DIR = ".planning/codebase/wiki"`). This simultaneously closes the
HAR-03b generator/reader mismatch: today `bench/wikigen.py` writes the SINGLE FILE
`wiki.md` while the reader globs an article DIRECTORY, so the semantic corpus is
produced by nothing. The reader contract (corpus-of-articles) is CORRECT and stays
untouched — this plan fixes the PRODUCER side.

Purpose: give the `wiki` arm a real producer whose output the reader actually
consumes, so the arm can measure something instead of silently falling back to empty.
Output: `bench/distiller.py` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

flowstate/memory.py:
```python
class MemoryKind(StrEnum):
    RESEARCH = "research"; STRATEGY = "strategy"; DECISION = "decision"
    INSIGHT = "insight"; RUN = "run"

class MemoryEntry:  # dataclass
    kind: MemoryKind; content: str; summary: str; source: str = ""
    tags: list[str] = []

class MemoryStore:  # context manager; db_path = (root or cwd) / "memory.db"
    def __init__(self, root: Path | None = None, *, embedder: Embedder | None = None): ...
    def get_by_kind(self, kind: MemoryKind, *, limit: int = 20) -> list[MemoryEntry]: ...
    def close(self) -> None: ...
```

flowstate/context_prefix.py (READER — DO NOT MODIFY, shown for the corpus contract):
```python
_WIKI_CORPUS_DIR = ".planning/codebase/wiki"   # article directory the reader globs
# _semantic_wiki_layer(root, query, embedder): for p in sorted(corpus_dir.glob("**/*.md"))
```

bench/judge.py (reuse):
```python
def _locate_claude() -> str | None: ...   # stdlib claude locator
```
</interfaces>

@bench/wikigen.py
@flowstate/context_prefix.py
@flowstate/memory.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic memory→wiki distiller core + guards</name>
  <read_first>
    - bench/wikigen.py (the analog: argparse --root/--force/--model, missing-input guard, never-raises discipline, `if __name__ == "__main__": sys.exit(main())`)
    - flowstate/memory.py (MemoryStore, MemoryKind, MemoryEntry, get_by_kind)
    - flowstate/context_prefix.py lines 63-66 and 224-305 (the `_WIKI_CORPUS_DIR` constant and `_semantic_wiki_layer` glob contract the corpus must satisfy)
  </read_first>
  <behavior>
    - A memory.db containing entries of ≥2 MemoryKinds → distiller writes ≥2 `*.md` files under `.planning/codebase/wiki/`, one article per non-empty kind, deterministic filename ordering.
    - Each article file contains a top-level `# {Kind}` heading and per-entry blocks built from `entry.summary` + `entry.content` (deterministic, no LLM).
    - Empty or absent memory.db (0 entries across all kinds) → prints a message to stderr, returns non-zero, writes NO files (no partial/empty corpus).
    - Existing corpus dir with articles and no `--force` → skips (returns 0), writes nothing; `--force` overwrites.
    - The written files are globbed by `sorted(corpus_dir.glob("**/*.md"))` — i.e. real `.md` files directly under `.planning/codebase/wiki/`.
  </behavior>
  <action>
    Create `bench/distiller.py` modeled structurally on `bench/wikigen.py`. Define
    `_WIKI_CORPUS_REL = ".planning/codebase/wiki"` (matching `context_prefix._WIKI_CORPUS_DIR`).
    Implement `main(argv: list[str] | None = None) -> int` with argparse flags
    `--root` (required Path), `--force` (store_true), `--model` (default "opus"),
    and `--llm` (store_true, wired in Task 2). Open a `MemoryStore(root)` and read
    entries across the meaningful kinds (DECISION, INSIGHT, RESEARCH, STRATEGY, RUN)
    via `get_by_kind`. Group into one article per non-empty kind; write files with a
    deterministic numeric+kind name (e.g. `01-decisions.md`, `02-insights.md`) under
    `root / _WIKI_CORPUS_REL`, creating the dir with `parents=True`. Each article:
    a `# {Kind title}` heading followed by one `## {summary}` block per entry with the
    entry content beneath. Guard order mirroring wikigen: (1) total entries == 0 →
    stderr message + `return 1`, no writes; (2) corpus dir already populated and not
    `--force` → "wiki corpus up to date; skipping" + `return 0`; (3) close the store
    in a finally/never-raises wrapper. Do NOT emit CANON text. Do NOT modify the
    reader in `context_prefix.py`. End with `if __name__ == "__main__": sys.exit(main())`.
  </action>
  <acceptance_criteria>
    - `tests/test_bench_distiller.py` asserts: a MemoryStore seeded with ≥2 kinds then run through `distiller.main(["--root", str(tmp)])` produces ≥2 files matching `(tmp/".planning/codebase/wiki").glob("**/*.md")`, each non-empty and containing its `# ` kind heading.
    - A test asserts an empty memory.db → `main()` returns non-zero and `(tmp/".planning/codebase/wiki")` contains no `.md` files.
    - A test asserts a populated corpus without `--force` returns 0 and does not overwrite (mtime/content unchanged); with `--force` it rewrites.
    - `python -m pytest tests/test_bench_distiller.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_bench_distiller.py -x -q</automated>
  </verify>
  <done>Deterministic distiller writes the article corpus the reader globs; empty-memory and force guards enforced by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optional one-pass --llm article densification (never-raises)</name>
  <read_first>
    - bench/distiller.py (Task 1 output being extended)
    - bench/wikigen.py lines 79-115 (the `_locate_claude` reuse, subprocess `claude --print --max-turns 1 --model`, returncode/empty-stdout handling, never-raises)
    - bench/judge.py (`_locate_claude` signature)
  </read_first>
  <behavior>
    - With `--llm`: each deterministic article is passed through ONE `claude --print` densification call; a returncode==0, non-empty stdout replaces the article body, otherwise the deterministic text is kept.
    - `--llm` when claude is not locatable (`_locate_claude()` is None) → deterministic articles are written unchanged, a message is printed, `main()` still returns 0 (corpus was produced).
    - A raising/timing-out subprocess is caught; the deterministic article is written; the run never raises.
    - Without `--llm` (default) NO subprocess is spawned.
  </behavior>
  <action>
    Extend `bench/distiller.py`: import `_locate_claude` from `bench.judge` (reuse,
    matching wikigen). Add a bounded `_densify(article_text, claude, model, timeout)`
    helper that runs `[claude, "--print", "--max-turns", "1", "--model", model, "--", prompt]`
    with `subprocess.run(..., capture_output=True, text=True, timeout=_DISTILL_TIMEOUT)`
    inside a try/except that returns the original text on any exception or non-zero/empty
    output. Define a `_DISTILL_TIMEOUT` constant and a `PROMPT_HEADER`-style densification
    prompt (dense, high-signal, knowledge-not-code; Markdown; concrete names). In `main`,
    when `--llm` is set and `_locate_claude()` returns a path, route each article body
    through `_densify` before writing; when the locator is None, print a message and write
    the deterministic bodies unchanged. Keep the without-`--llm` path subprocess-free.
    Preserve every Task-1 guard and the never-raises discipline.
  </action>
  <acceptance_criteria>
    - A test monkeypatches `_locate_claude` to None and asserts `--llm` still writes the deterministic corpus and returns 0 (no crash, no subprocess).
    - A test monkeypatches the subprocess to raise and asserts the deterministic article is written and `main()` does not raise.
    - A test asserts the default (no `--llm`) path spawns no subprocess (e.g. patch `subprocess.run` to fail the test if called).
    - `python -m pytest tests/test_bench_distiller.py -x` passes at full coverage gate.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_bench_distiller.py -x -q && ruff check bench/distiller.py</automated>
  </verify>
  <done>`--llm` densifies each article via one bounded claude call, degrading to deterministic text on any failure; default path is subprocess-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local memory.db → distiller | trusted local SQLite content authored by prior FlowState runs |
| distiller → claude subprocess (--llm) | `claude --print` invoked on local memory content; no untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service | `_densify` subprocess hang | mitigate | `subprocess.run(..., timeout=_DISTILL_TIMEOUT)`; timeout caught → deterministic fallback |
| T-17-02 | Tampering | partial/empty corpus written on failure | mitigate | empty-memory guard returns non-zero and writes nothing; per-article fallback keeps deterministic text |
| T-17-SC | Tampering | no new package installs | accept | stdlib + existing deps only; no npm/pip/cargo install introduced |
</threat_model>

<verification>
- `python -m bench.distiller --root <fixture-with-memory>` writes ≥2 `.md` files under `.planning/codebase/wiki/` that `context_prefix._semantic_wiki_layer` would glob.
- Empty memory.db → non-zero exit, no files written.
- `python -m pytest tests/test_bench_distiller.py -x` passes; `ruff check bench/distiller.py` clean.
</verification>

<success_criteria>
- Distiller produces the article-corpus directory shape the Phase-11 reader consumes (HAR-03a + generator/reader-mismatch fix HAR-03b).
- Fail-loud on empty memory; never-raises on subprocess failure.
- No reader changes; no new runtime deps.
</success_criteria>

<output>
Create `.planning/phases/17-no-silent-no-op-arms-producers-wired-e2e/17-01-SUMMARY.md` when done
</output>
