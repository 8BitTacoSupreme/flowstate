---
phase: 07-gotchas-accumulator
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - flowstate/memory_handlers.py
  - flowstate/orchestrator.py
  - flowstate/journal.py
  - tests/test_memory_handlers.py
  - tests/test_orchestrator.py
  - tests/test_journal.py
autonomous: true
requirements: [GOT-01]
must_haves:
  truths:
    - "on_step_failed still stores the existing TOOL_RUN failure entry AND additionally captures an executor gotcha (source=executor, severity=error)"
    - "run_pipeline calls harvest_planning_gotchas(memory, root) once at pipeline start (after MemoryStore opens, before adapters run), best-effort, never fatal"
    - "the RUN journal entry's metadata gotchas slot lists this run's captured gotcha signatures (delta-only — signatures, not full content)"
    - "the RUNLOG.md gotchas line reflects this run's captured signatures instead of the (none this phase) placeholder"
  artifacts:
    - path: "flowstate/memory_handlers.py"
      provides: "on_step_failed extended to also capture an executor gotcha"
      contains: "capture_gotcha"
    - path: "flowstate/orchestrator.py"
      provides: "harvest_planning_gotchas call at pipeline start"
      contains: "harvest_planning_gotchas"
    - path: "flowstate/journal.py"
      provides: "gotchas metadata slot + RUNLOG line populated from this run's signatures"
      contains: "gotchas"
  key_links:
    - from: "flowstate/memory_handlers.py on_step_failed"
      to: "flowstate.gotchas.capture_gotcha"
      via: "executor failure → gotcha (alongside existing TOOL_RUN store)"
      pattern: "capture_gotcha"
    - from: "flowstate/orchestrator.py run_pipeline"
      to: "flowstate.gotchas.harvest_planning_gotchas"
      via: "best-effort harvest at pipeline start"
      pattern: "harvest_planning_gotchas"
    - from: "flowstate/journal.py append_run_entry"
      to: "this run's gotcha signatures"
      via: "query INSIGHT gotchas with matching run_id"
      pattern: "gotchas"
---

<objective>
Wire the gotchas accumulator into the pipeline runtime: extend the executor failure handler to
ALSO capture a gotcha (source 2/4), harvest prior-phase GSD-artifact gotchas at run_pipeline start
(sources 3-4 become durable context), and populate the Phase-6 journal `gotchas` slot with this
run's captured signatures — closing the 6→7 link.

Purpose: GOT-01 (executor source + harvest of verifier/plan-checker sources + journal gotchas slot).
Output: extended on_step_failed, orchestrator harvest call, journal gotchas population, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gotchas-accumulator/07-CONTEXT.md
@.planning/phases/07-gotchas-accumulator/07-PATTERNS.md
@flowstate/memory_handlers.py
@flowstate/journal.py

<interfaces>
<!-- From 07-01 (Wave 1) -->
- flowstate.gotchas.capture_gotcha(memory, *, source, message, root, severity="warning", run_id="", timestamp=None) -> None
- flowstate.gotchas.harvest_planning_gotchas(memory, root) -> None
- gotcha entries: MemoryKind.INSIGHT, tags include "gotcha", metadata has "signature", run_id set from capture.
<!-- Existing seams to extend -->
- memory_handlers.on_step_failed (L104-118): closure has `store`, `root`, `run_id`; today stores a
  TOOL_RUN entry tagged [tool,"failure"]. KEEP that; add gotcha capture after it.
- orchestrator.run_pipeline (L176-181): `run_id = uuid4().hex[:12]`, `memory = MemoryStore(root=root)`
  opened at L177, handlers registered L180. Harvest goes right after memory opens, before adapters.
  Existing append_run_entry call wrapped in try/except at L313-317 is the never-fatal pattern to copy.
- journal.append_run_entry (L73-116): metadata["gotchas"] = [] at L79; _append_runlog gotchas line at L158.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend on_step_failed to also capture an executor gotcha</name>
  <files>flowstate/memory_handlers.py, tests/test_memory_handlers.py</files>
  <read_first>
    flowstate/memory_handlers.py L104-120 (on_step_failed — the closure with store/root/run_id);
    07-PATTERNS.md "memory_handlers.py — extend on_step_failed" (add gotcha capture AFTER the existing store.add; lazy import to avoid circular import).
  </read_first>
  <behavior>
    - Test: firing a step.failed event still stores the existing TOOL_RUN entry tagged [tool,"failure"] (regression — count of TOOL_RUN entries unchanged behavior).
    - Test: the same step.failed event ALSO creates an INSIGHT gotcha tagged ["gotcha","executor"] with severity error and run_id set.
    - Test: two step.failed events for the same tool+error produce ONE gotcha with count==2 (dedup via signature) while still recording TOOL_RUN entries per the existing behavior.
    - Test: if capture_gotcha import/call fails, the handler still completes (existing TOOL_RUN store unaffected).
  </behavior>
  <action>
    In on_step_failed, AFTER the existing `store.add(MemoryEntry.create(MemoryKind.TOOL_RUN, ...))`
    (never before — preserve existing behavior), add a best-effort gotcha capture: try a LAZY
    import `from flowstate.gotchas import capture_gotcha` then call `capture_gotcha(store,
    source="executor", message=f"Tool '{tool_name}' failed: {error}", root=root, severity="error",
    run_id=run_id)`; wrap in `except Exception: pass`. The lazy import avoids circular-import risk
    (gotchas.py and memory_handlers.py both import from memory.py; gotchas may import doctor). Use
    the `root` and `run_id` already captured by the create_memory_handlers closure.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_memory_handlers.py -q</automated>
  </verify>
  <done>on_step_failed preserves the TOOL_RUN store and additionally captures a deduped executor gotcha with run_id; capture failure is non-fatal.</done>
  <acceptance_criteria>
    - `grep -c 'capture_gotcha' flowstate/memory_handlers.py` >= 1
    - `grep -c 'source="executor"' flowstate/memory_handlers.py` >= 1
    - `grep -c 'MemoryKind.TOOL_RUN' flowstate/memory_handlers.py` >= 1  (existing store preserved)
    - `python -m pytest tests/test_memory_handlers.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harvest planning gotchas at run_pipeline start + populate journal gotchas slot</name>
  <files>flowstate/orchestrator.py, flowstate/journal.py, tests/test_orchestrator.py, tests/test_journal.py</files>
  <read_first>
    flowstate/orchestrator.py L176-181 (memory open + handler registration — harvest insertion point);
    flowstate/orchestrator.py L313-317 (append_run_entry try/except — never-fatal wrapper pattern to copy);
    flowstate/journal.py L73-116 (metadata["gotchas"]=[] at L79; memory.add at L111) and L137-163 (_append_runlog gotchas line L158);
    07-PATTERNS.md "orchestrator.py — harvest at pipeline start" and "journal.py — populate gotchas metadata slot".
  </read_first>
  <behavior>
    - Test (orchestrator): run_pipeline (dry-run / mocked adapters) calls harvest_planning_gotchas exactly once after the MemoryStore is opened and before the first adapter step; a harvest that raises does not abort the pipeline (non-fatal, logged).
    - Test (journal): when a gotcha with run_id==this run exists, append_run_entry sets metadata["gotchas"] to a list containing that gotcha's signature; gotchas from OTHER run_ids are excluded.
    - Test (journal): with no gotchas this run, metadata["gotchas"] == [] and the RUNLOG gotchas line reads "(none this run)".
    - Test (journal): the RUNLOG.md gotchas line lists the captured signatures joined by ", " when present.
  </behavior>
  <action>
    Orchestrator: immediately after `memory = MemoryStore(root=root)` (and the handler
    registration is fine either side, but place it before the first adapter step), add a
    best-effort harvest wrapped exactly like the existing append_run_entry guard: try `from
    flowstate.gotchas import harvest_planning_gotchas` then `harvest_planning_gotchas(memory, root)`;
    `except Exception as exc: console.print(f"  [yellow]gotchas harvest: non-fatal error: {exc}[/yellow]")`.
    Do not move or alter the existing append_run_entry call.

    Journal: in append_run_entry, replace the `metadata["gotchas"] = []` placeholder with a
    best-effort query: try fetch `memory.get_by_kind(MemoryKind.INSIGHT, limit=200)`, collect
    `metadata.signature` for entries where `run_id and entry.run_id == run_id and "gotcha" in
    entry.tags`, assign that list to metadata["gotchas"]; on Exception assign []. Thread the
    resulting list into _append_runlog (add a `gotchas: list[str]` parameter) and replace the
    `fh.write("- gotchas: (none this phase)\n")` line with
    `gotchas_str = ", ".join(gotchas) if gotchas else "(none this run)"` then
    `fh.write(f"- gotchas: {gotchas_str}\n")`. Keep the never-raises contract on both functions.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_orchestrator.py tests/test_journal.py -q</automated>
  </verify>
  <done>harvest runs once at pipeline start (non-fatal); journal metadata + RUNLOG gotchas line reflect this run's signatures; no-gotchas case reads "(none this run)".</done>
  <acceptance_criteria>
    - `grep -c 'harvest_planning_gotchas' flowstate/orchestrator.py` >= 1
    - `grep -c 'none this phase' flowstate/journal.py` == 0  (placeholder replaced)
    - `grep -c 'gotchas' flowstate/journal.py` >= 3  (metadata population + param + RUNLOG line)
    - `python -m pytest tests/test_orchestrator.py tests/test_journal.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline runtime → harvest | harvest reads .planning/phases/* (handled in 07-01); orchestrator must keep it non-fatal |
| event payload → executor capture | tool/error strings from failed steps feed capture/SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Denial of Service | harvest raising and aborting run_pipeline | mitigate | try/except with console log around harvest_planning_gotchas (copy the append_run_entry guard) |
| T-07-13 | Denial of Service | gotcha capture breaking the failure handler | mitigate | best-effort try/except: pass after the existing TOOL_RUN store; existing behavior preserved |
| T-07-14 | Denial of Service | journal gotchas query raising and breaking the journal | mitigate | query wrapped → metadata["gotchas"]=[] fallback; append_run_entry stays never-raises |
| T-07-15 | Tampering | circular import via gotchas → doctor → memory | mitigate | lazy import of capture_gotcha/harvest inside the call sites (not module-level) |
| T-07-SC | Tampering | npm/pip installs | accept | No new dependencies this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_memory_handlers.py tests/test_orchestrator.py tests/test_journal.py -q` exits 0
- `grep -c 'harvest_planning_gotchas' flowstate/orchestrator.py` >= 1
- `grep -c 'none this phase' flowstate/journal.py` == 0
- ruff: `python -m ruff check flowstate/memory_handlers.py flowstate/orchestrator.py flowstate/journal.py` clean
</verification>

<success_criteria>
- Executor step failures captured as deduped gotchas alongside the preserved TOOL_RUN entry.
- harvest_planning_gotchas runs once at pipeline start, non-fatal.
- Journal metadata + RUNLOG gotchas line populated from this run's captured signatures.
</success_criteria>

<output>
Create `.planning/phases/07-gotchas-accumulator/07-04-SUMMARY.md` when done (status: complete).
</output>
