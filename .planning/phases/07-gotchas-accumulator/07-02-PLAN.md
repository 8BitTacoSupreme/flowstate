---
phase: 07-gotchas-accumulator
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - flowstate/context_prefix.py
  - tests/test_context_prefix.py
autonomous: true
requirements: [GOT-02, GOT-03]
must_haves:
  truths:
    - "build_context_prefix emits a ## Gotchas section positioned BEFORE the ## Prior Knowledge memory layer and AFTER the pack layer"
    - "The gotchas layer is capped to gotchas_max_entries (default 10) ranked by (count desc, last_seen desc) then trimmed to gotchas_budget_tokens (default 1500)"
    - "gotchas_enabled=false omits the section entirely (no empty heading)"
    - "The gotchas layer participates in BOTH the pack fit-ladder candidate estimates AND the final budget guard — the assembled prefix never silently exceeds budget"
    - "_read_gotchas_layer never raises and context_prefix.py imports nothing from flowstate.bridge"
  artifacts:
    - path: "flowstate/context_prefix.py"
      provides: "_read_gotchas_layer, _load_gotchas_max_entries, _load_gotchas_budget_tokens, _load_gotchas_enabled, gotchas_layer integrated into assembly + fit-ladder + final guard"
      contains: "def _read_gotchas_layer"
  key_links:
    - from: "flowstate/context_prefix.py"
      to: "MemoryKind.INSIGHT gotchas in memory.db"
      via: "memory.get_by_kind(INSIGHT) filtered by gotcha tag"
      pattern: "_read_gotchas_layer"
    - from: "gotchas_layer"
      to: "pack fit-ladder candidates + final budget guard"
      via: "_SEPARATOR.join filter lists include gotchas_layer"
      pattern: "gotchas_layer"
---

<objective>
Add the `## Gotchas` prefix layer to build_context_prefix, positioned BETWEEN pack and memory so
it benefits from the prompt cache. The layer is bounded (max entries + token budget), gated by
config, and — critically — joins every budget-accounting candidate so it can never silently blow
the prefix budget (Phase-6 CR-01 was exactly this bug for since-last-run; do not reintroduce it).

Purpose: GOT-02 (the `## Gotchas` prefix layer before memory) and GOT-03's cap/budget enforcement.
Output: `_read_gotchas_layer` + three config helpers + integrated assembly/fit-ladder/guard, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gotchas-accumulator/07-CONTEXT.md
@.planning/phases/07-gotchas-accumulator/07-PATTERNS.md
@flowstate/context_prefix.py

<interfaces>
<!-- Stable seam from 07-01 (Wave 1) this plan reads -->
- MemoryStore.get_by_kind(MemoryKind.INSIGHT, limit=N) -> list[MemoryEntry]
- gotcha entries carry metadata: {signature, source, severity, first_seen, last_seen, count}
  and tags include "gotcha".
<!-- Existing context_prefix.py contracts to mirror -->
- _load_budget / _load_journal_prefix_n idiom (L69-104): json.loads config, guard
  isinstance(int) and not isinstance(bool) and value > 0.
- _read_since_last_run_layer (L107-124): the EXACT structural analog for _read_gotchas_layer.
- Assembly L282-284; pack fit-ladder candidates L226-227 + L242-247; final guard L271-279.
- _estimate_tokens (L64-66), _SEPARATOR (L47).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gotchas config helpers + _read_gotchas_layer</name>
  <files>flowstate/context_prefix.py, tests/test_context_prefix.py</files>
  <read_first>
    flowstate/context_prefix.py L69-104 (_load_budget / _load_journal_prefix_n — copy the idiom);
    flowstate/context_prefix.py L107-124 (_read_since_last_run_layer — exact structural analog);
    07-PATTERNS.md "context_prefix.py — add _read_gotchas_layer" (helper keys/defaults + layer swap).
  </read_first>
  <behavior>
    - Test: _load_gotchas_max_entries returns 10 when config absent; returns the configured int; rejects bool/negative/zero (falls back to 10).
    - Test: _load_gotchas_budget_tokens returns 1500 default; honors valid int; rejects bool.
    - Test: _load_gotchas_enabled returns True default; returns False only when the key is literally bool False.
    - Test: _read_gotchas_layer with N gotcha entries emits a "## Gotchas" heading and one block per entry, ranked (count desc, last_seen desc), capped to max_entries then trimmed so the layer's _estimate_tokens <= gotchas_budget_tokens.
    - Test: _read_gotchas_layer returns "" when no gotchas exist, when gotchas_enabled is False, and when memory.get_by_kind raises (never raises).
  </behavior>
  <action>
    Add three config readers mirroring _load_budget exactly:
    `_load_gotchas_max_entries(root)` key "gotchas_max_entries" default 10 (int guard);
    `_load_gotchas_budget_tokens(root)` key "gotchas_budget_tokens" default 1500 (int guard);
    `_load_gotchas_enabled(root)` key "gotchas_enabled" default True (return value only when
    `isinstance(value, bool)`, else default). Add module constants _DEFAULT_GOTCHAS_MAX_ENTRIES=10,
    _DEFAULT_GOTCHAS_BUDGET_TOKENS=1500.

    Add `_read_gotchas_layer(root, memory) -> str` structurally identical to
    _read_since_last_run_layer: wrap in try/except returning "". If not _load_gotchas_enabled →
    return "". Fetch gotchas via `memory.get_by_kind(MemoryKind.INSIGHT, limit=max_entries*5)` and
    filter `"gotcha" in entry.tags`; sort by (metadata.count desc, metadata.last_seen desc); take
    top _load_gotchas_max_entries. Build "## Gotchas\n" header + one block per gotcha
    (source / severity / count / last_seen / message summary). After building, trim trailing blocks
    until `_estimate_tokens(layer) <= _load_gotchas_budget_tokens(root)` (greedy fit so the layer
    self-caps). Return "" when no gotchas. NO bridge import.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_context_prefix.py -q -k "gotcha or Gotcha"</automated>
  </verify>
  <done>Three config helpers + _read_gotchas_layer exist; layer is ranked, capped, token-bounded, gated, never-raises.</done>
  <acceptance_criteria>
    - `grep -c 'def _read_gotchas_layer' flowstate/context_prefix.py` >= 1
    - `grep -c 'def _load_gotchas_max_entries' flowstate/context_prefix.py` >= 1
    - `grep -c 'def _load_gotchas_budget_tokens' flowstate/context_prefix.py` >= 1
    - `grep -c 'def _load_gotchas_enabled' flowstate/context_prefix.py` >= 1
    - `grep -c 'import.*bridge' flowstate/context_prefix.py` == 0
    - `python -m pytest tests/test_context_prefix.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire gotchas_layer into assembly, pack fit-ladder candidates, and final budget guard</name>
  <files>flowstate/context_prefix.py, tests/test_context_prefix.py</files>
  <read_first>
    flowstate/context_prefix.py L216-284 (build_context_prefix: where since_last_run_layer is built,
      both pack candidate joins, the final guard, and the final assembly list);
    07-PATTERNS.md "CR-01 lesson — budget participation is mandatory" + the exact candidate/guard/assembly edits.
  </read_first>
  <behavior>
    - Test (order): with fixtures + pack + gotchas + memory all present, the result indexes satisfy pack_idx < gotchas_idx < memory_idx ("## Gotchas" before "## Prior Knowledge").
    - Test (budget participation): a fat gotchas set under a tiny budget_tokens forces a drop — the assembled prefix _estimate_tokens stays < budget, and the drop is logged via the injected console (not silent). Drop order: since_last_run first, then gotchas.
    - Test (omission): no gotchas → no "## Gotchas" heading appears anywhere in the result.
    - Test (no regression): existing pack/memory/since-last-run order and budget tests still pass.
  </behavior>
  <action>
    In build_context_prefix, build `gotchas_layer = _read_gotchas_layer(root, memory)` alongside the
    other layers (before the pack fit-ladder). Add gotchas_layer to BOTH pack candidate joins
    (candidate and candidate2) in the filter(None, [...]) lists, positioned between the pack slot
    and memory_layer — so the pack fit decision accounts for the gotchas cost. Update the final
    budget guard: build full_assembly including gotchas_layer between pack_layer and memory_layer;
    keep the existing since_last_run drop-first behavior, then add a second guard that sets
    gotchas_layer = "" (with a Rich console log identifying the dropped gotchas layer) if the
    assembly still exceeds budget after dropping since_last_run. Update the final `layers` list to
    `[fixtures_layer, pack_layer, gotchas_layer, memory_layer, since_last_run_layer]`. Preserve the
    existing `[l for l in layers if l]` omission filter. Every drop logs — never silent.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_context_prefix.py -q</automated>
  </verify>
  <done>gotchas_layer sits before memory in assembly, joins both pack candidates and the final guard, drops-with-log when over budget, omitted when empty; full prefix never silently exceeds budget.</done>
  <acceptance_criteria>
    - `grep -c 'gotchas_layer' flowstate/context_prefix.py` >= 5  (build site + 2 candidates + guard + assembly)
    - `python -m pytest tests/test_context_prefix.py -q` exits 0
    - test asserting pack_idx < gotchas_idx < memory_idx passes
    - test asserting over-budget assembly stays < budget AND a drop is logged passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory.db gotchas → prefix string | gotcha content is injected into every bridge user-prompt; size must stay bounded |
| config.json → layer sizing | gotchas_max_entries / budget / enabled are operator-controlled integers/bools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Denial of Service | unbounded gotchas layer growing the prefix past budget | mitigate | max_entries cap + token-budget trim in _read_gotchas_layer AND participation in pack fit-ladder + final guard (CR-01 lesson) |
| T-07-06 | Tampering | malformed config values (bool/negative/string) | mitigate | _load_gotchas_* guard isinstance(int) and not isinstance(bool) and >0; enabled requires literal bool |
| T-07-07 | Denial of Service | _read_gotchas_layer raising into prefix assembly | mitigate | try/except return "" (mirrors _read_since_last_run_layer); never raises |
| T-07-SC | Tampering | npm/pip installs | accept | No new dependencies this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_context_prefix.py -q` exits 0
- `grep -c 'import.*bridge' flowstate/context_prefix.py` == 0
- ruff: `python -m ruff check flowstate/context_prefix.py` clean
- Order assertion (gotchas before memory) and budget-drop-with-log assertion both pass.
</verification>

<success_criteria>
- ## Gotchas appears before ## Prior Knowledge and after the pack layer.
- Layer is capped by entries and token budget; gated by gotchas_enabled.
- gotchas_layer joins fit-ladder candidates and the final guard — prefix never silently over budget.
- No bridge import; layer never raises.
</success_criteria>

<output>
Create `.planning/phases/07-gotchas-accumulator/07-02-SUMMARY.md` when done (status: complete).
</output>
