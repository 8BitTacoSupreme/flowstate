---
phase: 23-linux-parity-core-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [flowstate/sandbox.py, tests/test_sandbox.py]
autonomous: true
requirements: [SBX-02]

must_haves:
  truths:
    - "flowstate/sandbox.py imports cleanly on macOS with no new runtime dependency"
    - "wrap(cmd, surface, project_root, env) returns a (argv, env) tuple and never spawns a process"
    - "observe tier strips credential-shaped vars but preserves ANTHROPIC_API_KEY / CLAUDE_CODE_OAUTH_TOKEN / CLAUDE_CONFIG_DIR"
    - "observe tier never mutates argv"
  artifacts:
    - path: "flowstate/sandbox.py"
      provides: "wrap() seam, _scrub_env denylist with _AUTH_EXEMPT carve-out, observe tier, confine-dispatch contract stubs"
      contains: "def wrap("
    - path: "tests/test_sandbox.py"
      provides: "observe-tier and env-scrub unit tests including the named auth-carve-out regression"
      contains: "test_observe_never_strips_claude_auth_vars"
  key_links:
    - from: "flowstate/sandbox.py:wrap"
      to: "flowstate/sandbox.py:_scrub_env"
      via: "observe tier calls _scrub_env(env) unconditionally"
      pattern: "_scrub_env\\("
---

<objective>
Build the SBX-02 non-blocking foundation: `flowstate/sandbox.py` with the `wrap(cmd, surface, project_root, env)` seam (D-04), the `observe` tier (env-scrub only, never blocks, D-01), and the `_AUTH_EXEMPT` carve-out (RESEARCH Pitfall 1). This plan produces ONLY the pure, I/O-free core — no subprocess, no ctypes, no platform confinement. It also declares the confine-path function contracts (stubs) so plans 02/03 implement against a fixed in-file interface.

Purpose: Every later phase (24 wiring, 25 confinement) threads through this seam; getting the observe tier and the auth carve-out right is the load-bearing correctness of the whole milestone's default posture.
Output: `flowstate/sandbox.py` (observe path complete, confine path stubbed) + `tests/test_sandbox.py` (observe + scrub tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-linux-parity-core-seam/23-CONTEXT.md
@.planning/phases/23-linux-parity-core-seam/23-RESEARCH.md
@.planning/phases/23-linux-parity-core-seam/23-PATTERNS.md

<interfaces>
<!-- The seam signature the plan must implement verbatim (D-04, RESEARCH Code Examples). -->
<!-- surface is accepted and reserved for per-surface policy in Phase 24/25; observe ignores it. -->
<!-- tier defaults to "observe" in Phase 23 (no config field yet — that is Phase 24 / SBX-04). -->

wrap(cmd: list[str], surface: str, project_root: Path, env: dict[str, str], *, tier: str = "observe") -> tuple[list[str], dict[str, str]]

_scrub_env(env: dict[str, str]) -> dict[str, str]

_AUTH_EXEMPT = {"ANTHROPIC_API_KEY", "CLAUDE_CODE_OAUTH_TOKEN", "CLAUDE_CONFIG_DIR"}

<!-- Confine-path contracts declared here as stubs, implemented in plans 02/03: -->
build_macos_profile(project_root: Path) -> str
build_linux_bwrap_args(project_root: Path) -> list[str]
check_bwrap_available() -> bool
_wrap_macos(cmd: list[str], project_root: Path, env: dict[str, str]) -> tuple[list[str], dict[str, str]]
_wrap_linux(cmd: list[str], project_root: Path, env: dict[str, str]) -> tuple[list[str], dict[str, str]]
</interfaces>

Existing env-transform shape being generalized (NOT modified this phase): flowstate/bridge.py:299-315
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: env-scrub denylist with the _AUTH_EXEMPT carve-out</name>
  <read_first>
    - flowstate/sandbox.py (does not exist yet — this task creates it)
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Common Pitfalls → Pitfall 1: the finalized denylist pattern set and the auth carve-out; Pitfall 2: denylist-not-allowlist)
    - .planning/phases/23-linux-parity-core-seam/23-PATTERNS.md (Shared Patterns → "Env dict transform" and the `_scrub_env` mapping of bridge.py:300-304)
    - flowstate/bridge.py:299-315 (the `env = {**os.environ}` + selective-pop shape being generalized)
    - flowstate/embeddings.py:1-19 (module-docstring / degradation-contract style to mirror)
  </read_first>
  <behavior>
    - _scrub_env({"AWS_SECRET_ACCESS_KEY": "x"}) drops AWS_SECRET_ACCESS_KEY (prefix AWS_)
    - _scrub_env({"FOO_API_KEY": "x"}) drops FOO_API_KEY (suffix _API_KEY)
    - _scrub_env({"SOME_TOKEN": "x"}) drops SOME_TOKEN (suffix _TOKEN)
    - _scrub_env({"PASSWORD": "x"}) drops PASSWORD (exact)
    - _scrub_env({"ANTHROPIC_API_KEY": "sk-ant"}) KEEPS ANTHROPIC_API_KEY (carve-out beats the _API_KEY suffix)
    - _scrub_env({"CLAUDE_CODE_OAUTH_TOKEN": "t"}) KEEPS it (carve-out beats the _TOKEN suffix)
    - _scrub_env({"CLAUDE_CONFIG_DIR": "/p"}) KEEPS it (carve-out; not secret-shaped but auth-relevant)
    - _scrub_env({"ENABLE_PROMPT_CACHING_1H": "1"}) KEEPS it (not caught by any pattern — regression guard)
    - _scrub_env({"PATH": "/usr/bin", "HOME": "/h"}) KEEPS both (pass-through default)
    - A bare "ANTHROPIC_" prefix is NOT a denylist entry (FlowState's own auth lives there — RESEARCH Pitfall 1)
  </behavior>
  <files>flowstate/sandbox.py, tests/test_sandbox.py</files>
  <action>
    Create flowstate/sandbox.py with `from __future__ import annotations` and a module docstring that states the observe-never-blocks contract in the first sentence and cross-references D-01..D-04 by ID (mirror embeddings.py's contract-first docstring). Define module constants exactly per RESEARCH Pitfall 1: `_DENY_PREFIXES` = ("AWS_", "AZURE_", "GCP_", "GCLOUD_", "SSH_", "GPG_", "DOCKER_", "KUBE", "GITHUB_", "GITLAB_", "BITBUCKET_", "STRIPE_", "TWILIO_", "SENDGRID_", "SLACK_", "DISCORD_", "DATABASE_", "DB_", "REDIS_", "MONGO"); `_DENY_SUFFIXES` = ("_API_KEY", "_TOKEN", "_SECRET", "_PASSWORD", "_CREDENTIALS"); `_DENY_EXACT` = frozenset({"SECRET_KEY", "PASSWORD", "PASSWD"}); `_AUTH_EXEMPT` = frozenset({"ANTHROPIC_API_KEY", "CLAUDE_CODE_OAUTH_TOKEN", "CLAUDE_CONFIG_DIR"}). Deliberately do NOT add a bare "ANTHROPIC_" prefix (unlike sandflox) — cite Pitfall 1 in a comment. Implement `_scrub_env(env) -> dict[str, str]` as a shallow-copy-then-filter: for each key, keep if `key in _AUTH_EXEMPT` (checked FIRST); else drop if it startswith any `_DENY_PREFIXES`, endswith any `_DENY_SUFFIXES`, or is in `_DENY_EXACT`; else keep. Never mutate the input dict; return a new dict. Docstring on the public helper. Write the behavior cases above as a `TestScrubEnv` class in tests/test_sandbox.py (class-per-concept style from tests/test_pack.py).
  </action>
  <verify>
    <automated>python -m pytest tests/test_sandbox.py -k ScrubEnv -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "_AUTH_EXEMPT" flowstate/sandbox.py` and `grep -q "def _scrub_env" flowstate/sandbox.py`
    - `python -c "from flowstate.sandbox import _scrub_env; e=_scrub_env({'ANTHROPIC_API_KEY':'k','AWS_X':'v','PATH':'/b'}); assert e['ANTHROPIC_API_KEY']=='k' and 'AWS_X' not in e and e['PATH']=='/b'"` exits 0
    - tests/test_sandbox.py contains a `TestScrubEnv` class; `python -m pytest tests/test_sandbox.py -k ScrubEnv` exits 0
    - ANTHROPIC_ bare prefix is NOT present as a denylist entry: `grep -v '^#' flowstate/sandbox.py | grep -c '"ANTHROPIC_"' ` returns 0
  </acceptance_criteria>
  <done>_scrub_env drops every credential-shaped var, keeps the three _AUTH_EXEMPT names and all unmatched vars, and the input dict is never mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wrap() seam + observe tier + confine-dispatch contract stubs</name>
  <read_first>
    - flowstate/sandbox.py (created in Task 1 — extend it)
    - tests/test_sandbox.py (created in Task 1 — extend it)
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Architecture Patterns → Pattern 2 "observe tier never calls subprocess"; Code Examples → "D-04 seam shape with the Pitfall-1 auth carve-out")
    - .planning/phases/23-linux-parity-core-seam/23-PATTERNS.md (§5 "Core wrap() seam pattern"; test §4 the exact `test_observe_scrubs_known_secret_patterns` shape)
  </read_first>
  <behavior>
    - wrap(["echo","hi"], surface="llm", project_root=Path("/tmp/p"), env={...}) with default tier returns (["echo","hi"], scrubbed) — argv byte-identical to input
    - wrap(...) default tier equals tier="observe": returned env == _scrub_env(input env)
    - Named regression test_observe_never_strips_claude_auth_vars: env with ANTHROPIC_API_KEY + CLAUDE_CODE_OAUTH_TOKEN survives wrap() under observe
    - wrap(..., tier="confine") on an UNSUPPORTED platform (neither darwin nor linux) returns (cmd, scrubbed_env) — env-scrub only, never raises
    - wrap() never calls subprocess (assert via absence — no monkeypatch of subprocess needed in observe tests)
  </behavior>
  <files>flowstate/sandbox.py, tests/test_sandbox.py</files>
  <action>
    Add `import sys` and `from pathlib import Path` as needed. Implement `wrap(cmd, surface, project_root, env, *, tier="observe") -> tuple[list[str], dict[str, str]]` exactly per the D-04 seam shape: compute `scrubbed = _scrub_env(env)`; if `tier == "observe"` return `(cmd, scrubbed)` (argv untouched); for `tier == "confine"` dispatch on `sys.platform` — `"darwin"` -> `_wrap_macos(cmd, project_root, scrubbed)`, `sys.platform.startswith("linux")` -> `_wrap_linux(cmd, project_root, scrubbed)`, else return `(cmd, scrubbed)` (unsupported platform degrades to env-scrub only, never hard-fails). Declare the confine-path CONTRACT STUBS in this file with correct signatures and docstrings, each body `raise NotImplementedError("implemented in plan 23-02/23-03")  # pragma: no cover`: `build_macos_profile(project_root)`, `build_linux_bwrap_args(project_root)`, `check_bwrap_available()`, `_wrap_macos(cmd, project_root, env)`, `_wrap_linux(cmd, project_root, env)`. Do NOT implement confinement here. In tests, add a `TestWrapObserve` class covering the behavior cases, including the named `test_observe_never_strips_claude_auth_vars` and `test_observe_never_mutates_argv`, and `test_unsupported_platform_confine_returns_scrubbed` (monkeypatch `flowstate.sandbox.sys.platform` to a fake value like "sunos5" and call tier="confine"). Do not test the stubbed confine functions on darwin/linux in this plan.
  </action>
  <verify>
    <automated>python -m pytest tests/test_sandbox.py -x -q && ruff check flowstate/sandbox.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def wrap(" flowstate/sandbox.py` and the signature contains `tier: str = "observe"`
    - tests/test_sandbox.py contains `test_observe_never_strips_claude_auth_vars`; it passes
    - `python -c "from pathlib import Path; from flowstate.sandbox import wrap; a,e=wrap(['echo','hi'],'llm',Path('/tmp/p'),{'ANTHROPIC_API_KEY':'k','AWS_X':'v'}); assert a==['echo','hi'] and e['ANTHROPIC_API_KEY']=='k' and 'AWS_X' not in e"` exits 0
    - The five confine-path stubs exist with the exact signatures in the `<interfaces>` block
    - `python -m pytest tests/test_sandbox.py` exits 0 and `ruff check flowstate/sandbox.py` is clean
  </acceptance_criteria>
  <done>wrap() returns the transformed (argv, env) tuple, observe leaves argv untouched and preserves auth vars, unsupported-platform confine degrades to scrub-only, and the confine contracts are declared for plans 02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller env -> child subprocess env | os.environ (may contain unrelated secrets) crosses into a child process at the future call sites; `_scrub_env` is the filter |
| observe tier's non-blocking guarantee | observe intentionally provides ZERO filesystem confinement — only env hygiene |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `_scrub_env` leaks a credential-shaped var the denylist doesn't match | accept | Documented accepted risk in D-01: observe is env hygiene, not hard confinement; a novel non-pattern secret may pass. Confine/allowlist is the Phase-25 answer. |
| T-23-02 | Denial of Service (self-inflicted) | denylist strips `claude`'s own auth vars, breaking the tool it protects | mitigate | `_AUTH_EXEMPT` checked BEFORE denylist match; named regression `test_observe_never_strips_claude_auth_vars`; no bare `ANTHROPIC_` prefix in the denylist |
| T-23-03 | Repudiation / Tampering | observe silently gives a false sense of confinement | accept | By design observe never blocks (D-01/SBX-02); confine is the enforcing tier (plans 02/03, phase 25). observe's contract is stated in the module docstring so callers are not misled. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed this phase (stdlib only); no install task exists. |
</threat_model>

<verification>
- `python -m pytest tests/test_sandbox.py -q` passes; coverage of `flowstate/sandbox.py` observe path is exercised (confine stubs carry `# pragma: no cover`).
- `python -m pytest --cov=flowstate --cov-fail-under=80` still passes overall.
- `ruff check flowstate/sandbox.py tests/test_sandbox.py` clean.
</verification>

<success_criteria>
- `flowstate/sandbox.py` imports on macOS with no new dependency.
- `wrap()` returns `(argv, env)`, never spawns, observe leaves argv untouched, auth vars survive.
- Confine-path contracts declared for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/23-linux-parity-core-seam/23-01-SUMMARY.md` when done.
</output>
