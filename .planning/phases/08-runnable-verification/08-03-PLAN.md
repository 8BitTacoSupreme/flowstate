---
phase: 08-runnable-verification
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: [flowstate/cli.py, tests/test_cli.py]
autonomous: true
requirements: [VER-01, VER-02]

must_haves:
  truths:
    - "`flowstate verify` prints a Rich PASS/FAIL/SKIP report grouped by status with a summary line"
    - "`flowstate verify` exit code equals the count of FAIL results (sys.exit(fails)); 0 when no fails"
    - "`flowstate verify` exits 0 with a 'no fixtures to verify' message when .planning/fixtures/ is absent or empty"
    - "On each FAIL, the command captures a gotcha via capture_gotcha(source='verify')"
    - "Every verify run appends one run-journal entry via append_verify_entry"
    - "The command never raises an unhandled exception"
  artifacts:
    - path: "flowstate/cli.py"
      provides: "flowstate verify @main.command — report + exit + loop wiring"
      contains: "def verify"
    - path: "tests/test_cli.py"
      provides: "CLI tests: no-fixtures exit 0, fail exit non-zero, gotcha+journal wiring"
      contains: "verify"
  key_links:
    - from: "flowstate/cli.py:verify"
      to: "flowstate.verify.run_verify"
      via: "import + call with load_state(root)"
      pattern: "run_verify"
    - from: "flowstate/cli.py:verify"
      to: "gotchas.capture_gotcha + journal.append_verify_entry"
      via: "best-effort MemoryStore block"
      pattern: "capture_gotcha|append_verify_entry"
    - from: "flowstate/cli.py:verify"
      to: "process exit code"
      via: "sys.exit(fails)"
      pattern: "sys.exit"
---

<objective>
Add the `flowstate verify` command to `flowstate/cli.py` — a clone of the `doctor` command (cli.py L783-845) that calls `run_verify`, prints a Rich PASS/FAIL/SKIP report, exits with the count of FAILs, handles the absent/empty-fixtures case as exit 0, and closes the loop by capturing a gotcha per FAIL and appending a verify run-journal entry on every run. This completes VER-01 (report/exit) and VER-02 (loop wiring).

Purpose: Make verification composable in CI/pre-commit alongside `flowstate doctor` (non-zero == failure), and wire failures into the compounding loop so the next `flowstate run` injects them via the `## Gotchas` and `## Since Last Run` prefix layers.
Output: `flowstate/cli.py` (extended), `tests/test_cli.py` (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-runnable-verification/08-CONTEXT.md
@.planning/phases/08-runnable-verification/08-PATTERNS.md
@.planning/phases/08-runnable-verification/08-01-PLAN.md
@.planning/phases/08-runnable-verification/08-02-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Plans 01 and 02 must be complete first. -->

From flowstate/verify.py (Plan 01):
- @dataclass(frozen=True) class VerifyResult: gate: str; status: Literal["pass","fail","skip"]; message: str; fixture: str
- def run_verify(state: FlowStateModel, root: Path) -> list[VerifyResult]

From flowstate/journal.py (Plan 02):
- def append_verify_entry(memory: MemoryStore, root: Path, results: list[Any], *, timestamp=None) -> None

From flowstate/gotchas.py (Phase 7, existing):
- def capture_gotcha(memory, *, source, message, root, severity="warning", run_id="", timestamp=None) -> None

From flowstate/cli.py (existing — the doctor command to clone, L783-845):
- @main.command("doctor") with --root option; resolve_root(root, option_was_explicit=_root_was_explicit())
- console.print(Panel(BANNER, ...)); load_state(root); run_doctor; best-effort gotcha capture block (L809-821); Rich Table; sys.exit(errors)

From flowstate/memory.py:
- class MemoryStore: context manager (with MemoryStore(root=root) as store: ...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: flowstate verify @main.command in flowstate/cli.py</name>
  <files>flowstate/cli.py</files>
  <read_first>
    - flowstate/cli.py L783-845 (the entire doctor command — clone wholesale, substitute verify names/columns)
    - flowstate/cli.py around resolve_root / _root_was_explicit usage (L803)
    - flowstate/verify.py run_verify + VerifyResult (Plan 01)
    - flowstate/journal.py append_verify_entry (Plan 02)
    - flowstate/gotchas.py capture_gotcha (Phase 7)
    - 08-PATTERNS.md "flowstate/cli.py" section (8-point verify-command adaptation list)
  </read_first>
  <behavior>
    - `flowstate verify --root <empty-project>` (no .planning/fixtures/) returns exit 0, output contains "no fixtures to verify".
    - `flowstate verify --root <project with a failing gate>` returns exit code == count of FAIL results (> 0).
    - `flowstate verify --root <project, all pass/skip>` returns exit 0.
    - On a FAIL run, a gotcha with source="verify" is captured in memory.db.
    - On every run (pass or fail), one MemoryKind.RUN entry tagged "verify" is appended.
    - The command never raises even if memory.db is corrupt or fixtures are malformed.
  </behavior>
  <action>
    In `flowstate/cli.py`, add `@main.command("verify")` with the same `--root` click option as doctor, decorating `def verify(root: Path | None):`. Docstring: explain it runs runnable verification of fixture gates against produced artifacts and exits non-zero (count of failing gates) so it composes in CI / pre-commit. Inside: `import sys`; `from rich.table import Table`; `from flowstate.verify import run_verify`; `from flowstate.state import load_state`. Resolve root via `root = resolve_root(root, option_was_explicit=_root_was_explicit())`. Print the BANNER Panel exactly like doctor (L804).

    EARLY no-fixtures guard (before rendering): compute `fixtures_dir = root / ".planning" / "fixtures"`; if `not fixtures_dir.exists()` OR `not any(fixtures_dir.glob("*.json"))` then `console.print` a clear "no fixtures to verify" message and `return` (exit 0). Honor this message-and-return contract per CONTEXT.md success criterion #4.

    `state = load_state(root)`; `results = run_verify(state, root)`.

    Best-effort loop-closure block (mirror doctor L809-821: a single MemoryStore context, the whole block wrapped in `try/except Exception: pass`): inside, import `capture_gotcha` from flowstate.gotchas, `append_verify_entry` from flowstate.journal, and `MemoryStore as _MemoryStore` from flowstate.memory. Open `with _MemoryStore(root=root) as _store:`; for each `r` in results where `r.status == "fail"`, call `capture_gotcha(_store, source="verify", message=f"{r.gate}: {r.message}", root=root, severity="error")`; then ALWAYS call `append_verify_entry(_store, root, results)` once after the gotcha loop (journal entry on every run; gotchas only on fail).

    Render: build `Table(title="flowstate verify", border_style="blue")` with columns "Gate", "Status", "Fixture", "Message". `status_style = {"pass": "green", "fail": "red", "skip": "dim"}`; add one row per result with the status colorized via the style map. `console.print(table)`.

    Summary + exit: `fails = sum(1 for r in results if r.status == "fail")`; compute `passes` and `skips` the same way. `console.print(f"\n[bold]Summary:[/bold] {fails} fail(s), {passes} pass(es), {skips} skip(s)")`. `if fails: sys.exit(fails)` — mirror doctor's exit idiom exactly. Keep line-length ≤ 100, double quotes, snake_case.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -c "from click.testing import CliRunner; from flowstate.cli import main; import tempfile; d=tempfile.mkdtemp(); r=CliRunner().invoke(main,['verify','--root',d]); assert r.exit_code==0, r.output; assert 'no fixtures' in r.output.lower(), r.output; print('OK')"</automated>
  </verify>
  <done>`flowstate verify` exists as a @main.command; empty project exits 0 with "no fixtures to verify"; report renders PASS/FAIL/SKIP; exit code == count of fails; gotcha+journal loop wired in a best-effort never-raises block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_cli.py — flowstate verify CLI cases</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py L26-74 (healthy_install fixture — .planning/ + PROJECT.md + install_manifest + memory.db) and L78-82 (doctor CliRunner + exit-code assert pattern)
    - flowstate/context.py generate_starter_fixture() (build realistic fixtures into .planning/fixtures/)
    - 08-PATTERNS.md "tests/test_cli.py" + "healthy_install fixture" sections
  </read_first>
  <behavior>
    - test: `verify --root <empty tmp_path>` → exit_code 0 and output contains "no fixtures".
    - test: `verify --root <healthy_install + a fixtures dir with a passing/skippable gate>` → exit_code 0.
    - test: `verify` against a project whose install_manifest references a MISSING artifact → exit_code > 0 (integrity FAIL) and the failing gate appears in output. (Build a manifest entry with checksum != None pointing at a non-existent path; ensure a fixtures dir exists so the no-fixtures guard does not short-circuit.)
    - test: after a FAIL run, a gotcha with source="verify" exists in memory.db (query MemoryStore / gotchas).
    - test: after any run, a MemoryKind.RUN entry tagged "verify" exists in memory.db.
    - test: a malformed fixture file present → command does not crash (exit code is an int, no traceback).
  </behavior>
  <action>
    Extend `tests/test_cli.py` (do NOT create a new file). Add a `TestVerify` class using `CliRunner` and the existing `healthy_install` fixture style (adapt or build a local fixture that writes `.planning/PROJECT.md`, an install_manifest with a present non-empty artifact + a `checksum=None` memory.db + optionally a MISSING artifact for the fail test, and `.planning/fixtures/starter.json` from `generate_starter_fixture()` output). Invoke `runner.invoke(main, ["verify", "--root", str(tmp_path)])` and assert `result.exit_code` and output substrings per the behavior bullets. For the loop-closure asserts, open `MemoryStore(root=tmp_path)` after the invoke and assert a `source="verify"` gotcha (INSIGHT tagged gotcha) exists after a FAIL and a RUN entry tagged "verify" exists after any run. Keep line-length ≤ 100, double quotes.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_cli.py -q -k verify</automated>
  </verify>
  <done>tests/test_cli.py passes for the verify cases: no-fixtures exit 0, integrity FAIL exit non-zero, gotcha(source=verify) + RUN(tagged verify) wiring, malformed-fixture no-crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation → verify command | User/CI-supplied --root; on-disk fixtures + manifest cross here |
| verify command → process exit code | CI/pre-commit relies on non-zero == failure |
| verify command → memory.db (gotcha + journal writes) | SQLite write surface during loop closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service | verify command raising on corrupt memory.db / malformed fixture | mitigate | Loop-closure block wrapped in try/except: pass; run_verify itself never-raises (Plan 01); command degrades to a report + exit code |
| T-08-11 | Tampering | exit-code integrity (CI trusts non-zero == fail) | mitigate | sys.exit(fails) where fails = count of FAIL results only; SKIP/PASS never contribute; deterministic |
| T-08-12 | Spoofing | a verify pass when fixtures are absent masking missing artifacts | accept | no-fixtures path returns exit 0 by contract (CONTEXT #4); integrity backbone still surfaces via run_verify when fixtures exist; documented behavior |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime deps introduced this phase (stdlib re + xml.etree only); no install tasks to gate |
</threat_model>

<verification>
- `python -m pytest tests/test_cli.py -q -k verify` passes.
- `flowstate verify --root <empty>` exits 0 with "no fixtures to verify".
- `flowstate verify` against a project with a missing manifest artifact exits non-zero and prints the failing gate.
- After a FAIL run: a `source="verify"` gotcha and a RUN entry tagged "verify" exist in memory.db.
- `grep -c "import.*bridge" flowstate/cli.py` is unchanged (verify adds no bridge/LLM seam).
- `ruff check flowstate/cli.py tests/test_cli.py` clean.
</verification>

<success_criteria>
- `flowstate verify` reads fixtures, prints a human-readable PASS/FAIL/SKIP report, pure-Python (no LLM).
- Exits non-zero (count of fails) for CI/pre-commit composition; exit 0 on no-fixtures with a clear message; never raises.
- Each FAIL captures a gotcha (source=verify); every run appends a verify run-journal entry — the loop closes (VER-02).
</success_criteria>

<output>
Create `.planning/phases/08-runnable-verification/08-03-SUMMARY.md` when done.
</output>
