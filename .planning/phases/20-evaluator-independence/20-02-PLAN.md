---
phase: 20-evaluator-independence
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified: [bench/compound_eval.py, bench/close_loop.py, tests/test_bench_judge_independence.py]
autonomous: true
requirements: [IND-01, IND-03]
must_haves:
  truths:
    - "The real judging path in compound_eval.py calls the shared independence guard BEFORE invoking judge_run, using the producer model it already knows"
    - "close_loop.py's live judge path calls the same shared guard before judging a real run"
    - "compounding_score is computed only from RunSnapshots and is unchanged regardless of any judge score (multi-judge included)"
    - "The judge output is marked EXCLUDED from compounding_score in the written report under the multi-judge path"
  artifacts:
    - path: "bench/compound_eval.py"
      provides: "shared-guard call on the real judge path (D-06)"
      contains: "_validate_judges"
    - path: "bench/close_loop.py"
      provides: "shared-guard call on the live judge path (D-06)"
      contains: "_validate_judges"
    - path: "tests/test_bench_judge_independence.py"
      provides: "IND-03 exclusion test + caller-guard tests"
      contains: "def test_"
  key_links:
    - from: "bench/compound_eval.py judge path"
      to: "bench.judge._validate_judges"
      via: "guard call before judge_run, producer model as the argument"
      pattern: "_validate_judges"
    - from: "bench/metrics.py::compute_scorecard"
      to: "RunSnapshot list only"
      via: "no judge/JudgeResult input — deterministic; the exclusion test asserts this"
      pattern: "compute_scorecard"
---

<objective>
Wire the shared independence guard (built in 20-01) into the two real caller paths that actually judge a run — `bench/compound_eval.py` and `bench/close_loop.py` — so the guard protects live runs, not just the CLI (D-06). Then lock in IND-03 with a test proving `bench/metrics.py`'s `compounding_score` stays the authoritative deterministic scorer and the LLM judge (single- OR multi-judge) is excluded from it.

Purpose: IND-01's guard is only defensible if the callers that know the producer model enforce it; IND-03 is the milestone-wide integrity invariant made executable under the new multi-judge path.
Output: guard calls on both caller judge paths + a new exclusion test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-evaluator-independence/20-CONTEXT.md
@.planning/phases/20-evaluator-independence/20-01-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 1 (20-01) and the existing callers. Use directly. -->

From bench/judge.py (added in 20-01 — the shared guard, reuse it; exact name may differ, confirm from 20-01-SUMMARY):
```python
def _validate_judges(judge_models: list[str], producer_model: str) -> None:
    """Raises ValueError when judge set is empty or any judge == producer."""
```

From bench/compound_eval.py (current judge call site — the guard goes IMMEDIATELY BEFORE this):
```python
# _real_loop(...):
if do_judge:
    judged.append(judge_run(i, collect_artifacts(target), fixture, model=judge_model))
# CLI already exposes: parser.add_argument("--judge-model", default=None, ...)
# do_judge gate: _judge_allowed(args, console) requires --judge AND --mode real AND --allow-llm
```

From bench/metrics.py (the authority — DO NOT change; the test asserts it):
```python
def compute_scorecard(snapshots: list[RunSnapshot]) -> Scorecard: ...
# Scorecard.compounding_score is derived ONLY from RunSnapshots; no JudgeResult input.
```

From bench/report.py (already marks the judge EXCLUDED — the test asserts this holds under multi-judge):
```python
def write_json(scorecard, out, *, judge_results=None, producers=(), ...): ...
# payload["judge"]["note"] == "Tier-2 output-quality judge — EXCLUDED from compounding_score"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the shared guard on the real caller judge paths (IND-01 / D-06)</name>
  <files>bench/compound_eval.py, bench/close_loop.py, tests/test_bench_judge_independence.py</files>
  <read_first>
    - bench/compound_eval.py lines 140-167 (parser: --judge / --judge-model / --allow-llm), 280-380 (_real_loop, judge_run call, _judge_allowed gate)
    - bench/close_loop.py lines 100-190 (live judge trajectory path via replicate._run_trial; the CI note "EXCLUDED from compounding_score")
    - bench/judge.py (the `_validate_judges` helper added in 20-01 — confirm exact name/signature from 20-01-SUMMARY)
    - 20-01-SUMMARY.md (the guard's final name and the producer-model flag chosen in Wave 1)
  </read_first>
  <behavior>
    - When the real judge path is about to run with a judge model equal to the producer model, the guard raises/aborts BEFORE any judge_run call (no silent same-model grading on a real run)
    - When no judge model is configured on a path that will judge, the guard aborts before judging
    - A distinct judge/producer configuration proceeds normally (no behavior change for valid configs)
    - The guard is the SAME `_validate_judges` from bench.judge (imported, not duplicated — D-06)
    - The producer model passed to the guard is the one the caller already knows (the pipeline/producer model; sourcing it is discretion — a --producer-model arg or the known pipeline default)
  </behavior>
  <action>
    In `bench/compound_eval.py`, import `_validate_judges` from `bench.judge` and call it on the real judge path — immediately before the `judge_run(...)` loop in `_real_loop` (or at the point `do_judge` is confirmed true), passing the configured judge model(s) as a list and the producer model. Determine the producer-model source (discretion per CONTEXT: a new `--producer-model` arg mirroring 20-01's CLI, or the pipeline's known default model) and document the choice in a one-line comment. On guard failure, abort the judging path with a clear operator-facing message and a nonzero exit — this is operator/config error (fail-loud), NOT a runtime judging failure, so keep it OUT of any never-raise try/except. Do NOT alter the `do_judge` semantics for valid configs, and do NOT let the guard touch the mechanical scorecard path.

    In `bench/close_loop.py`, apply the same guard on its live judge path (the `replicate._run_trial` real-mode branch, before live judging begins), reusing the identical `_validate_judges` import. The deterministic/seeded (LLM-free) trajectory path does not judge a real producer and does not need the guard — gate the guard to the real/live path only.

    Add tests to a NEW file tests/test_bench_judge_independence.py asserting: (a) the real judge path aborts (raises or nonzero) when judge == producer; (b) it aborts when the judge set is empty/absent on a judging path; (c) a distinct config does not abort. Mock/monkeypatch to avoid real subprocess/LLM calls (follow the test_bench_compound.py monkeypatch conventions).
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_judge_independence.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_validate_judges" bench/compound_eval.py` returns a match
    - `grep -n "_validate_judges" bench/close_loop.py` returns a match
    - `grep -n "import" bench/compound_eval.py | grep -c "bench.judge"` is >= 1 (guard imported, not duplicated)
    - A test asserts the real judge path aborts when judge model == producer model
    - A test asserts a distinct judge/producer config does NOT abort
    - `uv run python -m pytest tests/test_bench_judge_independence.py tests/test_bench_compound.py tests/test_bench_close_loop.py -q --no-cov` exits 0
    - `uv run ruff check bench/compound_eval.py bench/close_loop.py` exits 0
  </acceptance_criteria>
  <done>Both real caller judge paths call the shared `_validate_judges` guard before judging, aborting fail-loud on same-model/absent configs while leaving valid configs and the mechanical scorecard untouched; caller tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IND-03 exclusion test — compounding_score stays deterministic, judge excluded under multi-judge</name>
  <files>tests/test_bench_judge_independence.py</files>
  <read_first>
    - bench/metrics.py lines 38-75 (RunSnapshot, Scorecard, compute_scorecard signature — no JudgeResult input) and 158+ (compute_scorecard body)
    - bench/report.py lines 142-195 (write_json judge block + "EXCLUDED from compounding_score" note)
    - bench/judge.py (the multi-judge aggregation from 20-01 — the "new multi-judge path" IND-03 names)
    - tests/test_bench_judge.py::test_write_json_includes_judge_when_present (the existing exclusion-note convention to extend)
  </read_first>
  <behavior>
    - compute_scorecard's result (including compounding_score) is byte-identical for a fixed RunSnapshot list regardless of any judge scores supplied elsewhere
    - Under the new multi-judge aggregation path, the judge output does NOT feed compute_scorecard (compounding_score unchanged whether judges score 0 or 10)
    - write_json emits the judge block with its "EXCLUDED from compounding_score" note when multi-judge results are present, and compounding_score in the same payload matches the scorecard-only value
  </behavior>
  <action>
    Add tests to tests/test_bench_judge_independence.py that make IND-03 executable: (1) build a fixed list of `RunSnapshot`s, compute `compute_scorecard(...)` twice and assert `compounding_score` (and the axis verdicts) are identical — proving determinism with no judge input; (2) drive the multi-judge aggregation from 20-01 with two different score sets (e.g. all-0 vs all-10) and assert the resulting `compounding_score` for the SAME snapshots is unchanged — proving the LLM judge (multi-judge path) is excluded from the mechanical score; (3) call `write_json` with multi-judge `JudgeResult`s present and assert the payload contains the "EXCLUDED from compounding_score" note AND that its `compounding_score` equals the scorecard-only value. Construct `RunSnapshot`/`JudgeResult` directly and avoid any subprocess/LLM call. Follow the exclusion-note convention already in tests/test_bench_judge.py.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_judge_independence.py -q --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - A test constructs a fixed RunSnapshot list and asserts `compute_scorecard(...).compounding_score` is identical across two calls (determinism)
    - A test asserts `compounding_score` is unchanged when judge scores differ (all-0 vs all-10) under the multi-judge path (judge excluded)
    - A test asserts `write_json` output contains the substring `EXCLUDED from compounding_score` when multi-judge results are present
    - `grep -n "compute_scorecard" tests/test_bench_judge_independence.py` returns a match
    - `uv run python -m pytest tests/test_bench_judge_independence.py -q --no-cov` exits 0
  </acceptance_criteria>
  <done>Tests assert compounding_score is deterministic from RunSnapshots alone and provably unaffected by single- or multi-judge scores, and that the report marks the judge EXCLUDED under the multi-judge path — IND-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> compound_eval/close_loop CLI | judge/producer model config crosses into a real judging run |
| caller -> bench.judge guard | the shared `_validate_judges` enforces independence before live LLM calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege (misconfiguration) | a real run judging its own producer's output | mitigate | callers invoke the shared `_validate_judges` guard before any live judge_run — same-model/absent configs abort fail-loud before LLM calls |
| T-20-05 | Repudiation / Info integrity | LLM judge silently contaminating the load-bearing metric | mitigate | IND-03 test asserts compounding_score is computed from RunSnapshots only and is invariant to judge scores; report keeps the EXCLUDED note under the multi-judge path |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | none — zero runtime dependencies added this phase (stdlib + flowstate only); no package-install tasks |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_bench_judge_independence.py tests/test_bench_compound.py tests/test_bench_close_loop.py -q --no-cov` exits 0
- `uv run ruff check bench/compound_eval.py bench/close_loop.py tests/test_bench_judge_independence.py` exits 0
- Full suite gate before ship: `uv run python -m pytest tests/ -q` passes the ≥80% flowstate coverage gate (no regression)
</verification>

<success_criteria>
- IND-01 (caller half): compound_eval.py and close_loop.py enforce the shared guard before judging a real run (D-06)
- IND-03: a test proves compounding_score stays deterministic from RunSnapshots and the LLM judge (single- and multi-judge) is excluded from it, with the report's EXCLUDED note intact
- No change to compute_scorecard / metrics.py authority; valid judge configs behave exactly as before
</success_criteria>

<output>
Create `.planning/phases/20-evaluator-independence/20-02-SUMMARY.md` when done
</output>
