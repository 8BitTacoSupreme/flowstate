---
phase: 04-integration-layered-cag-assembly-cache-lean-in
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/context_prefix.py
  - flowstate/orchestrator.py
  - flowstate/bridge.py
  - tests/test_context_prefix.py
  - tests/test_orchestrator.py
  - tests/test_bridge.py
autonomous: true
requirements: [CAG-01, CAG-02, CAG-03]

must_haves:
  truths:
    - "A single build_context_prefix() call returns an ordered string (fixtures → pack-or-omit → memory) threaded into all 3 adapters via the existing prior_knowledge seam — no adapter calls build_context_prefix() itself"
    - "When the pack pushes the prefix over budget, build_context_prefix() retries with run_pack(compress=True); if still over, it omits the pack and logs the decision (no silent truncation)"
    - "The Karpathy canon does NOT appear in build_context_prefix() output (it lives in the bridge system prompt from Phase 3 — re-emitting would double-inject)"
    - "Consecutive runs against an unchanged codebase produce a byte-identical prefix up to the memory section; the prefix is byte-identical across the 3 adapter constructor calls within one run (preserves the m9v cache property)"
    - "The token budget is a configurable .planning/config.json knob with a sensible default; the bridge optionally sets ENABLE_PROMPT_CACHING_1H and documents the most-stable-first cache behavior"
  artifacts:
    - path: "flowstate/context_prefix.py"
      provides: "build_context_prefix() assembler + fit/compress/omit ladder + budget config read"
      exports: ["build_context_prefix"]
      min_lines: 60
    - path: "flowstate/orchestrator.py"
      provides: "build_context_prefix() called once, result threaded as prior_knowledge into all 3 adapters"
      contains: "build_context_prefix"
    - path: "flowstate/bridge.py"
      provides: "ENABLE_PROMPT_CACHING_1H opt-in env + most-stable-first cache docstring on ClaudeBridge"
      contains: "ENABLE_PROMPT_CACHING_1H"
    - path: "tests/test_context_prefix.py"
      provides: "layer-order, fit→inline, over-budget→compress, still-over→omit+log, byte-identical-across-adapters, canon-absent coverage"
      contains: "def test_"
  key_links:
    - from: "flowstate/orchestrator.py"
      to: "flowstate/context_prefix.py::build_context_prefix"
      via: "single call replacing memory.get_context(_pk_query) at ~L238, result assigned to prior_knowledge"
      pattern: "build_context_prefix\\("
    - from: "flowstate/context_prefix.py"
      to: "flowstate/pack.py::run_pack"
      via: "compress retry on over-budget pack"
      pattern: "run_pack\\(.*compress=True"
    - from: "flowstate/context_prefix.py"
      to: "flowstate/memory.py::get_context"
      via: "memory is the last (most-dynamic) layer of the composed prefix"
      pattern: "get_context\\("
---

<objective>
Compose the Phase 3 ingredients into ONE ordered, cache-optimized context prefix
(`build_context_prefix()`), built once per run and threaded through the existing
`prior_knowledge` seam into all three adapters. Add the fit→compress→omit ladder for the
pack layer (CAG-02) and lean into Anthropic's implicit server-side prompt cache (CAG-03).

Purpose: This is the integration heart of v0.4.0 — it wires what Phase 3 built (pack, canon,
fixtures) into the live pipeline while preserving the m9v byte-identical-prefix property that
makes the cross-step prompt cache fire.

Output: `flowstate/context_prefix.py` (new assembler module), an orchestrator that calls it
once, a bridge that documents + opts into 1h caching, and `tests/test_context_prefix.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-layered-cag-assembly-cache-lean-in/04-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

From flowstate/memory.py:
```python
class MemoryStore:
    def get_context(self, query: str, *, max_tokens: int = 2000) -> str: ...
    # returns "" when no results; markdown begins with "## Prior Knowledge\n"
    # token estimate is char_budget = max_tokens * 4  (~4 chars/token)
```

From flowstate/pack.py:
```python
def run_pack(root: Path, *, compress: bool = False) -> PackResult: ...
def is_pack_stale(root: Path, state) -> bool: ...

@dataclass
class PackResult:
    success: bool
    output_path: Path | None = None
    exit_code: int = 0
    error: str | None = None
# pack artifact path: <root>/.planning/codebase/repomix-pack.xml
```

From flowstate/context.py:
```python
# fixture artifact path: <root>/.planning/fixtures/starter.json
# .planning/config.json is the GSD config; generate_gsd_config() seeds its defaults.
```

From flowstate/orchestrator.py (the prior_knowledge seam, ~L228-291):
```python
_pk_query = " ".join(filter(None, [answers.core_problem, answers.ten_x_vision, answers.research_focus])).strip()
prior_knowledge = memory.get_context(_pk_query) if _pk_query else ""   # ~L238 — THIS is what build_context_prefix replaces
# threaded into ResearchAdapter(...), StrategyAdapter(...), GSDAdapter(...) via prior_knowledge=prior_knowledge
```

From flowstate/bridge.py:
```python
# CANON lives in the SYSTEM prompt (run() prepends it when config.inject_canon).
# build_context_prefix() builds the USER-prompt-side context — canon must NOT appear here.
class BridgeConfig:
    inject_canon: bool = True
# run() builds env = {**os.environ}; env.pop("CLAUDECODE", None) before subprocess.run.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create build_context_prefix() assembler with fit→compress→omit ladder</name>
  <read_first>
    - flowstate/context_prefix.py (new — does not yet exist; this task creates it)
    - flowstate/memory.py (get_context signature + the ~4-chars/token estimate in get_context)
    - flowstate/pack.py (run_pack(compress=...), is_pack_stale, PackResult, pack path)
    - flowstate/context.py (fixture path .planning/fixtures/starter.json; .planning/config.json layout via generate_gsd_config)
    - flowstate/orchestrator.py (~L220-238 the _pk_query + memory.get_context seam this prefix replaces)
    - .planning/phases/04-integration-layered-cag-assembly-cache-lean-in/04-CONTEXT.md (LOCKED layer order + "do NOT include canon" rule)
  </read_first>
  <behavior>
    - Layer order in returned string: fixtures → pack (if it fits) → memory, joined with the existing "\n\n---\n\n" separator. Each layer clearly delimited (fixtures under a "## Eval Fixtures" heading, pack as its own block, memory keeps its own "## Prior Knowledge" heading from get_context).
    - Canon-absent: the returned string never contains the bridge CANON text (assert a stable canon marker substring, e.g. "Behavioral guidelines to reduce common LLM coding mistakes", is NOT present).
    - Fit ladder (pack layer only): estimate prefix tokens via the same ~4-chars/token rule as memory.py. (1) fixtures+pack+memory under budget → inline full pack. (2) over budget → call run_pack(root, compress=True), re-read pack, retry. (3) still over → omit pack entirely; the returned string contains fixtures + memory only.
    - No silent truncation: every compress and every omit decision is logged via a Rich console (assert the log line is emitted — capture console output or inject a logger — and that it names what was dropped/compressed).
    - Budget source: read the budget from .planning/config.json (a new "context_prefix_budget_tokens" key) with a sensible default when the key/file is absent (default ~12000 tokens; document the default in the function docstring).
    - Determinism: given identical fixtures + pack + memory inputs, two calls return byte-identical strings (the function performs no timestamping / no nondeterministic ordering).
    - Missing-artifact tolerance: absent fixture file → fixtures layer omitted (no raise); absent pack → pack layer omitted (no raise); empty memory get_context("") → memory layer omitted.
  </behavior>
  <action>
    Create flowstate/context_prefix.py with a single public `build_context_prefix(root, memory, query, *, budget_tokens=None, console=None) -> str` plus small private helpers (`_read_fixtures_layer`, `_read_pack_layer`, `_estimate_tokens`, `_load_budget`). Compose layers in the LOCKED order fixtures → pack → memory using the existing `"\n\n---\n\n"` separator. Reuse the ~4-chars/token estimate from memory.py (do not import a private; replicate the `len(text) // 4` approximation in `_estimate_tokens`). For the PACK layer implement the three-rung ladder exactly per 04-CONTEXT.md: inline if `_estimate_tokens(fixtures + pack + memory) < budget`; else `run_pack(root, compress=True)` and re-read the pack at `.planning/codebase/repomix-pack.xml` then re-check; else omit the pack. Log each compress/omit via the passed-in Rich `console` (default to a module-level `Console()` when None) — message must state the action and the byte/token size dropped. Read the budget from `.planning/config.json` key `context_prefix_budget_tokens`, falling back to a documented default (~12000) when missing or unparseable. CRITICAL: do NOT emit the bridge CANON in this output — canon ships in the system prompt (Phase 3). Do NOT import CANON, inject_canon, or anything else from flowstate.bridge into context_prefix.py — canon injection is the bridge's exclusive responsibility. The fixtures layer reads `.planning/fixtures/starter.json`, renders it under a `## Eval Fixtures` heading (compact JSON or a few key lines — keep it deterministic); tolerate a missing file by omitting the layer. The memory layer is `memory.get_context(query)` appended last; omit when it returns "". No new Python runtime deps; ruff line-length 100, double quotes, snake_case.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_context_prefix.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `from flowstate.context_prefix import build_context_prefix` imports cleanly.
    - Test asserts layer order: fixtures heading index < pack block index < "## Prior Knowledge" index in the returned string when all three present.
    - Test asserts the canon marker substring is NOT in the output.
    - Test (fit→inline): small pack + generous budget → output contains the pack content, run_pack(compress=True) NOT called (monkeypatch run_pack to record calls).
    - Test (over→compress): oversized pack + tight budget → run_pack called once with compress=True (monkeypatch a fake repomix-free run_pack that writes a smaller pack); compressed pack content present.
    - Test (still-over→omit+log): pack still over budget after compress → output excludes pack, includes fixtures+memory, and the captured Rich console output contains an omit log naming the dropped size.
    - Test (determinism): two calls with identical inputs return byte-identical strings.
    - Test (missing artifacts): no fixture file / no pack file / empty memory each omit their layer without raising.
    - `ruff check flowstate/context_prefix.py tests/test_context_prefix.py` passes.
  </acceptance_criteria>
  <done>
    build_context_prefix() exists with the fixtures→pack→memory order, the three-rung fit ladder, logged (never silent) compress/omit decisions, configurable budget, canon excluded, and full unit coverage in tests/test_context_prefix.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread build_context_prefix() through the orchestrator seam once per run</name>
  <read_first>
    - flowstate/orchestrator.py (~L162-296: run_pipeline, the _pk_query build + memory.get_context at ~L238, and the 3 adapter constructions at ~L241/262/285)
    - flowstate/context_prefix.py (the build_context_prefix signature created in Task 1)
    - flowstate/tools/base.py (ToolAdapter.__init__ prior_knowledge param ~L33-43)
    - flowstate/tools/research.py (~L91-96 prior prepend), flowstate/tools/strategy.py (~L95-97 prior prepend)
    - tests/test_orchestrator.py, tests/test_orchestrator_extended.py (existing wiring + prior_knowledge assertions)
    - .planning/phases/04-integration-layered-cag-assembly-cache-lean-in/04-CONTEXT.md (CAG-01: built ONCE, no adapter calls it independently)
  </read_first>
  <action>
    In run_pipeline, replace the line `prior_knowledge = memory.get_context(_pk_query) if _pk_query else ""` (~L238) with a single `prior_knowledge = build_context_prefix(root, memory, _pk_query, console=console)` call. Keep the existing `_pk_query` construction unchanged. Add the import `from flowstate.context_prefix import build_context_prefix`. The same `prior_knowledge` string must continue to be passed via `prior_knowledge=prior_knowledge` into ResearchAdapter, StrategyAdapter, and GSDAdapter — DO NOT add per-adapter prefix construction (CAG-01: built once, threaded uniformly). Update the seam comment block to reflect that the prior_knowledge block is now the full layered CAG prefix (fixtures → pack → memory), not just memory.get_context. Do not change adapter signatures or the prepend logic in research.py/strategy.py.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_orchestrator.py tests/test_orchestrator_extended.py tests/test_context_prefix.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "build_context_prefix" flowstate/orchestrator.py` shows exactly one call site (built once).
    - `grep -nv '^#' flowstate/orchestrator.py | grep -c "build_context_prefix"` is 2 (import + single call) — no adapter constructs it.
    - A test asserts the SAME prefix object/string is passed to all three adapters (e.g. capture the prior_knowledge each adapter receives and assert byte-identical across Research/Strategy/GSD).
    - Existing test_orchestrator.py / test_orchestrator_extended.py pass unchanged in intent (update only assertions that depended on the old bare get_context call, if any).
    - `ruff check flowstate/orchestrator.py` passes.
  </acceptance_criteria>
  <done>
    The orchestrator builds the layered prefix exactly once and threads the byte-identical string into all three adapters via the existing prior_knowledge seam; no adapter builds its own prefix.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cache lean-in — ENABLE_PROMPT_CACHING_1H opt-in + most-stable-first docs on ClaudeBridge</name>
  <read_first>
    - flowstate/bridge.py (module docstring ~L11-23 prompt-cache notes; ClaudeBridge class ~L153; run() env assembly ~L236-238 where CLAUDECODE is popped)
    - tests/test_bridge.py (existing env/argv assertion patterns to mirror)
    - .planning/phases/04-integration-layered-cag-assembly-cache-lean-in/04-CONTEXT.md (CAG-03: optional ENABLE_PROMPT_CACHING_1H, document cache behavior + layer ordering)
  </read_first>
  <action>
    Add an opt-in: when a BridgeConfig flag (e.g. `enable_prompt_caching_1h: bool = False`) is set, run() sets `env["ENABLE_PROMPT_CACHING_1H"] = "1"` alongside the existing `env.pop("CLAUDECODE", None)` block — do NOT set it unconditionally (1h caching is an API-key-tier feature). Extend the ClaudeBridge docstring (or the existing module-level prompt-cache docstring) to document the most-stable-first layer ordering of the full CAG stack: system-prompt canon (outermost, Phase 3) → user-prompt [fixtures → pack → memory → step prompt] (Phase 4), and explain that the byte-identical prefix across pipeline steps is what makes the implicit server-side cache hit. Keep the default behavior unchanged (flag defaults False). No new runtime deps.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_bridge.py -q && grep -q "ENABLE_PROMPT_CACHING_1H" flowstate/bridge.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ENABLE_PROMPT_CACHING_1H" flowstate/bridge.py` shows the env set guarded by the new config flag.
    - A test asserts: flag False (default) → ENABLE_PROMPT_CACHING_1H NOT in the subprocess env; flag True → present and equal to "1". (Mirror the env-capture pattern already in test_bridge.py.)
    - The ClaudeBridge docstring contains the most-stable-first ordering description (canon → fixtures → pack → memory → step prompt).
    - Default invocations are byte-identical in argv/env to pre-change behavior (no regression) — existing test_bridge.py passes.
    - `ruff check flowstate/bridge.py` passes.
  </acceptance_criteria>
  <done>
    ClaudeBridge optionally sets ENABLE_PROMPT_CACHING_1H behind a default-False config flag and documents the most-stable-first cache ordering; default behavior unchanged.
  </done>
</task>

</tasks>

<verification>
- `cd /Users/jhogan/frameworx && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` passes (coverage gate held).
- `ruff check flowstate/ tests/` and `ruff format --check flowstate/ tests/` pass.
- `grep -c "build_context_prefix" flowstate/orchestrator.py` confirms a single call site (built once).
- The canon marker substring does NOT appear in build_context_prefix output (no double-injection).
- Compress and omit decisions are logged (no silent truncation) — asserted via captured Rich console output in test_context_prefix.py.
</verification>

<success_criteria>
- CAG-01: one build_context_prefix() composing fixtures → pack(if-fits) → memory, built once, threaded into all 3 adapters via the prior_knowledge seam; no adapter builds its own.
- CAG-02: over-budget pack → run_pack(compress=True) retry → still-over → omit + log; budget configurable via .planning/config.json; no silent truncation.
- CAG-03: most-stable-first ordering documented on ClaudeBridge; ENABLE_PROMPT_CACHING_1H optional opt-in; prefix byte-identical across the 3 adapter calls within a run.
- Coverage ≥80%, ruff clean, repomix not required on PATH (compress-path tests monkeypatch run_pack / a fake repomix binary).
</success_criteria>

<output>
Create `.planning/phases/04-integration-layered-cag-assembly-cache-lean-in/04-01-SUMMARY.md` when done.
</output>
