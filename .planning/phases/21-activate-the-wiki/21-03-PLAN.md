---
phase: 21-activate-the-wiki
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - tests/test_wiki_dogfood.py
autonomous: true
requirements: [WIKI-06]
must_haves:
  truths:
    - "D-08: a dogfood integration test runs the promoted distiller against THIS project's real memory.db, calls build_context_prefix with the standard-union-plus-wiki include_layers, and asserts the wiki layer DEMONSTRABLY FIRES (corpus globbed + top-k article content present in the assembled prefix), run green"
    - "D-08 acceptance = 'the layer fires', NOT 'quality improved'"
    - "D-08 graceful skip: if [semantic] absent, assert firing via whichever path is available (static _read_wiki_layer); if neither semantic nor any corpus can be produced, skip with an explicit reason rather than fail; mark slow/integration if it makes a real claude/embedder call"
  artifacts:
    - path: "tests/test_wiki_dogfood.py"
      provides: "Dogfood smoke-test proving the wiki layer fires in production wiring"
      contains: "def test_"
  key_links:
    - from: "tests/test_wiki_dogfood.py"
      to: "flowstate.distiller.main + flowstate.context_prefix.build_context_prefix"
      via: "distill real memory.db -> build prefix with _STANDARD_LAYERS | {wiki} -> assert article content in prefix"
      pattern: "build_context_prefix|include_layers"
---

<objective>
Prove the wiki layer actually fires end-to-end on FlowState's own data. This is WIKI-06: a dogfood smoke-test that distills this project's real memory.db into the corpus, assembles the context prefix with the wiki union, and asserts the top-k articles land in the prefix — with a green run. Acceptance is "the layer fires," not a quality claim (that is Phase 22).

Purpose: The whole phase exists to move the wiki layer from dormant to firing in production. This test is the phase's proof of that, and a regression guard against the layer silently going dark again.

Output: `tests/test_wiki_dogfood.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-activate-the-wiki/21-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. Executor: use these directly. -->

Producer (Plan 01): `from flowstate.distiller import main, _WIKI_CORPUS_REL` — main(["--root", <root>, "--force"]) writes .planning/codebase/wiki/NN-*.md; rc=1 on empty memory.

Consumer (Plan 02): `from flowstate.context_prefix import build_context_prefix, _STANDARD_LAYERS`
  build_context_prefix(root, memory, query, *, include_layers=_STANDARD_LAYERS | {"wiki"}, console=None) -> str
  Wiki content in the prefix is headed by "## Codebase Wiki" (see _semantic_wiki_layer line 302 and _read_wiki_layer line 433).

Memory: `from flowstate.memory import MemoryStore` — the real store lives at <project_root>/memory.db. Distiller article kinds: DECISION, INSIGHT, RESEARCH, STRATEGY, RUN.

Embedder gate: `from flowstate.context_prefix import get_embedder`; get_embedder(root).available() is False when the `[semantic]` extra / fastembed is not installed.

Repo root for the dogfood run: the actual FlowState checkout (the directory containing memory.db). Resolve it from the test file location (parents up to the repo root) — do NOT hardcode an absolute path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dogfood smoke-test that the wiki layer fires (WIKI-06)</name>
  <files>tests/test_wiki_dogfood.py</files>
  <read_first>
    - /Users/jhogan/frameworx/flowstate/distiller.py (main, _WIKI_CORPUS_REL — Plan 01)
    - /Users/jhogan/frameworx/flowstate/context_prefix.py (build_context_prefix, _STANDARD_LAYERS, _semantic_wiki_layer 224-302, _read_wiki_layer 419-434, get_embedder)
    - /Users/jhogan/frameworx/flowstate/memory.py (MemoryStore, MemoryKind — the real memory.db source)
    - /Users/jhogan/frameworx/tests/test_context_prefix.py (fixture/corpus + build_context_prefix usage conventions)
    - /Users/jhogan/frameworx/pyproject.toml (pytest markers — confirm how `slow`/`integration` markers are declared, add one if the marker is not registered)
  </read_first>
  <action>
    Add tests/test_wiki_dogfood.py implementing D-08. Resolve the real project root from the test file path (parents until the dir containing memory.db); if memory.db is absent, `pytest.skip` with an explicit reason. Step 1: run `flowstate.distiller.main(["--root", str(root), "--force"])` against the real memory.db; if it returns nonzero (empty/undistillable memory) `pytest.skip("no distillable corpus")`. Step 2: assert the corpus globbed — `list((root / _WIKI_CORPUS_REL).glob("*.md"))` is non-empty. Step 3: open the real MemoryStore(root), build a query from real content (e.g. a decision/insight summary substring pulled from the store, or a broad fixed query), and call `build_context_prefix(root, store, query, include_layers=_STANDARD_LAYERS | {"wiki"})`. Step 4: assert the wiki layer FIRED — the prefix contains the "## Codebase Wiki" heading AND at least one substring drawn from a written article file (glob a corpus file, take a distinctive line, assert it appears in the prefix). Degrade per D-08: if `get_embedder(root).available()` is False, the semantic path is skipped and firing is asserted via the static `_read_wiki_layer` path instead — so the assertion must accept firing through EITHER path (heading + some article content present). Acceptance is firing, not quality — do NOT assert any score. Mark the test with a registered `@pytest.mark.integration` (and `slow` if it can trigger a real embedder/claude call), and ensure it never makes a real `claude --print` call (do not pass --llm). Guard against writing over a developer's working corpus: run the distill against a COPY of the repo — copy memory.db (and any existing .planning/codebase/wiki) into a tmp_path root, or otherwise isolate — so the dogfood never mutates the live checkout's committed corpus. Keep the test green and self-cleaning.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && uv run python -m pytest tests/test_wiki_dogfood.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_wiki_dogfood.py` exists and defines at least one `def test_` function marked `@pytest.mark.integration`
    - The test distills into an ISOLATED root (copied memory.db), never mutating the live `.planning/codebase/wiki` of the checkout
    - When a corpus is produced, the test asserts the assembled prefix contains `## Codebase Wiki` AND at least one distinctive substring from a written article (proves globbed + injected)
    - The test asserts NO quality/score metric (acceptance = "layer fires")
    - When memory.db is absent or yields no distillable corpus, the test `skip`s with an explicit reason rather than failing
    - When `[semantic]` is absent, firing is asserted via the static path (test still passes, no crash)
    - `uv run python -m pytest tests/test_wiki_dogfood.py -q` exits 0 (pass or skip, never error)
  </acceptance_criteria>
  <done>The dogfood test demonstrably proves the wiki layer fires on real FlowState memory via the production union wiring, degrading to skip/static as needed, and guards the layer against silently going dormant again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> real memory.db | the dogfood test reads the checkout's real memory.db |
| test -> local FS | distills a corpus; must not corrupt the live committed corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | dogfood distill overwriting the live wiki corpus | mitigate | run against an isolated/copied root (tmp_path), never mutate the checked-in `.planning/codebase/wiki` |
| T-21-08 | Denial of Service | test making a real network/claude call in CI | mitigate | never pass `--llm`; mark integration/slow; the deterministic distiller path spawns no subprocess |
| T-21-09 | Denial of Service | absent memory.db / no corpus failing the suite | accept | explicit `pytest.skip` with reason on absence; never a hard failure |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase; test-only addition |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_wiki_dogfood.py -q` exits 0 (green or skipped, never errored)
- `uv run ruff check tests/test_wiki_dogfood.py` clean
- `git status .planning/codebase/wiki` shows no unintended mutation to the live corpus after the test run
</verification>

<success_criteria>
- WIKI-06: a dogfood smoke-test proves the wiki layer demonstrably fires (corpus globbed + top-k injected) on this project's real memory.db, run green, with graceful skip/static degradation — acceptance is "the layer fires," not "quality improved."
</success_criteria>

<output>
Create `.planning/phases/21-activate-the-wiki/21-03-SUMMARY.md` when done
</output>
