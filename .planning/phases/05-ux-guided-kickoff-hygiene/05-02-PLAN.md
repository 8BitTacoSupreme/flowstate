---
phase: 05-ux-guided-kickoff-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260525-m9v-unify-memory-injection-at-orchestrator-b/260525-m9v-SUMMARY.md
  - .planning/quick/260525-o6h-spike-confirm-claude-print-server-side-p/260525-o6h-SUMMARY.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [DX-01]
must_haves:
  truths:
    - "Both existing quick-task SUMMARY files carry a frontmatter status: complete field"
    - "audit-open reports quick_tasks count == 0 (no false-positive in-flight flags)"
    - "The status: convention (complete/verified/blocked/paused/drafted) is documented for future quick tasks"
  artifacts:
    - path: ".planning/quick/260525-m9v-unify-memory-injection-at-orchestrator-b/260525-m9v-SUMMARY.md"
      provides: "status: complete added to existing YAML frontmatter"
      contains: "status: complete"
    - path: ".planning/quick/260525-o6h-spike-confirm-claude-print-server-side-p/260525-o6h-SUMMARY.md"
      provides: "Minimal valid YAML frontmatter block prepended with status: complete"
      contains: "status: complete"
    - path: ".claude/CLAUDE.md"
      provides: "Documented status: frontmatter convention for quick-task / phase SUMMARYs"
      contains: "status:"
  key_links:
    - from: "quick SUMMARY frontmatter status field"
      to: "audit-open scanner (audit.cjs treats status==complete as terminal)"
      via: "frontmatter status key read by the milestone-close audit"
      pattern: "status: complete"
---

<objective>
Standardize a `status:` SUMMARY-frontmatter convention and backfill the two existing quick-task
summaries so the milestone-close audit (`gsd-sdk query audit-open`) stops false-flagging shipped
work as in-flight.

The audit (`audit.cjs`) scans `.planning/quick/*/`-SUMMARY.md and treats a quick task as complete
ONLY when its frontmatter `status` is `complete` (or `resolved`). Both existing summaries currently
report `[missing]` — m9v HAS frontmatter but no `status:` key; o6h has NO frontmatter at all. This
plan adds the field to both and documents the convention.

Purpose: clean milestone close — `quick_tasks` open-count drops from 2 to 0.
Output: two backfilled SUMMARY files + a short convention note. DOCS-ONLY — no Python source touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-ux-guided-kickoff-hygiene/05-CONTEXT.md

<audit_contract>
Audit contract (from $HOME/.claude/get-shit-done/bin/lib/audit.cjs, quick-task scan ~L78-156):
- A quick task is "open" unless its SUMMARY frontmatter `status` equals `complete`.
- Allowed status vocabulary for this convention: complete / verified / blocked / paused / drafted.
- Only `complete` (and `resolved`) are terminal for the quick-task scan.
- Frontmatter is parsed as a leading YAML block delimited by triple-dash fences.

m9v SUMMARY current head (HAS frontmatter — add the field inside the existing block): the file
opens with a triple-dash fence then `phase: quick-260525-m9v`, `plan: 01`, `subsystem: orchestrator`,
`tags: [...]`, closed by a triple-dash fence.

o6h SUMMARY current head (NO frontmatter — body starts immediately; prepend a block): the file's
first line is the `# 260525-o6h — SUMMARY` heading, then a `**Task:** Spike: ...` line.
</audit_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill status: complete into both quick-task SUMMARY files</name>
  <files>.planning/quick/260525-m9v-unify-memory-injection-at-orchestrator-b/260525-m9v-SUMMARY.md, .planning/quick/260525-o6h-spike-confirm-claude-print-server-side-p/260525-o6h-SUMMARY.md</files>
  <read_first>
    - .planning/quick/260525-m9v-unify-memory-injection-at-orchestrator-b/260525-m9v-SUMMARY.md (existing frontmatter)
    - .planning/quick/260525-o6h-spike-confirm-claude-print-server-side-p/260525-o6h-SUMMARY.md (no frontmatter)
    - $HOME/.claude/get-shit-done/bin/lib/audit.cjs (quick-task scan, to confirm `status: complete` is the terminal token)
  </read_first>
  <action>
    For m9v: add a single `status: complete` line to the EXISTING YAML frontmatter block (insert it
    near the top, e.g. directly under `plan: 01`). Do not reorder or rewrite other keys; do not touch
    the body. For o6h: PREPEND a minimal valid YAML frontmatter block (a triple-dash fence, then
    `status: complete`, `phase: quick-260525-o6h`, `plan: 01`, then a closing triple-dash fence)
    before the current first line (the `# 260525-o6h — SUMMARY` heading). Leave the existing markdown
    body intact below the new block. Use plain `status: complete` (lowercase, no quotes) so it matches
    the audit's terminal token exactly.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python3 scripts/_dx01_verify.py 2>/dev/null || gsd-sdk query audit-open --json | python3 -c "import sys,json; d=json.load(sys.stdin); print('quick_tasks=', d['counts']['quick_tasks']); sys.exit(0 if d['counts']['quick_tasks']==0 else 1)"</automated>
  </verify>
  <done>Both SUMMARY files contain a frontmatter `status: complete` line; `gsd-sdk query audit-open` reports `counts.quick_tasks == 0`. Existing summary bodies are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document the status: SUMMARY-frontmatter convention</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (FlowState's own project doc — where the convention note lands)
    - .planning/phases/05-ux-guided-kickoff-hygiene/05-CONTEXT.md (DX-01 locked decision: document the convention)
  </read_first>
  <action>
    Add a short subsection to `.claude/CLAUDE.md` (a surgical append — do NOT restructure the file)
    documenting the SUMMARY-frontmatter `status:` convention: every quick-task and phase SUMMARY
    should carry a `status:` frontmatter field whose value is one of `complete`, `verified`,
    `blocked`, `paused`, or `drafted`; `complete` (and `resolved`) are what `gsd-sdk query audit-open`
    treats as terminal at milestone close, so shipped work must be marked `complete` to avoid
    false-positive in-flight flags. Note this is the local FlowState convention (there is no official
    GSD quick-task frontmatter template). Keep it to a few lines under a clear heading; do not add a
    Python dependency or touch source.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -q 'status:' .claude/CLAUDE.md && grep -Eq 'verified|drafted|paused' .claude/CLAUDE.md && echo "convention documented"</automated>
  </verify>
  <done>`.claude/CLAUDE.md` contains a short section naming the `status:` field and its allowed values, explaining the audit-open terminal-status behavior. No source files touched.</done>
</task>

</tasks>

<verification>
- `gsd-sdk query audit-open --json` reports `counts.quick_tasks == 0` (was 2).
- `grep -l 'status: complete' .planning/quick/*/*-SUMMARY.md` lists BOTH quick-task summaries.
- `.claude/CLAUDE.md` documents the convention (allowed values + audit behavior).
- No files under `flowstate/` or `tests/` are modified by this plan.
</verification>

<success_criteria>
- DX-01: both existing quick-task SUMMARY files carry a `status:` field; `audit-open` no longer
  false-flags them as in-flight (quick_tasks count == 0); the convention is documented for future
  summaries. Docs-only change — no Python source, no new dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-ux-guided-kickoff-hygiene/05-02-SUMMARY.md` when done.
</output>
