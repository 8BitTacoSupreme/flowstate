---
phase: 06-run-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/memory.py
  - flowstate/journal.py
  - flowstate/orchestrator.py
  - tests/test_memory.py
  - tests/test_journal.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "After a pipeline run, exactly one MemoryKind.RUN entry exists for that run_id in memory.db"
    - "A .planning/RUNLOG.md section is appended for the run (newest-at-bottom) with steps, artifacts changed, decisions, gotchas, and a one-line delta"
    - "The first run records a full snapshot with delta noting 'first run'; later runs record a delta diffed against the prior RUN entry's stored snapshot"
    - "Dry-run still writes a RUN entry and RUNLOG section, both tagged/marked dry_run"
    - "Calling append_run_entry twice for the same run_id does not create a second entry (idempotent)"
    - "Journal generation involves no bridge/LLM call"
  artifacts:
    - path: "flowstate/journal.py"
      provides: "append_run_entry() pure-Python journal writer"
      contains: "def append_run_entry"
    - path: "flowstate/memory.py"
      provides: "MemoryKind.RUN enum member"
      contains: "RUN = \"run\""
    - path: "flowstate/orchestrator.py"
      provides: "append_run_entry call before memory.close()"
      contains: "append_run_entry"
  key_links:
    - from: "flowstate/orchestrator.py"
      to: "flowstate.journal.append_run_entry"
      via: "call before memory.close() with run_id and dry_run"
      pattern: "append_run_entry\\(memory, state, run_id"
    - from: "flowstate/journal.py"
      to: "memory.get_by_kind(MemoryKind.RUN)"
      via: "prior-entry fetch for delta + idempotency guard"
      pattern: "get_by_kind\\(MemoryKind.RUN"
---

<objective>
Add the run-journal substrate: a new `MemoryKind.RUN`, a pure-Python `flowstate/journal.py::append_run_entry()` that writes exactly one delta-only journal entry per run (mirrored to `.planning/RUNLOG.md`), and the orchestrator wiring that calls it at the end of every pipeline run. (RUN-01)

Purpose: Each pipeline run leaves an append-only, delta-only trail the next run reads first — the foundation of the v0.5.0 compounding loop. Plans 02 and 03 consume the `MemoryKind.RUN` entries this plan produces.

Output: `flowstate/journal.py` (new), `MemoryKind.RUN` in `memory.py`, an `append_run_entry` call in `run_pipeline()`, and tests keeping coverage ≥80%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-run-journal/06-CONTEXT.md
@.planning/phases/06-run-journal/06-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

From flowstate/memory.py (MemoryKind StrEnum, L70-75):
- members today: RESEARCH, STRATEGY, DECISION, TOOL_RUN, INSIGHT
- add: RUN = "run"

From flowstate/memory.py:
- MemoryEntry.create(kind, content, summary, *, source="", tags=None, metadata=None, run_id="") -> MemoryEntry
- MemoryStore.add(entry: MemoryEntry) -> str
- MemoryStore.get_by_kind(kind: MemoryKind, *, limit: int = 20) -> list[MemoryEntry]  (ORDER BY created_at DESC)
- MemoryStore.count(kind: MemoryKind | None = None) -> int
- MemoryEntry fields: id, kind, content, summary, source, tags, metadata, created_at, run_id

From flowstate/state.py:
- InstallEntry: path: str, owner: str, kind: Literal["config","context","memory","research","artifact","pack","fixture"], created_at, checksum: str | None = None
- FlowStateModel.install_manifest: list[InstallEntry]
- FlowStateModel.tools: dict[str, ToolState]; ToolState.status: ToolStatus
- step/tool names present in state.tools: "research", "strategy", "gsd", "discipline"

From flowstate/orchestrator.py:
- run_id = uuid4().hex[:12] (L175)
- run_pipeline(state, root); dry_run = state.preferences.dry_run (L164)
- call-site: insert append_run_entry between save_state(state, root) (L310) and memory.close() (L312)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MemoryKind.RUN and extend memory tests</name>
  <files>flowstate/memory.py, tests/test_memory.py</files>
  <read_first>
    - flowstate/memory.py (MemoryKind enum L70-75; the StrEnum to extend)
    - tests/test_memory.py (store fixture L12-15; existing kind/add/get_by_kind coverage to mirror)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "add `RUN = \"run\"` to MemoryKind")
  </read_first>
  <action>
    Append `RUN = "run"` as the last member of the `MemoryKind` StrEnum in flowstate/memory.py (after `INSIGHT`). No other change to memory.py — `add`, `get_by_kind`, and `count` already accept any MemoryKind.

    Extend tests/test_memory.py using the existing `store` fixture: add `test_run_kind_value` asserting `MemoryKind.RUN == "run"`; add `test_count_includes_run` asserting `store.count(MemoryKind.RUN) == 0` on a fresh store; add `test_add_run_entry_and_get_by_kind` that adds a MemoryEntry.create(MemoryKind.RUN, ...) and asserts get_by_kind(MemoryKind.RUN, limit=1) returns it.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_memory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' flowstate/memory.py | grep -c 'RUN = "run"'` returns 1
    - `python -c "from flowstate.memory import MemoryKind; assert MemoryKind.RUN == 'run'"` exits 0
    - `python -m pytest tests/test_memory.py -q` exits 0
  </acceptance_criteria>
  <done>MemoryKind.RUN exists and is exercised by passing tests in tests/test_memory.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create flowstate/journal.py::append_run_entry (RUN-01)</name>
  <files>flowstate/journal.py, tests/test_journal.py</files>
  <read_first>
    - flowstate/memory.py (MemoryEntry.create L90-111; MemoryStore.add L152-169; get_by_kind L248-253)
    - flowstate/state.py (InstallEntry L47-54; FlowStateModel.install_manifest L82; ToolState.status L20-21)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "flowstate/journal.py (service, CRUD)" — signature, idempotency guard, RUNLOG append patterns)
    - flowstate/context_prefix.py (L74-80 try/except non-fatal idiom to mirror for the RUNLOG write)
  </read_first>
  <behavior>
    - Idempotency: calling append_run_entry twice with the same run_id writes only ONE MemoryKind.RUN entry (guard via get_by_kind(MemoryKind.RUN, limit=...) checking e.run_id == run_id).
    - First run (no prior RUN entry): metadata holds a full {path: checksum} snapshot from install_manifest (excluding memory.db where checksum is None); delta_line contains "first run".
    - Subsequent run: delta computed by diffing current install_manifest checksum snapshot against the prior RUN entry's stored snapshot in metadata; changed/added paths appear in the artifacts-changed field.
    - Per-step status captured from state.tools["research"/"strategy"/"gsd"/"discipline"].status (skip tools absent from the dict).
    - metadata includes forward-compat empty slots: decisions=[], gotchas=[] (Phase 7/8 will populate).
    - Dry-run: entry gets tag "dry_run" and metadata dry_run=True; RUNLOG section notes dry_run.
    - timestamp seam: append_run_entry(..., timestamp=fixed) uses the passed datetime; default datetime.now(UTC). created_at on the MemoryEntry uses this timestamp.
    - RUNLOG.md: appended (newest-at-bottom) as "## <ISO ts> — run <id>" plus bullets for steps, artifacts changed, decisions, gotchas, delta.
    - Never raises: a RUNLOG write failure is swallowed (try/except) so the pipeline is unaffected; the memory entry is the canonical write.
  </behavior>
  <action>
    Create flowstate/journal.py with header `from __future__ import annotations`. Imports: stdlib (json, datetime UTC/datetime, pathlib.Path, typing.Any) + `from flowstate.memory import MemoryEntry, MemoryKind, MemoryStore` + `from flowstate.state import FlowStateModel`. NO import of flowstate.bridge anywhere.

    Implement `append_run_entry(memory: MemoryStore, state: FlowStateModel, run_id: str, *, root: Path, dry_run: bool = False, timestamp: datetime | None = None) -> None` per the signature in PATTERNS.md.

    Order of operations: (1) idempotency guard — fetch get_by_kind(MemoryKind.RUN, limit=50); if any e.run_id == run_id, return immediately. (2) Fetch prior RUN entry via the same list (first element = newest) for delta. (3) Build current checksum snapshot dict {entry.path: entry.checksum for entry in state.install_manifest if entry.checksum is not None} — this excludes memory.db (checksum=None). (4) Diff vs prior entry's metadata snapshot to produce the artifacts-changed list and the one-line delta string (e.g. "research+strategy re-ran; ROADMAP.md changed; N new memories" — keep short; first run → "first run"). (5) Build per-step status map from state.tools. (6) metadata dict carries: run_id, snapshot (current {path: checksum}), steps (status map), artifacts_changed (list), decisions ([]), gotchas ([]), delta_line, dry_run (bool). (7) Build a concise human summary and content string. (8) memory.add(MemoryEntry.create(MemoryKind.RUN, content, summary, source="journal", tags=["run"] + (["dry_run"] if dry_run else []), metadata=metadata, run_id=run_id)) — set created_at via the resolved timestamp (construct MemoryEntry directly if create() does not accept created_at, matching the dataclass field). (9) Mirror to RUNLOG.md via Path.open("a") inside a try/except that swallows write errors. Build the runlog path as `root / ".planning" / "RUNLOG.md"` and mkdir parents — do NOT accept an arbitrary path argument (path stays inside project root by construction).

    Create tests/test_journal.py covering all behaviors above using a real MemoryStore(root=tmp_path) and a FlowStateModel() with install_manifest seeded. Pin timestamp for determinism. Use one test per behavior bullet: first-run snapshot, second-run delta, idempotency (call twice → count == 1), dry_run tagging, RUNLOG.md content, and no-raise on RUNLOG write failure (e.g. monkeypatch Path.open to raise, assert the memory entry still landed and no exception propagated).
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_journal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import.*bridge' flowstate/journal.py` returns 0
    - `grep -c 'def append_run_entry' flowstate/journal.py` returns 1
    - `python -m pytest tests/test_journal.py -q` exits 0
    - A test asserts that two append_run_entry calls with the same run_id leave `store.count(MemoryKind.RUN) == 1`
    - A test asserts a dry_run entry carries the "dry_run" tag and a RUNLOG.md section exists noting dry_run
  </acceptance_criteria>
  <done>append_run_entry writes exactly one MemoryKind.RUN entry per run_id with delta+snapshot metadata, mirrors to RUNLOG.md, never raises, and is fully tested.</done>
</task>

<task type="auto">
  <name>Task 3: Wire append_run_entry into run_pipeline (RUN-01)</name>
  <files>flowstate/orchestrator.py, tests/test_orchestrator.py</files>
  <read_first>
    - flowstate/orchestrator.py (run_pipeline L163-320; run_id L175; call-site between save_state L310 and memory.close L312; try/except non-fatal idiom around context gen L211-218)
    - tests/test_orchestrator.py (test_dry_run_pipeline L9-37 to mirror)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "orchestrator.py — call append_run_entry before memory.close()")
  </read_first>
  <action>
    In flowstate/orchestrator.py, insert the journal call AFTER the final `save_state(state, root)` (L310) and BEFORE `memory.close()` (L312). Import `append_run_entry` from flowstate.journal at module level (journal.py does not import orchestrator, so module-level is safe). Wrap the call in `try/except Exception` that logs a yellow non-fatal message via console (mirroring the L211-218 context-gen failure idiom) so a journal failure never aborts the pipeline. Pass `append_run_entry(memory, state, run_id, root=root, dry_run=dry_run)`.

    Extend tests/test_orchestrator.py with `test_run_pipeline_writes_run_journal_entry`: patch `flowstate.orchestrator.append_run_entry`, run_pipeline on a dry-run FlowStateModel under tmp_path, assert call_count == 1 and that the third positional arg (run_id) is a 12-char hex string. Add a second test patching nothing that asserts the entry actually lands: after run_pipeline with dry_run, open a MemoryStore on tmp_path and assert count(MemoryKind.RUN) == 1.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_orchestrator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'append_run_entry' flowstate/orchestrator.py` returns at least 1 (import + call)
    - The append_run_entry call appears textually before `memory.close()` in flowstate/orchestrator.py
    - `python -m pytest tests/test_orchestrator.py -q` exits 0
    - A test asserts exactly one MemoryKind.RUN entry exists in memory.db after a dry-run pipeline
  </acceptance_criteria>
  <done>run_pipeline calls append_run_entry once per run before memory.close(); failures are non-fatal; tests prove the single-entry-per-run behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → filesystem | append_run_entry writes .planning/RUNLOG.md and inserts into memory.db within the project root |
| state.install_manifest → journal metadata | checksums/paths recorded by prior pipeline steps are read and snapshotted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | RUNLOG.md path | mitigate | Path is constructed as `root / ".planning" / "RUNLOG.md"` — no caller-supplied path; no traversal surface |
| T-06-02 | Denial of Service | RUNLOG write / corrupt memory.db | mitigate | RUNLOG write wrapped in try/except; orchestrator call wrapped in try/except — journal failure never aborts pipeline |
| T-06-03 | Tampering | duplicate journal entries | mitigate | Idempotency guard on run_id prevents double-write corrupting the delta chain |
| T-06-04 | Information Disclosure | snapshot metadata | accept | Snapshot is {path: checksum} of files FlowState already owns in the repo; no secrets, local-only store |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added (stdlib + existing flowstate modules only) |
</threat_model>

<verification>
- `python -m pytest tests/test_memory.py tests/test_journal.py tests/test_orchestrator.py -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0 (coverage gate green)
- `ruff check flowstate/journal.py flowstate/memory.py flowstate/orchestrator.py` exits 0
- `ruff format --check flowstate/journal.py` exits 0
- `grep -rc 'bridge' flowstate/journal.py` returns 0 (no LLM/bridge import)
- `python -m pytest tests/test_state.py -q` exits 0 (v0.1→0.2→0.3 migration still loads — journal does not touch flowstate.json schema)
</verification>

<success_criteria>
- After a (dry-run or live) pipeline run, exactly one MemoryKind.RUN entry exists in memory.db for that run_id, and a corresponding RUNLOG.md section is appended.
- append_run_entry is pure-Python (no bridge import) and never raises into the pipeline.
- First-run vs subsequent-run delta logic both covered by passing tests.
- Coverage stays ≥80%.
</success_criteria>

<output>
Create `.planning/phases/06-run-journal/06-01-SUMMARY.md` when done.
</output>
