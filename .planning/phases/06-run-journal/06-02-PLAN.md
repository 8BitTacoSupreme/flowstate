---
phase: 06-run-journal
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - flowstate/context_prefix.py
  - .planning/config.json
  - tests/test_context_prefix.py
autonomous: true
requirements: [RUN-02]
must_haves:
  truths:
    - "build_context_prefix appends a '## Since Last Run' layer AFTER the memory layer (order: fixtures → pack → memory → since-last-run)"
    - "The layer sources the last N MemoryKind.RUN entries newest-first via memory.get_by_kind"
    - "N defaults to 3 and is configurable via top-level key run_journal_prefix_entries in .planning/config.json"
    - "An empty journal omits the section entirely (no empty heading)"
    - "context_prefix.py still does not import flowstate.bridge"
  artifacts:
    - path: "flowstate/context_prefix.py"
      provides: "_read_since_last_run_layer + _load_journal_prefix_n + assembly wiring"
      contains: "## Since Last Run"
    - path: ".planning/config.json"
      provides: "run_journal_prefix_entries default"
      contains: "run_journal_prefix_entries"
  key_links:
    - from: "flowstate/context_prefix.py"
      to: "memory.get_by_kind(MemoryKind.RUN)"
      via: "_read_since_last_run_layer reads last N run entries"
      pattern: "get_by_kind\\(MemoryKind.RUN"
    - from: "_read_since_last_run_layer"
      to: "build_context_prefix layers list"
      via: "appended after memory_layer"
      pattern: "since_last_run_layer"
---

<objective>
Add a `## Since Last Run` layer to `build_context_prefix()`, appended after the memory layer, sourced from the last N `MemoryKind.RUN` journal entries (N configurable via `run_journal_prefix_entries`, default 3). Empty journal omits the section. (RUN-02)

Purpose: The next run reads the journal first — the journal entries from Plan 01 become live context injected into every bridge call, closing the compounding loop on the read side.

Output: `_read_since_last_run_layer` + `_load_journal_prefix_n` helpers in `context_prefix.py`, the assembly wiring, a `run_journal_prefix_entries` config default, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-run-journal/06-CONTEXT.md
@.planning/phases/06-run-journal/06-PATTERNS.md
@.planning/phases/06-run-journal/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and existing context_prefix.py. Use directly. -->

From flowstate/memory.py (after Plan 01):
- MemoryKind.RUN exists
- MemoryStore.get_by_kind(kind: MemoryKind, *, limit: int = 20) -> list[MemoryEntry]  (newest-first, ORDER BY created_at DESC)
- MemoryEntry fields used here: summary, content, metadata (metadata may carry "delta_line")

From flowstate/context_prefix.py:
- _CONFIG_PATH = ".planning/config.json"; _DEFAULT_BUDGET_TOKENS = 12_000
- _load_budget(root) idiom to copy for _load_journal_prefix_n (L65-81)
- _read_fixtures_layer / _read_pack_layer private-helper pattern (return "" on absence/failure, L84-113)
- final assembly L214-217: layers = [fixtures_layer, pack_layer, memory_layer]; non_empty = [l for l in layers if l]; return _SEPARATOR.join(non_empty)
- build_context_prefix(root, memory, query, *, budget_tokens=None, console=None) — memory param already in scope
- HARD RULE: context_prefix.py must NOT import flowstate.bridge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config default and journal-prefix layer to context_prefix.py (RUN-02)</name>
  <files>flowstate/context_prefix.py, .planning/config.json</files>
  <read_first>
    - flowstate/context_prefix.py (full file — especially _load_budget L65-81, _read_fixtures_layer/_read_pack_layer L84-113, assembly L214-217, module docstring L1-28)
    - .planning/config.json (top-level keys to extend)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "context_prefix.py — add ## Since Last Run layer" — _load_journal_prefix_n, _read_since_last_run_layer, assembly target)
  </read_first>
  <action>
    Add `from flowstate.memory import MemoryKind` to the imports of flowstate/context_prefix.py (alongside the existing flowstate.pack import). Do NOT add any flowstate.bridge import.

    Add module-level constant `_DEFAULT_JOURNAL_PREFIX_N = 3`.

    Add `_load_journal_prefix_n(root: Path) -> int` by cloning `_load_budget` exactly: read `.planning/config.json`, return `run_journal_prefix_entries` when it is an int > 0, else `_DEFAULT_JOURNAL_PREFIX_N`. The `isinstance(value, int) and value > 0` guard rejects non-int and negative values.

    Add `_read_since_last_run_layer(root: Path, memory: Any) -> str` following the _read_*_layer pattern: wrap everything in try/except returning ""; read N via _load_journal_prefix_n; `entries = memory.get_by_kind(MemoryKind.RUN, limit=n)`; if no entries return "" (so the assembly's `if layer` filter omits it); otherwise build a `## Since Last Run` markdown section iterating entries newest-first, leading each with the entry's delta_line (metadata.get("delta_line") or summary) then a short body. Return the joined string.

    Wire it in: in build_context_prefix, after the memory layer is computed and before the assembly, compute `since_last_run_layer = _read_since_last_run_layer(root, memory)`, then change the layers list to `[fixtures_layer, pack_layer, memory_layer, since_last_run_layer]`. The existing `[l for l in layers if l]` filter handles omission. Update the module docstring layer-order summary (L10-16) and the build_context_prefix docstring to mention the fourth since-last-run layer.

    Add `"run_journal_prefix_entries": 3` as a top-level key to .planning/config.json (place it near the other top-level scalars; valid JSON, trailing-comma-free).
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -c "import json; json.load(open('.planning/config.json'))" &amp;&amp; ruff check flowstate/context_prefix.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import.*bridge' flowstate/context_prefix.py` returns 0
    - `grep -c 'def _read_since_last_run_layer' flowstate/context_prefix.py` returns 1
    - `grep -c 'def _load_journal_prefix_n' flowstate/context_prefix.py` returns 1
    - `grep -c '## Since Last Run' flowstate/context_prefix.py` returns at least 1
    - `python -c "import json; d=json.load(open('.planning/config.json')); assert d['run_journal_prefix_entries']==3"` exits 0
    - the `since_last_run_layer` token appears after `memory_layer` in the layers list assignment
  </acceptance_criteria>
  <done>build_context_prefix emits a since-last-run layer after memory, gated by config N (default 3), and never imports bridge.</done>
</task>

<task type="auto">
  <name>Task 2: Test the since-last-run layer (RUN-02)</name>
  <files>tests/test_context_prefix.py</files>
  <read_first>
    - tests/test_context_prefix.py (full file — _make_memory_stub L51-55, existing omission/ordering tests L63-85, and the existing no-bridge-import test)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "test_context_prefix.py patterns to extend" — exact new cases)
  </read_first>
  <action>
    Extend the `_make_memory_stub` helper to also stub `get_by_kind` (accept a `run_entries` param defaulting to None → []), so existing callers are unaffected.

    Add these test cases (one assertion focus each):
    - test_since_last_run_omitted_when_empty: get_by_kind returns [] → "## Since Last Run" NOT in output.
    - test_since_last_run_present_when_populated: get_by_kind returns one stub run entry → "## Since Last Run" appears in output AND its index is greater than the "## Prior Knowledge" memory-layer index (ordering: after memory).
    - test_since_last_run_respects_limit_from_config: write a .planning/config.json under tmp_path with run_journal_prefix_entries=2, call build_context_prefix, assert the memory stub's get_by_kind was called with limit=2.
    - test_load_journal_prefix_n_rejects_bad_values: assert _load_journal_prefix_n falls back to 3 for missing key, non-int, and negative values (write small config variants under tmp_path).

    Confirm the existing no-bridge-import test still passes unchanged (do not modify it).
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_context_prefix.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_context_prefix.py -k "since_last_run or journal_prefix" -q` exits 0 and collects at least 4 tests
    - `python -m pytest tests/test_context_prefix.py -q` exits 0 (all existing tests still pass)
    - a test asserts the "## Since Last Run" heading index is greater than the memory-layer heading index
    - a test asserts get_by_kind was called with limit=2 when config sets run_journal_prefix_entries=2
  </acceptance_criteria>
  <done>The since-last-run layer's omission, ordering, config-N, and bad-value-fallback behaviors are covered by passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/config.json → _load_journal_prefix_n | user-editable integer config crosses into prefix sizing |
| memory.db → context prefix | journal entries are read and injected into every bridge user-prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | run_journal_prefix_entries config value | mitigate | `isinstance(value, int) and value > 0` guard rejects non-int/negative; falls back to default 3 (mirrors _load_budget) |
| T-06-06 | Denial of Service | corrupt/absent config or memory.db | mitigate | _read_since_last_run_layer and _load_journal_prefix_n wrap all reads in try/except returning "" / default — prefix assembly never raises |
| T-06-07 | Elevation of Privilege | canon double-injection / bridge coupling | mitigate | No flowstate.bridge import added; preserves the existing context_prefix module boundary |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (stdlib + existing flowstate modules only) |
</threat_model>

<verification>
- `python -m pytest tests/test_context_prefix.py -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0
- `ruff check flowstate/context_prefix.py` exits 0 and `ruff format --check flowstate/context_prefix.py` exits 0
- `grep -c 'bridge' flowstate/context_prefix.py` returns 0
- `python -c "import json; json.load(open('.planning/config.json'))"` exits 0 (config still valid JSON)
</verification>

<success_criteria>
- The context prefix gains a `## Since Last Run` section after `## Prior Knowledge`, drawn from the last N run-journal entries.
- N is configurable (default 3, documented in config.json), bad values fall back safely, empty journal omits the section.
- No bridge import; coverage ≥80%.
</success_criteria>

<output>
Create `.planning/phases/06-run-journal/06-02-SUMMARY.md` when done.
</output>
