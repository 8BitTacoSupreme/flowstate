---
phase: 06-run-journal
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - flowstate/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [RUN-03]
must_haves:
  truths:
    - "`flowstate journal` lists recent MemoryKind.RUN entries newest-first in a Rich table"
    - "Default shows 10 entries; --limit N overrides"
    - "An empty journal prints 'no journal entries yet' and exits 0"
    - "A missing or corrupt memory.db degrades gracefully (no traceback, exit 0)"
    - "The command performs a pure-Python read with no bridge/LLM call"
  artifacts:
    - path: "flowstate/cli.py"
      provides: "@main.command('journal')"
      contains: "def journal"
  key_links:
    - from: "flowstate/cli.py journal command"
      to: "MemoryStore.get_by_kind(MemoryKind.RUN)"
      via: "open store, read, close"
      pattern: "get_by_kind\\(MemoryKind.RUN"
---

<objective>
Add a top-level `flowstate journal` command that lists recent `MemoryKind.RUN` entries newest-first in a Rich table, defaulting to 10 (`--limit N` override), exiting 0 on a missing/empty/corrupt journal. (RUN-03)

Purpose: Give the operator a read surface for the run trail (handoff/inspection) that never breaks, mirroring the existing `flowstate memory stats` command shape.

Output: a `journal` command in `cli.py` and CLI tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-run-journal/06-CONTEXT.md
@.planning/phases/06-run-journal/06-PATTERNS.md
@.planning/phases/06-run-journal/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and existing cli.py. Use directly. -->

From flowstate/memory.py (after Plan 01):
- MemoryKind.RUN exists
- MemoryStore(root=...) ; MemoryStore.get_by_kind(MemoryKind.RUN, *, limit=...) -> list[MemoryEntry] (newest-first)
- MemoryStore.close()
- MemoryEntry fields used: run_id, created_at (datetime), summary, metadata (carries "delta_line", "dry_run")

From flowstate/cli.py:
- @main group exists (top-level commands attach via @main.command(...))
- console = module-level Rich Console
- resolve_root(root, option_was_explicit=_root_was_explicit()) — standard root resolution in every command
- memory_stats command (L380-409) is the template: open MemoryStore, build Rich Table, close, console.print(table)
- CLI commands open/close MemoryStore explicitly (no `with` block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the flowstate journal command (RUN-03)</name>
  <files>flowstate/cli.py</files>
  <read_first>
    - flowstate/cli.py (memory_stats L380-409 as the template; memory_search L344-377 for Table styling; resolve_root usage L350)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "cli.py — new flowstate journal command" — target structure, graceful-degrade try/except, exit-0-on-empty)
  </read_first>
  <action>
    Add a top-level `@main.command("journal")` (NOT under the `memory` group) named `journal` in flowstate/cli.py. Options: `--limit` (type=int, default=10, help text) and the standard `--root` option matching memory_stats. Function-local imports: `from rich.table import Table` and `from flowstate.memory import MemoryKind, MemoryStore` (match memory_stats' local-import style). No flowstate.bridge import.

    Body: resolve root via `resolve_root(root, option_was_explicit=_root_was_explicit())`. Wrap the MemoryStore open + get_by_kind(MemoryKind.RUN, limit=limit) + close in a single try/except Exception; on exception print `[dim]no journal entries yet[/dim]` and return (exit 0) — this covers a corrupt/absent DB. If entries is empty, print the same message and return. Otherwise build a Rich Table ("Run Journal") with columns Run ID, Timestamp, Delta, Dry Run; for each entry add a row with run_id, created_at.isoformat()[:19], metadata.get("delta_line", summary), and "yes"/"no" from metadata.get("dry_run"). console.print(table).

    Entries are already newest-first from get_by_kind (ORDER BY created_at DESC) — do not re-sort.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -c "from flowstate.cli import main; assert any(c=='journal' for c in main.commands)" &amp;&amp; ruff check flowstate/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@main.command("journal")' flowstate/cli.py` returns 1
    - `python -c "from flowstate.cli import main; assert 'journal' in main.commands"` exits 0
    - `grep -c 'get_by_kind(MemoryKind.RUN' flowstate/cli.py` returns at least 1
    - `ruff check flowstate/cli.py` exits 0
  </acceptance_criteria>
  <done>`flowstate journal` is registered, reads MemoryKind.RUN newest-first, renders a Rich table, and degrades gracefully on empty/corrupt DB.</done>
</task>

<task type="auto">
  <name>Task 2: Test the journal command (RUN-03)</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (CliRunner usage, _isolate_config fixture L16-22, existing memory command tests for store-seeding patterns)
    - .planning/phases/06-run-journal/06-PATTERNS.md (section "test_cli.py patterns to extend" — exact new cases)
  </read_first>
  <action>
    Add CLI tests using CliRunner.invoke(main, ["journal", "--root", str(tmp_path), ...]):
    - test_journal_empty_exits_zero: fresh tmp_path (no entries) → exit_code == 0 and "no journal entries yet" in output.
    - test_journal_populated_shows_table: open a MemoryStore on tmp_path, add 2 MemoryKind.RUN entries (distinct run_ids, e.g. via MemoryEntry.create with metadata delta_line), close it, invoke journal → exit 0 and both run_ids appear in output.
    - test_journal_limit_option: seed 5 RUN entries, invoke with --limit 2 → exactly 2 run IDs present in output (assert the 3 oldest run_ids are absent).
    - test_journal_corrupt_db_exits_zero: write a non-SQLite junk file at the memory.db path under tmp_path, invoke journal → exit_code == 0 and "no journal entries yet" in output (graceful degrade, no traceback).

    Seed entries through the public MemoryStore API (MemoryEntry.create + store.add); do not poke private SQL. For the corrupt-db case, locate the db path the same way MemoryStore resolves it under root.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx &amp;&amp; python -m pytest tests/test_cli.py -k journal -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli.py -k journal -q` exits 0 and collects at least 4 tests
    - a test asserts exit_code == 0 with "no journal entries yet" for an empty journal
    - a test asserts exit_code == 0 for a corrupt memory.db (no traceback in output)
    - a test asserts --limit 2 shows exactly 2 of 5 seeded entries
  </acceptance_criteria>
  <done>journal command behavior (empty, populated, --limit, corrupt-db) is verified by passing CLI tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg --limit → query | user-supplied integer crosses into the get_by_kind limit |
| memory.db → terminal | journal entries read and rendered; DB may be absent/corrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service | corrupt/absent memory.db | mitigate | MemoryStore open + read wrapped in try/except → prints "no journal entries yet", exit 0; never tracebacks |
| T-06-09 | Tampering | --limit value | mitigate | Click `type=int` rejects non-integers at parse time; get_by_kind binds limit as a parameterized SQL value (no injection) |
| T-06-10 | Information Disclosure | rendered journal rows | accept | Shows only run_id/timestamp/delta/dry_run flag from the local repo's own run history; no secrets |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (stdlib + rich, already present) |
</threat_model>

<verification>
- `python -m pytest tests/test_cli.py -k journal -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0
- `ruff check flowstate/cli.py` exits 0 and `ruff format --check flowstate/cli.py` exits 0
- `grep -c 'bridge' flowstate/cli.py` count unchanged by this plan (no new bridge import in the journal command)
</verification>

<success_criteria>
- `flowstate journal` prints recent run entries newest-first, defaults to 10, honors --limit, and exits 0 even when RUNLOG.md/memory.db is absent, empty, or corrupt.
- Pure-Python read, no bridge call; coverage ≥80%.
</success_criteria>

<output>
Create `.planning/phases/06-run-journal/06-03-SUMMARY.md` when done.
</output>
