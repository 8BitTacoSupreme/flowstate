---
phase: 22-the-verdict
plan: 03
type: execute
wave: 2
depends_on: [22-01, 22-02]
files_modified:
  - .planning/phases/22-the-verdict/22-VERDICT.md
autonomous: false
requirements: [VERD-02, VERD-03]

must_haves:
  truths:
    - "A free --mode cheap plumbing check + pristine-worktree assertion (D-01a) run against /Users/jhogan/floxybot2 BEFORE any spend, proving the 4-contrast driver + Holm + report work end-to-end and the subject is contamination-free (run-1-empty-memory). Its pre-flight record is written to the plan SUMMARY / scratch, NOT to 22-VERDICT.md (which the real run owns)."
    - "The paid 5x3 --mode real run does not start until the cost estimate is shown AND the user explicitly greenlights it (D-08 checkpoint)."
    - "The committed 22-PREREGISTRATION.md (Plan 01) precedes the first real trial (D-04) — verified in git before the real run."
    - "The final 22-VERDICT.md is written by the real run and EMBEDS the assert_pristine_worktree (D-01a) result — the pristine-control evidence survives in the verdict artifact and is not clobbered by the driver's --out overwrite."
    - "22-VERDICT.md reports quality AND tax per arm, the compounding curve run 1->3, and the raw + Holm-corrected PASS/NULL per the D-02 three-part gate for all 4 contrasts (VERD-02, VERD-03, D-05, D-06, D-07)."
    - "Any null contrast (including wiki-none) is accepted and documented as a valid outcome that licenses stripping the layer — NOT retried to chase significance (VERD-03, D-02)."
    - "The run executes on an isolated worktree copy of floxybot2; the real repo is never mutated (D-01)."
  artifacts:
    - path: ".planning/phases/22-the-verdict/22-VERDICT.md"
      provides: "The final verdict: per-arm quality+tax, compounding curve, Holm-corrected pass/null, and the embedded D-01a pristine-control record (VERD-03)"
      contains: "Holm"
  key_links:
    - from: ".planning/phases/22-the-verdict/22-VERDICT.md"
      to: ".planning/phases/22-the-verdict/22-PREREGISTRATION.md"
      via: "applies the frozen, pre-committed rules"
      pattern: "PREREGISTRATION"
---

<objective>
Execute the pre-registered verdict: a free cheap plumbing check + pristine control on the real
subject repo (recorded to the SUMMARY / scratch, not the verdict file), then — behind an explicit
cost greenlight — the single paid 5x3 --mode real run on /Users/jhogan/floxybot2, which writes
22-VERDICT.md applying the frozen D-02 three-part gate with Holm correction and embeds the D-01a
pristine-control result into the verdict. This is the milestone's one real spend.

Purpose: VERD-02 (the paired-design run on a real repo across 5 arms with the compounding curve)
and VERD-03 (report quality AND tax, apply the pre-registered rules, accept null).
Output: `.planning/phases/22-the-verdict/22-VERDICT.md`.

GATE: this plan is autonomous=false. Task 2 is a blocking human checkpoint; Task 3 (the paid run)
MUST NOT run unattended or before that greenlight (D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-the-verdict/22-CONTEXT.md
@.planning/phases/22-the-verdict/22-PREREGISTRATION.md
@bench/verdict.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Free cheap plumbing check + pristine control on floxybot2 (recorded to SUMMARY / scratch)</name>
  <read_first>
    - bench/verdict.py — main(), assert_pristine_worktree (built in Plan 02).
    - .planning/phases/22-the-verdict/22-CONTEXT.md — D-01a (contamination control), D-08 sequence step 1.
  </read_first>
  <files>.planning/phases/22-the-verdict/22-03-SUMMARY.md</files>
  <action>
    Run the FREE steps of the D-08 sequence — no LLM spend. IMPORTANT: do NOT write anything to
    22-VERDICT.md in this task — the paid real run in Task 3 owns that file via `--out` (which
    overwrites it). Record everything below in the plan SUMMARY (and a scratch dir under
    /tmp if convenient), so nothing here can be clobbered by the real run.
    (a) `assert_pristine_worktree(/Users/jhogan/floxybot2)` — confirm no memory.db, flowstate.json,
        .planning/, root PROJECT.md/ROADMAP.md/research/ (a bare .claude/ is legitimate, not a flag).
        This is the D-01a control that guarantees run-1-empty-memory. Capture its returned result;
        the same assertion is re-run and EMBEDDED into 22-VERDICT.md by the real run (Task 3), so the
        durable D-01a evidence lives in the verdict artifact — this pre-flight copy is just the go/no-go.
    (b) `python -m bench.verdict --root /Users/jhogan/floxybot2 --mode cheap --out /tmp/verdict_smoke.md`
        — proves the 4-contrast driver + Holm + report run end-to-end against the real subject path
        (synthetic numbers, zero spend). Confirm the report has all 5 arms, 4 Holm-corrected contrasts,
        per-arm tax + compounding curve, and the embedded pristine-control section.
    (c) Verify the pre-registration integrity anchor: `git log --oneline --
        .planning/phases/22-the-verdict/22-PREREGISTRATION.md` returns a commit that PRECEDES this work
        (D-04). If it does not, STOP — do not proceed to any real run.
    Record a short "Dry-run / pristine control (D-01a)" section in the plan SUMMARY capturing these
    results and the exact `--mode real` command that WOULD run (root, trials 5, runs 3, pinned seed) —
    but do not execute it here, and do not touch 22-VERDICT.md.
  </action>
  <verify>
    <automated>uv run python -c "from bench.verdict import assert_pristine_worktree; assert_pristine_worktree('/Users/jhogan/floxybot2')" && uv run python -m bench.verdict --root /Users/jhogan/floxybot2 --mode cheap --out /tmp/verdict_smoke.md && grep -qi "pristine" /tmp/verdict_smoke.md && git log --oneline -- .planning/phases/22-the-verdict/22-PREREGISTRATION.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - floxybot2 passes the pristine control (D-01a) with no stray FlowState state.
    - The cheap driver emits a full verdict-shaped report (including the embedded pristine-control section) against the real subject path with zero spend.
    - 22-PREREGISTRATION.md is confirmed committed before any real run (D-04).
    - Task 1 did NOT write to 22-VERDICT.md — the pre-flight record lives in the SUMMARY / scratch only.
  </acceptance_criteria>
  <done>The apparatus is proven free and the subject is contamination-free; the pre-flight record is safely in the SUMMARY; the only remaining step is the gated paid run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Cost estimate + explicit greenlight gate (D-08)</name>
  <action>Blocking human checkpoint. Produce and show a real cost estimate (from one real `none`-arm trial or the Phase-19 tax accounting, extrapolated to 5x5x3 runs), then WAIT for the user to choose an option below. Do not invoke any --mode real verdict run until the user explicitly approves. This task performs no automated implementation — its output is the recorded decision.</action>
  <decision>Approve the single paid 5x3 --mode real verdict run on /Users/jhogan/floxybot2?</decision>
  <context>
    This is the milestone's one real LLM spend: 5 arms x 5 trials x 3 runs, each run a live
    producer + independent-judge call (judge != producer). Task 1 already proved the plumbing free.
    Per D-08, surface a cost estimate BEFORE approval: run ONE real trial of the `none` arm (or read
    the Phase-19 tax accounting) to measure real per-run tokens_in/out + wall_clock_s, then extrapolate
    to the full 5x5x3 = 75 runs (plus judge calls). Present the estimated total tokens, dollar cost,
    and wall-clock. No re-running to chase significance is permitted once results are in (D-02).
  </context>
  <options>
    <option id="approve-full">
      <name>Approve the full pre-registered 5x3 real run</name>
      <pros>Delivers the pre-registered verdict at the locked n; honors D-08's "straight to full 5x3" posture.</pros>
      <cons>The full spend; irreversible cost.</cons>
    </option>
    <option id="smoke-first">
      <name>Reduced-n smoke first (e.g. trials 2, runs 2), then decide</name>
      <pros>Cheaper confidence that real producers/judge fire end-to-end before the full spend (matches the ROADMAP "smoke before scaling" note).</pros>
      <cons>Smoke numbers are NOT the pre-registered verdict — the frozen claim still requires the full 5x3.</cons>
    </option>
    <option id="abort">
      <name>Do not run — accept the verdict cannot be produced now</name>
      <pros>Zero spend.</pros>
      <cons>VERD-02/03 remain unmet; the milestone's headline question stays unanswered.</cons>
    </option>
  </options>
  <resume-signal>Select: approve-full, smoke-first, or abort (and, if approving, confirm the shown cost estimate is acceptable).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Paid --mode real run + write 22-VERDICT.md (GATED — only after Task 2 approval)</name>
  <read_first>
    - bench/verdict.py — main() real path, assert_pristine_worktree embed, _EXIT_NO_PAIRED_DATA fail-loud.
    - .planning/phases/22-the-verdict/22-PREREGISTRATION.md — the frozen rules to apply verbatim.
    - .planning/phases/22-the-verdict/22-CONTEXT.md — D-02 three-part win rule, D-05/D-06/D-07 reporting.
  </read_first>
  <files>.planning/phases/22-the-verdict/22-VERDICT.md</files>
  <action>
    ONLY if Task 2 returned approve-full (or smoke-first at the approved reduced n): execute the run
    the user greenlit, e.g. `python -m bench.verdict --root /Users/jhogan/floxybot2 --mode real
    --trials 5 --runs 3 --seed <pinned> --out .planning/phases/22-the-verdict/22-VERDICT.md`.

    This `--out` writes the whole file, so the DRIVER (not Task 1) is responsible for the D-01a
    evidence: main() runs assert_pristine_worktree(/Users/jhogan/floxybot2) before the real trials and
    the writer embeds its result as the "Pristine control (D-01a)" section of 22-VERDICT.md (built in
    Plan 02). Confirm that section is present in the emitted file — the pristine-control evidence must
    survive in the final verdict, not just the pre-flight SUMMARY.

    Requirements on the output (all inherited from the driver; verify they landed):
    - The embedded "Pristine control (D-01a)" section recording the pristine-worktree assertion result
      for /Users/jhogan/floxybot2 (D-01a).
    - Per-arm quality (independent-judge 0-10) AND per-arm tax (tokens_in/out/cache_read/wall_clock_s),
      side by side (D-03).
    - The compounding curve run 1->3 per arm, paired-normalized to run-0 (D-07).
    - For each of the 4 contrasts (pack/memory/wiki/full - none): CI (excludes-0?), Cohen's d, raw p,
      Holm-corrected p, and the D-02 PASS/NULL verdict per the three-part gate (CI-excludes-0 AND
      d>=0.8 AND Holm-reject — Holm is GATING).
    - For every NULL contrast (including wiki-none if it lands null), an explicit "accepted, documented,
      licenses stripping this layer" statement (VERD-03). Do NOT re-run to chase significance.
    - If the run exits _EXIT_NO_PAIRED_DATA (measured nothing), record the fail-loud outcome honestly
      and STOP — do not fabricate a null CI as a clean result.
    - A header linking to the committed 22-PREREGISTRATION.md and stamping mode=real, seed, trials, runs.

    If Task 2 returned abort, mark this plan blocked/paused in the SUMMARY and do not run.
  </action>
  <verify>
    <human-check>
      1. 22-VERDICT.md exists with mode=real, all 5 arms, and 4 Holm-corrected contrasts.
      2. 22-VERDICT.md contains the embedded "Pristine control (D-01a)" section for floxybot2 (the pre-flight evidence survives the --out overwrite).
      3. Every arm shows quality AND tax; the compounding curve run 1->3 is present per arm.
      4. Each contrast has CI + Cohen's d + raw p + Holm p + a pass/null verdict per the D-02 three-part gate.
      5. Null contrasts carry the "accepted / licenses stripping" language; no evidence of re-running to chase significance.
      6. The report references 22-PREREGISTRATION.md and the pre-reg commit precedes the run (D-04).
    </human-check>
  </verify>
  <acceptance_criteria>
    - 22-VERDICT.md applies the frozen D-02 three-part gate with Holm correction across the 4 contrasts and reports quality + tax + compounding curve per arm.
    - 22-VERDICT.md provably contains the embedded D-01a pristine-control evidence (not clobbered by the --out overwrite).
    - A null result is documented as accepted (VERD-03); the run was not retried to chase significance.
    - The real run ran on an isolated worktree; floxybot2 is unmodified (git status in floxybot2 is clean).
  </acceptance_criteria>
  <done>The pre-registered verdict is written, applies the committed rules, embeds the D-01a control, and honestly reports pass/null per arm — VERD-02/VERD-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> paid LLM run | The one real spend of the milestone; must be human-gated. |
| driver -> floxybot2 | Live `claude --print` reads an external repo on an isolated worktree copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Denial of service (cost runaway) | --mode real 5x3x5 | mitigate | Free cheap check (Task 1) + a blocking greenlight with a measured cost estimate (Task 2) + pinned n (D-08); Task 3 never runs unattended (autonomous=false). |
| T-22-08 | Repudiation | post-hoc rule changes / re-running to chase significance | mitigate | Rules are the committed 22-PREREGISTRATION.md (D-04); Task 1 verifies the commit precedes the run; null is a pre-accepted outcome (D-02). |
| T-22-09 | Tampering | mutating the subject repo | mitigate | Driver runs inside compound_eval._worktree (isolated copy); acceptance checks floxybot2 git status stays clean (D-01). |
| T-22-10 | Repudiation | "measured nothing" reported as a clean null | mitigate | Inherit _EXIT_NO_PAIRED_DATA fail-loud; the human-check confirms no fabricated null CI. |
| T-22-12 | Repudiation | Task 1 pre-flight evidence clobbered by Task 3 --out overwrite | mitigate | Task 1 writes only to the SUMMARY / scratch; the durable D-01a evidence is embedded into 22-VERDICT.md by the driver's own pre-run pristine assertion (Plan 02 writer). |
| T-22-11 | Information disclosure | reading /Users/jhogan/floxybot2 | accept | User's own repo, isolated worktree, never mutated (D-01/D-01a). |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new runtime deps; nothing installed this phase. |
</threat_model>

<verification>
- Task 1 free checks pass (pristine control + cheap smoke + pre-reg commit precedes run) and are recorded in the SUMMARY, not 22-VERDICT.md.
- Task 2 records an explicit greenlight with a shown cost estimate.
- 22-VERDICT.md (real) contains the embedded D-01a pristine-control section, all 5 arms, 4 Holm-corrected contrasts, per-arm quality+tax+curve, and pass/null per the D-02 three-part gate.
- floxybot2 remains unmodified (isolated worktree, D-01).
</verification>

<success_criteria>
The pre-registered paired-design verdict is produced on a real repo, applies the committed D-02
three-part gate with Holm correction, reports quality AND tax per arm with the compounding curve,
embeds the D-01a pristine control, and accepts a null result as valid — VERD-02 and VERD-03 met,
the capstone of v0.8.0 delivered.
</success_criteria>

<output>
Create `.planning/phases/22-the-verdict/22-03-SUMMARY.md` when done.
</output>
