---
phase: 03-ingredients-pack-canon-fixtures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/bridge.py
  - tests/test_bridge.py
autonomous: true
requirements: [CANON-01]
must_haves:
  truths:
    - "Every claude --print invocation has the Karpathy canon block prepended to its system prompt as the first (most stable) layer"
    - "Setting BridgeConfig.inject_canon = False suppresses the canon prefix"
    - "Canon injection works even when no caller-supplied system_prompt is passed"
  artifacts:
    - path: "flowstate/bridge.py"
      provides: "CANON module constant + BridgeConfig.inject_canon field + prepend logic in run()"
      contains: "CANON ="
  key_links:
    - from: "flowstate/bridge.py CANON constant"
      to: "claude --print --system-prompt"
      via: "run() prepends CANON to final system prompt when inject_canon is True"
      pattern: "inject_canon"
---

<objective>
Ship the Karpathy canon as a `CANON` module constant in flowstate/bridge.py and prepend it
to every `claude --print` system prompt as the first (most stable) CAG layer, suppressible
via a new `BridgeConfig.inject_canon: bool = True` flag.

Purpose: The canon is the most-stable prefix layer for Phase 4's cache-optimized CAG
assembly — placing it first maximizes Anthropic's implicit server-side prompt-cache hits
(confirmed in spike o6h). Independent of the pack work, so runs in Wave 1 alongside Plan 01.
Output: extended flowstate/bridge.py, new canon tests in tests/test_bridge.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-CONTEXT.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md

@flowstate/bridge.py
@/Users/jhogan/CLAUDE.md

<interfaces>
<!-- The canon source text is /Users/jhogan/CLAUDE.md sections §1-4, verbatim:
     "1. Think Before Coding", "2. Simplicity First", "3. Surgical Changes",
     "4. Goal-Driven Execution". Lift the text; do not paraphrase or invent. -->

From flowstate/bridge.py — BridgeConfig (add inject_canon after effort):
```python
@dataclass
class BridgeConfig:
    claude_bin: str | None = None
    project_root: Path = field(default_factory=Path.cwd)
    timeout: int = 300
    allowed_tools: list[str] = field(default_factory=list)
    max_turns: int = 10
    model: str | None = None
    max_budget_usd: float | None = None
    effort: str | None = None
    # add: inject_canon: bool = True
```

From flowstate/bridge.py — current system-prompt assembly in run() (~L144-145):
```python
if system_prompt:
    cmd.extend(["--system-prompt", system_prompt])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CANON constant + inject_canon field + prepend logic</name>
  <files>flowstate/bridge.py, tests/test_bridge.py</files>
  <read_first>
    - flowstate/bridge.py (module docstring + _SENTINEL ~L33; BridgeConfig ~L44-58; run() system-prompt assembly ~L132-162; dry_run/not-available guards ~L115-130)
    - /Users/jhogan/CLAUDE.md (sections "## 1. Think Before Coding" through "## 4. Goal-Driven Execution" — verbatim source text for CANON)
    - tests/test_bridge.py (env-override + fake-binary that echoes argv patterns ~L46-82)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (bridge.py section lines 236-275)
  </read_first>
  <behavior>
    - BridgeConfig().inject_canon defaults to True.
    - With inject_canon=True and a fake claude that echoes its argv, the value passed after --system-prompt starts with the CANON text and includes the caller's system_prompt after it.
    - With inject_canon=True and no system_prompt argument, --system-prompt is still emitted carrying the CANON text alone.
    - With inject_canon=False, the CANON text does NOT appear in the emitted command; behavior matches the pre-change path (only the caller's system_prompt, if any, is passed).
  </behavior>
  <action>
    In flowstate/bridge.py define a module-level `CANON` string constant after the module
    docstring and before `_SENTINEL` (per CANON-01). Populate it verbatim from
    /Users/jhogan/CLAUDE.md §1-4 (Think Before Coding / Simplicity First / Surgical Changes /
    Goal-Driven Execution) as a single triple-quoted string. Add `inject_canon: bool = True`
    to BridgeConfig immediately after the `effort` field. In `run()`, replace the existing
    `if system_prompt: cmd.extend(["--system-prompt", system_prompt])` block with assembly
    that computes `canon_prefix = CANON + "\n\n" if self.config.inject_canon else ""`, then
    `final_system = canon_prefix + (system_prompt or "")`, and emits
    `cmd.extend(["--system-prompt", final_system])` only when `final_system.strip()` is
    non-empty. Keep the dry_run and not-available early returns unchanged. Do not alter any
    other flag assembly.

    Add tests to tests/test_bridge.py (class TestCanonInjection) using a fake claude binary
    that echoes its arguments so the test can assert on what is passed to --system-prompt:
    inject_canon default True prepends CANON before the caller system_prompt; inject_canon=True
    with no system_prompt still emits CANON; inject_canon=False omits CANON entirely. Reuse the
    fake-binary + FLOWSTATE_CLAUDE_BIN monkeypatch pattern already in test_bridge.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_bridge.py -x -q && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^CANON = ' flowstate/bridge.py` returns 1 (module-level constant).
    - flowstate/bridge.py BridgeConfig contains `inject_canon: bool = True`.
    - `grep -v '^#' flowstate/bridge.py | grep -c inject_canon` returns ≥2 (field + run() usage).
    - tests/test_bridge.py contains class TestCanonInjection with both inject_canon=True and inject_canon=False assertions.
    - `python -m pytest tests/test_bridge.py -x -q` exits 0.
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0.
  </acceptance_criteria>
  <done>CANON constant present, prepended to every system prompt by default, suppressible via inject_canon=False; bridge tests pass; coverage ≥80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlowState → claude --print system prompt | FlowState-authored canon text injected into every spawned agent's system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | CANON constant content | accept | Static FlowState-authored constant lifted verbatim from operator's own CLAUDE.md; no external/user input flows into it |
| T-03-05 | Repudiation | inject_canon suppression | accept | Default True ensures canon present unless explicitly disabled in code; flag is dev-controlled, not runtime-attacker-reachable |
</threat_model>

<verification>
- `python -m pytest tests/test_bridge.py -x -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0
- `ruff check flowstate/bridge.py` reports no errors
- A fake-claude echo test confirms CANON precedes the caller system_prompt and is absent when inject_canon=False
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 4 met: canon prepended to every invocation; inject_canon=False suppresses it
- Canon text matches /Users/jhogan/CLAUDE.md §1-4 verbatim (no paraphrase)
- Coverage ≥80%
</success_criteria>

<output>
Create `.planning/phases/03-ingredients-pack-canon-fixtures/03-02-SUMMARY.md` when done
</output>
