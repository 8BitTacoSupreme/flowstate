---
phase: 03-ingredients-pack-canon-fixtures
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - flowstate/context.py
  - .claude/CLAUDE.md
  - tests/test_context.py
autonomous: true
requirements: [FIX-01, FIX-02, DX-02]
must_haves:
  truths:
    - "flowstate init writes a starter fixture under .planning/fixtures/ registered on install_manifest"
    - "The fixture file includes retrieval_questions, acceptance_gates, forbidden_actions, a system contract, and at least one few-shot exemplar"
    - ".mcp.json contains a repomix-MCP entry (mcpServers.repomix with command/args) and is registered on install_manifest"
    - ".mcp.json appears in state.context_files (or the write_context_files created list) after init"
    - "generate_claude_md() and FlowState's own .claude/CLAUDE.md both carry repomix-pack guidance"
  artifacts:
    - path: "flowstate/context.py"
      provides: "generate_starter_fixture(), scaffold_mcp_json(), fixture+mcp registration in write_context_files, DX-02 guidance in generate_claude_md"
      contains: "def generate_starter_fixture("
    - path: ".claude/CLAUDE.md"
      provides: "Repomix pack guidance section (DX-02, FlowState's own docs)"
      contains: "repomix-pack"
  key_links:
    - from: "flowstate/context.py write_context_files"
      to: "install_manifest"
      via: "_register fixture (kind=fixture) and .mcp.json (kind=config)"
      pattern: "kind=\"fixture\""
    - from: "flowstate/context.py generate_starter_fixture"
      to: "state.interview"
      via: "derives fixture content from InterviewAnswers fields"
      pattern: "answers\\.(core_problem|milestones|ten_x_vision)"
---

<objective>
Build the ECC-modeled eval fixture ingredient and the repomix-MCP registration: extend
flowstate/context.py to (1) generate a starter fixture from interview answers and write it
to `.planning/fixtures/<name>.json` registered as `kind="fixture"`, (2) write `.mcp.json`
registering the repomix-MCP server, registered as `kind="config"`, and (3) add DX-02
repomix-pack guidance to both `generate_claude_md()` (downstream projects) and FlowState's
own `.claude/CLAUDE.md`.

Purpose: The fixture format (rubric/contract/exemplars) and the .mcp.json registration are
the remaining Phase 3 ingredients Phase 4 will fold into the CAG prefix and overflow path.
Depends on Plan 01 only for the `fixture` kind Literal in state.py.
Output: extended flowstate/context.py, updated .claude/CLAUDE.md, new tests in tests/test_context.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-CONTEXT.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-01-SUMMARY.md

@flowstate/context.py
@flowstate/state.py
@.claude/CLAUDE.md

<interfaces>
<!-- Contracts extracted from codebase. Use directly. -->

From flowstate/state.py — InterviewAnswers (fixture content derives from these fields):
```python
class InterviewAnswers(BaseModel):
    research_focus: str = ""
    core_problem: str = ""
    ten_x_vision: str = ""
    milestones: list[str] = Field(default_factory=list)
    test_coverage: int = 80
    architecture_pattern: str = ""
```

From flowstate/context.py — _register helper (reuse for fixture + .mcp.json):
```python
def _register(state, root, path, *, owner: str, kind: str) -> None:
    rel = str(path.relative_to(root))
    checksum = _sha256_of(path) if kind != "memory" else None
    ...
```

From flowstate/context.py — write_context_files dir/write/register pattern (~L177-217):
```python
planning = root / ".planning"
planning.mkdir(exist_ok=True)
project_path = planning / "PROJECT.md"
project_path.write_text(...)
_register(state, root, project_path, owner="context", kind="context")
created.append(project_path)
```

Required fixture top-level keys (ECC scenario.json shape, FIX-01):
retrieval_questions[], acceptance_gates[], forbidden_actions[], system_contract (str),
few_shot_exemplars[] (each: input, expected_output, rationale) — ≥1 exemplar.

Required .mcp.json shape (MEDIUM-3 — exact dict):
{"mcpServers": {"repomix": {"command": "npx", "args": ["repomix", "--mcp"]}}}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generate_starter_fixture() and scaffold_mcp_json()</name>
  <files>flowstate/context.py, tests/test_context.py</files>
  <read_first>
    - flowstate/context.py (generate_project_md / generate_roadmap_md for the dedent/derive style; _register ~L23-47; imports header)
    - flowstate/state.py (InterviewAnswers ~L28-34, FlowStateModel)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (context.py section lines 279-366; fixture shape lines 345-366)
    - tests/test_context.py (existing generate_* offline test patterns)
  </read_first>
  <behavior>
    - generate_starter_fixture(answers, project_name) returns a dict with all five required keys: retrieval_questions (list, ≥1), acceptance_gates (list, ≥1), forbidden_actions (list, ≥1), system_contract (non-empty str), few_shot_exemplars (list with ≥1 dict having input/expected_output/rationale).
    - Content derives from interview answers: core_problem → system_contract; milestones → acceptance_gates seeds; ten_x_vision → a retrieval_questions seed (verifiable by passing distinctive answers and asserting they appear).
    - generate_starter_fixture with empty InterviewAnswers() still returns a valid dict satisfying all key/min-length constraints (sensible defaults, no crash).
    - scaffold_mcp_json(root) returns exactly {"mcpServers": {"repomix": {"command": "npx", "args": ["repomix", "--mcp"]}}} — assert d["mcpServers"]["repomix"]["command"] == "npx".
  </behavior>
  <action>
    In flowstate/context.py add two functions following the existing generate_* style
    (dedent/derive-from-answers, no LLM, no I/O inside the generators):

    `generate_starter_fixture(answers: InterviewAnswers, project_name: str = "") -> dict`
    (FIX-01) — return a dict with keys retrieval_questions, acceptance_gates,
    forbidden_actions, system_contract, few_shot_exemplars. Derive system_contract from
    `answers.core_problem` (fall back to a generic contract when empty); seed acceptance_gates
    from `answers.milestones` plus a coverage gate referencing `answers.test_coverage`; seed
    retrieval_questions from `answers.ten_x_vision` and `answers.architecture_pattern`; provide
    sensible default forbidden_actions (e.g. "do not invent requirements not in PROJECT.md")
    and at least one few_shot_exemplar dict with input/expected_output/rationale. Guarantee
    every list has ≥1 element even for empty InterviewAnswers.

    `scaffold_mcp_json(root: Path) -> dict` (PACK-03 registration half) — return the .mcp.json
    content as a dict registering the repomix MCP server. The returned dict MUST have this
    EXACT shape (MEDIUM-3):
    `{"mcpServers": {"repomix": {"command": "npx", "args": ["repomix", "--mcp"]}}}`.
    Pure function, no write.
  </action>
  <verify>
    <automated>python -m pytest tests/test_context.py -x -q -k "fixture or mcp"</automated>
  </verify>
  <acceptance_criteria>
    - flowstate/context.py contains `def generate_starter_fixture(` and `def scaffold_mcp_json(`.
    - `python -c "from flowstate.context import generate_starter_fixture; from flowstate.state import InterviewAnswers; d=generate_starter_fixture(InterviewAnswers()); assert all(k in d for k in ['retrieval_questions','acceptance_gates','forbidden_actions','system_contract','few_shot_exemplars']); assert len(d['few_shot_exemplars'])>=1"` exits 0.
    - `python -c "from flowstate.context import scaffold_mcp_json; from pathlib import Path; d=scaffold_mcp_json(Path('.')); assert 'mcpServers' in d and d['mcpServers']['repomix']['command']=='npx' and d['mcpServers']['repomix']['args']==['repomix','--mcp']"` exits 0.
    - tests/test_context.py asserts derived content (distinctive interview answers appear in the fixture) AND that scaffold_mcp_json returns the mcpServers key with the repomix entry's command field == "npx" (not merely a substring "repomix").
    - `python -m pytest tests/test_context.py -x -q -k "fixture or mcp"` exits 0.
  </acceptance_criteria>
  <done>Both generators produce valid ECC-shaped fixture dicts and a repomix .mcp.json dict (exact mcpServers/command/args shape) derived from interview answers; targeted tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire fixture + .mcp.json into write_context_files; add DX-02 guidance</name>
  <files>flowstate/context.py, .claude/CLAUDE.md, tests/test_context.py</files>
  <read_first>
    - flowstate/context.py (write_context_files ~L171-217; _register ~L23-47; generate_claude_md ~L114-140; state.context_files assignment)
    - flowstate/context.py generate_starter_fixture / scaffold_mcp_json (from Task 1)
    - .claude/CLAUDE.md (FlowState's own project doc — where to add the Repomix Pack guidance section)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (DX-02 lines 327-333; manifest registration lines 459-466)
  </read_first>
  <behavior>
    - write_context_files creates .planning/fixtures/, writes a starter fixture JSON there, and registers it on install_manifest with kind="fixture".
    - write_context_files writes .mcp.json at the project root and registers it on install_manifest with kind="config".
    - Both new paths appear in the returned created list and in state.install_manifest.
    - .mcp.json appears in state.context_files (or the returned created list) after write_context_files.
    - generate_claude_md() output contains the repomix-pack guidance (a section instructing readers to consult .planning/codebase/repomix-pack.xml instead of crawling source each wave).
    - FlowState's own .claude/CLAUDE.md contains the same repomix-pack guidance.
  </behavior>
  <action>
    In flowstate/context.py extend `write_context_files` (after the existing PROJECT/ROADMAP/
    config/CLAUDE/brief writes) to: (FIX-02) create `.planning/fixtures/` via mkdir(exist_ok=
    True), write `generate_starter_fixture(answers, project_name)` as JSON
    (`json.dumps(..., indent=2) + "\n"`) to `.planning/fixtures/starter.json`, call
    `_register(state, root, fixture_path, owner="context", kind="fixture")`, append to created;
    and (PACK-03) write `scaffold_mcp_json(root)` as JSON to `root / ".mcp.json"`, call
    `_register(state, root, mcp_path, owner="context", kind="config")`, append to created.
    Place both before `state.context_files = [...]` so they are tracked, and ensure `.mcp.json`'s
    relative path is included in the `state.context_files` list (MEDIUM-5).

    (DX-02) In `generate_claude_md` append a "## Repomix Pack" guidance section to the returned
    string instructing downstream agents to consult `.planning/codebase/repomix-pack.xml`
    (produced by `flowstate pack`) instead of crawling source files each wave.

    (DX-02, own docs) In FlowState's own `.claude/CLAUDE.md` add the same "consult the Repomix
    pack instead of crawling source every wave" guidance as a new section, referencing
    `.planning/codebase/repomix-pack.xml` and the `flowstate pack` command. Surgical addition
    only — do not restructure existing content.

    Extend tests/test_context.py: assert write_context_files produces .planning/fixtures/
    starter.json and .mcp.json, both registered on install_manifest with the correct kinds
    (fixture/config) and a non-None checksum; assert `.mcp.json` appears in state.context_files
    (or in the returned created list); assert generate_claude_md() output contains the repomix
    guidance string.
  </action>
  <verify>
    <automated>python -m pytest tests/test_context.py -x -q && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - After write_context_files on a tmp root: `.planning/fixtures/starter.json` and `.mcp.json` both exist.
    - install_manifest contains an entry with kind="fixture" for the fixture path and kind="config" for .mcp.json (both with non-None checksum).
    - `.mcp.json` (its relative path) appears in state.context_files OR the write_context_files return value's created list (asserted by a test).
    - `grep -c 'repomix-pack' flowstate/context.py` returns ≥1 (DX-02 in generate_claude_md).
    - `grep -c 'repomix-pack' .claude/CLAUDE.md` returns ≥1 (DX-02 own docs).
    - tests/test_context.py asserts both new artifacts, their manifest kinds, the .mcp.json context_files membership, and the generate_claude_md guidance.
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0.
  </acceptance_criteria>
  <done>init scaffolds a registered starter fixture and .mcp.json (tracked in context_files); DX-02 guidance present in both the template and FlowState's own docs; full suite ≥80% coverage passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interview answers → fixture JSON | Operator-supplied interview text serialized into a fixture file under .planning/ |
| .mcp.json → spawned claude --print agents | Declares the repomix MCP server agents may invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | fixture JSON content from interview answers | accept | Operator's own interview input written to their own project; json.dumps serialization prevents injection into JSON structure; no eval/exec of fixture content this phase |
| T-03-07 | Elevation of Privilege | .mcp.json registers an MCP server agents can call | mitigate | Only the repomix server (read-only codebase grep) is registered; written under operator-controlled project root, no remote/network MCP servers added |
| T-03-08 | Information Disclosure | .mcp.json + fixtures under .planning/ | accept | Contents are the operator's own project metadata; .planning/ follows existing project gitignore hygiene |
</threat_model>

<verification>
- `python -m pytest tests/test_context.py -x -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0
- `ruff check flowstate/context.py` reports no errors
- write_context_files on a tmp root yields .planning/fixtures/starter.json + .mcp.json, both registered with correct kinds, and .mcp.json present in context_files
- `grep -c repomix-pack flowstate/context.py .claude/CLAUDE.md` returns ≥1 in each
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 3 (remainder) met: .mcp.json contains a repomix-MCP entry with exact mcpServers/command/args shape (allowed-tools passthrough completed in Plan 01)
- ROADMAP Phase 3 success criterion 5 met: init writes a registered starter fixture with all required keys + ≥1 exemplar
- DX-02 met: repomix-pack guidance in both generate_claude_md() and FlowState's own .claude/CLAUDE.md
- Coverage ≥80%; no new Python runtime dependencies
</success_criteria>

<output>
Create `.planning/phases/03-ingredients-pack-canon-fixtures/03-03-SUMMARY.md` when done
</output>
</content>
