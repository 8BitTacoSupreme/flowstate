---
phase: 03-ingredients-pack-canon-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/pack.py
  - flowstate/state.py
  - flowstate/cli.py
  - tests/test_pack.py
  - tests/test_install_manifest.py
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03]
must_haves:
  truths:
    - "flowstate pack produces .planning/codebase/repomix-pack.xml and registers it on install_manifest with a checksum"
    - "flowstate pack exits non-zero with a clear message when repomix is absent from PATH and FLOWSTATE_REPOMIX_BIN"
    - "A second flowstate pack reuses the existing pack when no source file is newer than the pack's manifest created_at; touching a source file triggers regeneration"
  artifacts:
    - path: "flowstate/pack.py"
      provides: "repomix locator, run_pack(), is_pack_stale(), PackResult/PackConfig"
      contains: "def run_pack("
    - path: "flowstate/cli.py"
      provides: "flowstate pack Click command"
      contains: "def pack"
    - path: "flowstate/state.py"
      provides: "InstallEntry.kind extended with pack and fixture"
      contains: "\"pack\""
  key_links:
    - from: "flowstate/cli.py"
      to: "flowstate/pack.py"
      via: "run_pack(root, compress=compress) call in pack command"
      pattern: "run_pack\\("
    - from: "flowstate/pack.py"
      to: "flowstate/state.py"
      via: "_register pack artifact on install_manifest with kind=pack"
      pattern: "kind=\"pack\""
---

<objective>
Build the Repomix pack ingredient: a new `flowstate/pack.py` service that locates the
external `repomix` Node CLI (mirroring `bridge._find_claude()`), produces a pack artifact
at `.planning/codebase/repomix-pack.xml`, registers it on `install_manifest` with a
checksum, and only repacks when stale. Wire a `flowstate pack` Click command and extend
`InstallEntry.kind` to accept the new `pack` and `fixture` kinds (the `fixture` value is
consumed by Plan 03; added here because the Literal lives in state.py).

Purpose: Establishes the durable, independently-testable pack artifact that Phase 4's
`build_context_prefix()` will fold into the CAG prefix. Pure-Python subprocess, no bridge
MCP dependency, no new Python runtime deps (repomix is external like `claude`).
Output: flowstate/pack.py, extended state.py + cli.py, tests/test_pack.py, updated test_install_manifest.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-CONTEXT.md
@.planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md

@flowstate/bridge.py
@flowstate/state.py
@flowstate/cli.py
@flowstate/context.py

<interfaces>
<!-- Exact contracts the executor must mirror. Extracted from codebase. Use directly; no exploration. -->

From flowstate/bridge.py — locator + result/config patterns to mirror:
```python
def _find_claude() -> str:
    env_path = os.environ.get("FLOWSTATE_CLAUDE_BIN")
    if env_path and Path(env_path).is_file():
        return env_path
    found = shutil.which("claude")
    if found:
        return found
    candidates = [
        Path.home() / ".local" / "bin" / "claude",
        Path("/usr/local/bin/claude"),
        Path("/opt/homebrew/bin/claude"),
    ]
    for c in candidates:
        if c.is_file():
            return str(c)
    return ""

@dataclass
class BridgeResult:
    success: bool
    output: str
    exit_code: int = 0
    error: str | None = None
```

From flowstate/state.py — current InstallEntry.kind Literal (to extend):
```python
kind: Literal["config", "context", "memory", "research", "artifact"]
```

From flowstate/context.py — the _register helper to reuse for the pack artifact:
```python
def _register(state, root, path, *, owner: str, kind: str) -> None:
    rel = str(path.relative_to(root))
    checksum = _sha256_of(path) if kind != "memory" else None
    state.install_manifest = [e for e in state.install_manifest if e.path != rel]
    state.install_manifest.append(InstallEntry(path=rel, owner=owner, kind=kind, ...))
```

From flowstate/cli.py — check_bridge command skeleton (analog for pack command):
```python
@main.command("check")
@click.option("--root", type=click.Path(path_type=Path), default=None, help="...")
def check_bridge(root: Path | None):
    from flowstate.bridge import BridgeConfig, ClaudeBridge
    root = resolve_root(root, option_was_explicit=_root_was_explicit())
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend InstallEntry.kind Literal with pack and fixture</name>
  <files>flowstate/state.py, tests/test_install_manifest.py</files>
  <read_first>
    - flowstate/state.py (InstallEntry model ~L46-54; FlowStateModel.version default; migration block ~L91-128)
    - tests/test_install_manifest.py (TestInstallEntry::test_install_entry_accepts_all_valid_kinds ~L41)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (state.py section, lines 202-232)
  </read_first>
  <behavior>
    - InstallEntry(kind="pack") and InstallEntry(kind="fixture") validate successfully.
    - Existing kinds (config/context/memory/research/artifact) still validate.
    - test_install_entry_accepts_all_valid_kinds asserts all seven kinds accepted.
  </behavior>
  <action>
    In flowstate/state.py extend the `InstallEntry.kind` Literal from
    `Literal["config", "context", "memory", "research", "artifact"]` to add `"pack"` and
    `"fixture"` (per PACK-01 and FIX-02; `fixture` is consumed by Plan 03 but the Literal
    lives here). Bump `FlowStateModel.version` default from "0.3.0" to "0.4.0" and add the
    v0.3.0 → v0.4.0 migration block in the existing migration ladder following the established
    pattern (set `data["version"] = "0.4.0"` when `version < "0.4.0"`; the Literal extension
    is backward-compatible, so existing entries need no transform). In
    tests/test_install_manifest.py update the valid-kinds test to include `"pack"` and
    `"fixture"`. Do not introduce new fields on InstallEntry.
  </action>
  <verify>
    <automated>python -m pytest tests/test_install_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - flowstate/state.py contains `"pack"` and `"fixture"` inside the InstallEntry.kind Literal.
    - flowstate/state.py FlowStateModel.version default is "0.4.0".
    - `python -m pytest tests/test_install_manifest.py -x -q` exits 0.
    - `python -c "from flowstate.state import InstallEntry; InstallEntry(path='p', owner='pack', kind='pack')"` exits 0.
  </acceptance_criteria>
  <done>InstallEntry accepts pack and fixture kinds; version bumped to 0.4.0; manifest tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create flowstate/pack.py — locator, run_pack, staleness</name>
  <files>flowstate/pack.py, tests/test_pack.py</files>
  <read_first>
    - flowstate/bridge.py (_find_claude ~L60-81; BridgeResult ~L36-42; BridgeConfig + __post_init__ ~L44-58; subprocess invocation ~L168-196; not-available guard ~L120-130)
    - flowstate/state.py (load_state, save_state, FlowStateModel, install_manifest, InstallEntry.created_at)
    - flowstate/context.py (_register ~L23-47, _sha256_of)
    - tests/test_bridge.py (env-override + fake-binary monkeypatch patterns ~L46-82)
    - tests/test_doctor.py (subprocess monkeypatch pattern ~L127-144)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (pack.py section, lines 24-143)
  </read_first>
  <behavior>
    - _find_repomix() returns FLOWSTATE_REPOMIX_BIN when set to an existing file; else shutil.which("repomix"); else a known candidate path; else "".
    - run_pack(root) with a fake repomix on FLOWSTATE_REPOMIX_BIN writes .planning/codebase/repomix-pack.xml, returns PackResult(success=True, output_path=that path), and registers a kind="pack" InstallEntry with a non-None checksum.
    - run_pack(root) with no repomix available returns PackResult(success=False, error contains "repomix") and exit_code non-zero; writes nothing.
    - run_pack(root, compress=True) includes the --compress flag in the repomix argv.
    - is_pack_stale(root, state) returns False when the pack's manifest created_at is newer than all tracked .py source mtimes; returns True (and True when no pack entry exists) when any source file is newer.
  </behavior>
  <action>
    Create flowstate/pack.py mirroring flowstate/bridge.py structure (same import header:
    `from __future__ import annotations`, os, shutil, subprocess, dataclass/field, Path).
    Implement `_find_repomix() -> str` by copying `_find_claude()` and substituting env var
    `FLOWSTATE_REPOMIX_BIN`, `shutil.which("repomix")`, and candidate paths
    `~/.local/share/pnpm/repomix`, `~/.npm-global/bin/repomix`, `/usr/local/bin/repomix`.
    Define `@dataclass PackResult(success: bool, output_path: Path | None = None,
    exit_code: int = 0, error: str | None = None)` and `@dataclass PackConfig(repomix_bin:
    str | None = None, project_root: Path = field(default_factory=Path.cwd), output_path:
    Path | None = None, timeout: int = 300, compress: bool = False)` with `__post_init__`
    calling `_find_repomix()` when repomix_bin is None and defaulting output_path to
    `project_root / ".planning" / "codebase" / "repomix-pack.xml"`.

    Implement `run_pack(root: Path, *, compress: bool = False) -> PackResult` (PACK-01):
    build PackConfig; if repomix_bin is falsy return PackResult(success=False, exit_code=1,
    error="repomix CLI not found. Install repomix or set FLOWSTATE_REPOMIX_BIN to the binary
    path."). Otherwise `output_path.parent.mkdir(parents=True, exist_ok=True)`, run the
    repomix subprocess (argv: `[repomix_bin, "--output", str(output_path), "--style", "xml"]`
    plus `"--compress"` when compress is True, plus the target dir `str(root)`) using the
    bridge subprocess pattern (cwd=root, capture_output, text, timeout, the same
    TimeoutExpired/FileNotFoundError handling returning PackResult failures). On
    returncode==0, load state via load_state(root), call the context `_register` helper to
    register output_path with owner="pack", kind="pack" (checksum auto-computed), save_state,
    and return PackResult(success=True, output_path=output_path). Import `_register` and
    `_sha256_of` from flowstate.context to avoid duplicating manifest logic.

    Implement `is_pack_stale(root: Path, state: FlowStateModel) -> bool` (PACK-02): find the
    InstallEntry whose path == relative ".planning/codebase/repomix-pack.xml"; if none, return
    True. Otherwise return True if `max(p.stat().st_mtime for p in root.rglob("*.py"))` is
    greater than `entry.created_at.timestamp()`, else False. Guard the rglob for an empty
    sequence.

    Write tests/test_pack.py using the import header and class layout from 03-PATTERNS.md:
    TestFindRepomix (env override, which detection via monkeypatched PATH, missing returns ""),
    TestRunPack (success writes the XML and registers a pack manifest entry; absent repomix
    returns graceful failure; compress passes --compress), TestIsPackStale (fresh pack not
    stale; touched source newer than pack is stale; missing entry is stale). Use a fake repomix
    shell script that writes a sentinel XML file and `monkeypatch.setenv("FLOWSTATE_REPOMIX_BIN",
    ...)`. NEVER shell out to a real repomix.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pack.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - flowstate/pack.py contains `def _find_repomix(`, `def run_pack(`, `def is_pack_stale(`, `class PackResult`, `class PackConfig`.
    - `grep -v '^#' flowstate/pack.py | grep -c FLOWSTATE_REPOMIX_BIN` returns ≥1.
    - `python -m pytest tests/test_pack.py -x -q` exits 0.
    - tests/test_pack.py contains a fake-repomix binary written to a tmp path and monkeypatch of FLOWSTATE_REPOMIX_BIN (no real repomix invocation).
  </acceptance_criteria>
  <done>pack.py locates repomix, produces and registers the pack artifact, detects staleness; all test_pack.py tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add flowstate pack CLI command + repomix-MCP allowed-tools passthrough</name>
  <files>flowstate/cli.py, tests/test_pack.py</files>
  <read_first>
    - flowstate/cli.py (check_bridge ~L500-524; doctor non-zero exit ~L539-574; fresh --force flag; _root_was_explicit + resolve_root ~L30-33)
    - flowstate/pack.py (run_pack, is_pack_stale, PackResult — from Task 2)
    - flowstate/bridge.py (BridgeConfig.allowed_tools ~L49; run() --allowedTools assembly ~L140-142)
    - .planning/phases/03-ingredients-pack-canon-fixtures/03-PATTERNS.md (cli.py section lines 147-200; PACK-03 notes)
  </read_first>
  <action>
    In flowstate/cli.py add a `@main.command("pack")` command (analog: check_bridge + doctor)
    with `--root` (resolved via `resolve_root(root, option_was_explicit=_root_was_explicit())`),
    a `--compress` flag, and a `--force` flag (mirroring `fresh --force`). Import run_pack and
    is_pack_stale from flowstate.pack inside the function. Behavior (PACK-01/02): load state;
    unless `--force`, if a pack already exists and `is_pack_stale(root, state)` is False, print
    `[dim]Pack up to date; skipping (use --force to repack).[/dim]` and return without
    invoking repomix. Otherwise call `run_pack(root, compress=compress)`. On
    `result.success`, print `[green]Pack written:[/green] {result.output_path.relative_to(root)}`.
    On failure print `[red]{result.error}[/red]` and `sys.exit(1)` (non-zero exit when repomix
    absent, per PACK-01).

    PACK-03: ensure spawned agents can grep the pack via repomix-MCP. Add the repomix MCP tool
    name to `BridgeConfig.allowed_tools` default coverage — locate where ClaudeBridge configs
    are constructed for adapter spawns (the orchestrator `_make_bridge` builder referenced in
    CLAUDE.md) and append the repomix MCP tool identifier (`"mcp__repomix"`) to allowed_tools
    so it flows through the existing `--allowedTools` assembly in bridge.run(). Do NOT modify
    bridge.run()'s flag assembly — only the allowed_tools list it already consumes. If the
    construction point is solely in orchestrator._make_bridge, make the minimal edit there and
    add the file to this plan's note in the SUMMARY.

    Extend tests/test_pack.py with TestPackCommand (CliRunner): `pack --root` exits 0 and prints
    "Pack written" with a fake repomix; `pack --root` exits 1 and prints the not-found message
    when FLOWSTATE_REPOMIX_BIN is unset and repomix is not on PATH; `pack --root` on a fresh
    non-stale pack prints the skip message and does not re-invoke repomix; `pack --root --force`
    re-invokes even when not stale.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pack.py -x -q && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - flowstate/cli.py registers a `pack` command (`grep -c '@main.command("pack")' flowstate/cli.py` returns 1).
    - `python -c "from click.testing import CliRunner; from flowstate.cli import main; print(CliRunner().invoke(main, ['pack','--help']).exit_code)"` prints 0.
    - tests/test_pack.py TestPackCommand asserts exit_code 1 with the repomix-not-found message and exit_code 0 with "Pack written".
    - The allowed_tools list used for adapter bridge spawns contains the repomix MCP tool identifier (verifiable via grep for `mcp__repomix` in flowstate/).
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0.
  </acceptance_criteria>
  <done>flowstate pack works end-to-end (produce/skip/force, graceful failure), repomix-MCP is in adapter allowed_tools, coverage ≥80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlowState → repomix subprocess | External Node CLI invoked with project paths; untrusted binary resolution via PATH/env |
| spawned claude --print → repomix-MCP | Agents granted grep access to the packed codebase via allowed-tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | _find_repomix PATH/env resolution | accept | Mirrors trusted _find_claude pattern; operator-controlled env; no privilege escalation, local-only tool |
| T-03-02 | Denial of Service | repomix subprocess (large repo) | mitigate | PackConfig.timeout (default 300s) bounds the subprocess via subprocess.run timeout, returns PackResult failure on TimeoutExpired |
| T-03-03 | Information Disclosure | repomix-pack.xml content exposed to agents | accept | Pack contains only the project's own source the operator already owns; written under .planning/ (gitignored per project hygiene) |
| T-03-SC | Tampering | repomix is an external Node CLI, not an npm install in this repo | accept | No package-manager install task in this phase; repomix located like claude, not added to uv.lock. No new Python deps. |
</threat_model>

<verification>
- `python -m pytest tests/test_pack.py tests/test_install_manifest.py -x -q` exits 0
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` exits 0
- `ruff check flowstate/pack.py flowstate/cli.py flowstate/state.py` reports no errors
- `flowstate pack --root <tmp>` with fake repomix produces .planning/codebase/repomix-pack.xml and a kind=pack manifest entry
- `flowstate pack --root <tmp>` with no repomix exits 1 with a clear message
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 1 met: pack produced, registered with checksum, non-zero exit when repomix absent
- ROADMAP Phase 3 success criterion 2 met: stale-only repack (reuse when fresh, regenerate on source touch)
- ROADMAP Phase 3 success criterion 3 (partial: allowed-tools passthrough) met; .mcp.json registration completed in Plan 03
- Coverage ≥80%; no new Python runtime dependencies
</success_criteria>

<output>
Create `.planning/phases/03-ingredients-pack-canon-fixtures/03-01-SUMMARY.md` when done
</output>
