---
phase: 17-no-silent-no-op-arms-producers-wired-e2e
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - bench/prepare_fixture.py
  - tests/test_bench_prepare_fixture.py
autonomous: true
requirements: [HAR-03]
must_haves:
  truths:
    - "One entry point `python -m bench.prepare_fixture --root <proj>` generates what each arm needs before the arm matrix runs: the repomix pack (pack arm) and the wiki article corpus (wiki arm)"
    - "prepare-fixture invokes flowstate.pack.run_pack for the pack producer and bench.distiller for the wiki producer — it does not reimplement either"
    - "prepare-fixture reports per-producer success/failure and returns non-zero when a requested producer could not be built (fail loud, no silent skip)"
    - "The wiki corpus prepare-fixture produces is exactly the directory compound_eval's HAR-02 gate checks and the Phase-11 reader globs"
  artifacts:
    - path: "bench/prepare_fixture.py"
      provides: "single fixture-preparation entry point wiring the per-arm producers"
      contains: "def main"
  key_links:
    - from: "bench/prepare_fixture.py"
      to: "bench.distiller.main"
      via: "wiki producer invocation"
      pattern: "distiller"
    - from: "bench/prepare_fixture.py"
      to: "flowstate.pack.run_pack"
      via: "pack producer invocation"
      pattern: "run_pack"
---

<objective>
Ship the ONE `prepare-fixture` path (HAR-03, success criterion 3): a single entry
point that generates what each arm needs BEFORE the arm matrix runs. This is the seam
that makes HAR-02 fail-loud actionable — prepare-fixture produces the artifacts, the
Plan-02 gate verifies their presence at arm time. It wires the existing producers:
`flowstate.pack.run_pack` for the `pack` arm and `bench.distiller` (Plan 01) for the
`wiki` arm's article corpus. It does not reimplement either producer.

Purpose: one command provisions every arm's producer; a failed producer is reported
loud and returns non-zero (no silent partial fixture).
Output: `bench/prepare_fixture.py` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- The producers prepare-fixture orchestrates. -->

bench/distiller.py (Plan 01 — the wiki producer):
```python
def main(argv: list[str] | None = None) -> int   # writes .planning/codebase/wiki/*.md; non-zero on empty memory
```

flowstate/pack.py (the pack producer):
```python
@dataclass
class PackResult:
    success: bool
    output_path: Path | None = None
    error: str | None = None
def run_pack(root: Path, *, compress: bool = False) -> PackResult
```
</interfaces>

@bench/distiller.py
@flowstate/pack.py
@bench/wikigen.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prepare_fixture entry point wiring per-arm producers</name>
  <read_first>
    - bench/distiller.py (Plan 01 output — its `main(argv)` contract and non-zero-on-empty behavior)
    - flowstate/pack.py lines 48-149 (`PackResult`, `run_pack(root, *, compress=False)` and its success/error semantics)
    - bench/wikigen.py (argparse/never-raises/`python -m` idiom to match)
  </read_first>
  <behavior>
    - `python -m bench.prepare_fixture --root <proj>` builds the producers for the default arm set (pack + wiki): calls `run_pack(root)` for pack and `bench.distiller.main(["--root", str(root)])` for wiki.
    - A `--arms` option (choices pack, wiki; repeatable or comma-list) limits which producers run; default builds both.
    - Each producer's outcome is reported on one line (built / failed + reason); the overall return code is non-zero if ANY requested producer failed, 0 when all requested producers succeeded.
    - Never raises: a producer exception is caught and reported as a failure for that producer.
    - Passing arms with no producer (e.g. none/memory/full if accepted) is a no-op that does not fail.
  </behavior>
  <action>
    Create `bench/prepare_fixture.py` with `main(argv: list[str] | None = None) -> int`,
    argparse `--root` (required Path), `--arms` (limit the producer set; default pack+wiki),
    `--force` (passed through to the distiller), and `--model` (passed to the distiller's
    `--llm`/model path — keep LLM off by default). For the pack producer call
    `flowstate.pack.run_pack(root)` and treat `PackResult.success` as the outcome (surface
    `.error` on failure). For the wiki producer call `bench.distiller.main` with
    `["--root", str(root)]` (+ `--force`/`--model`/`--llm` as flagged) and treat a zero
    return as success. Collect per-producer outcomes, print a one-line status each via the
    Rich console, and `return 0` only when every requested producer succeeded, else a
    non-zero code. Wrap each producer call so an exception becomes a reported failure, never
    an uncaught raise. End with `if __name__ == "__main__": sys.exit(main())`.
  </action>
  <acceptance_criteria>
    - `tests/test_bench_prepare_fixture.py` asserts a run with `--arms wiki` on a root whose memory.db has entries produces `.planning/codebase/wiki/*.md` and returns 0.
    - Asserts a run with `--arms wiki` on a root with empty memory.db returns non-zero and reports the wiki producer as failed (distiller's non-zero surfaced).
    - Asserts the pack producer path is invoked via `run_pack` (monkeypatch `run_pack` to a stub returning `PackResult(success=False, error=...)` and assert the overall return is non-zero and the failure is reported).
    - Asserts default `--arms` builds BOTH producers (both invoked), and an all-success run returns 0.
    - `python -m pytest tests/test_bench_prepare_fixture.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_bench_prepare_fixture.py -x -q</automated>
  </verify>
  <done>Single entry point provisions each arm's producer, reports per-producer status, and fails loud (non-zero) when any requested producer fails.</done>
</task>

<task type="auto">
  <name>Task 2: repo lint + full-suite coverage gate</name>
  <read_first>
    - bench/prepare_fixture.py (Task 1 output)
    - pyproject.toml (the `--cov-fail-under=80` gate and ruff config)
  </read_first>
  <action>
    Run the full test suite with coverage and ruff across the phase's new/changed bench
    modules (`bench/distiller.py`, `bench/compound_eval.py`, `bench/prepare_fixture.py`).
    Fix any lint (ruff check + format) or coverage regressions introduced by this phase so
    the ≥80% gate stays green. Do NOT modify unrelated code — only address failures your
    Phase-17 changes caused.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80` passes.
    - `ruff check flowstate/ bench/ tests/` reports no errors.
    - `ruff format --check bench/ tests/` reports all files formatted.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q && ruff check flowstate/ bench/ tests/</automated>
  </verify>
  <done>Full suite green at ≥80% coverage; ruff clean across the phase's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepare_fixture → run_pack (repomix subprocess) | repomix invoked on local repo files; no untrusted input |
| prepare_fixture → distiller (optional claude subprocess) | inherits Plan-01 distiller boundaries; LLM off by default here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Repudiation | a producer silently skipped, leaving an arm unprovisioned | mitigate | per-producer status reporting + non-zero overall exit when any requested producer fails |
| T-17-06 | Denial of Service | repomix/claude subprocess hang | mitigate | inherited timeouts in `run_pack` / distiller `_DISTILL_TIMEOUT`; exceptions caught per-producer |
| T-17-SC | Tampering | no new package installs | accept | stdlib + existing deps only |
</threat_model>

<verification>
- `python -m bench.prepare_fixture --root <proj-with-memory>` builds the pack and the wiki corpus, returns 0.
- Empty memory.db → wiki producer reported failed, non-zero exit.
- Full suite + ruff green.
</verification>

<success_criteria>
- One prepare-fixture path provisions every arm's producer before the matrix runs (HAR-03 criterion 3).
- Producers are wired, not reimplemented; failures are loud.
</success_criteria>

<output>
Create `.planning/phases/17-no-silent-no-op-arms-producers-wired-e2e/17-03-SUMMARY.md` when done
</output>
