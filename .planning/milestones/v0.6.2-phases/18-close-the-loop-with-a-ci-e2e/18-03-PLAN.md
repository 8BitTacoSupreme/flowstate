---
phase: 18-close-the-loop-with-a-ci-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bench_e2e_smoke.py
autonomous: true
requirements: [HAR-05]
must_haves:
  truths:
    - "A green, CI-safe E2E smoke test exercises every arm's plumbing (none/pack/memory/wiki/full) with no live LLM/bridge and no network."
    - "The smoke test asserts the harness fails loud (exit 3) when an arm's required producer artifact is absent."
    - "The smoke test asserts each producer-less arm and each producer-satisfied arm returns 0 in cheap mode."
  artifacts:
    - path: "tests/test_bench_e2e_smoke.py"
      provides: "the 'harness of harnesses works E2E' acceptance gate"
      contains: "def test_"
  key_links:
    - from: "tests/test_bench_e2e_smoke.py"
      to: "bench.compound_eval.main"
      via: "cheap-mode per-arm invocation + _EXIT_PRODUCER_ABSENT assertion"
      pattern: "_EXIT_PRODUCER_ABSENT"
---

<objective>
Deliver HAR-05: a green, CI-safe E2E smoke test that exercises EVERY arm's plumbing and
asserts the harness fails loud on a missing producer — the "harness of harnesses works
E2E" acceptance gate for v0.6.2.

Purpose: v0.6.2 exists to make the eval harness itself run end-to-end and fail loud. This
test is the milestone's closing gate: it proves, deterministically and without any live
LLM, that all five arms' plumbing runs and that an absent producer exits `3`
(`_EXIT_PRODUCER_ABSENT`) rather than reporting a bare number. It depends only on the
Phase-17 fail-loud gate and cheap-mode loop already on `main`, so it runs in parallel with
Plan 01.

Output: `tests/test_bench_e2e_smoke.py`, green under `uv run python -m pytest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- E2E targets — all on main (Phase 16/17). No modification, only invocation. -->

bench/compound_eval.py:
  - `main(argv=None) -> int` — entry point. `--mode {cheap,real}` (cheap is CI-safe, no network),
    `--layers {full,none,pack,memory,wiki}`, `--root <path>`.
  - `_EXIT_PRODUCER_ABSENT = 3` — returned by main() when `_missing_producer(arm, root)` is truthy;
    the gate is checked for BOTH modes BEFORE the loop runs.
  - Producer requirements: `pack` -> non-empty `.planning/codebase/repomix-pack.xml`;
    `wiki` -> >=1 non-empty `.planning/codebase/wiki/*.md` OR non-empty `.planning/codebase/wiki.md`;
    `full`/`memory`/`none` -> no requirement (always pass the gate).
  - Existing precedent test (tests/test_bench_compound.py:1057): `main(["--mode","cheap",
    "--layers","wiki","--root",str(tmp_path)])` returns non-zero when the wiki producer is absent.

bench/project.py:
  - `scaffold(root, *, synthetic=True) -> None` — seeds a minimal `.planning/` fixture tree under root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_bench_e2e_smoke.py — every-arm plumbing + fail-loud gate</name>
  <files>tests/test_bench_e2e_smoke.py</files>
  <read_first>
    - bench/compound_eval.py lines 348-419 (`main`), 82-116 (`_missing_producer`), 56-79 (`_EXIT_PRODUCER_ABSENT`, `_ARM_PRODUCERS`, producer path literals).
    - tests/test_bench_compound.py lines 1007-1063 — existing zero-byte/absent producer tests and the `test_main_fails_loud_on_missing_wiki_producer` precedent to mirror.
    - bench/project.py line 192 (`scaffold`) — how to seed a CI-safe fixture tree under tmp_path.
  </read_first>
  <action>
    Create `tests/test_bench_e2e_smoke.py`. Import `bench.compound_eval` and reference
    `_EXIT_PRODUCER_ABSENT` directly (do not hardcode `3`). Build the fixture with
    `bench.project.scaffold(tmp_path)` (or copy `bench/fixtures/sample_project` into tmp_path).
    Write four smoke tests, ALL in `--mode cheap` so no `claude` binary / network is needed:
      (1) `test_producerless_arms_run_green`: for each arm in `("full", "none", "memory")`,
          assert `compound_eval.main(["--mode","cheap","--layers",arm,"--root",str(root)])` == 0.
      (2) `test_producer_arms_fail_loud_when_absent`: with no producer artifacts present, for each
          arm in `("pack", "wiki")`, assert `main([... "--layers", arm ...])` == `_EXIT_PRODUCER_ABSENT`.
      (3) `test_producer_arms_run_green_when_present`: create non-empty producer artifacts directly
          (write `.planning/codebase/repomix-pack.xml` with `<repomix/>` and
          `.planning/codebase/wiki/overview.md` with real text — the CI-safe way, no repomix/npx),
          then assert both `pack` and `wiki` arms return 0. This proves the arm plumbing runs once
          its producer is satisfied.
      (4) `test_every_arm_covered`: assert the smoke set exercises the full arm vocabulary by
          checking the union of arms tested equals `set(compound_eval._ARM_PRODUCERS)` (guards
          against a future arm being added without an E2E smoke).
    No test may spawn a live LLM, require `claude` on PATH, or hit the network.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_e2e_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_bench_e2e_smoke.py -q` is green with no `claude` binary present and no network (CI-safe).
    - The absent-producer test asserts equality to `bench.compound_eval._EXIT_PRODUCER_ABSENT` (grep the test: `grep -c "_EXIT_PRODUCER_ABSENT" tests/test_bench_e2e_smoke.py` >= 1) — not a bare literal `3`.
    - The five arms full/none/memory/pack/wiki are ALL exercised (test (4) asserts coverage against `_ARM_PRODUCERS`).
    - Producer-present pack and wiki arms both return 0; producer-absent pack and wiki arms both return the exit-3 gate code.
    - Full suite stays green: `uv run python -m pytest -q` passes and `--cov-fail-under=80` holds.
  </acceptance_criteria>
  <done>A CI-safe E2E smoke test exercises every arm's plumbing and asserts fail-loud exit 3 on a missing producer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke test → compound_eval (cheap) | Runs the harness in cheap mode against a local tmp fixture only; no LLM, no network, no producer subprocesses. No external attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Denial of service | smoke test flaking on missing `claude`/network in CI | mitigate | test is `--mode cheap` only and writes producer artifacts directly (no repomix/npx) — asserted no live LLM path |
| T-18-06 | Repudiation | a new arm shipped without an E2E smoke | mitigate | coverage test asserts the tested arm set equals `_ARM_PRODUCERS`, failing loud on an unguarded arm |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_bench_e2e_smoke.py -q` green with no claude binary.
- `uv run ruff check tests/test_bench_e2e_smoke.py` clean.
- `uv run python -m pytest -q` full suite green, coverage ≥80%.
</verification>

<success_criteria>
- A green, CI-safe E2E smoke test exercises every arm's plumbing (no live LLM).
- The test asserts the harness fails loud (exit 3) on a missing producer.
- The full arm vocabulary is covered and guarded against future drift.
</success_criteria>

<output>
Create `.planning/phases/18-close-the-loop-with-a-ci-e2e/18-03-SUMMARY.md` when done.
</output>
