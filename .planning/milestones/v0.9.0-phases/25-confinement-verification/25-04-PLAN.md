---
phase: 25-confinement-verification
plan: 04
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md
  - flowstate/sandbox.py
  - tests/test_sandbox.py
autonomous: false
requirements: [SBX-05]

user_setup:
  - service: claude-credential-for-container
    why: "A real confined `claude --print` must run inside a Linux Docker container to prove auth survives the shipped bwrap profile with the FILE-based credential; this needs a working credential the human must provide"
    dashboard_config:
      - task: "Provide a working ~/.claude/.credentials.json (0600) for the throwaway container — mount it read-only, NEVER paste the value into any committed file or command-line arg"
        location: "local machine — file-path credential, per D-02 (file-path is the WR-03 concern being closed)"

must_haves:
  truths:
    - "A confined claude --print inside Docker, using the EXACT shipped build_linux_bwrap_args argv (read-only HOME via --ro-bind / /) AND the FILE-based ~/.claude/.credentials.json (0600), succeeds (auth survives, API reachable) — OR the minimal writable-HOME fix is applied and re-verified"
    - "Under the same confined profile, a write outside project_root is DENIED and a read of ~/.ssh is DENIED"
    - "The probe outcome (works-as-is, or needed a bound-writable HOME) is recorded in a committed verification artifact"
    - "No credential value ever appears in any committed file"
  artifacts:
    - path: ".planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md"
      provides: "committed Linux D-02 re-probe + D-03 denial verification (mirrors 23-SPIKE-LINUX.md)"
      contains: "build_linux_bwrap_args"
    - path: "flowstate/sandbox.py"
      provides: "CONDITIONAL: minimal bound-writable HOME added to build_linux_bwrap_args ONLY if the re-probe fails for a filesystem-write reason"
  key_links:
    - from: ".planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md"
      to: "flowstate.sandbox.build_linux_bwrap_args"
      via: "records the exact shipped argv used + the file-credential + the denial assertions"
      pattern: "build_linux_bwrap_args"
---

<objective>
Close WR-03 and prove SBX-05 on Linux in ONE shared Docker step (D-02 re-probe + D-03 denial E2E share the harness — do not duplicate). Re-run the confined `claude --print` probe inside the Phase-23 Docker recipe using the EXACT shipped `build_linux_bwrap_args` argv (read-only HOME under `--ro-bind / /` — NOT the Phase-23 writable-HOME `--setenv HOME /tmp/chome` shortcut) AND the FILE-based credential `~/.claude/.credentials.json` (0600, the production `bridge.py` default — NOT the token-path shortcut). In the same container run, assert the D-03 denials: a write outside `project_root` is denied and a read of `~/.ssh` is denied. Record the outcome in a committed `25-SPIKE-LINUX-REPROBE.md`. ONLY if confined claude actually fails for a filesystem-write reason do we add a minimal bound-writable HOME/cache dir to `build_linux_bwrap_args` (mirroring the macOS profile's `/private/var/folders` re-allow) and re-verify — empirical, no speculative loosening.

Purpose: The Linux `confine` tier cannot be trusted in production until the exact shipped argv + the real file credential are proven together. Privileged Docker is off-CI, so the proof is a committed verification artifact (like SBX-01's spike).
Output: A committed Linux verification artifact + (conditionally) a minimal writable-HOME tweak to the builder with an updated golden test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-confinement-verification/25-CONTEXT.md
@.planning/phases/25-confinement-verification/25-PATTERNS.md
@.planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md
@flowstate/sandbox.py
@tests/test_sandbox.py

<interfaces>
<!-- Extracted — the executor reads these; no exploration needed. -->
- flowstate.sandbox.build_linux_bwrap_args(project_root) -> list[str] (sandbox.py:219-248):
    ships `--ro-bind / /`, `--bind <project> <project>`, `--tmpfs <home>/.ssh`, `--dev /dev`,
    `--proc /proc`, `--unshare-pid/uts/ipc`, `--die-with-parent`. NOTE: HOME is read-only under
    `--ro-bind / /` — this is exactly the WR-03 divergence to test. Args-only (no bwrap binary,
    no --, no cmd); _wrap_linux prepends _find_bwrap() and appends ["--", *cmd].
- 23-SPIKE-LINUX.md recipe: ubuntu:24.04 --rm, claude installed via npm fallback
    (@anthropic-ai/claude-code), Landlock ABI probe, bwrap 0.9.0. Section 3 used the token-path
    (--setenv HOME /tmp/chome + CLAUDE_CODE_OAUTH_TOKEN via --env-file) — the two shortcuts
    D-02 explicitly replaces with (read-only HOME + file credential).
- Credential handling: mount ~/.claude/.credentials.json read-only into the container
    (docker run -v ...:ro) — NEVER as a CLI arg, NEVER echoed, NEVER written into any
    committed artifact.
- If a writable HOME is needed: the minimal fix mirrors the macOS `/private/var/folders`
    re-allow — add a `--bind <writable-home-or-cache> <same>` (or `--tmpfs` for the cache
    subdir claude writes to) to build_linux_bwrap_args, keeping `--ro-bind / /` as the base.
    Update the golden test in tests/test_sandbox.py that asserts the builder's exact arg list.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human provides a working file-based credential for the throwaway container</name>
  <action>PAUSE for the human to place a working `~/.claude/.credentials.json` (0600) that Task 2 will mount READ-ONLY into the throwaway container. This is a checkpoint, not autonomous work — Claude cannot mint the credential. Do not proceed to the container probe until the human confirms the credential is in place.</action>
  <what-built>The verification harness (Docker recipe from 23-SPIKE-LINUX.md) is ready to run a confined `claude --print` under the exact shipped bwrap argv, but it needs a real, working credential to prove auth survives.</what-built>
  <how-to-verify>
    Provide a working `~/.claude/.credentials.json` (0600) to be mounted READ-ONLY into the ubuntu:24.04 `--rm` container (`docker run -v $HOME/.claude/.credentials.json:/root/.claude/.credentials.json:ro ...`). This is the FILE-based credential (the production `bridge.py` default and the WR-03 concern), NOT the token-path env shortcut the Phase-23 spike used.
    Constraints (NON-NEGOTIABLE): the credential value must NEVER be pasted into any committed file, task action, or command-line argument; mount it as a read-only file only; use a throwaway `--rm` container so nothing persists.
    Confirm the credential file exists and is readable, then approve to run the container probe.
  </how-to-verify>
  <resume-signal>Type "approved" once the credential file is in place and ready to mount, or describe a blocker.</resume-signal>
  <read_first>
    - .planning/phases/25-confinement-verification/25-CONTEXT.md (the human-gated credential note: prefer the FILE-based path, NEVER commit the value)
    - .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md (Section 1 environment + Section 3 auth-preservation — the recipe to reuse)
  </read_first>
  <acceptance_criteria>
    - A working `~/.claude/.credentials.json` (0600) is present and confirmed by the human.
    - The credential is mounted read-only; no value is written into any file under version control.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the shared Docker probe — D-02 exact-argv/file-cred auth + D-03 denials</name>
  <files>flowstate/sandbox.py, tests/test_sandbox.py</files>
  <read_first>
    - .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md (the full Docker recipe: ubuntu:24.04 --rm, claude npm-fallback install, Landlock/bwrap versions, the write-inside/write-outside denial checks in Section 2)
    - flowstate/sandbox.py (build_linux_bwrap_args 219-248 — use these EXACT args; _wrap_linux 499-559 for how the binary/`--`/cmd are composed; build_macos_profile's /private/var/folders re-allow 207-216 as the analog if a writable HOME is needed)
    - .planning/phases/25-confinement-verification/25-CONTEXT.md (D-02 verify-first: exact shipped argv + file credential, read-only HOME; add writable HOME ONLY if claude fails for a filesystem-write reason; D-03 denial assertions)
    - tests/test_sandbox.py (the existing golden test asserting build_linux_bwrap_args' exact output — must be updated IF and ONLY IF the builder changes)
  </read_first>
  <action>
    Build the confined argv by composing `_find_bwrap()` + the EXACT `build_linux_bwrap_args(project_root)` output + `["--", "claude", "--print", "reply with only the digit 4"]` (mirror the spike's minimal prompt). Run it inside a throwaway `ubuntu:24.04 --rm` container per the 23-SPIKE-LINUX.md recipe (install claude via the npm fallback), with the human-provided credential mounted READ-ONLY at `/root/.claude/.credentials.json` and HOME left read-only under `--ro-bind / /` (do NOT pass `--setenv HOME /tmp/chome`). Capture: (a) the confined claude exit code + stdout (auth/API proof); (b) a confined write to a path OUTSIDE project_root is denied (assert the file is absent / nonzero exit, capturing the EACCES/EROFS shape like the spike); (c) a confined read of `~/.ssh` is denied (the `--tmpfs <home>/.ssh` shadow). Save raw container output to the scratchpad for transcription into the artifact. DECISION GATE: if confined claude exit 0 with real output, the shipped profile ships UNCHANGED (do NOT touch build_linux_bwrap_args). ONLY if claude fails for a filesystem-write reason (e.g. it cannot write a config/cache file under the read-only HOME), add the MINIMAL bound-writable HOME/cache dir to build_linux_bwrap_args (mirroring the macOS /private/var/folders re-allow), update the golden test in tests/test_sandbox.py to match the new exact arg list, and RE-RUN the container probe to confirm the fix. Never loosen beyond the minimum the failure demands (no `--ro-bind / /` removal, no broad `--bind /`). Record which branch fired for Task 3.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_sandbox.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The container ran the confined claude with `_find_bwrap()` + the shipped `build_linux_bwrap_args` argv (read-only HOME) + the file-mounted credential; the exit code and stdout are captured.
    - The confined out-of-root write is denied (file absent / nonzero exit; EACCES or EROFS shape captured) and the `~/.ssh` read is denied.
    - IF the builder was unchanged: `tests/test_sandbox.py` golden assertion still passes unmodified. IF a writable HOME was added: the golden test is updated to the new exact arg list and passes, and the change is the minimal bind the failure required.
    - `python -m pytest tests/test_sandbox.py -q` exits 0.
    - No credential value is present in any scratchpad transcript that will be committed (redact before use in Task 3).
  </acceptance_criteria>
  <done>The exact shipped Linux confine argv + file credential is empirically proven to preserve auth (or the minimal writable-HOME fix is applied + re-verified), and the D-03 denials are captured — all in one container run.</done>
</task>

<task type="auto">
  <name>Task 3: Write the committed 25-SPIKE-LINUX-REPROBE.md verification artifact</name>
  <files>.planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md</files>
  <read_first>
    - .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md (1-20 frontmatter + section structure to mirror: Environment / Mechanism / Auth-preservation / Verdict)
    - .planning/phases/25-confinement-verification/25-CONTEXT.md (D-02/D-03 — what the artifact must record)
    - the Task-2 captured container output (from scratchpad — redacted of any credential value)
  </read_first>
  <action>
    Write the artifact mirroring 23-SPIKE-LINUX.md's frontmatter (status: complete, phase: 25-confinement-verification, plan: 04, requirement: SBX-05, verdict set to PROVEN or FIX-APPLIED, date) and section shape. Record: (1) Environment (container image, kernel, Landlock ABI, bwrap/claude versions — reuse the 23 values if identical, else the observed ones); (2) the EXACT build_linux_bwrap_args argv used (read-only HOME, no --setenv HOME shortcut) and the FILE credential path (mounted read-only — state explicitly that the value was never committed); (3) the confined claude result (exit code, real stdout digit); (4) the D-03 denial results (out-of-root write denied with its EACCES/EROFS errno, ~/.ssh read denied); (5) the DECISION outcome — either "shipped profile unchanged, WR-03 closed as-is" OR "minimal bound-writable HOME added to build_linux_bwrap_args (with the exact arg added and why), re-verified PASS". Explicitly note this artifact closes the two WR-03 caveats from 23-SPIKE-LINUX.md Section 5 (writable HOME + file-path credential). Do NOT include any credential value.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && test -f .planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md && grep -q "build_linux_bwrap_args" .planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The artifact exists, has 23-SPIKE-LINUX.md-style frontmatter (status/phase/plan/requirement/verdict/date), and names `build_linux_bwrap_args`.
    - It records the confined claude exit code + output, the out-of-root-write denial (with errno), and the ~/.ssh-read denial.
    - It states the DECISION outcome (profile unchanged, or minimal writable-HOME added + re-verified) and explicitly closes the two 23-SPIKE-LINUX.md WR-03 caveats.
    - A scan for credential-shaped strings (`grep -riE "sk-ant|oauth|Bearer |sessionKey"`) finds NO credential value in the artifact.
  </acceptance_criteria>
  <done>A committed Linux verification artifact records the WR-03 re-probe outcome and the D-03 denials, closing both 23-spike caveats with no credential value committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined child (Linux) -> host filesystem | The bwrap+landlock profile must deny writes outside project_root and reads of ~/.ssh |
| credential file -> container -> committed artifact | The real credential must reach the confined claude but NEVER land in version control |
| container npm install -> confined claude | claude-code is installed into a throwaway container from public npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Elevation of Privilege | Confined Linux process escaping the bwrap profile (write outside root / read ~/.ssh) | mitigate | D-03 denial assertions in the shared container run capture EACCES/EROFS for the out-of-root write and confirm the `--tmpfs ~/.ssh` shadow denies the read. |
| T-25-10 | Information Disclosure | Credential value leaking into the committed verification artifact | mitigate | Human-gated file credential mounted read-only; NEVER pasted/echoed/committed; Task-3 acceptance greps for credential-shaped strings and must find none. |
| T-25-11 | Spoofing | Read-only HOME breaking file-based auth (false GAP or false PASS) | mitigate | D-02 verify-first with the EXACT shipped argv + the real file credential; a filesystem-write failure triggers the minimal writable-HOME fix + re-verify, not a speculative loosening. |
| T-25-SC | Tampering | npm install of @anthropic-ai/claude-code into the throwaway container | accept | Ephemeral `ubuntu:24.04 --rm` container, no project dependency added (mirrors 23 threat register T-23-SC); public package from public npm; nothing persists to the repo. |
</threat_model>

<verification>
- `python -m pytest tests/test_sandbox.py -q` exits 0 (golden builder test passes — unchanged, or updated to the minimal writable-HOME arg list).
- `.planning/phases/25-confinement-verification/25-SPIKE-LINUX-REPROBE.md` exists, mirrors the 23-SPIKE-LINUX.md structure, and contains no credential value.
- If the builder changed: `ruff check flowstate/sandbox.py tests/test_sandbox.py` clean; full suite + coverage gate `python -m pytest --cov=flowstate --cov-fail-under=80 -q` at phase close.
</verification>

<success_criteria>
- SBX-05 Linux half proven: the EXACT shipped `build_linux_bwrap_args` argv + the FILE credential preserve confined `claude` auth (or the minimal writable-HOME fix is applied and re-verified), with out-of-root writes and ~/.ssh reads denied — recorded in a committed artifact.
- The two WR-03 caveats from 23-SPIKE-LINUX.md (writable HOME, file-path credential) are closed.
- No credential value is committed anywhere.
</success_criteria>

<output>
Create `.planning/phases/25-confinement-verification/25-04-SUMMARY.md` when done.
</output>
