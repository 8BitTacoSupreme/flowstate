---
phase: 25-confinement-verification
plan: 03
type: execute
wave: 2
depends_on: [25-02]
files_modified:
  - tests/test_sandbox_e2e_macos.py
autonomous: true
requirements: [SBX-05]

must_haves:
  truths:
    - "On macOS, a confined process (sandbox-exec under the shipped build_macos_profile) can WRITE inside project_root"
    - "On macOS, a confined process is DENIED a write outside project_root (e.g. under $HOME)"
    - "On macOS, a confined process is DENIED a read of ~/.ssh"
    - "On macOS with claude available, a confined claude --print succeeds (Keychain auth survives, API reachable)"
    - "The test SKIPS cleanly on non-darwin platforms and when claude is unavailable (auth subcheck only)"
  artifacts:
    - path: "tests/test_sandbox_e2e_macos.py"
      provides: "skip-if-not-darwin real-subprocess denial E2E for the macOS confine profile"
      contains: "skipif"
  key_links:
    - from: "tests/test_sandbox_e2e_macos.py"
      to: "flowstate.sandbox.wrap(..., tier='confine')"
      via: "real sandbox-exec + subprocess.run asserting allow-inside / deny-outside / deny-~/.ssh"
      pattern: "tier=.confine."
---

<objective>
Prove SBX-05 on macOS with a real, CI-runnable test. Add `tests/test_sandbox_e2e_macos.py` — a `skip-if-not-darwin` pytest that exercises the ACTUAL shipped confine path (`wrap(..., tier="confine")` -> real `sandbox-exec` under `build_macos_profile`) and asserts: a write inside `project_root` succeeds, a write outside `project_root` (under `$HOME`) is DENIED, and a read of `~/.ssh` is DENIED. Plus a claude-availability-gated subcheck that a confined `claude --print` succeeds (Keychain auth survives, API reachable) via `ClaudeBridge(sandbox="confine")`, proving the production cleanup path from 25-02.

Purpose: The macOS half of "E2E-proven" must be real (not mocked) and CI-runnable on macOS runners. The deterministic denial assertions need no credential; the auth subcheck is gated on claude being present so the file stays green on the dev machine and skips elsewhere.
Output: One committed test file proving the macOS confine profile denies escape and preserves auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-confinement-verification/25-CONTEXT.md
@.planning/phases/25-confinement-verification/25-PATTERNS.md
@flowstate/sandbox.py
@flowstate/bridge.py
@tests/test_discipline.py

<interfaces>
<!-- Extracted — the executor reads these; no exploration needed. -->
- flowstate.sandbox.wrap(cmd, "llm", project_root, env, tier="confine") on darwin returns
    `[sandbox-exec, "-f", <profile_path>, *cmd]` — build_macos_profile ships:
      (allow default); (deny file-write*) re-allowing project_root / /private/tmp /
      /private/var/folders / /dev; (deny file-read* (subpath ~/.ssh)).
- The test must UNLINK the returned argv[2] profile path in a finally after each direct
    wrap()+subprocess.run (25-02 wires this for the bridge path; the direct-wrap denial
    checks own their cleanup).
- ClaudeBridge(BridgeConfig(sandbox="confine", project_root=..., claude_bin=...)).run("...")
    is the production path for the auth subcheck (exercises 25-02's cleanup).
- Skip-gate pattern (copy from tests/test_discipline.py:1-24, 124): module-level
    `_not_darwin = sys.platform != "darwin"`; `@pytest.mark.skipif(_not_darwin, reason=...)`;
    a `_REAL_RUN = subprocess.run` captured before any monkeypatch. For the auth subcheck:
    `_claude_missing = shutil.which("claude") is None and not os.environ.get("FLOWSTATE_CLAUDE_BIN")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: macOS confine denial E2E — allow-inside / deny-outside / deny-~/.ssh</name>
  <files>tests/test_sandbox_e2e_macos.py</files>
  <read_first>
    - tests/test_discipline.py (1-24 and the @pytest.mark.skipif usage at 124/184 — the binary/platform-gated real-subprocess skip pattern to translate)
    - flowstate/sandbox.py (wrap 138-170, build_macos_profile 191-216, _wrap_macos 470-493 — the exact profile shape and returned argv the test asserts against)
    - .planning/phases/25-confinement-verification/25-CONTEXT.md (D-03 macOS spec + <specifics>: confined write to $PROJECT OK, write to $HOME denied, read ~/.ssh denied)
    - .planning/phases/25-confinement-verification/25-PATTERNS.md (test_sandbox_e2e_macos.py section: the skip-gate translation and the WR-09-cleanup-first note)
  </read_first>
  <action>
    Create `tests/test_sandbox_e2e_macos.py`. Module-level `_not_darwin = sys.platform != "darwin"` and `@pytest.mark.skipif(_not_darwin, reason="macOS sandbox-exec only")` on every test. Capture `_REAL_RUN = subprocess.run` at import. For each denial assertion: call `flowstate.sandbox.wrap(["/bin/sh", "-c", <script>], "llm", project_root, os.environ.copy(), tier="confine")` where project_root is a tmp_path dir, then run the returned argv via `_REAL_RUN(..., capture_output=True)`, and in a `finally` unlink `argv[2]` (the temp .sb profile). Assertions: (1) a write to `<project_root>/inside.txt` exits 0; (2) a write to `<HOME>/flowstate_sbx_escape.txt` (outside project_root) exits NON-zero / is denied — assert the file was NOT created; (3) a `cat ~/.ssh/<anything>` (or a read probe of `~/.ssh`) is denied — assert non-zero exit / no leaked content. Use `/private/tmp`-based tmp_path if pytest's tmp_path is not already under an allowed subpath (build_macos_profile re-allows /private/tmp and /private/var/folders — pytest tmp_path on macOS is under /private/var/folders, which is allowed; the escape target must be OUTSIDE those, e.g. directly under $HOME). Do not assert on exact errno; assert on the observable outcome (file absent / nonzero exit) per the spike's broad-catch precedent.
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_sandbox_e2e_macos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with `@pytest.mark.skipif(_not_darwin, ...)` on the tests; on non-darwin CI the tests SKIP (not fail).
    - On darwin: a confined write inside `project_root` succeeds (exit 0, file present).
    - On darwin: a confined write to a path under `$HOME` outside `project_root` is denied — the escape file is asserted absent.
    - On darwin: a confined read of `~/.ssh` is denied (nonzero exit, no content leaked).
    - Each direct wrap()+run path unlinks its temp `.sb` profile in a `finally` (no leak from the test itself).
    - `python -m pytest tests/test_sandbox_e2e_macos.py -q` exits 0 on the darwin dev machine.
  </acceptance_criteria>
  <done>The macOS confine profile is proven to allow project-root writes while denying out-of-root writes and ~/.ssh reads, via real sandbox-exec, skipping cleanly off-darwin.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-survival subcheck — confined claude --print succeeds (Keychain preserved)</name>
  <files>tests/test_sandbox_e2e_macos.py</files>
  <read_first>
    - flowstate/bridge.py (run() and the wrap() call at 309 + the 25-02 temp-profile cleanup — this test drives the production path)
    - .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md (§3 — the "reply with only the digits 4" minimal auth probe shape to mirror)
    - .planning/phases/25-confinement-verification/25-CONTEXT.md (D-03: "claude auth (Keychain) survives" is the macOS auth assertion)
  </read_first>
  <action>
    Add a claude-availability-gated test to the same file: `_claude_missing = shutil.which("claude") is None and not os.environ.get("FLOWSTATE_CLAUDE_BIN")`; decorate with BOTH `@pytest.mark.skipif(_not_darwin, ...)` and `@pytest.mark.skipif(_claude_missing, reason="claude CLI/auth not available")`. Construct `ClaudeBridge(BridgeConfig(sandbox="confine", project_root=<tmp under /private/tmp or a real project dir>))` and call `.run("reply with only the digit 4")` with a small `max_turns`. Assert the result succeeds (`result.success is True`, exit 0) and the output contains a real model response — proving Keychain auth survived confinement and the API was reachable. This routes through 25-02's bridge cleanup, so it also implicitly exercises the no-leak path. Keep the prompt minimal to bound cost. If the maintainer prefers to bound cost further, mark it with a `pytest.mark` that CI can opt into — but default it to run when claude is available on darwin (the dev-machine green-path).
  </action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -m pytest tests/test_sandbox_e2e_macos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The auth test is double-gated: skips on non-darwin AND when claude is unavailable.
    - When it runs, it asserts `result.success is True` for a confined `ClaudeBridge(sandbox="confine").run(...)` call.
    - The assertion proves auth survived (nonzero-length real output, not an error string).
    - `python -m pytest tests/test_sandbox_e2e_macos.py -q` exits 0 (runs or skips, never errors).
  </acceptance_criteria>
  <done>A confined claude --print is proven to authenticate via Keychain and reach the API through the production bridge path, gated to skip when claude is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confined child -> host filesystem | The sandboxed process must not write outside project_root or read ~/.ssh |
| confined child -> Anthropic API + Keychain | Auth material must remain reachable through the confinement (allow-default baseline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Elevation of Privilege | Confined process escaping the SBPL profile (write outside root / read ~/.ssh) | mitigate | D-03 macOS E2E asserts the escape write is absent and the ~/.ssh read is denied via REAL sandbox-exec — a profile regression fails the test. |
| T-25-07 | Spoofing/Denial | A deny-default profile breaking Keychain auth (false confinement) | mitigate | The auth subcheck runs a real confined `claude --print` and asserts success — proving the allow-default+selective-deny baseline preserves auth. |
| T-25-08 | Repudiation | Test silently passing by mocking sandbox-exec away | accept/mitigate | Test uses `_REAL_RUN` real subprocess + real sandbox-exec, skip-gated (not mocked) off-darwin; a skip is visible in the report, not a false pass. |
</threat_model>

<verification>
- `python -m pytest tests/test_sandbox_e2e_macos.py -q` exits 0 on the darwin dev machine (denial tests run; auth test runs if claude available).
- On a non-darwin runner the file's tests SKIP cleanly.
- `ruff check tests/test_sandbox_e2e_macos.py` clean.
- No `.sb` temp file remains after the run (direct-wrap tests unlink in finally; bridge path cleaned by 25-02).
</verification>

<success_criteria>
- SBX-05 macOS half proven: real confined write-inside succeeds; write-outside-root and ~/.ssh-read denied; confined claude auth survives.
- The test is CI-runnable on macOS and skips cleanly elsewhere / when claude is absent.
</success_criteria>

<output>
Create `.planning/phases/25-confinement-verification/25-03-SUMMARY.md` when done.
</output>
