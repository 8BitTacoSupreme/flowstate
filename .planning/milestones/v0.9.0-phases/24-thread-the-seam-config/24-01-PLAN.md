---
phase: 24-thread-the-seam-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flowstate/state.py
  - flowstate/bridge.py
  - flowstate/orchestrator.py
  - flowstate/distiller.py
  - tests/test_state.py
  - tests/test_bridge.py
  - tests/test_distiller.py
autonomous: true
requirements: [SBX-03, SBX-04]

must_haves:
  truths:
    - "ProjectPreferences().sandbox == 'observe' by default and an old flowstate.json with no sandbox key loads without a migration bump"
    - "bridge.py's claude --print call routes through wrap() and the passed env still carries every _AUTH_EXEMPT var (claude auth survives)"
    - "bridge.py's env prep ordering is preserved: CLAUDECODE stays popped and ENABLE_PROMPT_CACHING_1H stays set after wrap()'s scrub"
    - "distiller.py's claude densify call routes through wrap() at surface 'llm' with claude auth preserved"
    - "The sandbox level flows ProjectPreferences.sandbox -> BridgeConfig.sandbox -> wrap(tier=...) with no default other than observe"
  artifacts:
    - path: "flowstate/state.py"
      provides: "ProjectPreferences.sandbox defaulted field"
      contains: "sandbox"
    - path: "flowstate/bridge.py"
      provides: "BridgeConfig.sandbox field + wrap() call at the llm site"
      contains: "wrap("
    - path: "flowstate/orchestrator.py"
      provides: "_make_bridge maps preferences.sandbox into BridgeConfig"
      contains: "sandbox"
    - path: "flowstate/distiller.py"
      provides: "wrap() call at the llm densify site"
      contains: "wrap("
  key_links:
    - from: "flowstate/orchestrator.py:_make_bridge"
      to: "BridgeConfig.sandbox"
      via: "kwargs mapping mirroring enable_prompt_caching_1h at :115"
      pattern: "sandbox"
    - from: "flowstate/bridge.py:run"
      to: "flowstate.sandbox.wrap"
      via: "cmd, env = wrap(cmd, 'llm', project_root, env, tier=self.config.sandbox)"
      pattern: "wrap\\(.*llm"
    - from: "flowstate/distiller.py:_densify"
      to: "flowstate.sandbox.wrap"
      via: "cmd, env = wrap(cmd, 'llm', root, env, tier=tier)"
      pattern: "wrap\\(.*llm"
---

<objective>
Add the defaulted `ProjectPreferences.sandbox` config field (SBX-04) and thread it into the two auth-load-bearing `claude`-spawning sites (SBX-03): `bridge.py:308` and `distiller.py:92`, both at surface `"llm"`. Default `observe` makes env-scrub live by default; the plan proves claude auth (`_AUTH_EXEMPT`) survives the scrub on both sites.

Purpose: The Phase-23 `wrap()` seam ships but fires nowhere. This plan wires it into the highest-risk (auth-carrying) call sites first and establishes the config-threading spine every other wrapped site mirrors.
Output: `sandbox` field on `ProjectPreferences` + `BridgeConfig`, `_make_bridge` mapping, two `wrap()` calls, and regression + auth-survival tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-thread-the-seam-config/24-CONTEXT.md

<interfaces>
<!-- The wrap() contract from Phase 23 (flowstate/sandbox.py). Use directly; do not rebuild. -->
wrap(cmd: list[str], surface: str, project_root: Path, env: dict[str, str], *, tier: str = "observe") -> tuple[list[str], dict[str, str]]
  - tier "observe" (default): returns (cmd unchanged, _scrub_env(env)). Never spawns, never fails hard.
  - _AUTH_EXEMPT = {"ANTHROPIC_API_KEY", "CLAUDE_CODE_OAUTH_TOKEN", "CLAUDE_CONFIG_DIR"} always survive the scrub.
  - surface is descriptive-only under observe (ignored). Pass "llm" for claude-spawning sites.
  - unknown tier values fail safe to observe (WR-01).

Existing analogs to mirror:
  - orchestrator.py:114-115 threads preferences.enable_prompt_caching_1h -> BridgeConfig unconditionally. Mirror this exact pattern for sandbox.
  - state.py:48 ProjectPreferences.enable_prompt_caching_1h is a defaulted field added with zero _migrate_state change. sandbox follows suit.
  - bridge.py:300-304 env prep: env = {**os.environ} -> env.pop("CLAUDECODE", None) -> set ENABLE_PROMPT_CACHING_1H. wrap()'s scrub composes onto the END of this.
  - BridgeConfig.project_root (bridge.py:132) already exists — wrap()'s project_root arg needs no new plumbing at the llm site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the defaulted sandbox config field (SBX-04) + BridgeConfig field + _make_bridge mapping</name>
  <files>flowstate/state.py, flowstate/bridge.py, flowstate/orchestrator.py, tests/test_state.py</files>
  <read_first>
    - flowstate/state.py:38-51 (ProjectPreferences; enable_prompt_caching_1h at :48 is the defaulted-field analog) and :215-227 (load_state / migration gate — the sandbox field must NOT touch this)
    - flowstate/bridge.py:129-144 (BridgeConfig dataclass; project_root at :132, enable_prompt_caching_1h at :140)
    - flowstate/orchestrator.py:105-117 (_make_bridge; the :114-115 unconditional enable_prompt_caching_1h mapping is the exact pattern to mirror)
    - tests/test_state.py (existing backward-compat load tests to extend)
  </read_first>
  <behavior>
    - ProjectPreferences().sandbox == "observe"
    - FlowStateModel.model_validate on a dict with no "sandbox" key (an old preferences blob) yields sandbox == "observe" with no exception and no version bump
    - BridgeConfig().sandbox == "observe"
    - _make_bridge(root, dry_run, preferences) with preferences.sandbox == "confine" produces a BridgeConfig whose .sandbox == "confine"
  </behavior>
  <action>Add `sandbox: str = "observe"` to `ProjectPreferences` in state.py (D-03/D-04), immediately after the `wiki_layer` field, with a one-line comment noting values `observe`/`confine`, default `observe`, and that it is a defaulted field so no `_migrate_state` change is required (backward-compatible load). Do NOT modify `_migrate_state` or `load_state`. Add `sandbox: str = "observe"` to `BridgeConfig` in bridge.py directly after `enable_prompt_caching_1h`. In orchestrator.py `_make_bridge`, add one unconditional kwargs line mirroring `:115` — `kwargs["sandbox"] = preferences.sandbox` inside the `if preferences:` block — so the level flows ProjectPreferences -> BridgeConfig. Do not add validation/enum types; `wrap()` already fails unknown values safe to observe (WR-01).</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && python -c "from flowstate.state import ProjectPreferences, FlowStateModel; assert ProjectPreferences().sandbox=='observe'; assert FlowStateModel.model_validate({'preferences':{'project_name':'x'}}).preferences.sandbox=='observe'" && python -c "from flowstate.bridge import BridgeConfig; assert BridgeConfig().sandbox=='observe'" && python -m pytest tests/test_state.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sandbox" flowstate/state.py` shows the defaulted field; `git diff flowstate/state.py` shows NO change to `_migrate_state`/`load_state`
    - `ProjectPreferences().sandbox == "observe"` and an old blob without the key loads to `"observe"`
    - `BridgeConfig().sandbox == "observe"`; `_make_bridge` mapping present (`grep -n "kwargs\[.sandbox.\]" flowstate/orchestrator.py`)
    - `python -m pytest tests/test_state.py` exits 0
  </acceptance_criteria>
  <done>ProjectPreferences and BridgeConfig both carry a defaulted `sandbox` field; `_make_bridge` threads it; no migration touched; state tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap the bridge.py:308 claude call at surface "llm" (auth + env-ordering preserved)</name>
  <files>flowstate/bridge.py, tests/test_bridge.py</files>
  <read_first>
    - flowstate/bridge.py:299-315 (env prep at :300-304 then subprocess.run at :308 — the exact insertion point) and :226-235 (run() signature; self.config.project_root and self.config.sandbox available)
    - flowstate/sandbox.py:105-170 (_scrub_env denylist, _AUTH_EXEMPT at :102, wrap() contract)
    - tests/test_bridge.py (existing bridge tests + how subprocess.run is patched/asserted)
  </read_first>
  <behavior>
    - The env dict passed to subprocess.run at the llm site is the scrubbed env from wrap(); it retains all three _AUTH_EXEMPT vars when they are present in os.environ
    - CLAUDECODE is absent from the passed env (popped pre-wrap, never re-added)
    - ENABLE_PROMPT_CACHING_1H == "1" survives in the passed env when enable_prompt_caching_1h is True (not credential-shaped, not denied)
    - A credential-shaped var (e.g. AWS_SECRET_ACCESS_KEY) present in os.environ is absent from the passed env
    - Existing bridge output/behavior is unchanged (text-mode and json-mode results byte-identical to before)
  </behavior>
  <action>In bridge.py `run()`, import `wrap` from `flowstate.sandbox` at module top. After the existing env prep block (the `env = {**os.environ}` / `env.pop("CLAUDECODE", None)` / `enable_prompt_caching_1h` set at :300-304) and BEFORE the `subprocess.run` at :308, insert one line: `cmd, env = wrap(cmd, "llm", self.config.project_root, env, tier=self.config.sandbox)`. This preserves the canonical ordering (scrub composes onto the end of the existing env mutation) per Claude's Discretion in CONTEXT.md. Do not otherwise change the subprocess.run call, the json-parse path, or the return shape — this is a ~2-line diff (import + wrap line). Surface literal is exactly `"llm"` (D-02).</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -n "wrap(cmd, \"llm\"" flowstate/bridge.py && python -m pytest tests/test_bridge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wrap(cmd, "llm"' flowstate/bridge.py` == 1
    - A test in tests/test_bridge.py sets ANTHROPIC_API_KEY + AWS_SECRET_ACCESS_KEY + CLAUDECODE + enable_prompt_caching_1h in the environment, captures the `env=` kwarg handed to a patched subprocess.run, and asserts: ANTHROPIC_API_KEY present, ENABLE_PROMPT_CACHING_1H=="1" present, CLAUDECODE absent, AWS_SECRET_ACCESS_KEY absent
    - `python -m pytest tests/test_bridge.py` exits 0
  </acceptance_criteria>
  <done>The claude --print site routes through wrap("llm"); auth vars survive, cache var survives, CLAUDECODE stays popped, credential-shaped vars are scrubbed; all bridge tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap the distiller.py:92 claude densify call at surface "llm" and thread the tier</name>
  <files>flowstate/distiller.py, tests/test_distiller.py</files>
  <read_first>
    - flowstate/distiller.py:79-97 (_densify: builds cmd at :81-90, subprocess.run at :92 with NO env kwarg — inherits os.environ) and :159-215 (distill main: root=args.root at :161, _densify called at :214)
    - flowstate/state.py:215 (load_state(root) -> FlowStateModel; .preferences.sandbox is the level source)
    - flowstate/sandbox.py wrap() contract (see interfaces block)
    - tests/test_distiller.py (existing densify tests + subprocess patching)
  </read_first>
  <behavior>
    - _densify passes an explicit scrubbed env (from wrap) to subprocess.run; _AUTH_EXEMPT vars survive so the distiller's claude call still authenticates
    - The tier reaches wrap(): default resolution is "observe"; when the project's saved preferences.sandbox is "confine", _densify receives tier="confine"
    - On any subprocess failure the function still returns the original article text unchanged (existing degradation contract intact)
  </behavior>
  <action>Add a `tier: str = "observe"` keyword-only parameter to `_densify` (after `model`) and a `root: Path` parameter it can pass to wrap() as project_root — pass `root` positionally from the single call site. Import `wrap` from `flowstate.sandbox`. Inside `_densify`, before `subprocess.run` at :92, build the explicit env and wrap: `cmd, env = wrap(cmd, "llm", root, {**os.environ}, tier=tier)`, then pass `env=env` to the existing `subprocess.run`. In `distill` main, resolve the level once from saved preferences — `tier = load_state(root).preferences.sandbox` (import `load_state` from `flowstate.state`) — and pass `root=root, tier=tier` into the `_densify(...)` call at :214. Keep the try/except degradation path untouched. Surface literal exactly `"llm"` (both claude sites share it, D-02).</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -n "wrap(cmd, \"llm\"" flowstate/distiller.py && python -m pytest tests/test_distiller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wrap(cmd, "llm"' flowstate/distiller.py` == 1
    - A test patches subprocess.run, calls `_densify(text, "claude", "opus", root=tmp_path, tier="observe")` with ANTHROPIC_API_KEY + AWS_SECRET_ACCESS_KEY set, and asserts the captured `env=` retains ANTHROPIC_API_KEY and drops AWS_SECRET_ACCESS_KEY
    - A test asserts distill main resolves tier from load_state(root).preferences.sandbox (defaults to "observe" on a fresh root)
    - `python -m pytest tests/test_distiller.py tests/test_bench_distiller.py` exits 0
  </acceptance_criteria>
  <done>The distiller's claude densify site routes through wrap("llm") with an explicit scrubbed env; auth survives; tier flows from saved preferences; degradation contract intact; distiller tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlowState process -> spawned `claude` subprocess | os.environ (incl. credentials) crosses into an agent-directed process that executes model-chosen actions |
| Saved flowstate.json -> BridgeConfig/distill tier | user-controlled config selects the confinement posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Denial of Service (availability regression, the D-04 concern) | bridge.py + distiller.py claude calls under default observe | mitigate | Task 2/3 assert (via captured `env=`) that only credential-shaped vars are dropped and all non-secret vars pass through; full test suites for both modules stay green — no legit var starved |
| T-24-02 | Spoofing/Elevation (auth loss) | _AUTH_EXEMPT coverage at BOTH claude-spawning sites | mitigate | Explicit tests at bridge.py AND distiller.py assert ANTHROPIC_API_KEY / CLAUDE_CODE_OAUTH_TOKEN / CLAUDE_CONFIG_DIR survive the scrub; macOS Keychain auth is env-independent so it is unaffected |
| T-24-03 | Tampering (env-ordering regression) | bridge.py env prep vs wrap scrub | mitigate | Task 2 test asserts CLAUDECODE stays popped and ENABLE_PROMPT_CACHING_1H stays set after wrap() — scrub composes onto, never reorders, the existing mutation |
| T-24-04 | Repudiation (silent behavior flip for existing users) | default observe going live on load | accept | Env-scrub is conservative (credential-shaped names only) and non-blocking by design (milestone integrity rule); the change is documented in the field comment and SUMMARY. No secret env has a legitimate consumer among these subprocesses |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure wiring against the already-shipped flowstate.sandbox); no legitimacy gate needed |
</threat_model>

<verification>
- `python -m pytest tests/test_state.py tests/test_bridge.py tests/test_distiller.py tests/test_bench_distiller.py -q` all green
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` stays above the 80% gate
- Both claude-spawning sites route through `wrap("llm", ...)`; auth-survival tests present for each
</verification>

<success_criteria>
- ProjectPreferences.sandbox and BridgeConfig.sandbox are defaulted `observe` fields; no `_migrate_state` change; backward-compat load proven
- bridge.py:308 and distiller.py:92 route through `wrap()` at surface `"llm"` with claude auth preserved and env-prep ordering intact
- SBX-04 satisfied; the SBX-03 auth-load-bearing subset (the two llm sites) satisfied
</success_criteria>

<output>
Create `.planning/phases/24-thread-the-seam-config/24-01-SUMMARY.md` when done (status: complete)
</output>
