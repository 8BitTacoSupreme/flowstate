---
phase: 24-thread-the-seam-config
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - flowstate/tools/base.py
  - flowstate/orchestrator.py
  - flowstate/pack.py
  - flowstate/gsd_vendor.py
  - flowstate/cli.py
  - flowstate/discipline.py
  - tests/test_tools.py
  - tests/test_pack.py
  - tests/test_gsd_vendor.py
  - tests/test_discipline.py
autonomous: true
requirements: [SBX-03]

must_haves:
  truths:
    - "ToolAdapter.run_cmd routes through wrap() at surface 'tool' with an explicit scrubbed env"
    - "pack.py's repomix call routes through wrap() at surface 'tool'"
    - "gsd_vendor.py's npm install AND node parity calls both route through wrap() at surface 'tool'"
    - "discipline.py's git-read + pytest sites are deliberately left bare with a visible code comment marking the SBX-03/D-01 exclusion decision"
    - "The full existing test suite stays green under default observe — no wrapped tool subprocess regresses (repomix/npm/node/adapter all still function)"
  artifacts:
    - path: "flowstate/tools/base.py"
      provides: "run_cmd wrap() call + ToolAdapter sandbox param"
      contains: "wrap("
    - path: "flowstate/pack.py"
      provides: "run_pack wrap() call at the repomix site"
      contains: "wrap("
    - path: "flowstate/gsd_vendor.py"
      provides: "two wrap() calls (npm install, node parity)"
      contains: "wrap("
    - path: "flowstate/discipline.py"
      provides: "deliberate bare-exclusion comment at the git-read sites"
      contains: "SBX-03"
  key_links:
    - from: "flowstate/orchestrator.py"
      to: "ToolAdapter.sandbox"
      via: "adapter construction passes sandbox=state.preferences.sandbox"
      pattern: "sandbox=.*preferences.sandbox"
    - from: "flowstate/pack.py:run_pack"
      to: "flowstate.sandbox.wrap"
      via: "cmd, env = wrap(cmd, 'tool', root, env, tier=sandbox)"
      pattern: "wrap\\(.*tool"
    - from: "flowstate/gsd_vendor.py:refresh"
      to: "flowstate.sandbox.wrap"
      via: "wrap at both subprocess sites with surface 'tool'"
      pattern: "wrap\\(.*tool"
---

<objective>
Thread `wrap()` into the remaining agent-directed "tool" subprocess sites (SBX-03): `tools/base.py:73` (adapter `run_cmd`), `pack.py:115` (repomix), and `gsd_vendor.py:325`/`:376` (npm install + node parity) — all at surface `"tool"`. Record the deliberate bare-exclusion of the `discipline.py` internal git/pytest reads (D-01) with a visible comment, and prove no wrapped tool subprocess regresses under default observe.

Purpose: Complete the site inventory so every subprocess carrying agent-directed or remote-fetched content runs through the confinement seam, while the internal read-only git commands stay bare by explicit decision, not omission.
Output: four `wrap("tool")` calls, a `sandbox` param threaded through each construction path, the discipline exclusion comment, and regression tests confirming repomix/npm/node/adapter all still function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-thread-the-seam-config/24-CONTEXT.md
@.planning/phases/24-thread-the-seam-config/24-01-PLAN.md

<interfaces>
<!-- wrap() contract (flowstate/sandbox.py, Phase 23). Use directly. -->
wrap(cmd, surface, project_root, env, *, tier="observe") -> (argv, env)
  - observe (default): (cmd unchanged, _scrub_env(env)); never spawns, never fails hard; surface descriptive-only.
  - Pass surface "tool" for repomix / npm / node / generic adapter run_cmd (D-02).

Threading source (added by plan 24-01):
  - ProjectPreferences.sandbox: str = "observe"   (flowstate/state.py)
  - load_state(root).preferences.sandbox           (flowstate/state.py:215) — the level for CLI-invoked sites.

Site construction paths:
  - tools/base.py:66 run_cmd(self, cmd) — has self.root. Currently NO live adapter caller (research/strategy use self.bridge.run), but D-01 mandates wrapping the generic method. subprocess.run at :73 has no env kwarg today.
  - pack.py:75 run_pack(root, *, compress) — has root; subprocess.run at :115, no env kwarg today. Callers: cli.py:146, cli.py:787, context_prefix.py:607.
  - gsd_vendor.py:244 refresh(version, *, ...) — npm install subprocess at :325 (cwd=scratch), node parity subprocess at :376 (cwd=parity_cwd or Path.cwd()). Caller: cli.py:883.
  - discipline.py:43/53/63 (_read_git_state git reads) + :92 (_run_project_tests pytest) — LEAVE BARE (D-01), internal read-only, no injection surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap ToolAdapter.run_cmd at surface "tool" + thread sandbox from the orchestrator</name>
  <files>flowstate/tools/base.py, flowstate/orchestrator.py, tests/test_tools.py</files>
  <read_first>
    - flowstate/tools/base.py:24-50 (ToolAdapter.__init__ signature + self.root) and :66-96 (run_cmd: subprocess.run at :73 with no env kwarg)
    - flowstate/orchestrator.py:263-292 (ResearchAdapter/StrategyAdapter construction — where sandbox=preferences.sandbox is passed)
    - flowstate/sandbox.py wrap() contract (see interfaces)
    - tests/test_tools.py / tests/test_tools_extended.py (existing run_cmd tests + subprocess patching)
  </read_first>
  <behavior>
    - ToolAdapter(root, sandbox="observe") stores self.sandbox; default is "observe"
    - run_cmd passes an explicit scrubbed env (from wrap "tool") to subprocess.run; a credential-shaped var is dropped, a benign var survives
    - The orchestrator constructs the research + strategy adapters with sandbox=state.preferences.sandbox
    - dry_run path is unchanged (still returns the "[dry-run] Would execute" result with no spawn)
  </behavior>
  <action>Add a keyword `sandbox: str = "observe"` parameter to `ToolAdapter.__init__` (after `prior_knowledge`) and store `self.sandbox = sandbox`. Import `wrap` from `flowstate.sandbox` in tools/base.py. In `run_cmd`, before the `subprocess.run` at :73, insert `cmd, env = wrap(cmd, "tool", self.root, {**os.environ}, tier=self.sandbox)` (add `import os` if absent) and pass `env=env` to the existing `subprocess.run`. Leave the dry_run branch, FileNotFoundError, and TimeoutExpired handlers untouched. In orchestrator.py, add `sandbox=state.preferences.sandbox` to the `ResearchAdapter(...)` and `StrategyAdapter(...)` constructor calls so the level flows to the adapter path. Surface literal exactly `"tool"` (D-02).</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -n "wrap(cmd, \"tool\"" flowstate/tools/base.py && grep -c "sandbox=state.preferences.sandbox" flowstate/orchestrator.py && python -m pytest tests/test_tools.py tests/test_tools_extended.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wrap(cmd, "tool"' flowstate/tools/base.py` == 1; `ToolAdapter(root).sandbox == "observe"`
    - `grep -c "sandbox=state.preferences.sandbox" flowstate/orchestrator.py` == 2
    - A test patches subprocess.run, calls run_cmd on an adapter with AWS_SECRET_ACCESS_KEY + PATH set, and asserts the captured `env=` drops AWS_SECRET_ACCESS_KEY and keeps PATH
    - `python -m pytest tests/test_tools.py tests/test_tools_extended.py tests/test_orchestrator.py` exits 0
  </acceptance_criteria>
  <done>run_cmd routes through wrap("tool") with an explicit scrubbed env; the sandbox level threads from preferences via the orchestrator; dry_run + error paths intact; tool tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap pack.py repomix + gsd_vendor.py npm/node sites at surface "tool"</name>
  <files>flowstate/pack.py, flowstate/gsd_vendor.py, flowstate/cli.py, tests/test_pack.py, tests/test_gsd_vendor.py</files>
  <read_first>
    - flowstate/pack.py:75-140 (run_pack signature at :75, subprocess.run at :115 with no env kwarg) and cli.py:126-146, cli.py:774-787 (run_pack callers)
    - flowstate/gsd_vendor.py:244-255 (refresh signature) + :311-339 (npm install subprocess at :325, cwd=scratch) + :366-399 (node parity subprocess at :376, cwd=parity_cwd or Path.cwd())
    - flowstate/cli.py:870-885 (gsd_vendor.refresh caller)
    - tests/test_pack.py, tests/test_gsd_vendor.py (existing subprocess patching)
  </read_first>
  <behavior>
    - run_pack(root, sandbox="observe") passes an explicit scrubbed env to the repomix subprocess.run; benign vars survive, credential-shaped vars dropped
    - refresh() wraps BOTH its subprocess sites (npm install, node parity) at surface "tool" at the DEFAULT observe tier (refresh is not project-scoped, so no preference is threaded)
    - Default is observe everywhere; existing pack/vendor success + failure result shapes are byte-identical to before
  </behavior>
  <action>Add a keyword `sandbox: str = "observe"` parameter to `run_pack` (pack.py) ONLY. Import `wrap` from `flowstate.sandbox` in both pack.py and gsd_vendor.py. In `run_pack`, before the repomix `subprocess.run` at :115, insert `cmd, env = wrap(cmd, "tool", root, {**os.environ}, tier=sandbox)` and pass `env=env`. In `gsd_vendor.refresh` — which is NOT project-scoped (its only caller, the `flowstate gsd-version --refresh` install/maintenance command in `gsd_version()`, has no `root` variable and never calls `resolve_root()`) — do NOT add a `sandbox` param and do NOT thread a preference; wrap both subprocess sites at the DEFAULT observe tier: at :325 insert `cmd, env = wrap(cmd, "tool", Path.cwd(), {**os.environ})` (pass `env=env`), and at :376 build the parity argv first, then `parity_cmd, parity_env = wrap([node_bin, str(gsd_sdk), "query", "roadmap.get-phase", str(parity_phase)], "tool", Path.cwd(), {**os.environ})` and pass `env=parity_env` to that subprocess.run. `observe` ignores `project_root`, so `Path.cwd()` is the correct placeholder. Add a one-line rationale comment in `refresh` stating its npm/node subprocess uses the default observe tier because the GSD-refresh path is not project-scoped. For the pack CLI callers, REUSE the `state` already loaded in scope (kickoff at cli.py:137, pack command at cli.py:778): pass `sandbox=state.preferences.sandbox` to `run_pack` at cli.py:146 and cli.py:787 — do NOT re-call `load_state`, matching how `wiki_layer`/`enable_prompt_caching_1h` are read. Make NO edit inside `gsd_version` (no change at ~cli.py:883). Leave the internal auto-pack caller in context_prefix.py:607 at the run_pack default (observe) — env-scrub still fires there; document that in the SUMMARY. Surface literal exactly `"tool"` at all three sites (D-02).</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -c "wrap(cmd, \"tool\"" flowstate/pack.py && grep -c 'wrap(' flowstate/gsd_vendor.py && python -m pytest tests/test_pack.py tests/test_gsd_vendor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wrap(' flowstate/pack.py` == 1 (surface "tool"); `grep -c 'wrap(' flowstate/gsd_vendor.py` == 2 (npm + node), both surface "tool" at the DEFAULT observe tier (no `sandbox` param added to `refresh`)
    - `git diff flowstate/cli.py` shows NO edit inside `gsd_version` (no change at ~cli.py:883); the two `run_pack` callers read `sandbox=state.preferences.sandbox` from the already-loaded `state` variable (no new `load_state` call)
    - A pack test asserts the repomix `env=` drops a credential-shaped var and keeps PATH; a vendor test asserts both npm and node subprocess calls receive a scrubbed `env=`
    - `python -m pytest tests/test_pack.py tests/test_gsd_vendor.py` exits 0
  </acceptance_criteria>
  <done>repomix, npm install, and node parity all route through wrap("tool") with explicit scrubbed envs; the pack level threads from the in-scope `state` at the CLI callers while gsd_vendor.refresh uses the default observe tier (not project-scoped); pack + vendor result shapes unchanged; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the discipline.py bare-exclusion (D-01) + full-suite no-regression proof (D-04)</name>
  <files>flowstate/discipline.py, tests/test_discipline.py</files>
  <read_first>
    - flowstate/discipline.py:33-107 (_read_git_state git reads at :43/:53/:63, _run_project_tests pytest at :92 — the deliberately-bare sites)
    - .planning/phases/24-thread-the-seam-config/24-CONTEXT.md D-01/D-04 + the milestone integrity rule (observe ships without breaking a single run)
    - tests/test_discipline.py (existing tests — confirm they still pass unchanged)
  </read_first>
  <action>CONTEXT D-01 lists FOUR bare sites (discipline.py:43/53/63/92), split across two functions ~50 lines apart, so mark BOTH physical locations. (1) Add a visible code comment above the `_read_git_state` git-read block stating the deliberate SBX-03/D-01 exclusion: these are internal read-only `git` commands with no agent-directed/untrusted input and no injection surface, so they are intentionally left bare — confining a read-only `git status` buys nothing and scrubbing `GIT_*` could only risk breaking them; a `vcs` profile is reserved for Phase 25 only if a real threat surfaces. Reference "SBX-03" and "D-01" in the comment. (2) Add a SECOND one-line comment directly above the `subprocess.run` at :92 in `_run_project_tests`, e.g. `# SBX-03/D-01: internal read-only command, deliberately left un-wrapped — see _read_git_state`, so the pytest site visibly carries the marker too and is not read as a forgotten omission. Change NO executable line in discipline.py — comments only. Then run the FULL test suite with coverage as the D-04 no-regression proof: every previously-wrapped subprocess (claude/repomix/npm/node/adapter) plus these bare git/pytest reads must still function under the default observe posture.</action>
  <verify>
    <automated>cd /Users/jhogan/frameworx && grep -n "SBX-03" flowstate/discipline.py && git diff --stat flowstate/discipline.py && python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SBX-03" flowstate/discipline.py` >= 2 — one marker above the `_read_git_state` git block (also naming D-01 + the read-only/no-injection rationale) AND a second marker directly above the pytest `subprocess.run` at :92; both physical locations carry it
    - `git diff flowstate/discipline.py` shows comment-only additions (no changed executable line — the git/pytest subprocess.run calls are untouched)
    - `python -m pytest tests/ --cov=flowstate --cov-fail-under=80` exits 0 (full suite green, coverage gate held) — the D-04 proof that no wrapped subprocess regresses under default observe
  </acceptance_criteria>
  <done>The discipline.py bare-git exclusion is a visible, sourced decision; the full suite passes at >=80% coverage, proving default observe ships without breaking a single existing run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlowState -> repomix subprocess | repomix ingests the WHOLE repo (agent-directed content surface) |
| FlowState -> npm/node subprocess | fetches + executes remote package content (gsd_vendor) |
| FlowState -> generic adapter run_cmd | arbitrary agent-chosen command |
| FlowState -> internal git/pytest | read-only, no untrusted input (deliberately bare) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Denial of Service (availability regression, D-04) | repomix/npm/node/run_cmd under default observe | mitigate | Task 3 runs the full suite at the 80% gate; per-site tests assert only credential-shaped vars drop and benign vars (PATH) survive — npm/node/repomix have no legitimate secret-env need |
| T-24-06 | Information Disclosure (missed site) | 8-site inventory completeness | mitigate | This plan wraps the 4 remaining "tool" sites; plan 24-01 wrapped the 2 "llm" sites; discipline.py's git/pytest are the ONLY unwrapped subprocess.run calls and are covered by the explicit D-01 bare-exclusion comment — inventory closed |
| T-24-07 | Elevation (remote package tampering) | gsd_vendor npm install | accept | Scope-fenced: confine profiles (real deny) are Phase 25; observe env-scrub reduces credential exposure to the npm process now. gsd_vendor already enforces pinned-semver + 10M ceiling + parity gate independently |
| T-24-08 | Tampering (VCS env stripped) | discipline.py git reads | accept | Deliberately NOT wrapped (D-01): scrubbing GIT_* off a read-only status/branch/count would be a pure availability risk with zero security benefit; recorded as a visible comment |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | This plan runs gsd_vendor's npm install only through existing tested paths and adds no new package deps to FlowState itself; no legitimacy gate needed |
</threat_model>

<verification>
- `python -m pytest tests/ --cov=flowstate --cov-fail-under=80 -q` green (the D-04 no-regression proof)
- All four "tool" sites route through `wrap("tool", ...)`; discipline.py git/pytest reads confirmed bare with a sourced comment
- No subprocess.run in flowstate/ carries agent-directed content while unwrapped except the documented discipline.py exclusion
</verification>

<success_criteria>
- tools/base.py:73, pack.py:115, gsd_vendor.py:325 and :376 all route through `wrap()` at surface `"tool"` with explicit scrubbed envs
- discipline.py git/pytest reads left bare with a visible SBX-03/D-01 exclusion comment
- Full suite green at >=80% coverage under default observe — SBX-03 site inventory complete
</success_criteria>

<output>
Create `.planning/phases/24-thread-the-seam-config/24-02-SUMMARY.md` when done (status: complete)
</output>
