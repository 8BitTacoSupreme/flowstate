---
phase: 23-linux-parity-core-seam
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md]
autonomous: false
requirements: [SBX-01]

user_setup:
  - service: claude-cli-oauth-token
    why: "The auth-preservation half of the spike needs a headless-usable claude credential inside the Linux container; minting an OAuth token via `claude setup-token` is an interactive browser flow (human-only)."
    env_vars:
      - name: CLAUDE_CODE_OAUTH_TOKEN
        source: "Run `claude setup-token` on the host (opens browser); paste the resulting token at the checkpoint"

must_haves:
  truths:
    - "The Landlock filesystem-confinement mechanism is demonstrated on a real Linux kernel (write allowed inside project root/tmp, denied outside)"
    - "The bwrap+landlock combined confinement is demonstrated on Linux"
    - "A confined `claude --print` on Linux either returns real model output (auth preserved) OR the parity gap is recorded with its consequence"
    - "The spike outcome is a COMMITTED artifact (23-SPIKE-LINUX.md), not a silent skip"
  artifacts:
    - path: ".planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md"
      provides: "recorded SBX-01 spike findings: environment, mechanism result, auth result, degradation ladder observed, VERDICT, consequence for phases 24-25"
      contains: "VERDICT"
  key_links:
    - from: ".planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md"
      to: "phases 24-25 planning"
      via: "records whether Linux confine ships or degrades to warned observe-only (D-03 asymmetric degrade)"
      pattern: "phase 2[45]"
---

<objective>
Retire (or honestly document) the milestone's gating unknown: does a Linux `bwrap`+landlock allow-default + selective-deny profile confine filesystem writes while preserving `claude` auth and API reachability, mirroring the passed macOS Seatbelt spike? Run the spike using the RESEARCH-verified Docker recipe, then record the outcome as a COMMITTED artifact (`23-SPIKE-LINUX.md`). Per SBX-01 and SEED-003, a FAILED spike is a valid recorded outcome that reshapes phases 24-25 (D-03 asymmetric degrade), NOT a blocker to hide.

Purpose: Phase 24 wires `bridge.py`'s auth-load-bearing `claude --print` through the seam; it must know whether Linux confine preserves auth before committing to cross-platform parity.
Output: `.planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md` — the recorded, committed spike finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-linux-parity-core-seam/23-CONTEXT.md
@.planning/phases/23-linux-parity-core-seam/23-RESEARCH.md
@.planning/seeds/SEED-003-sandbox-guardrail.md

<references>
- RESEARCH.md Code Examples: the verified ctypes Landlock script and the Docker/Lima spike-environment recipes (kernel 6.12 arm64, Landlock ABI v6, --privileged for the bwrap half).
- RESEARCH.md Open Questions #2 (recorded-artifact form) and #3 (file-path vs token-path auth proof).
- RESEARCH.md Anti-Patterns: catch OSError broadly for denied writes (EROFS from bwrap --ro-bind vs EACCES from Landlock).
- Memory flowstate-sandbox-spike: the passed macOS spike this Linux spike mirrors.
- Environment Availability table: Docker Desktop (confirmed) and Lima 2.0.3 (confirmed) both available on this machine.
</references>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the Landlock + bwrap filesystem-confinement mechanism spike (Docker)</name>
  <files>scratchpad/landlock_test.py, scratchpad/landlock_spike_output.txt</files>
  <read_first>
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Code Examples: the exact ctypes Landlock script + the Docker recipe; Environment Availability; Anti-Patterns OSError vs PermissionError for denied writes; Pitfalls 4 and 5)
    - .planning/seeds/SEED-003-sandbox-guardrail.md (the macOS spike result this mirrors; the network paradox)
  </read_first>
  <action>
    Write the verified ctypes Landlock script from RESEARCH Code Examples to the scratchpad as a .py file, then run the two mechanism checks via Docker (available per Environment Availability):
    (a) LANDLOCK-ONLY (no privilege): run ubuntu:24.04 with the scratchpad mounted, apt-get install python3, and execute the script. The script must ALLOW a write to /tmp and DENY a write to /root, asserting the denial catches OSError broadly (EACCES or EROFS, per Anti-Patterns), printing a clear ALLOW/DENY line for each.
    (b) BWRAP+LANDLOCK COMBINED (requires --privileged for CLONE_NEWUSER inside Docker): run ubuntu:24.04 --privileged, apt-get install bubblewrap python3, then invoke bwrap with the mount-namespace flags (--ro-bind / /, --bind /tmp /tmp, --dev /dev, --proc /proc, --unshare-pid/uts/ipc, --die-with-parent) executing the same script. Confirm write inside the bound project/tmp succeeds and outside is denied (EROFS from --ro-bind or EACCES from Landlock).
    Capture stdout/stderr, kernel version (uname -r), and the Landlock ABI to a scratchpad file (landlock_spike_output.txt) for the findings artifact. If Docker is unavailable or the bwrap half fails on nested seccomp, note it and fall back to the Lima recipe (limactl 2.0.3 confirmed) OR record the specific failure verbatim. A failure is a recordable outcome, do not fake a pass. Do NOT run the auth half here (that is Task 2's checkpoint).
  </action>
  <verify>
    <automated>test -f "$SCRATCHPAD/landlock_spike_output.txt" && grep -Eiq "allow|deny" "$SCRATCHPAD/landlock_spike_output.txt"</automated>
  </verify>
  <acceptance_criteria>
    - The ctypes Landlock script ran on a real Linux kernel (kernel version captured, >= 5.13)
    - Output shows a WRITE-ALLOWED result inside /tmp and a WRITE-DENIED result outside (or an explicit, captured failure if the mechanism did not confine)
    - The bwrap+landlock combined invocation ran (or its specific failure, e.g. nested seccomp, is captured verbatim)
    - Denial assertions caught OSError broadly (not narrowly PermissionError) per RESEARCH Anti-Patterns
    - Raw stdout/stderr + uname -r saved to the scratchpad for Task 3
  </acceptance_criteria>
  <done>The filesystem-confinement mechanism (Landlock alone, and bwrap+landlock combined) is demonstrated allow-inside / deny-outside on a real Linux kernel, with raw evidence captured, OR the specific failure is captured verbatim.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Auth-preservation checkpoint — provide a headless claude credential</name>
  <action>Pause for the human to mint and paste a headless `claude` credential (`claude setup-token` -> CLAUDE_CODE_OAUTH_TOKEN), then run the confined `claude --print` auth-reachability probe with the provided credential and capture the exit code + whether real model output returned. This is a checkpoint: the human action is minting the credential (browser flow Claude cannot perform); Claude automates the confined invocation once the credential is supplied.</action>
  <what-built>Task 1 proved the Linux filesystem-confinement MECHANISM (Landlock allow/deny + bwrap+landlock). The remaining SBX-01 unknown is AUTH PRESERVATION: does a confined `claude --print` still reach the API and authenticate on Linux?</what-built>
  <decision>The auth-preservation half needs a headless-usable `claude` credential inside a fresh Linux container. Minting one is an interactive browser flow (human-only). Provide the credential so Claude can run the confined invocation.</decision>
  <how-to-verify>
    1. On the host, run `claude setup-token` (opens a browser; produces a long-lived OAuth token, per RESEARCH A4 / Sources).
    2. Paste the token at this checkpoint (it will be passed as CLAUDE_CODE_OAUTH_TOKEN into the container env; note per RESEARCH it is deliberately in _AUTH_EXEMPT so the future observe scrub will not strip it).
    3. Claude then runs a confined `claude --print "reply with the digits 4"` inside the Docker Ubuntu env under the bwrap+landlock profile with the token in env, and reports exit code + whether REAL model output returned (auth preserved) vs a 401/timeout (gap).
    4. File-path caveat: the production default for subscription users is ~/.claude/.credentials.json (RESEARCH Q3). If minting a token is not feasible, instead report the result of running the confined `claude --print` on YOUR host with your real credentials (mirroring how the macOS spike was run ad-hoc), and Claude records that verbatim.
  </how-to-verify>
  <resume-signal>Paste the token OR paste the confined `claude --print` result (exit code + whether real model text returned), then type "recorded"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write and commit the 23-SPIKE-LINUX.md findings artifact</name>
  <files>.planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md</files>
  <read_first>
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Open Questions #2 recorded-artifact form; #1 the two-rung ladder; State of the Art degradation-ladder facts)
    - .planning/phases/23-linux-parity-core-seam/23-CONTEXT.md (D-02 kernel constraint, D-03 asymmetric degrade for the consequence framing)
    - the scratchpad evidence captured in Task 1 + the checkpoint result from Task 2
  </read_first>
  <action>
    Write .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md recording the SBX-01 outcome as a committed artifact. Required sections: (1) Environment (Docker/Lima, distro, uname -r, Landlock ABI, arch); (2) Mechanism result (Landlock allow/deny evidence + bwrap+landlock combined evidence, pasting the captured ALLOW/DENY lines); (3) Auth-preservation result (confined `claude --print` exit code + whether real model output returned = PASS, or the gap, with token-path vs file-path noted per RESEARCH Q3); (4) Degradation ladder observed (the D-03 two rungs: bwrap+landlock -> bwrap-only when kernel<5.13/landlock absent -> observe when bwrap unavailable/AppArmor-blocked, explicitly noting the Landlock-only rung is a REJECTED future refinement per Open Q #1); (5) VERDICT (one of: PARITY PROVEN = Linux confine ships in phase 25; or PARITY GAP = Linux confine degrades to warned observe-only per D-03 while macOS still ships); (6) Consequence for phases 24-25 (concrete: what Phase 24 wiring and Phase 25 confine must assume given the verdict). Add YAML frontmatter with status: complete (per project SUMMARY convention). Then commit it with git add + git commit -m "docs(23-linux-parity-core-seam): record SBX-01 Linux bwrap+landlock spike finding" (branch first if on main, per repo policy). NEVER write the OAuth token value into the artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md && grep -q "VERDICT" .planning/phases/23-linux-parity-core-seam/23-SPIKE-LINUX.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-SPIKE-LINUX.md exists with all six sections (Environment, Mechanism result, Auth-preservation result, Degradation ladder, VERDICT, Consequence for phases 24-25)
    - The VERDICT line states PARITY PROVEN or PARITY GAP unambiguously
    - The consequence section is concrete about what Phase 24 and Phase 25 must assume
    - The file carries `status:` frontmatter and is committed; no OAuth token value appears anywhere in the file
  </acceptance_criteria>
  <done>The Linux spike outcome is a committed markdown artifact with an unambiguous verdict and a concrete consequence for phases 24-25 (SBX-01 recorded-outcome requirement satisfied), with no secret leaked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host OAuth token -> Docker container env | a real `claude` credential crosses into a container to prove confined auth reachability |
| spike verdict -> phases 24-25 scope | a false PASS would ship an unproven Linux confine tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | Repudiation | spike silently skipped or a failure hidden, giving false confidence in Linux parity | mitigate | SBX-01 requires a recorded outcome; Task 3 commits 23-SPIKE-LINUX.md with an explicit VERDICT; a failure is recorded as PARITY GAP, never dropped. |
| T-23-13 | Information Disclosure | the minted OAuth token leaks into the committed artifact, logs, or shell history | mitigate | The token is passed only as a container env var at the checkpoint; the findings artifact records exit code + PASS/GAP, NEVER the token value; Task 3 acceptance asserts no token appears in the file. |
| T-23-14 | Tampering | mechanism "proof" is asserted with a too-narrow exception (PermissionError only) and misses an EROFS denial, faking a pass | mitigate | Denial assertions catch OSError broadly (RESEARCH Anti-Patterns); both allow and deny lines are captured as raw evidence. |
| T-23-SC | Tampering | apt-get installs inside the container (bubblewrap, python3) | accept | Installs occur inside a throwaway `--rm` container from the official ubuntu:24.04 image, not in the FlowState project env; no project dependency is added. |
</threat_model>

<verification>
- 23-SPIKE-LINUX.md exists, committed, with an explicit VERDICT and the six required sections.
- The mechanism evidence (allow/deny) and auth result are recorded from real runs, not asserted.
- No token or secret is present in the committed artifact.
</verification>

<success_criteria>
- The Linux bwrap+landlock filesystem-confinement mechanism is demonstrated on a real kernel.
- The confined-`claude` auth-preservation result is recorded (PASS or GAP).
- SBX-01 is satisfied: the outcome is a committed artifact with a verdict and a concrete phase-24/25 consequence.
</success_criteria>

<output>
Create `.planning/phases/23-linux-parity-core-seam/23-04-SUMMARY.md` when done (in addition to the committed 23-SPIKE-LINUX.md artifact).
</output>
