---
phase: 23-linux-parity-core-seam
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified: [flowstate/sandbox.py, tests/test_sandbox.py]
autonomous: true
requirements: [SBX-02]

must_haves:
  truths:
    - "check_bwrap_available runs a FUNCTIONAL smoke test (bwrap --ro-bind / / -- /bin/true), not a presence-only check"
    - "the Linux Landlock ctypes helper applies path-beneath rules via syscalls 444/445/446 with PR_SET_NO_NEW_PRIVS first"
    - "_wrap_linux implements the D-03 two-rung degradation ladder: bwrap+landlock -> bwrap-only -> observe, never raising"
    - "the Landlock-only (bwrap-absent) rung is explicitly REJECTED and recorded as a documented future refinement"
  artifacts:
    - path: "flowstate/sandbox.py"
      provides: "_apply_landlock, _landlock_available, check_bwrap_available, _wrap_linux degradation ladder"
      contains: "def _wrap_linux"
    - path: "tests/test_sandbox.py"
      provides: "degradation-ladder fallback tests (mocked availability) + never-raise guarantees"
      contains: "test_wrap_linux"
  key_links:
    - from: "flowstate/sandbox.py:_wrap_linux"
      to: "flowstate/sandbox.py:check_bwrap_available"
      via: "ladder consults the functional smoke test before choosing the bwrap rung"
      pattern: "check_bwrap_available\\("
    - from: "flowstate/sandbox.py:_wrap_linux"
      to: "flowstate/sandbox.py:build_linux_bwrap_args"
      via: "full/bwrap-only rungs prepend the located bwrap binary + args"
      pattern: "build_linux_bwrap_args\\("
---

<objective>
Complete the Linux confine path for SBX-02: the Landlock LSM ctypes helper (D-02's defense-in-depth layer), the FUNCTIONAL bwrap availability smoke test (not presence-only — RESEARCH Pitfall 3 / Ubuntu 24.04 AppArmor), the kernel/ABI Landlock availability probe, and the `_wrap_linux` degradation ladder wiring `wrap()`'s confine dispatch. Encodes the two open-question resolutions: (1) implement D-03's TWO rungs and explicitly REJECT the invented Landlock-only third rung; (4) availability is a functional smoke test.

These are BUILT and unit-tested here; nothing wires `_wrap_linux` into a live caller (Phase 24/25). The Landlock syscall body is Linux-only and behavior-verified against the SBX-01 spike (plan 23-04); on this Darwin dev machine it is import-guarded and coverage-excluded.
Output: `flowstate/sandbox.py` Linux confine path complete; `tests/test_sandbox.py` degradation-ladder tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-linux-parity-core-seam/23-CONTEXT.md
@.planning/phases/23-linux-parity-core-seam/23-RESEARCH.md
@.planning/phases/23-linux-parity-core-seam/23-PATTERNS.md

<interfaces>
<!-- From 23-01/23-02 (already in flowstate/sandbox.py). This plan implements the Linux path. -->
check_bwrap_available() -> bool                 # stub from 23-01 -> functional smoke test here
build_linux_bwrap_args(project_root) -> list[str]   # implemented in 23-02
_wrap_linux(cmd, project_root, env) -> tuple[list[str], dict[str, str]]   # stub from 23-01 -> ladder here

<!-- New in this plan: -->
_find_bwrap() -> str                            # FLOWSTATE_BWRAP_BIN > which("bwrap") > ""
_landlock_available() -> bool                   # kernel >= 5.13 AND landlock_create_ruleset version probe succeeds
_apply_landlock(project_root: Path) -> None     # Linux-only; applies path-beneath ruleset to the current process
</interfaces>

Verified ctypes Landlock recipe (RESEARCH Code Examples — copy the mechanism): syscalls NR_landlock_create_ruleset=444, NR_landlock_add_rule=445, NR_landlock_restrict_self=446 (identical x86_64/arm64); LANDLOCK_RULE_PATH_BENEATH=1; struct.pack("QQ", handled_access_fs, 0) for the ruleset attr; struct.pack("QiI", access, fd, 0) for the 16-byte-padded path_beneath attr (Pitfall 4); libc.prctl(38,1,0,0,0) BEFORE restrict_self (Pitfall 5, PR_SET_NO_NEW_PRIVS).
Functional smoke recipe (RESEARCH Pattern 3): shutil.which("bwrap") then subprocess.run(["bwrap","--ro-bind","/","/","--","/bin/true"], capture_output=True, timeout=5) returncode==0, except (OSError, TimeoutExpired) -> False.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landlock ctypes helper + kernel/ABI availability probe (Linux-only, import-guarded)</name>
  <files>flowstate/sandbox.py, tests/test_sandbox.py</files>
  <read_first>
    - flowstate/sandbox.py (the check_bwrap_available / _wrap_linux stubs; add _apply_landlock/_landlock_available)
    - tests/test_sandbox.py (extend)
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Code Examples: the verified ctypes Landlock ruleset; Common Pitfalls 4 (struct padding) and 5 (PR_SET_NO_NEW_PRIVS); Don't Hand-Roll: why ctypes is the LOCKED correct choice here, not a bug to "fix" with py-landlock)
    - .planning/phases/23-linux-parity-core-seam/23-PATTERNS.md (§4 the embeddings.py lazy-import/never-raise guard shape to mirror for the Linux-only path; "No Analog Found" note that RESEARCH's code example IS the pattern source)
    - flowstate/embeddings.py:99-124 (lazy-import + cached-unavailable-sentinel + never-raise pattern)
  </read_first>
  <action>
    Add `_landlock_access` constants exactly per RESEARCH (READ_ACCESS = (1<<0)|(1<<2)|(1<<3); WRITE_ACCESS = (1<<1)|(1<<4)|(1<<5)|(1<<8); FULL_ACCESS = READ|WRITE) and syscall numbers 444/445/446, LANDLOCK_RULE_PATH_BENEATH=1. Implement `_apply_landlock(project_root) -> None`: guard `if not sys.platform.startswith("linux"): return` FIRST; then via `ctypes.CDLL(None, use_errno=True)` create the ruleset (`struct.pack("QQ", FULL_ACCESS, 0)`), add path-beneath rules — FULL_ACCESS for `project_root` and `/tmp`, READ_ACCESS for `/usr /lib /bin /etc` and the `claude` credentials dir (`Path.home()/".claude"` so `.credentials.json` stays readable — RESEARCH auth model), open each via `os.open(path, os.O_PATH|os.O_CLOEXEC)`, pack attr with `struct.pack("QiI", access, fd, 0)` (16-byte, Pitfall 4), close fd — then `libc.prctl(38,1,0,0,0)` (Pitfall 5) BEFORE `libc.syscall(446, ruleset_fd, 0)`. Mark the syscall body `# pragma: no cover` (Linux-only, exercised in the 23-04 spike, not on this Darwin CI). Add a comment citing Don't Hand-Roll: this ctypes path is the LOCKED choice (D-02/D-04 + no-new-core-dependency) — a reviewer must NOT replace it with py-landlock. Implement `_landlock_available() -> bool`: return False on non-linux; else check `platform.release()` parses to >= (5,13) AND a best-effort `landlock_create_ruleset(NULL, 0, LANDLOCK_CREATE_RULESET_VERSION=1)` version probe returns > 0; wrap the whole probe in try/except returning False, never raise (mirror embeddings.py's cached-unavailable sentinel). Tests (run on Darwin): assert `_landlock_available()` returns False on darwin without raising, and `_apply_landlock(tmp_path)` is a no-op that returns None on darwin without raising.
  </action>
  <verify>
    <automated>python -m pytest tests/test_sandbox.py -k "landlock or Landlock" -x -q && ruff check flowstate/sandbox.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _apply_landlock" flowstate/sandbox.py` and `grep -q "def _landlock_available" flowstate/sandbox.py`
    - Syscall numbers present: `grep -c "44[456]" flowstate/sandbox.py` >= 3; `grep -q "prctl(38" flowstate/sandbox.py` (PR_SET_NO_NEW_PRIVS before restrict)
    - `python -c "import sys; from pathlib import Path; from flowstate.sandbox import _apply_landlock, _landlock_available; assert _landlock_available() is False; assert _apply_landlock(Path('/tmp')) is None"` exits 0 on this Darwin machine (both are no-ops here, never raise)
    - The ctypes syscall body carries `# pragma: no cover`; a comment cites the LOCKED ctypes decision (D-02/D-04, do-not-replace-with-py-landlock)
    - `python -m pytest tests/test_sandbox.py` exits 0; ruff clean
  </acceptance_criteria>
  <done>_apply_landlock applies the path-beneath ruleset via the verified ctypes recipe on Linux (no-op + never-raise off-Linux), and _landlock_available gates on kernel >= 5.13 plus a version probe, both matching the codebase's never-raise degradation style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bwrap functional smoke test + _wrap_linux two-rung degradation ladder</name>
  <files>flowstate/sandbox.py, tests/test_sandbox.py</files>
  <read_first>
    - flowstate/sandbox.py (implement check_bwrap_available + _wrap_linux; use _find_bwrap, build_linux_bwrap_args, _apply_landlock, _landlock_available)
    - tests/test_sandbox.py (extend)
    - .planning/phases/23-linux-parity-core-seam/23-RESEARCH.md (Architecture Patterns → Pattern 3 functional smoke test; Anti-Patterns → kernel-version-only gating is wrong; Open Questions #1 → the Landlock-only rung decision; CONTEXT D-03 two-rung ladder wording)
    - .planning/phases/23-linux-parity-core-seam/23-PATTERNS.md (test §3 graceful-degradation-never-raise test naming; §2 the check_bwrap_available smoke shape and FLOWSTATE_BWRAP_BIN locator)
    - .planning/phases/23-linux-parity-core-seam/23-CONTEXT.md (D-02 kernel constraint / D-03 asymmetric degrade)
  </read_first>
  <behavior>
    - check_bwrap_available() returns False when shutil.which("bwrap") is None (no smoke test attempted)
    - check_bwrap_available() runs `bwrap --ro-bind / / -- /bin/true` and returns True only on returncode 0; returns False on nonzero / OSError / TimeoutExpired (never raises) — RESEARCH Pitfall 3
    - RUNG 1 (mock check_bwrap_available->True, _landlock_available->True): _wrap_linux returns argv == [<bwrap>, *build_linux_bwrap_args(root), "--", *shim] where the shim self-invokes _apply_landlock before exec; env unchanged
    - RUNG 2 (mock check_bwrap_available->True, _landlock_available->False): _wrap_linux returns the bwrap-prefixed argv WITHOUT the landlock shim (bwrap-only); never raises
    - RUNG 3 (mock check_bwrap_available->False): _wrap_linux returns (cmd, env) unchanged (observe-only fallback) and emits a one-time stderr warning; never raises
    - REJECTED rung: bwrap-absent + landlock-present does NOT produce a landlock-only sandbox — it collapses to RUNG 3 (observe). A code comment records this as a documented future refinement (Open Q #1), not silently added.
  </behavior>
  <action>
    Add `_find_bwrap()` (FLOWSTATE_BWRAP_BIN > `shutil.which("bwrap")` > "") mirroring pack.py. Implement `check_bwrap_available()` exactly per RESEARCH Pattern 3: `shutil.which("bwrap") is None` -> False; else `subprocess.run(["bwrap","--ro-bind","/","/","--","/bin/true"], capture_output=True, timeout=5).returncode == 0`, catching `(OSError, subprocess.TimeoutExpired)` -> False. Remove its NotImplementedError/pragma. Implement `_wrap_linux(cmd, project_root, env)` as the D-03 TWO-rung ladder: if `check_bwrap_available()`: build `bwrap_prefix = [_find_bwrap(), *build_linux_bwrap_args(project_root), "--"]`; if `_landlock_available()` attach landlock by making the target a self-invoking shim so `_apply_landlock(project_root)` runs in the child before exec — express this as `[sys.executable, "-m", "flowstate.sandbox", "--apply-landlock", str(project_root), "--", *cmd]` placed AFTER the bwrap `--` (Phase 23 builds and golden-tests the argv SHAPE; the actual spawn/preexec wiring is Phase 25). If landlock unavailable, the target is `[*cmd]` directly (bwrap-only). Return `(bwrap_prefix + target, env)`. If NOT `check_bwrap_available()`: emit a one-time `print(..., file=sys.stderr)` warning "bwrap unavailable — falling back to observe (env-scrub only); kernel confinement pending" and return `(cmd, env)`. Add an explicit comment: the Landlock-only rung (bwrap absent, landlock present) is REJECTED per D-03's two-rung wording and Open Q #1 — recorded as a documented future refinement, NOT silently implemented. Add a minimal `if __name__ == "__main__":` argparse branch handling `--apply-landlock <root> -- <cmd...>` that calls `_apply_landlock(root)` then `os.execvp(cmd[0], cmd)` (guard `# pragma: no cover` — exercised only inside the confined child, Phase 25). Tests: `test_check_bwrap_available_false_when_absent` (monkeypatch shutil.which -> None); `test_wrap_linux_full_confine` / `test_wrap_linux_bwrap_only` / `test_wrap_linux_falls_back_to_observe` (monkeypatch `flowstate.sandbox.check_bwrap_available`, `_landlock_available`, `_find_bwrap`); assert argv shapes and that RUNG 3 returns cmd unchanged and never raises. Wire nothing into wrap() beyond what 23-01 already dispatched (confine+linux -> _wrap_linux is already routed).
  </action>
  <verify>
    <automated>python -m pytest tests/test_sandbox.py -x -q && ruff check flowstate/sandbox.py && python -m pytest --cov=flowstate --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def check_bwrap_available" flowstate/sandbox.py`; its body runs `bwrap --ro-bind / / -- /bin/true` (grep `"/bin/true"`) — NOT a which-only check
    - `grep -q "def _wrap_linux" flowstate/sandbox.py`; a comment records the REJECTED Landlock-only rung as a documented future refinement (grep for "Landlock-only" or "landlock-only")
    - Three ladder tests exist and pass: full-confine (argv has bwrap + landlock shim), bwrap-only (bwrap, no shim), observe-fallback (argv == cmd, no raise)
    - `python -c "from unittest import mock; from pathlib import Path; import flowstate.sandbox as s\nwith mock.patch.object(s,'check_bwrap_available',return_value=False):\n a,e=s._wrap_linux(['claude'],Path('/p'),{'X':'1'})\nassert a==['claude'] and e=={'X':'1'}"` exits 0 (observe fallback never raises)
    - `python -m pytest tests/test_sandbox.py` exits 0; overall `--cov-fail-under=80` still green; ruff clean
  </acceptance_criteria>
  <done>check_bwrap_available is a functional smoke test; _wrap_linux implements exactly the two D-03 rungs (bwrap+landlock -> bwrap-only -> observe), never raises, and the invented Landlock-only rung is explicitly rejected in a code comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host kernel capability -> confinement decision | availability probes decide whether real confinement or a silent no-op is chosen |
| Landlock ruleset construction | a struct-packing bug can apply a weaker-than-intended ruleset (Pitfall 4) |
| bwrap presence vs functional capability | Ubuntu 24.04 AppArmor makes "present" != "usable" (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering | struct-packing bug applies a weaker Landlock ruleset than intended (Pitfall 4) | mitigate | Use the RESEARCH-verified 16-byte `struct.pack("QiI", access, fd, 0)`; behavioral allow/deny proof is the SBX-01 spike (23-04); a comment pins the byte shape. |
| T-23-08 | Tampering / Repudiation | presence-only bwrap check gives a false sense of confinement on Ubuntu 24.04 (AppArmor userns block) | mitigate | `check_bwrap_available` is a FUNCTIONAL smoke test (`bwrap --ro-bind / / -- /bin/true`), not `which` — RESEARCH Pattern 3 / Pitfall 3. |
| T-23-09 | Elevation of Privilege | Landlock applied without PR_SET_NO_NEW_PRIVS silently fails to restrict (Pitfall 5) | mitigate | `libc.prctl(38,1,0,0,0)` is called BEFORE `landlock_restrict_self`; asserted by code order + spike behavioral check. |
| T-23-10 | Information Disclosure | landlock READ rule over-scoped, exposing `~/.ssh` / credentials beyond `~/.claude` | mitigate | READ_ACCESS granted only to system dirs + `~/.claude` (for `.credentials.json`); `~/.ssh` gets NO read rule (default-deny) and is tmpfs-shadowed by the bwrap layer (23-02). |
| T-23-11 | Denial of Service (self-inflicted) | degradation ladder raises instead of degrading, breaking a run | mitigate | Every rung returns a usable `(argv, env)`; RUNG 3 returns cmd unchanged; probes wrapped in try/except -> False; never-raise tests assert this. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (stdlib ctypes/subprocess/shutil/platform). py-landlock evaluated and REJECTED in RESEARCH; a comment forbids re-adding it. |
</threat_model>

<verification>
- `python -m pytest tests/test_sandbox.py -q` passes (smoke test + all three ladder rungs, mocked).
- `python -m pytest --cov=flowstate --cov-fail-under=80` passes (Linux-only syscall bodies carry `# pragma: no cover`).
- `ruff check flowstate/sandbox.py tests/test_sandbox.py` clean.
- Behavioral allow/deny + auth-preservation proof of the Landlock/bwrap mechanism is the SBX-01 spike (plan 23-04), not this plan.
</verification>

<success_criteria>
- Functional bwrap smoke test in place (not presence-only).
- Landlock ctypes helper built per the verified recipe with NO_NEW_PRIVS ordering.
- _wrap_linux implements the two D-03 rungs, never raises, and rejects the invented Landlock-only rung with a recorded note.
</success_criteria>

<output>
Create `.planning/phases/23-linux-parity-core-seam/23-03-SUMMARY.md` when done.
</output>
