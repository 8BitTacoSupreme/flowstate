---
phase: 19-the-tax
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - bench/metrics.py
  - bench/capture.py
  - flowstate/journal.py
  - flowstate/orchestrator.py
  - flowstate/tools/research.py
  - flowstate/tools/strategy.py
  - tests/test_bench_compound.py
  - tests/test_journal.py
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "RunSnapshot records real tokens_in/tokens_out/cache_read/wall_clock_s per run"
    - "compute_scorecard output is byte-identical after adding the new fields (Track-1 integrity preserved)"
    - "prefix_tokens keeps its Track-1 GROWTH role in axis_enrichment — it is NOT repurposed for consumption"
    - "Real-mode pipeline runs surface bridge totals into the RUN journal entry that capture reads"
    - "cheap/dry runs leave the new consumption fields at 0/None"
  artifacts:
    - path: "bench/metrics.py"
      provides: "RunSnapshot with tokens_in/tokens_out/cache_read (int) + wall_clock_s (float|None)"
      contains: "wall_clock_s"
    - path: "flowstate/journal.py"
      provides: "RUN entry metadata carrying tokens_in/tokens_out/cache_read/wall_clock_s"
      contains: "wall_clock_s"
    - path: "bench/capture.py"
      provides: "capture_run_snapshot reads the four consumption keys from the latest RUN entry metadata"
      contains: "wall_clock_s"
  key_links:
    - from: "flowstate/orchestrator.py::run_pipeline"
      to: "flowstate/journal.py::append_run_entry"
      via: "passing bridge.total_* totals as new kwargs into the RUN entry metadata"
      pattern: "total_wall_clock_s"
    - from: "bench/capture.py::capture_run_snapshot"
      to: "RunSnapshot consumption fields"
      via: "reading tokens_in/tokens_out/cache_read/wall_clock_s from the latest RUN entry metadata"
      pattern: "metadata.get\\(\"wall_clock_s\""
---

<objective>
Replace the `len(prefix)//4` estimate with real consumption as the source of truth. `RunSnapshot` gains real `tokens_in`/`tokens_out`/`cache_read`/`wall_clock_s` fields, sourced from the bridge `usage` totals (Plan 01) threaded through the pipeline into the RUN journal entry that `capture_run_snapshot` already reads. The existing `prefix_tokens` field keeps its distinct Track-1 GROWTH role in `axis_enrichment` — it is NOT repurposed — so `compute_scorecard` stays byte-identical.

Purpose: `prefix_tokens` measures input-context SIZE (a growth signal); consumption is a different quantity the harness never captured. This wires the real number end-to-end without disturbing the deterministic scorer.
Output: new RunSnapshot fields, journal metadata carriage, pipeline surfacing, capture reader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-the-tax/19-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (already shipped): the shared pipeline bridge exposes cumulative totals. -->
ClaudeBridge.total_tokens_in: int
ClaudeBridge.total_tokens_out: int
ClaudeBridge.total_cache_read: int
ClaudeBridge.total_wall_clock_s: float
BridgeResult.usage: BridgeUsage | None   # populated only on output_format="json" calls
BridgeResult.duration_s: float | None

<!-- Metadata key CONTRACT for the RUN journal entry (Task 1 defines; Task 2 writes; Task 3 reads).
     These live alongside the existing "artifacts_changed" key in the RUN entry metadata dict. -->
RUN entry metadata keys: "tokens_in" (int), "tokens_out" (int), "cache_read" (int), "wall_clock_s" (float|None)

Source-of-truth files the executor MUST read:
- bench/metrics.py — RunSnapshot @dataclass(frozen=True) (~39-53); axis_enrichment (~136-147)
  and _trend_up show prefix_tokens is a GROWTH signal; compute_scorecard (~150-191).
- bench/capture.py — capture_run_snapshot (~97-214); it already reads the latest RUN entry via
  store.get_by_kind(MemoryKind.RUN, limit=1) at ~137 and pulls metadata.get("artifacts_changed");
  _zeroed_snapshot (~45-59) must gain the new fields; _CHARS_PER_TOKEN (~42) stays for prefix_tokens.
- flowstate/journal.py — append_run_entry (~18-127); the metadata dict is built at ~84-93.
- flowstate/orchestrator.py — run_pipeline (~167-338); bridge created at ~169 (_make_bridge),
  append_run_entry called at ~326; adapters constructed with the shared `bridge` (~257,278,301).
- flowstate/tools/research.py (bridge.run at ~105,133) and strategy.py (bridge.run at ~148).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add consumption fields to RunSnapshot and prove the scorecard is unchanged</name>
  <read_first>
    - bench/metrics.py — RunSnapshot (~39-53), axis_enrichment (~136-147), compute_scorecard
      (~150-191): confirm prefix_tokens feeds _prefix_growth/axis_enrichment and MUST keep that role.
    - bench/capture.py — _zeroed_snapshot (~45-59) and the RunSnapshot construction at ~196-208.
  </read_first>
  <behavior>
    - RunSnapshot(...) accepts tokens_in/tokens_out/cache_read (int) and wall_clock_s (float|None),
      each with a safe default so existing construction sites stay valid.
    - compute_scorecard over any snapshot sequence returns the exact same Scorecard whether or not
      the new fields are set (they touch no axis).
    - prefix_tokens still drives axis_enrichment unchanged.
  </behavior>
  <action>
    In bench/metrics.py add four fields to the frozen RunSnapshot, APPENDED after
    `layers_present` with defaults (`tokens_in: int = 0`, `tokens_out: int = 0`,
    `cache_read: int = 0`, `wall_clock_s: float | None = None`) so positional/keyword
    construction elsewhere is unaffected. Do NOT touch axis_enrichment, _prefix_growth,
    _trend_up, or any axis — the new fields are pure carriage, never read by the scorer.
    Update bench/capture.py `_zeroed_snapshot` to set the four new fields to their defaults
    (0/0/0/None). Establish the RUN-entry metadata key contract here in the plan record:
    `tokens_in`, `tokens_out`, `cache_read`, `wall_clock_s` (Task 2 writes them, Task 3 reads them).
  </action>
  <acceptance_criteria>
    - RunSnapshot has tokens_in/tokens_out/cache_read/wall_clock_s with defaults; _zeroed_snapshot updated.
    - New test in tests/test_bench_compound.py builds a snapshot sequence twice (identical except
      the new consumption fields set to nonzero in one) and asserts compute_scorecard returns an
      equal Scorecard for both — i.e. the deterministic scorecard is byte-identical / unchanged by the new fields.
    - `uv run python -m pytest tests/test_bench_compound.py -x` passes; `uv run ruff check bench/metrics.py bench/capture.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_compound.py -x</automated>
  </verify>
  <done>RunSnapshot carries real consumption fields; compute_scorecard is provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface bridge totals into the RUN journal entry (producer side)</name>
  <read_first>
    - flowstate/orchestrator.py — run_pipeline: bridge at ~169, the four adapter steps
      (~257-322), append_run_entry call at ~326.
    - flowstate/tools/research.py (bridge.run ~105,133), strategy.py (bridge.run ~148):
      confirm they consume only `br.output` (byte-identical under Plan 01's json guarantee).
    - flowstate/journal.py — append_run_entry signature (~18-25) and the metadata dict (~84-93).
  </read_first>
  <behavior>
    - After a real (non-dry) pipeline run, the RUN journal entry's metadata carries the run's
      total tokens_in/tokens_out/cache_read and wall_clock_s from the shared bridge.
    - A dry-run pipeline writes 0/0/0 and wall_clock_s None (dry bridge measures no real work).
    - Adapter `.output` is unchanged (json-mode output is byte-identical per Plan 01), so no
      adapter behavior regresses.
  </behavior>
  <action>
    Switch the three pipeline adapter bridge calls to json mode so usage is captured: pass
    `output_format="json"` at research.py:105, research.py:133, and strategy.py:148 (Plan 01
    guarantees json-mode `.output` is byte-identical, so this is safe and adds no LLM call).
    Add four keyword-only params to journal.append_run_entry — `tokens_in: int = 0`,
    `tokens_out: int = 0`, `cache_read: int = 0`, `wall_clock_s: float | None = None` — and
    write them into the metadata dict (alongside `artifacts_changed`) using the exact keys
    from the Task 1 contract. In orchestrator.run_pipeline, after the discipline step and
    before/at the append_run_entry call (~326), read `bridge.total_tokens_in`,
    `bridge.total_tokens_out`, `bridge.total_cache_read`, `bridge.total_wall_clock_s` and pass
    them as those kwargs; when `dry_run` is True pass wall_clock_s=None (tokens are already 0
    from a dry bridge). Keep append_run_entry never-raising.
  </action>
  <acceptance_criteria>
    - append_run_entry accepts the four kwargs and writes keys tokens_in/tokens_out/cache_read/wall_clock_s into RUN metadata.
    - research.py and strategy.py pass output_format="json" at the three call sites; a grep
      confirms `output_format="json"` present and no other adapter behavior changed.
    - New test in tests/test_journal.py calls append_run_entry with sample totals and asserts the
      persisted RUN entry metadata carries them under the contract keys.
    - `uv run python -m pytest tests/test_journal.py tests/test_bench_compound.py -x` passes;
      `uv run ruff check flowstate/journal.py flowstate/orchestrator.py flowstate/tools/research.py flowstate/tools/strategy.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_journal.py -x</automated>
  </verify>
  <done>Real per-run bridge totals are stamped into the RUN journal entry; adapter output byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: capture_run_snapshot reads consumption from the RUN entry (reader side)</name>
  <read_first>
    - bench/capture.py — capture_run_snapshot (~97-214), specifically the block that already
      reads the latest RUN entry at ~136-145 (store.get_by_kind(MemoryKind.RUN, limit=1) →
      metadata.get("artifacts_changed")) and the RunSnapshot construction at ~196-208.
    - flowstate/journal.py metadata keys written in Task 2 (tokens_in/tokens_out/cache_read/wall_clock_s).
  </read_first>
  <behavior>
    - When the latest RUN entry metadata carries the four keys, the returned RunSnapshot's
      tokens_in/tokens_out/cache_read/wall_clock_s equal them.
    - When the keys are absent (older entries, cheap/dry runs), the snapshot fields default to
      0/0/0/None — never raising.
  </behavior>
  <action>
    In the existing RUN-entry read block of capture_run_snapshot (the same `run_entries[0]`
    already used for artifacts_changed), pull `metadata.get("tokens_in", 0)`,
    `metadata.get("tokens_out", 0)`, `metadata.get("cache_read", 0)`, and
    `metadata.get("wall_clock_s", None)`, guarding types the same defensive way as
    artifacts_changed (fall back to 0/None on any oddity). Pass the four values into the
    RunSnapshot(...) construction (~196-208). Leave prefix_tokens computation
    (`len(prefix)//_CHARS_PER_TOKEN`) exactly as-is — it remains the Track-1 growth signal.
  </action>
  <acceptance_criteria>
    - New test seeds a MemoryStore with a RUN entry whose metadata carries the four keys, calls
      capture_run_snapshot, and asserts the snapshot's four consumption fields match.
    - New test asserts a RUN entry WITHOUT the keys (or no RUN entry) yields tokens 0 and
      wall_clock_s None, and does not raise.
    - Test asserts prefix_tokens is still computed from prefix length (unchanged role).
    - `uv run python -m pytest tests/test_bench_compound.py -x` passes; `uv run ruff check bench/capture.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_compound.py -x</automated>
  </verify>
  <done>capture_run_snapshot populates real consumption from the RUN entry, defaulting safely when absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline bridge → journal → capture | Numeric usage totals flow through local memory.db RUN metadata; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | capture reading RUN metadata usage keys | mitigate | Read via metadata.get(...) with 0/None defaults and type-guarding, matching the never-raise discipline already used for artifacts_changed |
| T-19-04 | Information disclosure | token counts persisted in memory.db | accept | Counts are non-sensitive aggregate integers; memory.db is a local project artifact |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_bench_compound.py tests/test_journal.py -x` green.
- `uv run python -m pytest` full suite green, coverage ≥80%.
- `uv run ruff check bench/metrics.py bench/capture.py flowstate/journal.py flowstate/orchestrator.py flowstate/tools/research.py flowstate/tools/strategy.py` clean.
- Sanity: `compute_scorecard` output unchanged by the new fields (Task 1 test); `prefix_tokens` role unchanged.
</verification>

<success_criteria>
RunSnapshot records real tokens_in/tokens_out/cache_read/wall_clock_s per run, sourced from the bridge usage totals via the RUN journal entry capture already reads; cheap/dry runs default to 0/None. `prefix_tokens` keeps its Track-1 growth role and `compute_scorecard` is byte-identical. No new deps, no new LLM calls.
</success_criteria>

<output>
Create `.planning/phases/19-the-tax/19-02-SUMMARY.md` when done (status: complete).
</output>
