---
phase: 19-the-tax
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - bench/report.py
  - bench/compound_eval.py
  - tests/test_bench_compound.py
autonomous: true
requirements: [TAX-03, TAX-04]
must_haves:
  truths:
    - "The report shows per-arm total tokens and total seconds alongside the quality metrics"
    - "The tax block is explicitly labeled EXCLUDED from compounding_score (Track-2, not Track-1)"
    - "Tax numbers never touch bench/metrics.py or the compounding_score"
    - "The cost-per-success line divides tax by passed flowstate verify acceptance gates, named honestly (not 'per commit')"
  artifacts:
    - path: "bench/report.py"
      provides: "per-arm tax rendering (tokens/seconds) + cost-per-verified-gate line, Track-2 excluded"
      contains: "EXCLUDED from compounding_score"
    - path: "tests/test_bench_compound.py"
      provides: "tests: tax excluded from score, denominator = verify_pass, label names acceptance gates not commits"
  key_links:
    - from: "bench/report.py tax block"
      to: "RunSnapshot.tokens_in/tokens_out/cache_read/wall_clock_s + verify_pass"
      via: "summing snapshot consumption fields and dividing by summed verify_pass"
      pattern: "verify_pass"
---

<objective>
Surface the tax in `bench/report.py`: per-arm total tokens and total seconds rendered alongside the existing quality metrics, in a Track-2 block explicitly marked EXCLUDED from `compounding_score` (mirroring the existing judge exclusion note). Add a cost-per-success line whose denominator is the count of passed `flowstate verify` acceptance gates — named honestly as "per verified acceptance gate", NOT "per commit" (the pipeline produces artifacts, not commits).

Purpose: closes the loop — the measured consumption from Plan 02 becomes a visible, honestly-labeled cost that can license stripping a layer. The tax must never contaminate the deterministic scorer.
Output: tax rendering in the JSON payload, the Rich report, the markdown record, and the cost-per-gate line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-the-tax/19-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (already shipped): RunSnapshot now carries real consumption + existing verify counts. -->
RunSnapshot.tokens_in: int
RunSnapshot.tokens_out: int
RunSnapshot.cache_read: int
RunSnapshot.wall_clock_s: float | None
RunSnapshot.verify_pass: int    # pre-existing — count of PASSED flowstate verify acceptance gates this run

Source-of-truth files the executor MUST read:
- bench/report.py — the judge exclusion pattern to MIRROR: write_json (~89-140) adds a
  "judge" key whose note reads "EXCLUDED from compounding_score" (~127-137); _trend_table
  (~161-191), _markdown_record (~217-249), render_report (~252-278), render_judge_panel (~281-305).
- bench/compound_eval.py — main (~348-419): render_report and write_json are already called with
  the scorecard whose snapshots now carry the consumption fields; no new data needs collecting.
- flowstate/verify.py — run_verify / VerifyResult (~27-35, 123-212): the "acceptance gate"
  vocabulary and the fact that verify_pass counts PASSED acceptance gates (the TAX-04 denominator).
- bench/BENCHMARKING_SCOPE.md — Track-1 (deterministic, authoritative) vs Track-2 (the tax lives here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render per-arm tokens + seconds as a Track-2 block excluded from compounding_score</name>
  <read_first>
    - bench/report.py — write_json (~89-140) incl. the judge block's "EXCLUDED from
      compounding_score" note (~127-137) to mirror; _trend_table (~161-191); _markdown_record
      (~217-249); render_report (~252-278); _scorecard_panel (~194-214).
    - bench/metrics.py RunSnapshot consumption fields (from Plan 02).
  </read_first>
  <behavior>
    - The JSON payload gains a top-level "tax" key summing tokens_in/tokens_out/cache_read and
      wall_clock_s across the scorecard's snapshots, carrying a note "Track-2 tax — EXCLUDED
      from compounding_score".
    - The Rich report and markdown record display per-arm total tokens and total seconds.
    - No tax value is passed into compute_scorecard or affects compounding_score / any axis.
  </behavior>
  <action>
    In bench/report.py add a small helper (e.g. `_tax_totals(scorecard)`) that sums
    tokens_in/tokens_out/cache_read (ints) and wall_clock_s (treating None as 0.0) across
    scorecard.snapshots and returns the totals. In write_json add a top-level "tax" key with
    those totals plus a `"note"` string reading exactly like the judge note's intent —
    "Track-2 tax — EXCLUDED from compounding_score" — mirroring the existing judge exclusion at
    ~127-137. Render the totals in render_report (a dedicated panel or two extra summary lines,
    styled distinctly from the mechanical scorecard) and in _markdown_record. Do NOT add tax to
    _trend_table's per-run columns unless trivial; per-arm totals are the requirement. Keep all
    tax logic inside report.py — it must never import from or feed bench/metrics.py.
    compound_eval.main already passes the scorecard to render_report/write_json, so no
    collection change is needed there (verify by reading main ~391-414).
  </action>
  <acceptance_criteria>
    - write_json output contains a "tax" object with tokens_in/tokens_out/cache_read/wall_clock_s
      totals and a note containing "EXCLUDED from compounding_score".
    - New test builds a scorecard whose snapshots carry consumption values, calls write_json, and
      asserts (a) the tax totals equal the summed snapshot fields and (b) compounding_score is
      unchanged vs the same scorecard — i.e. tax does not alter the score.
    - New test asserts bench/report.py contains no import of bench.metrics internals that would
      let tax feed the scorer (tax stays presentation-only).
    - `uv run python -m pytest tests/test_bench_compound.py -x` passes; `uv run ruff check bench/report.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_compound.py -x</automated>
  </verify>
  <done>Per-arm tokens/seconds render as a Track-2 block explicitly excluded from compounding_score.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cost-per-success line — denominator = passed verify acceptance gates, named honestly</name>
  <read_first>
    - bench/report.py — the tax helper/rendering added in Task 1; write_json + render_report + _markdown_record.
    - flowstate/verify.py — VerifyResult.status "pass" (~27-35) and run_verify (~123-212):
      confirm verify_pass counts PASSED acceptance gates; RunSnapshot.verify_pass carries that count.
    - bench/BENCHMARKING_SCOPE.md — the denominator-naming rationale (artifacts, not commits).
  </read_first>
  <behavior>
    - A cost-per-success figure = total tax ÷ (sum of verify_pass across snapshots), reported for
      tokens (and/or seconds) per passed acceptance gate.
    - The label names the denominator honestly — "per verified acceptance gate" — and never uses
      the word "commit".
    - When total verify_pass is 0, the line reports "n/a" (no divide-by-zero).
  </behavior>
  <action>
    Extend the Task 1 tax rendering: compute `gates_passed = sum(s.verify_pass for s in
    scorecard.snapshots)` and emit a cost-per-success figure = total tokens (and total seconds)
    divided by `gates_passed`, guarding `gates_passed == 0` → "n/a". Label it explicitly as
    per verified acceptance gate (e.g. "tokens per verified acceptance gate") in write_json (a
    field under the "tax" object), render_report, and _markdown_record. The denominator MUST be
    named as flowstate verify's acceptance gates — do NOT use "commit" / "per commit" anywhere.
  </action>
  <acceptance_criteria>
    - write_json "tax" object includes a cost-per-success field computed as total tax ÷
      summed verify_pass, with an explicit "per verified acceptance gate" label.
    - New test asserts the denominator equals the summed verify_pass (not commit count) and that
      gates_passed==0 yields "n/a" without raising.
    - New test asserts the rendered/JSON label text contains "acceptance gate" and does NOT
      contain the substring "commit".
    - `uv run python -m pytest tests/test_bench_compound.py -x` passes; `uv run ruff check bench/report.py bench/compound_eval.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest tests/test_bench_compound.py -x</automated>
  </verify>
  <done>Cost-per-success divides tax by passed verify acceptance gates and is named honestly (never "commit").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scorecard → report | In-process numeric fields rendered to JSON/Rich; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Repudiation | tax presented as a Track-1 quality metric | mitigate | Tax block carries an explicit "EXCLUDED from compounding_score" note and denominator named as verify acceptance gates — the honest-reporting requirement |
| T-19-06 | Denial of Service | divide-by-zero on zero passed gates | mitigate | Guard gates_passed==0 → "n/a" |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_bench_compound.py -x` green.
- `uv run python -m pytest` full suite green, coverage ≥80%.
- `uv run ruff check bench/report.py bench/compound_eval.py` clean.
- Sanity: tax numbers absent from compute_scorecard / compounding_score; denominator label says "acceptance gate", never "commit".
</verification>

<success_criteria>
`bench/report.py` shows per-arm tokens and seconds alongside quality metrics, visibly excluded from `compounding_score` (Track-2). The cost-per-success line uses passed `flowstate verify` acceptance gates as the denominator, named honestly (not "commits"). Tax never touches `metrics.py`. No new deps, no new LLM calls.
</success_criteria>

<output>
Create `.planning/phases/19-the-tax/19-03-SUMMARY.md` when done (status: complete).
</output>
