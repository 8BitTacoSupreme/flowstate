---
phase: 22-the-verdict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-the-verdict/22-PREREGISTRATION.md
autonomous: true
requirements: [VERD-01]

must_haves:
  truths:
    - "A committed 22-PREREGISTRATION.md exists in the phase dir BEFORE any --mode real trial runs (D-04)."
    - "The pre-registration states the frozen COMBINED win rule verbatim (D-02): a treatment arm WINS iff ALL THREE hold — (1) paired-bootstrap 95% CI excludes 0, (2) Cohen's d >= 0.8, AND (3) the contrast survives Holm-Bonferroni across the 4 co-primary contrasts (Holm is GATING, not decorative); anything else = null-and-accepted."
    - "The pre-registration names the 5 arms and the 4 co-primary treatment-vs-none contrasts (D-05) and states that Holm-Bonferroni is GATING on the win decision — a contrast that does not survive Holm cannot win regardless of its raw CI/d — while both raw and Holm-corrected significance are reported (D-06)."
    - "The pre-registration pins n = trials 5, runs 3, --mode real, a fixed seed, and the compounding-curve run 1->3 paired-normalized to run-0 (D-07, D-08)."
    - "The pre-registration names the subject repo /Users/jhogan/floxybot2 and the isolated-worktree / never-mutate discipline (D-01, D-01a)."
  artifacts:
    - path: ".planning/phases/22-the-verdict/22-PREREGISTRATION.md"
      provides: "The frozen, committed measurement protocol (VERD-01)"
      contains: "Cohen's d"
  key_links:
    - from: ".planning/phases/22-the-verdict/22-PREREGISTRATION.md"
      to: "git history"
      via: "committed before Plan 03's real run"
      pattern: "22-PREREGISTRATION.md"
---

<objective>
Write and COMMIT the pre-registration artifact for the Phase 22 verdict — the frozen
measurement protocol — BEFORE any real trial runs (D-04). This is a scientific
pre-registration: every rule (win threshold, arms, contrasts, correction, sample size,
seed) is fixed here in writing so the verdict cannot be a post-hoc, data-peeked claim.

Purpose: pre-registration integrity (VERD-01). A verdict whose rules were written after
seeing data is worthless.
Output: a committed `.planning/phases/22-the-verdict/22-PREREGISTRATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-the-verdict/22-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and commit 22-PREREGISTRATION.md (the frozen protocol)</name>
  <read_first>
    - .planning/phases/22-the-verdict/22-CONTEXT.md — the locked decisions D-01..D-08; restate them, do NOT re-decide.
    - .planning/REQUIREMENTS.md — VERD-01/02/03 wording.
    - .planning/ROADMAP.md §"Phase 22: The Verdict" — the 3 success criteria.
  </read_first>
  <files>.planning/phases/22-the-verdict/22-PREREGISTRATION.md</files>
  <action>
    Create `22-PREREGISTRATION.md` transcribing the CONTEXT.md pre-registration decisions
    into a standalone, frozen protocol document. Required sections, each citing its D-ID:

    1. Subject repo (D-01, D-01a): `/Users/jhogan/floxybot2`, a real Python AI system,
       pristine of FlowState/GSD artifacts (no memory.db, flowstate.json, .planning/, root
       PROJECT.md/ROADMAP.md/research/). State that close_loop/verdict operate on an
       ISOLATED WORKTREE copy and the real repo is never mutated; state that run-1-empty-memory
       is naturally satisfied because floxybot2 ships no memory.db.
    2. Win rule (D-02) — the frozen COMBINED GATING rule the driver implements VERBATIM: a
       treatment arm WINS iff, for its (arm − none) quality-delta contrast, ALL THREE hold:
       (1) the paired-bootstrap 95% CI excludes 0, (2) Cohen's d >= 0.8 (large effect), AND
       (3) the contrast survives Holm-Bonferroni across the 4 co-primary contrasts (the Holm
       correction is GATING, not decorative). Anything else = NULL, documented as a valid
       outcome that licenses stripping that layer. State explicitly: no re-running to chase
       significance. This exact three-part rule MUST be byte-consistent with what bench/verdict.py
       implements.
    3. Endpoints (D-03): quality = Phase-20 independent multi-judge (0-10, judge != producer);
       tax = per-arm tokens_in/tokens_out/cache_read + wall_clock_s (Track-2, EXCLUDED from
       compounding_score). Report quality AND tax per arm side by side.
    4. Arms and contrasts (D-05): arms = none, pack, memory, wiki, full; co-primary endpoints =
       the 4 treatment-vs-none contrasts (pack-none, memory-none, wiki-none, full-none).
    5. Correction (D-06): Holm-Bonferroni across the 4 co-primary contrasts is GATING — a
       contrast that does not survive Holm cannot win regardless of its raw CI/d (this is the
       third gate of D-02, not a decoration). Report BOTH raw and Holm-corrected significance
       for transparency, but the WIN/null decision uses the Holm-corrected result.
    6. Compounding curve (D-07): report run 1->3 per arm, paired-normalized to run-0; wiki/memory
       value (if any) is expected only at run 2+ because run 1 has empty memory.
    7. Sample size and cost posture (D-08): n = trials 5, runs 3, --mode real, seed pinned
       (record the exact integer seed chosen). Sequence: (1) free --mode cheap plumbing check,
       (2) cost estimate + explicit user greenlight, (3) the full 5x3 real run.
    8. A "Frozen — do not amend after first real trial" banner at the top with the write date.

    Do NOT invent new rules or soften any decision. Use plain prose + tables; no code fences.

    After writing, COMMIT the file immediately so its git timestamp provably precedes the real
    run (D-04): run `git add .planning/phases/22-the-verdict/22-PREREGISTRATION.md` then
    `git commit -m "docs(22): pre-register the verdict protocol (VERD-01)"`. The commit is the
    integrity anchor — Plan 03's real run must come strictly after it.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-the-verdict/22-PREREGISTRATION.md && grep -q "Cohen" .planning/phases/22-the-verdict/22-PREREGISTRATION.md && grep -q "Holm" .planning/phases/22-the-verdict/22-PREREGISTRATION.md && grep -qi "gating" .planning/phases/22-the-verdict/22-PREREGISTRATION.md && git log --oneline -- .planning/phases/22-the-verdict/22-PREREGISTRATION.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - 22-PREREGISTRATION.md exists and states D-02 as the three-part gate verbatim
      (CI-excludes-0 AND d>=0.8 AND survives-Holm), D-05 (5 arms / 4 contrasts), D-06 (Holm is
      GATING; report raw + Holm-corrected), D-07 (compounding curve), D-08 (5x3, real, pinned seed).
    - The frozen win rule in the doc is byte-consistent with the rule bench/verdict.py implements
      (three-part gate; Holm-corrected result decides WIN/null).
    - The file is committed to git (git log for the path returns at least one commit) — the
      pre-registration provably precedes any real-mode run (D-04).
  </acceptance_criteria>
  <done>The frozen protocol is written, states the three-part gating win rule verbatim, cites D-01..D-08, and is committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> git history | The pre-registration's integrity value is entirely its commit-before-data ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Repudiation | 22-PREREGISTRATION.md | mitigate | Commit the file in this plan; Plan 03's real run depends_on this plan, so the paid run cannot precede the committed rules (D-04). |
| T-22-02 | Tampering | post-hoc rule edits | mitigate | "Frozen — do not amend after first real trial" banner + the committed timestamp make any later edit visible in git blame. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing (docs only); no new runtime deps this phase. |
</threat_model>

<verification>
- `git log --oneline -- .planning/phases/22-the-verdict/22-PREREGISTRATION.md` returns a commit.
- The file names all four contrasts and states the three-part gating win rule with Holm as the gating correction.
</verification>

<success_criteria>
The frozen, committed pre-registration exists before any real trial, states the three-part
gating win rule verbatim — VERD-01 satisfied and D-04 ordering guaranteed for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/22-the-verdict/22-01-SUMMARY.md` when done.
</output>
